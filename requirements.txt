# Web Framework
fastapi>=0.104.0
uvicorn>=0.24.0
a2wsgi>=1.10.0  # ASGI转WSGI，用于PythonAnywhere部署
python-multipart>=0.0.10  # FastAPI文件上传依赖

# Database
# SQLAlchemy 必须有上界：`postgresql://` 这种"没写 +driver"的连接串用哪个驱动，
# 是 SQLAlchemy 的**默认值**——2.0.x 默认 psycopg2（本仓装的），2.1 起默认改成 psycopg(v3)（没装）。
# 2026-09-25 那次部署失败就是构建时解析到 2.1：应用一起来就 `ModuleNotFoundError: No module named 'psycopg'`。
# 本机 1082 条用例跑的是 2.0.48 ⇒ 上界钉到 <2.1，让线上与测过的那一份对齐；
# `src/models/database.py` 那边同时把驱动名写死，两处不再依赖默认值。
sqlalchemy>=2.0.0,<2.1.0
psycopg2-binary>=2.9.0
alembic>=1.13.0,<2.0.0

# Configuration
python-dotenv>=1.0.0

# Data Validation
pydantic>=2.12.0,<3.0.0

# LLM API
openai>=1.6.0

# HTTP Client
requests>=2.31.0
aiohttp>=3.9.0

# HTML Parsing (爬虫模块需要)
beautifulsoup4>=4.12.0

# JSON5 Parser (for LLM response parsing)
json5>=0.9.0

# Optional: for better async support
# httpx>=0.25.0
tzdata>=2024.1   # zoneinfo 在容器里要它才拿得到 Asia/Shanghai（第 25 轮 B：`current_as_of` 缺它会静默退回系统时钟，页面"截至日"差一天）
