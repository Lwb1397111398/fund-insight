# S6 上线前检查单（每一步都带"我今天真跑过的命令 + 当时输出"）

生成：2026-09-23 00:5x（北京时间）。作者：AI。**这份清单的作用不是打勾，是让下一次动手的人
不必信任我的记忆。** 每条命令都在本机跑过；未跑过的写在最后一节，不混进来。

---

## 0. 门禁（未通过，后面全部不许动）

| 项 | 状态 |
| --- | --- |
| 第 26 轮两份独立复评 | 66 / 56（取低分）→ **未过 90** |
| 第 27 轮 | C **62** / D **72**（两份都逐条复现过、都判不过）；A/B 未回 |
| 第 28 轮 | F **74** / H **79** / I **82**（三份都可复现，取低分仍 **未过 90**）；G 报 76 但**不可采信** —— 它点名的 `--all-sectors`/`--d1-from-db` 与三条用例名在本仓库不存在，举的绕过例子 `卫星互联网产业` 实测被挡，且用量只有 4 次工具调用却报告做了 30 组变异；E 撞子代理步数上限、没出报告 |
| 第 28 轮 H/I 抓到的（已修） | ① 我新写的 `_literal_block_hit` 自己留了逃逸：`核电与半导体` 这种"名单词 + 不相干长键"整体放行 → 改成只在**同一个词内部**比较，并补用例；② `test_script_db_guards` 用整份文件正文做正则，把 pin 删掉仍 3 条全绿 → 改走 AST（注释/docstring/`add_argument` 字样不再算护栏），并覆盖"删除发生在 service 里"的脚本；③ 我说"第三态已透传给前端"说过头（`web/` 里 `relevance_state` 0 处命中）→ 已撤回，剩页面一档见 #32；④ 服务端把老板锁定行既计 refused 又计"定不了价"、真写回执没说"照样写入了"、push 脚本被剔行后仍打"[完成] 已落在被审计的状态" → 三处措辞与双计都改 |
| 第 29 轮（我自己这一批，待复评） | 把第 28 轮 I-③ 欠的那半还上了：**第三态与"机器改过什么"现在页面上看得见**。① `withWakeRetry` 覆盖首屏 5 个取数点，唤醒失败不再清老板口令（改成"连不上服务/重试连接"弹窗）；② "取不到数据"与"库里没有"分开写（博主榜、证据体检）；③ 博主表 + TOP 弹窗把两个口径写进正文；④ `名册无对口` 计数与筛选、行内第三种说法；⑤ "机器已纠正"横幅按 `core`/`kind`/`replaced`/兜底四条分支说话，`identity_view` 补透传 `replaced`。**浏览器实测**（本地镜像 `data/fund_insight.db`，2026-09-23 04:1x）：证据行"已判 1110 / ⚠197（17.75%）/ 43.96%~61.71% / 本地镜像库 / 截至 2026-09-23"、2 行升级记录渲染出"（原 010677 工银传媒指数C）""（原 162412 华宝医疗ETF联接A）"、四个失败态逐个看过。`no_literal` 计数**实测 0**（存量 evidence 是旧版写的，没有 `relevance_state` 键）⇒ 这一档要等下一轮 sweep 跑完才有数，按钮 `v-if` 所以 0 时不显示。基线：`tests/unit` **873/16/0**、`tests/` **882/16/0** |
| 第 29 轮两份独立复评 | **72 / 86**（都可复现，取低分仍 **未过 90**）。A 无 BLOCKER、3 条 MAJOR；B 无 BLOCKER、1 条 MAJOR。两份**共同**抓到同一条：`checkAuth` 只挡了"服务器没答话"，**502/503/504/500 仍然清老板口令**（本仓库没配 `ACCESS_PASSWORD` 时 `main.py` 回的就是 503）⇒ 服务端的错要老板重输密码，而这正是本轮标题声称修掉的事 |
| 第 29 轮返修（已做，待复评） | ① 状态码分档：`isAuthRejection`（只有 401/403 清口令）+ `isServiceDown`（没答话或 502/503/504 才排队等醒），500 原样抛出；② 并发失败**共用一次等待**（`wakeWait`），`finally` 里复位标记并清句柄；③ 统计卡不再把"没取到"报成 0（`statVal()` ⇒ `—` + 一行失败说明 + 重试），空状态按 `唤醒中 → 加载中 → 失败原因 → 真的空` 排序，错误只在成功时清；④ 第 6 个首屏取数点（`/api/predictions/verify-all/status`，在 `prediction-manager.js` 里）改走注入的 `withWakeRetry`；⑤ 内置行补齐 `relevance_state`/`identity_suggestions`/`is_fetchable` 键（缺键时第三态在 77/222 行上**结构上永远点不到**）；⑥ 筛选后的解释从 `title` 挪进正文；⑦ 助手定义整体挪到所有使用点之前（消 TDZ 前提）；⑧ 弹窗措辞改成实话（最坏约 3 分半，不是"约 90 秒"）。**判据自身的返修**：A 证明我两条文本判据结构上不可能响（字面 `「」` 永不出现在正文；`<th[^>]*>` 把属性吃掉），已重写并新增一条**行为判据** —— 用 node 执行页面里那份源码，喂 401/502/500/断网四种形状，断言"口令错不许去轮 /api/health、502 要等醒并重跑一次、500 原样抛出、等待结束后标记复位"。变异脚本也进了仓库：`scripts/mutation_proof_frontend.py`（21 处 / 10 条判据，逐条 RED，跑完逐文件回读比对还原；顺带修掉它自己"变异叠在上一个变异之上"的缺陷）。基线：`tests/unit` **878/16/0**、`tests/` **887/16/0** |
| 第 30 轮两份独立复评 | **78 / 84**（都可复现，取低分仍 **未过 90**，比第 29 轮的 72 前进 6 分）。共同 MAJOR：映射页同一行写着 `共 222 / 已审查 200 / 老板已确认 4 / 看过未确认 119`，而 `4+119=123` —— 那 77 条静态表合成行被算进了"已审查"，它们 `audited:False`、永不被四个体检档点到、也没有"确认"按钮 ⇒ **屏幕上就是一个对不上的数**。A 另抓两条：`restoreAnalysisJob/restoreViewpointTask` 的 catch 无条件 `clearJob()` ⇒ **唤醒期打开页面会把正在跑的批量分析任务号永久丢掉**；空状态那条判据的"顺序"是从固定元组里筛词、与源码顺序无关 ⇒ 把四支整个倒过来照样绿。B 另抓：第 6 张统计卡 `retentionPreview?.total \|\| 0` 绕过 `statVal()`（判据只筛含 `stats.overall` 的卡，对它结构上不可能响）、`submitPassword` 的 catch 无条件清口令（与它自己那句"密码没有丢"打脸）、变异脚本不能与 pytest 并发且"还原"只写回它读到的那份 |
| 第 30 轮返修（已做，待复评） | ① `reviewed_count`/`unreviewed_count` **只数在册行** + 新增 `builtin_count`，页面把"共 222（在册 145 + 内置 77）/ 已审查 123（在册行内）/ 内置 77 条未跑体检"分开写，四个数必须互相闭合（`tests/unit/test_stats_evidence_report.py::test_reviewed_counts_do_not_absorb_the_builtin_rows`；顺带改掉同一文件里那句把缺陷钉成期望的 `reviewed_count > custom_count`）；② 两个任务轮询的 catch 分档：服务不可用 ⇒ 留句柄、10 秒后再问，服务器答 4xx 才算结束（`isServiceDown` 由 `index.html` 注入，判据只此一份）；③ 第 6 张卡改 `retentionPreview ? … : '—'`，判据改成"每张卡要么 `statVal(` 要么显式 `—`"（`?.` 会糊过 `'?' not in` 那种写法，实测踩过）；④ `submitPassword` 只有 401/403 清口令，其余分支写明"口令没有丢"；⑤ 空状态顺序判据改为**从源码位置算**；⑥ 新增登录门行为判据：node 执行 `checkAuth`，跑 401/403/502/503/500/断网/叫不醒七种形状，断言口令留没留、弹哪个窗、探针几次、醒后重跑没重跑；⑦ 变异脚本加了"落盘核对 + NO-OP 检查 + 只从干净底本变异"，措辞不再声称能抗并发（与 pytest 同跑会互相踩，B 实测过）。实测：`tests/unit` **881/16/0**、`tests/` **890/16/0**、`scripts/mutation_proof_frontend.py` **28/28 RED**；浏览器（本地镜像，06:2x 北京）看到计数闭合 `123+22=145`、`4+119=123`、`145+77=222`，六张卡在 `statsError` 下全为 `—`，空状态四条分支按序各出一次 |
| 第 31 轮两份独立复评 | **80 / 80**（都可复现，取低分仍 **未过 90**；轨迹 72 → 78/86 → 80/80）。A 抓到两条 MAJOR：① 上一批新加的 `serviceWaited` **没进 `setup()` 的 return 名单** ⇒ 弹窗里"已经等过一轮唤醒"那一支永不渲染，本批第 6 条修的那句话等于没上屏；② 任务轮询的 catch 我改成"`isServiceDown` 为假就丢句柄"，而 401/403/500 全在"为假"那一侧，且 `restoreAnalysisJob()` 在 `onMounted` 里跑、不等登录门 ⇒ `ACCESS_PASSWORD` 轮换那天正在跑的批量分析静默永久失联。B 另抓两条同类：帖子/预测/观点三个视图仍是无条件"暂无X数据"（三条腿连 catch 都没有），板块映射页取数失败时写"共 0 条 / 已审查 0"。两人都报告：`submitPassword` 零判据、第 6 张统计卡绕过 `statVal()`、唤醒时长四处说法不一 |
| 第 31 轮返修（已做，待复评） | ① 新增**模板绑定机器闸** `test_everything_the_template_reads_is_actually_exported`（扫 `#app` 模板里所有根标识符，逐个要求出现在 `setup()` return 名单里；顺带扫出第二个同类缺陷：API Key 输入框读 `showApiKey`，而它从未声明 ⇒ 恒为 `password` 的假开关，已改成写死 `type="password"`）；② 任务句柄改成**只有 404 才丢**，其余失败留着句柄 10 秒后再问、上限 `MAX_POLL_FAILURES`≈15 分钟；③ 帖子/预测/观点/板块映射共用 `emptyText(view)` + `viewErrors`（`loadView` 里一处包住，不往子模块抄）；④ 第 6 张卡走 `retentionPreview ? … : '—'`，`statVal()` 还补了"字段缺失也算取不到"；⑤ `submitPassword` 进行为判据（401 清旧口令、502/500 不清且保留输入框内容）；⑥ 唤醒时长四处统一成"通常 30~60 秒，最长再等 90 秒"；⑦ 清理清单/保留分析两个失败按钮的文案与对象对错位（本来就写反了）；⑧ 密码弹窗按钮上两个 `class` 属性（HTML 只认第一个，`w-full` 一直是死的）。**更正第 29 轮那行**：`no_literal` 计数 0 的原因不是"存量行没有 `relevance_state` 键"——3b92357 之后 222 行都有这个键（实测 custom 144 relevant + 1 alternative_exists、builtin 77 为 null），0 的真实原因是**体检还没重跑**。变异脚本 33 处 / 14 条判据全 RED |
| 第 32 轮两份独立复评 | **83 / 84**（都可复现，取低分仍 **未过 90**；轨迹 72 → 78/84 → 80/80 → 83/84）。两份**共同**抓到规矩③只铺了一半：分页条"共 0 条"、观点洞察四张卡 `|| 0`、历史建议/别名 tab 的"暂无 X"、映射表体的"暂无数据"都在把"没取到"说成"库里没有"。A 另外抓到**我上一批自己引入的回归**（轮询数到 15 分钟上限后 `analyzing` 不放开 ⇒ 13 个按钮永久灰且页面一个字不说）；B 另外抓到两处过时数字（docstring 的"28 处"、AGENTS 的"web/ 里 relevance_state 0 处命中"）与一条通则：**"不能并发跑"只写在 docstring 里等于没有闸** |
| 第 32 轮返修（已做，待复评） | ① 规矩③铺满：分页条走 `viewErrors.posts/predictions`、洞察卡走 `numOrDash()`、映射表体走 `emptyText('mappings')`、历史建议 `adviceError`、别名 `aliasError`、生成建议的 200 拒绝弹一句话；② 轮询耗尽放开全局锁 + `taskStalled` 横幅（`onPollStalled` 接进两个 manager）；③ 失败态由"取数成功"自己清（三条 `watch`，`watch` 补进 Vue 解构）；④ 基金页三个筛选按钮的括号数失败时报 `—`，并加一行"那是**本页**不是全库"；⑤ 映射页 `success:false` 也进 `viewErrors.mappings`；⑥ 文档数字去文字化（处数改为一句话指向 `--list`）+ `relevance_state` 那条过时结论更正（实测 4 处命中，页面早有按钮/图例/行内灰字/计数）。判据 +7 条（16→23）、变异 +23 处 |
| 第 33 轮（本批，待复评）**含一次生产事故** | **事故**：我在 `tests/conftest.py` 顶层加锁检查时，把 `from src.utils import mutation_lock` 写在了"钉 `DATABASE_URL` 到临时 SQLite"那行**之前** —— `src/__init__.py` 会拉起 `src.core.config`，engine 就此绑上 `.env` 里的生产 Supabase。两批测试（北京 10:17、10:37）把夹具数据写进线上：**8 个假博主（`T-基金列表体检博主`）+ 8 篇帖 + 1 行 `system_config` + 1 行 `fund_info`（159877 占位基金）**。是 `test_database_url_routing.py`（断言 engine 必须是 sqlite）把它照出来的 —— 那条闸门 09-22 加的时候就是为了这类事。**已做**：① 顺序修好（锁检查移进 `pytest_configure`，延迟导入）；② conftest 钉库后立刻 `assert 'src' not in sys.modules`，把原因写成一条可读的断言；③ 只读预检定出影响面（`predictions`/`sector_fund_mapping`/`viewpoints` 各 0 行受影响，真数据没被改）；④ 18 行逐行备份进 `docs/迭代计划/run-20260923-test-rows/`；⑤ 备好 `scripts/purge_test_rows_from_prod.py`（按夹具名/键匹配而非时间窗、默认 dry-run、`--apply --confirm CLEAN-TEST-ROWS`、删前落 JSON、`--restore-from` 原样插回），它的 AST 护栏由 `test_script_db_guards.py` 新增的反向谓词 + 一条"白名单不是后门"用例钉住。**未做**：真删要老板点头（任务 #42）。本轮其余改动见上一条之外的部分：变异体检 +CONTROL 对照跑、`src/utils/mutation_lock.py` 让体检与 pytest 互斥、`scripts/serve_mirror.py`（把页面钉在镜像上做浏览器核验）、清理预览失败时关掉删除开关、"待清理"卡片与"实际删除 N 行"同口径、TOP 弹窗口径进表头并渲染接口自己的 `metric_note`。基线：`tests/unit` **898/16/0**、`tests/` **907/16/0**（2026-09-23 11:02 北京） |
| 第 33 轮两份独立复评 | **62 / 74**（都可复现，取低分 **未过 90**；轨迹 72 → 78/84 → 80/80 → 83/84 → 62/74 —— 分数后退不是评审变严，是本轮**实测到我自己的两条硬伤**）。共同 BLOCKER：① conftest 的 import 顺序把测试打到生产库（见上一行）；② 两份都独立跑到 `tests/unit` **897 passed / 1 failed / 16 skipped** ⇒ **HEAD 是红的，我报的 898/907 全绿不成立** —— 红的是我自己新加的子进程用例：子 pytest 按 cp936 出中文、父进程按 utf-8 解码 ⇒ 断言里的中文字串永不命中，我的环境是 UTF-8 mode 所以看不见（仓里 `test_database_url_routing.py` 早就写了"子进程必须显式 `PYTHONIOENCODING=utf-8`"，我没守自家规矩）。A 另抓到：上一批"失败态由取数成功自清"的三条 `watch` 里**两条是死的**（`postMeta` 是 `reactive()`，`postMeta.value` 恒 undefined ⇒ 翻页失败仍报上一页的"共 N 条"），守它的却是文本判据；"互斥闸"只做成了单向。B 另抓到：`purge_test_rows_from_prod.py` **零行为测试且一跑就炸**（`posts.post_date` 是 `Date` ⇒ `json.dump` 抛 `TypeError` 并在盘上留下一个截断的"备份"；`--restore-from` 按备份顺序插 = 先子后父 = 外键禁忌序；引用检查手挑两张表） |
| 第 33 轮返修（已做，待复评） | ① 子进程一律显式 `PYTHONIOENCODING=utf-8`（用例 + 体检），并在**默认 locale** 下重跑两个口径报数；② conftest 钉库后除"不许提前导入 src"的断言外，再加一条 **engine driver 必须是 sqlite** 的断言（那条 `routing` 用例是事后探测，等它响时夹具已经在写了）；③ 列表页失败态改由**取数点自己报**：`post-manager.js` / `prediction-manager.js` 的 `fetchPosts`/`fetchPredictions` 补 `else` + `catch`，通过 `options.onFetchFailure(key, msg)` 写进 `viewErrors`，`loadView` 不再盲目清空 —— 死 `watch` 删掉，判据换成**用 node 跑 manager 真实源码**的四种形状（成功清 / success:false 报 / 抛错报 / 再成功清）；④ 洞察卡第四张的 `pending_summary: []` 恒真 ⇒ 加 `insightsLoaded` 事实 + `insightsError` 单独报（`fetchInsights` 原本连 `success` 都不查、没有 catch）；⑤ 体检与 pytest 改成**双向互斥**（两把 OS 锁：`.mutation-harness.lock` 与 pytest 会话握的 `.pytest-session.lock`；`harness_may_start()` 真被调用，有用例钉）；⑥ CONTROL 对照跑改为**整份判据文件**，且"红"只认断言失败的红（退码 2/4 记 `HARNESS-FAIL`，不再混进满分）；⑦ `purge_test_rows_from_prod.py` 补离线往返用例（临时 SQLite 上真跑 `_row → json.dumps → loads → build_insert`，并断言"按备份原序插会撞外键"）、引用面**从 schema 推**（`tables_with_column`）、引用分 FK / 无外键两类（无外键只提醒不拦，FK 才 abort）、标识符白名单、备份先序列化成字符串再落盘、还原后回拨序列、谓词把时间下界写进 SQL；⑧ `taskStalled` 有 `onPollRecovered` 收尾；⑨ 预览失败时页面要有一句"执行清理为什么被按住"；⑩ `/api/bloggers/top` 补 3 条路由级用例钉响应形状（弹窗读的键 + ≥5 存活门槛 + 软删不凑数）。**事故已结（任务 #42）**：老板点头后 `--apply --confirm CLEAN-TEST-ROWS` 删掉 18 行，逐行回执 + 备份 `backup/purge-test-rows-20260923-115305.json`（18 行扁平列表，UTF-8 落盘 9764 字节 / 9394 字符，回读可解析）。**`backup/` 在 `.gitignore` 里** ⇒ 还原载荷另存了一份随仓库走的副本 `docs/迭代计划/run-20260923-test-rows/purge-test-rows-20260923-115305.json`（同字节 9764；`--restore-from` 指哪一份都行）；入库前按 `api_key|password|secret|token|eyJ|postgres(ql)://` 扫过这 18 行与同目录四份 TSV，命中 0（那一份 `system_config` 只是我那条 `nav_backfill_proof:TEST_BF` 凭据行，不含密钥）；只读复核 `bloggers 27 / posts 657 / fund_info 162`（与 09-22 记录一致）、`T-基金列表体检博主` 与 `nav_backfill_proof:TEST_BF` 各 0 行。基线（默认 locale，2026-09-23 12:3x 北京）：见下一节实测行 |
| 第 34 轮两份独立复评 | **74 / 78**（都可复现，取低分 **未过 90**；轨迹 72 → 78/84 → 80/80 → 83/84 → 62/74 → 74/78）。两份共同抓到：① 上一批给 `createViewpointManager` 注入的 `isServiceDown` **漏了**（观点页那一族失败态形同虚设），而这类"解构到 `undefined`/options 漏注入"的错**没有任何机器闸盯着**；② 停摆横幅 `taskStalled` 是单个 `ref` ⇒ 一条任务恢复会把另一条的警告擦掉（B-MAJOR-6）；③ "取到数据但刷新失败"的按钮把成功说成失败（B-MAJOR-5 / B-MINOR-11），老板会再点一次；④ 列表失败态靠"条数变了才清"的 `watch` 清，取到但条数没变时永不触发（A-MAJOR-4）。另：静态资源整体 `max-age=86400` ⇒ 部署后旧 `*-manager.js` 配新页面**静默**缺数（B-F-1）、TOP 空榜写成"暂无数据"（B-MAJOR-3）、翻页失败页码不回退（B-MINOR-15）、微信批量抓取的收尾刷新没包住会永久卡在"抓取中…"（A-MINOR） |
| 第 34 轮返修（已做，待复评） | ① **新增双向接线闸** `tests/unit/test_frontend_wiring.py`：页面 `setup()` 解构的每个名字必须是 manager 真导出的，manager 读的每个 `options.*` 必须被调用方注入 —— 两头各一条参数化用例 + 一条"闸自己不空转"的用例，`isServiceDown` 那一族从此藏不住；② `taskStalled` 改 `reactive({})` **按任务分槽** + `stallText` 汇总话术，`onPollRecovered(label)` 只擦自己那条；③ 三个 manager 的写操作分成三种话术：真失败 / "已改成，只是列表没刷新出来 —— 刷新页面即可" / 翻页失败把 `filters.page` 退回原值；④ 死 `watch` 删掉，失败态一律由**取数点自己** `options.onFetchFailure(key, msg)` 报（成功传空串），判据改成**用 node 跑 manager 真实源码**喂四种形状；⑤ 静态缓存策略从"按后缀"改成"**按是谁的**"：只有 `vue.global.prod.js` / `axios.min.js` 缓存一天，页面、`common.css`、三个 manager 一律 `no-cache, must-revalidate`（含 `/`、`/index.html`、`/import-data.html` 三条 `FileResponse` 路由），`TestClient` 逐条实测响应头；⑥ TOP 空榜写明门槛"至少 5 条已验证结论"，命中率/加权评分两列的分母写进表头正文；⑦ 变异体检：CONTROL 对照跑整份判据文件、退码非"断言失败"一律记 `HARNESS-FAIL`、每处变异**永远从干净底本出发**（不再叠在上一个变异之上）、`--only` 可单点复现；⑧ `purge_test_rows_from_prod.py` 与备份/还原那条链按第 33 轮清单补完并落了 18 行删除回执（任务 #42）；⑨ **把服务真停掉再点页面**照出来的新缺陷：洞察拉取失败时四张卡只有第四张被 `insightsLoaded` 守住，另外三张还挂着上一轮的真数（红字说"没取到"、卡上是 3/2/0）⇒ `fetchInsights` 的三种失败形状现在都会清空 `viewpointInsights`，判据跑 `loadViewpoints` 真实路径 + 三处变异。**已核验（本地镜像 8017，一次性口令，2026-09-23 14:0x 北京）**：统计卡 `27/657/1616(506待验)/71/242`、待清理卡未取到时 `—`；证据灰字 `已判 1110 / ⚠ 197（17.75%）/ 区间 43.96%~61.71% · 本地镜像库 · 截至 2026-09-23`；TOP 弹窗表头口径、分子分母 `66.7%（4/6）`、`接口自己的口径` 全部在正文里；断网后添加博主弹的是「添加失败: Network Error」（没把已存下的说成没存下），观点页红字「服务连不上（可能在唤醒）」且卡面随之清空。⑩ **同一族还有一面：取不到时旧数据不许冒充新数据** —— 帖子/预测/观点三条分页条现在统一说"条数没取到，下面是上一次取到的"（观点那条以前压根没有失败分支：71 行旧数据 + "共 71 条"配一句都没有），判据在 `test_every_list_page_shares_the_same_honesty_rule` 里按三条腿循环，另配变异。**变异体检本轮全量重跑**：末行 `共 73 处变异，覆盖 29 条判据`，逐条 RED、0 GREEN、0 HARNESS-FAIL，四个文件跑完逐字节还原（`--list` 那条命令为准，别抄这里的数）。**任务 #43（JS 版本戳）由⑤ 取代**：没有构建链的项目里，服务端说了算比给文件名加哈希更直接 |
| 第 35 轮两份独立复评 | **75 / 84**（都可复现，取低分 **未过 90**；轨迹 72 → 78/84 → 80/80 → 83/84 → 62/74 → 74/78 → 75/84）。A 的三条 MAJOR 全在"同一族还漏了文件"：① `viewpoint-manager.js` 整族没修（`deleteViewpoint`/`batchAnalyzeViewpoints`/`executeSummary` 写成功却报失败、`fetchSummaryStats` 静默留旧值），② `prediction-manager.js` 的 `savePrediction`/`executePredictionMaintenance` 同形，③ **破坏性按钮活过了自己的预览**：`previewPredictionMaintenance` 不查 `success:false`、抛错也不清 `maintenancePreview` ⇒ 红色「确认执行」挂在上一轮的清单上。而我把上一轮的判据写成**文件级 substring** ⇒ 一个文件里改对一处就整文件通过，且 `--list` 里没有任何变异打向它。B 的三条：① `POST /api/config/sector-mappings` 创建路径的"只允许基金不允许股票"**零覆盖**（删掉那 7 行判据，49 条用例照样绿），② 文档里被当唯一出处的命令 `--impact-against data/_old_map.py` **今天 FileNotFoundError**（`data/` 不入库），③ 两处文档对静态表条数各说 122 / 109。另 7 条 MINOR 是"数字没绑命令/已过时"（含 `emit_t3_goldset_seed.py` 默认往**已入库**的金标种子 CSV 写 ⇒ 谁按文档裸跑一次就把核对基准销毁一次） |
| 第 35 轮返修（已做，待复评） | ① 把"写成功不许说失败"从文件级 substring 改成**逐函数扫描闸**（`_arrow_fns` + 花括号配对：凡是"写之后还有刷新腿"的函数，刷新腿必须各自包 `try{…} catch (refreshError)`；再断言"至少看见 8 个"防闸空转）—— 它当场自己揪出 3 个我没列进去的：`cancelAnalysisJob`、`batchAnalyzePredictions`、`pollAnalysisJob`。本轮实修 **8 处**（`pollAnalysisJob`/`cancelAnalysisJob`、`savePrediction`/`batchAnalyzePredictions`/`executePredictionMaintenance`、`batchAnalyzeViewpoints`/`executeSummary`/`deleteViewpoint`）；我提交说明里手写的"7 处"是数错了，这条闸现在一共管着 **10 个**"写后刷新"的函数（复核：`python -c` 用同一段正则数 —— 见 `test_a_write_that_succeeded_is_never_reported_as_a_failure` 里 `seen >= 8` 那句，把阈值临时改成打印就能看到 10）；② `previewPredictionMaintenance` 先清旧预览 + 把 200+`success:false` 当失败，配**因果判据**（node 跑真实源码，断言"预览失败之后手里没有可执行清单"）+ 2 处变异；③ 翻页两条腿都退页码（软失败以前不退），判据同样改成逐函数，配 3 处变异；④ 创建路径补 3 条路由级用例，并**逐条用变异证明会红**（删门 ⇒ 2 条红；注入 `reviewed_by='owner'` ⇒ 第 3 条红）；⑤ `fetchSummaryStats` 加"取到了吗"旗标，取不到时弹窗不再说"没有待汇总的观点"；⑥ 静态缓存那条 A 问的"流量代价"已量：自有资源 gzip 后一次开页约 **79 KB**（免费层要约 130 万次开页才用完）⇒ 不是决定项；⑦ 文档数字全部绑命令：上一版静态表钉成 `tests/fixtures/sector_map_before_round27.py`（重跑得回 31 板块 / 78 条 / 4.8%）、122→109、105→2、30+→27、13→11、`:474`→`:489`、金标种子默认改写到不入库的 `data/`；⑧ 脚本守卫去掉"且直连 ORM"这个前置（量过：加宽后新增漏网 0，但以后只走 service 层的写脚本再也糊不过去），并加一条反退化断言 `受管数 ≥ 直连ORM数`。**新记 3 个待办**：#49/#48 已做完，#51 生产侧 ⚠/区间无可跑命令、#52（已做）。（另记一次诚实的自查：`viewpoint_delete_blames_the_write` 第一版报 GREEN，是因为我的锚点从外层 `try {` 起、把 `axios.delete` 一起吃掉了 —— 删掉证据本身当然不会红；改成从注释行起切后为 RED。全量重跑 **81 处 / 32 条判据：逐条 RED、0 GREEN、0 ANCHOR-MISS、0 HARNESS-FAIL**，四文件逐字节还原。基线（默认 cp936，17:01 北京）：`tests/unit` 929 / `tests/` 938，均 16 skipped、0 failed。**浏览器实测**（8021，断网法）：健康态 `27/657/1616(506待验)/71/242` + 翻页条走"共 N 条"分支；停服后点「预览映射同步」⇒ 页面出现红字「预览失败：Network Error —— 没有可信的预览，"确认执行"不会出现」+「重新预览」，且**红色执行按钮确实不在**（DOM 查 `hasExecute:false`），控制台只有那一条预期的连接被拒） |
| **门禁线变更（2026-09-23 18:3x 北京，老板指示）** | 老板原话："降低标准为八十分吧，因为我看你卡住了，允许适当降低标准" ⇒ **成功线从 90 改成"两份独立复评取低分 ≥80"**，其余纪律不变（两份都可复现、低分绑定、不采信不可复现的报告）。变更前后对照：第 36 轮 **82 / 78** ⇒ 取低分 78，**仍差 2 分未过新线**（此前轨迹最高 84）。这条变更只动门禁，不动"生产写入必须只读预检→dry-run→显式确认→逐行回执"那一条 |
| 第 36 轮两份独立复评 | **82 / 78**（都可复现；轨迹 72 → 78/84 → 80/80 → 83/84 → 62/74 → 74/78 → 75/84 → 82/78）。A 四条 MAJOR 全指向"判据自己不算数"：① 上一批新加的 `summaryStatsLoaded/summaryStatsError` **零判据**（把话术退回"没有待汇总的观点"、把取数退回只写成功，32 条判据全绿）；② 逐函数闸只看结构不看 catch 里说了什么（把刷新失败的话改成"保存失败"照样绿）；③ 刷新腿是**白名单**（把 `fetchPredictions()` 改名 `loadEverything()` 就隐身），且 `seen >= 8` 比实测低 2；④ 模板那半句"确认执行不会出现"仍非机器闸。B 三条 MAJOR：① `POST /sector-mappings/seed` 整条后门（不看退码就报成功 + 脚本无条件 `reviewed=True`、直改 `fund_code` 绕开 `retag_prediction`）；② `batch-review` 路由级 `owner_confirm` 转发零覆盖；③ `seed_owner_proxies.py` 这条 owner 豁免来源零用例、AGENTS"三条写入路径"漏数它 |
| **提交说明的自我更正（记两次了）** | `4125834` 的说明里列了"§7.2 补三个选法"，那件事其实在 `7a855c8`；`b177772` 的说明里第 4 条（AGENTS"222 映射"补口径、§1.7 的 9 天/136 条→10 天/141 条）也**不在该提交里**，它同属 `4125834`。两次的成因一样：**凭记忆列条目**而不是照 diff 列。以后提交说明写完必须与 `git show --stat -p` 逐条对一遍再发 |
| 第 36 轮返修（已提交 `77fa10e`，其 M-1 的续修在 `dd125cb`） | ① **seed 后门**：路由现在要 `ENABLE_SECTOR_SEED_IMPORT=true` + `X-Danger-Confirm: seed-sector-mappings`，**看子进程退码**、脚本不打 `SEED-RECEIPT` 就按失败处理，并把 cwd 少退一层那个 bug 修掉（旧 cwd 指向 `src/` ⇒ 脚本永远找不到 ⇒ 那条路由**每次都在说谎**，实测）；脚本语义改成**只补缺、不覆盖**、新行 `reviewed=False` + `match_source='seed_builtin'`、真写要 `--confirm SEED-MAP`。判据 `tests/unit/test_sector_seed_route_honesty.py` **10 条**。镜像上量过的爆炸半径写进 `docs/模块总览/板块与基金匹配.md`：163 键 / 新建 128 行全 `reviewed=True` / 21 行改码（15 行已审查、8 行署名 agent）/ 2 个码查无档案。② `batch-review` 路由级用例 1 条（转不转发 `owner_confirm` 现在会红）+ `verify-fund` 探针路由 1 条；③ `seed_owner_proxies` 的 `--owner-confirm SEED-PROXY` 闸 5 条用例（含"闸排在钉库之前"），AGENTS 那句改成"页面上三条 + 脚本一条，别说成只有三个入口"；④ 写后刷新闸：腿从白名单改成**任何 await**、catch 的**话**也要判（有"失败"却没有"没刷新"即红）、扫描范围扩到 `web/index.html`（callee 表跨整个页面包，因为 manager 与页面互相调用）、`seen` 从 `>= 8` 改成钉死 12 / index.html ≥ 19；当场抓出 `cleanupData` 一条真腿（`pollCleanupTask` 会 reject ⇒ 清理已发起却报"清理失败"＝诱导二次删除），已按同族修法单独包起来；⑤ `summaryStatsLoaded/Error` 补 node 行为判据（三种失败形状各自先跑成功），过程中**又抓出一个真缺陷**：`success:true` 但 `data` 为空被当成"取到了" ⇒ 弹窗会说"没有待汇总的观点"，已修；⑥ 模板祖先链闸（「确认执行」的祖先 `v-if` 必须含 `maintenancePreview`；红字那行必须有「重新预览」+ `@click` + 说明"为什么按钮不出现"），不需要 Chromium；⑦ 体检的 `_apply` 改成**全部命中都改**（旧写法只改第一处，三条 pager 名字说两条腿实际打一条 = A-MINOR-1），`--list` 现在自己打印会改写到哪些文件（docstring 不再手抄名单），`previewPredictionMaintenance`/`executePredictionMaintenance` 的判据从 `=== false` 改成 `!== true` 并补喂"回执没带 success 字段"这一形状。⑧ 任务 #53 的更正：我说 index.html"15 处说假话"是**只看形状没看 callee**，实测只有 1 处真会 reject（其余 callee 自己吞错）—— A 的读法是对的，我的数撤回，改成由闸来判。⑨ S6 前置证据：垃圾码"删 + 一键还原"在镜像副本上真跑过（§7.2b），生产侧复量与"先回写后删码"的顺序结论在 §7.2c |
| 第 37 轮两份独立复评 | **B=79 / A 未交卷**（B 可复现；A 席跑到一半被我叫停，因为那一批的回合数用尽、不能再让两个评审同时改测同一份 `web/`）⇒ **本轮没有"两份取低分"可算，门禁未达**。B 的清单：MAJOR-1 seed"只补缺"那条路**今天一行都写不进库**（外键 + 2 个码查无档案 ⇒ 单事务整体回滚，回执却报"新增 126"）—— 已修（`dd125cb`，补外键预报与逐行跳过）；MAJOR-2 回执契约只用一份手抄常量测（改键名 10 条用例仍绿）；**MAJOR-3 `scripts/run_migrations.py` 能对生产发 DDL 却被守卫判成"已声明"**；M-4 `AGENTS.md` 里"这条闸没有用例钉着"已成反话；M-5 模块总览用现在时描述已删掉的行为；MINOR-1 `n >= direct` 是空判；MINOR-2 "它连 `--dry-run` 都没有"这句注释过时 |
| 第 37 轮返修（本批，待复评） | ① **M-3 的真相比报告更糟**：守卫不是"判它已声明"，而是**旧判据压根扫不到它**（它既不带写开关、也不 `session.commit`），而放行它的是 `os.environ["DATABASE_URL"] = …` 这一条 —— 那个信号**不看方向**，赋成生产也算"有守卫"。现在扫描多了一路 AST 信号（`from alembic import command` + 真调 `command.upgrade`），并且**发 DDL 的脚本不认"赋值 DATABASE_URL"**，只认钉镜像 / 自报库名 / 见远程就拒跑；用例 `test_renaming_the_database_url_env_is_not_a_guard` 把这条缝钉住（把自报行删掉 ⇒ 2 条红）。② `run_migrations.py` 第一行自报：`[库] 本地镜像库（sqlite）—— alembic upgrade head 会向它发 DDL`（副本库真跑过；**它仍没有 dry-run、也没有"见远程就拒跑"**，因为 `render.yaml:11` 每次启动都在跑它，要加得改部署配置 ⇒ 见 §6 决定项）。③ **顺带照出一个更普遍的真 bug**：`database_label()` 只认 Session —— SQLAlchemy 2.0 起 `Connection` 没有 `get_bind()`，那个 `except` 把异常吞成"未知库" ⇒ 自报行自己说谎。现在 Session / Engine / Connection 三种都认，4 条用例钉（`tests/unit/test_database_label_targets.py`）。④ M-4/M-5/评分记录 ≥90 三处过时文字已改；MINOR-2 那句注释已改；MINOR-1（`n >= direct` 空判）**没动**，记在任务里。⑤ 基线：`tests/unit 955 / tests/ 964`，0 failed（2026-09-23 20:15 北京，默认 cp936 口径）；顺带把一条**环境级 flake**与真回归分开：满负载时 Windows 会把子进程打死（`0xC0000374`、输出全空），只对这种退码重跑，脚本自己返回 1 不许重试（A/B 已证明与本轮改动无关） |
| 第 38 轮两份独立复评 | **A=78 / B=76 ⇒ 取低分 76，仍未过新线 80**（轨迹 … 36:82/78 → 37:B=79/A 未交卷 → 38:78/76）。B 的 BLOCKER：`alembic/env.py:14-18` 把 `.env` 的生产串悄悄顶进 ini ⇒ 文档推荐的裸 `alembic upgrade head` / `downgrade …` 本地一跑就是对生产发 DDL（那支 downgrade 会 drop 审计台账表）；MAJOR：`push_sector_mappings_to_prod.py` 的预检判据 `nav_priced_here is False` 让"旧版服务端没回这一列"过了闸。A 的 MAJOR：① 帖子/预测两个列表页的 12 个数在"没取到"时仍报 0（而上一版判据的正则写死 `<div class="value">`，span 形状结构上看不见）；② 我上一轮自己写的 `/verify-fund` 用例给探针发明了真接口没有的键 ⇒ 同义反复；③ `seed_owner_proxies.py` 从不读探针的 `ok`，取不到也照写 `is_fetchable=True` + 空名幻影档案。两份共同：回执契约两端各测各的（`缺档案` 已被路由丢掉，10 条仍绿）。A 另指 B 的"未入库脚本"一条被我复核为**高估**：`scripts/_tmp_*` 全部未跟踪（`git ls-files scripts/ | grep -c _tmp` = 0）⇒ 降级为 MINOR |
| 第 38 轮返修（本批，待复评） | ① **env.py 不再按"有没有赋值"判断方向**：远程目标必须显式 `ALEMBIC_DATABASE_URL`，否则 `[abort]` 并只打 scheme（不打口令）；本地 sqlite 继承保留、`scripts/run_migrations.py`（Render 启动路径）实测不受影响。判据 4 条 `tests/unit/test_alembic_target_direction.py`（把拒跑条件改成恒假 ⇒ 当场红，实测）。② 守卫加宽两处：`--confirm` 即算写操作（这条**当场抓出** `push_sector_mappings_to_prod.py` 是全仓唯一往生产 POST 却不受管的脚本）；DDL 动词集从只认 `upgrade` 扩到 `downgrade`/`stamp`/裸 CLI；空判 `n >= direct` 换成"第二个证据源"交叉核对（正文带写开关 ⇒ 至少占一样）。③ 回写预检改成 fail-closed：回执缺 `nav_priced_here` 或答的行数对不上 ⇒ 退码 3、一行都不发（2 条新用例 + 变异实测）。④ `seed_owner_proxies.py` 现在读 `ok`：取不到 ⇒ 跳过、不造 `fund_info` 幻影行、有跳过就退码 5；并把"桩的键必须来自真返回值"钉成 AST 判据（我上一轮那条同义反复的用例已重写：现在断言路由原样透传 + 页面读的确实是 `d.ok`）。⑤ 两个列表页 12 个数全部改成 `viewErrors ? '—' : numOrDash(...)`，`postMeta/predictionMeta` 初值从 0 改成 null；三个预测队列的口径从 `title` 搬进正文一行灰字（新增 2 条判据 + 6 处变异全 RED）。⑥ 健康态在真浏览器（镜像服务 + 一次性口令）看过：`全部 (1616) / 待验证到期 (88) / 未到期 (418) / 待验证 (506) / 已验证 (1110) / 正确 (573) / 错误 (537) / 回收站 (562)` 与口径行都在；**但"冷启动失败态"没能在浏览器里等满 90 秒唤醒窗口** ⇒ 那一路只有 node 行为判据 + 6 处变异作证据，不说"页面实测过"。⑦ 文档账：`AGENTS.md` 那段"为什么不用 run_migrations（它从 base 跑）"改成事实（生产当时没有 `alembic_version`，现在 stamp 过了）、`DEPLOYMENT.md:172` 那句"Render 启动不会自动执行 Alembic"公开更正、`评分记录.md` 尾注按 ≥80 新判据重写并把手抄提交数换成命令。基线：`tests/unit 967 / tests/ 976`，0 failed（22:18 北京，cp936 口径）|
| 第 39 轮两份独立复评 | **A=81 / B=79 ⇒ 取低分 79，差 1 分未过线**（轨迹 … 37:B=79 → 38:78/76 → 39:81/79）。A 确认三件事都修对了（独立复跑基线 967、把 12 个数与 6 处变异逐条打红）。丢分集中在**"改完没扫干净的第二现场"**：B-MAJOR-1 = 上一版方向闸只判"ini 是否等于默认镜像串"，换一份 `alembic.ini` 或 `-c` 第二个 ini 指远程 ⇒ 整道闸连同自报静默失效（实测那条真去连了 `evil.invalid`），而"被批准的显式远程路径"0 自报 0 确认（同仓 `sync_db_columns.py` 动远程要三道旗子）；B-MAJOR-2 = `seed_owner_proxies.py` 只挡了 `ok=False`，没挡**"取到净值但取不到官方名"**（探针自己有这条文案，场内 ETF 常见），空名行照样盖 owner 豁免；A-MAJOR-1 = 同文件 `DEPLOYMENT.md:225` 仍把"不得把迁移绑到 Render 启动命令"写成现状（与 :181、`render.yaml:11` 三方打脸）；A-MAJOR-2 = **证据机器自身可以空跑满分**：`--only` 打错字 ⇒ 0 处变异、CONTROL 全绿、退码 0，且顶部 `import argparse` 从未调用（`--help` 会让它整套开跑改写 `web/`）。 |
| 第 39 轮返修（本批，待复评） | ① `env.py` 改判**最终解析值**：远程要么由调用方交连接（Render 启动路径实测 `rc=0` 不受影响），要么**同时**给 `ALEMBIC_DATABASE_URL` + `ALEMBIC_ALLOW_REMOTE=1`；放行的分支必须往 **stderr** 打 `[库]`（不脏 `--sql` 的 stdout）。八种形状实测 + 8 条判据（含"一个环境变量解不开第二道旗子""第二个 ini 指远程也拒""拒跑与放行都不许泄露口令"）。② `seed_owner_proxies.py` 补 `ok=True 且 api_name=''` 那一格（同样跳过、退码 5、不造幻影档案）。③ 体检 CLI 换成真 argparse：`--only` 匹配不到 ⇒ 非 0 退码（"满分通过一次什么都没测的体检"这条路堵死）、未知参数退码 2、`--help` 正常。④ 回写预检分开"服务端早期已拒收这行（带 `reason` ⇒ 剔掉继续、计入剔除数）"与"对端压根没回这一列（旧构建 ⇒ 整批退码 3）"。⑤ 守卫三处：解析失败的文件 **fail-closed 按能写处理**（旧兜底 dict 缺 `consts`/`raised` ⇒ 扫描器先 KeyError；这条是我新写的判据当场抓出来的）；`_` 前缀不再是整族豁免（只豁免 `_db_guard.py`，扫描面 44→51）；新增 HTTP 写与 `os.system` 起 CLI 两个触发器 + 合成判据（今天 0 实例，没判据就是死逻辑）。⑥ 未入库探针草稿 `_tmp_check_import_progress.py` 以前裸 `create_engine(os.environ["DATABASE_URL"])` 连生产，现在默认钉镜像、查线上要显式 `--production`。⑦ 文档：`DEPLOYMENT.md:142` 改条件表述、`:225` 公开标成**尚未执行的策略**并指向 §6/#57；`模块总览/板块与基金匹配.md` 预检段同步 fail-closed 并删掉手抄的"（4 条）"。⑧ 口径进正文那条判据补 2 处变异（`--only _title` / `--only caliber_note` 全 RED，`web/` 逐文件回读比对一致）。基线 `tests/unit 976 / tests/ 985`，0 failed（22:33 北京，cp936 口径）|
| 第 40 轮两份独立复评 | **A=78 / B=79 ⇒ 取低分 78**（轨迹 … 38:78/76 → 39:81/79 → 40:78/79）。A 抓到的三处都在"判据自己可以空转"这一族：A-M1 一条判据里缺"把符号反过来 / 把条件取反"这种极性对照（那一族只在正向成立的判据，从这一轮起逐条补反向锁）；A-M2 守卫的两条新判据依赖"我这台机器上恰好有 7 个未入库的 `_tmp_*.py`" ⇒ 干净克隆上必红（改成临时目录现造文件）；A-M4 "每条往下走的分支都报名"只用 `src.count('[库]') >= 2` 撑着，而 abort 文案里也有一句 `[库]` ⇒ 把本地那条自报整段删掉计数照样够。B 的 MAJOR-1 = 上一版远程放行**只要一道旗子**、目标还能完全来自 `.env`（那正是第 38 轮那条 BLOCKER 的机制本身，且与我自己在 abort 文案里写的"两道旗子"相反）；B-MAJOR-2 = §6 第 11 行那个"给 3 个替代码补档案"的 A 方案**今天根本执行不了**（生产净值停在 09-13）⇒ 本轮补 ⓪/D 并由老板定了 C |
| 第 40 轮返修（本批，待复评） | ① `env.py` 的远程放行改成**两道旗子**（`ALEMBIC_DATABASE_URL` 说出 + `ALEMBIC_ALLOW_REMOTE=1`），从 `.env` 继承来的远程一律拒 —— 那正是第 38 轮那条 BLOCKER 的机制本身，也与我自己在 abort 文案里写的"两道旗子"相反。判据按形状重跑 8 条（含反向对照：真目标印出来、口令一个字符不出现）。② `push_sector_mappings_to_prod.py` 预检改 fail-closed：缺 `nav_priced_here` 且带 `reason` ⇒ 剔该行继续；**没 reason**（对端压根没这列＝旧构建）或行数对不上 ⇒ 退码 3 一行不发；整批被剔完时不再"真写 0 行还报完成、退码 0"；`dropped` 从覆盖改成累加（两路同时剔时以前少报）。③ `seed_owner_proxies.py` 补"探针说取不到 ⇒ 不写"那一格，且 `ok=True` 但官方名为空同样不写。④ 守卫扫描面 44→51：`_` 前缀不再整族豁免（只豁免 `_db_guard.py`），新增 HTTP 写/起 CLI 两个触发器 + 现造文件的机器无关判据（第 40 轮 A-M2 抓到上一版判据依赖"我机器上恰好有 7 个 `_tmp_*.py`"，干净克隆上必红）。⑤ 基线 `tests/unit 984 / tests/ 993`，0 failed（cp936 口径） |
| 第 41 轮两份独立复评 | **A=77 / B=79 ⇒ 取低分 77**（连续五轮卡在 76-81）。两份第一次给出同一条**结构性**诊断：剩下的不是业务逻辑，是 ①"枚举式承诺"没有单一真源（每轮的 `N 条`/`N 处` 都在漂），② 读侧从没被当成与写侧同级的攻击面，③ "失败必须把旧数据放下"这一族只在做逐处补丁而不是一把通用闸。A 的 M-1/M-2/M-3 全是第 40 轮那三条的**第二现场**（自报措辞按分支分叉、博主榜失败态、"提交说明里的改动没进 diff"）；B 的 MAJOR-1 = 三个 L3/L1 分析脚本 `create_engine(os.getenv("DATABASE_URL"))` 默认连生产，而守卫扫描只判"能不能写"所以从不问它们连哪儿，自报还只印变量名不印机器名 |
| 第 41 轮返修（本批，待复评） | 按那条结构性诊断改，不再逐处补：**① 读侧进同一把门** —— `scripts/_db_guard.py` 新增 `resolve_read_target()`（定库不建连接）与 `read_only_connect()`（引擎级只读 + 真试一次写；pg 走 `postgresql_readonly`+临时表探针、sqlite 走 `file:…?mode=ro&uri=true`），自报**机器名**（`machine_name()` 剥掉口令）；三个肇事脚本改走这把门，`backtest_l1_weighting.py` 另把 `import src.*` 挪到定库**之后**（`src/services/l1_weighting.py:16` 会拉起 `src.models.database` ⇒ 导入那一刻全局 engine 就按 `.env` 的生产串建好了，之后再 pin 救不回来）。守卫新增 `engine_from_env` 触发器 + "顶层 import 可达 `src.models.database` 必须排在定库之后"（可达性判据，两侧都有控制断言防空转）。**② 文档条数账上机器** —— 新工具 `scripts/audit_doc_claims.py`（尺子 = `pytest tests --collect-only -q`；当场账对表、增量账必须写成 `+N 条`/`新增 N 条`）+ 闸门 `tests/unit/test_doc_claims.py`；实测抓到并改掉 2 处漂掉的数。**③ 失败态通用闸** —— `test_an_error_notice_must_be_reachable_while_its_list_is_still_on_screen`：凡 `XError` 与列表 `X` 同名的一对，红字必须写在**列表非空那条分支**里（今天命中 1 对：博主榜；范围在判据 docstring 里写明，不念成"全页都钉住了"）；`fetchBloggers` 另加 `bloggersStale` 数出"挂着几位"，`deleteBlogger` 补四种结局。三处各配变异（`bloggers_notice_moves_back_into_the_empty_branch` / `stale_rows_never_counted` / `refresh_death_blamed_on_the_delete`，`--only` 各自 RED）。**④ 桶分开了** —— 审计回写里"定不了价"的计数挪到写入判定**之后**（写失败的行以前同时进 `refused` 与 `no_nav_priced_in_this_db`，而回执那句是"本次已照样写入"）+ 反向对照。**⑤ 剔除数一把尺**（`push_...to_prod.py` 两路都按行数计）。**⑥ 豁免来源从"三条/四条"改成五条全列名**（第 5 条 = `purge_junk_funds.py --restore-owner-immunity`）。**⑦ 一条评审意见复现不了**：B41 MINOR-1 说的"证据弹窗永远说没有核验证据"—— 全仓没有那个弹窗（`verification_evidences`/`核验证据` 在 `web/` 与 `src/api/routes/` 均 0 命中），记为不适用、没照着改。**基线：`pytest tests/unit -q` 1000 passed / 16 skipped / 0 failed（223.34 秒）；`pytest tests/ -q` 1009 / 16 / 0（221.09 秒）**（2026-09-24 10:2x 北京，默认 cp936 口径，子进程显式 utf-8）；`audit_doc_claims.py` 退码 0；页面**健康态**在真浏览器（镜像服务 + 一次性口令）复看过：27 位博主、证据行"· 本地镜像库 · 截至 2026-09-24"、两列口径说明都在；**失败态那条横幅没能在浏览器里亲眼看到**（唤醒窗口 + 合成点击被弹窗挡住），所以只报"node 执行真源码 + 3 处变异 RED"作证据，不说"页面实测过" |
| 第 42 轮两份独立复评 | **A=82 / B=78 ⇒ 取低分 78**（轨迹 38:78/76 → 39:81/79 → 40:78/79 → 41:77/79 → 42:82/78；五轮都在 76-82，最高分首次到 82）。A 席抓到：MAJOR-1「条数尺子管不到中文数字与编号列表，`AGENTS.md` 当场两条就数错」、MINOR-1「把一条**不可达**的防御分支算成删除博主的第四种结局」、MINOR-3「受检集合仍跟着本机磁盘上那些未入库 `_tmp_*.py` 走 ⇒“我这台机器全绿”≠“这个仓库全绿”」、MINOR-4「`--fix` 只代改条数却不报它没改什么」。B 席抓到：MAJOR-1「一处弹窗证据机器查不到对应实现」（**不适用**：`核验证据`/`verification_evidences` 全仓 0 命中，报告描述的对象不存在，已记在 `docs/迭代计划/评分记录.md` 而不是照单改）、MAJOR-3「读侧触发器漏了“从 `src.core.config` 拿 URL 再自建 engine”这条形状」、MAJOR-4「钉库顺序只比**顶层** import 的行号 ⇒ 函数体里那 100 多处判不了」、MINOR-5「两个 L3 脚本没有 argparse ⇒ `--help` 会把整份报告写进 `docs/`」、MINOR-6「4 行已入库的 `数据源：\`DATABASE_URL\``」、以及 (c)「`database_label()` 不印机器名，与 `_db_guard.machine_name()` 是两条没绑在一起的尺子」 |
| 第 42 轮返修（本批，待复评） | 按两份报告共同的"别再逐处补"改：**① 钉库顺序交给运行期**（B-MAJOR-4）—— `pin_local_sqlite()` 一进函数就问 `sys.modules`：全局 engine 已建在**非 SQLite** ⇒ `[abort]` 退 4 并报出目标（口令不泄露），已建在 SQLite ⇒ `[警告]` 说清"写的就是这个文件"再放行（`tests/conftest.py` 正是这一档，一律 abort = 把闸门建成墙，会打死 8 条正经用例）；AST 那条顶层顺序判据保留，两档各有一条子进程用例 + 一条"先钉库必须放行"的控制断言。**② 库名要认得出个体**（B-c）—— `database_label()` 印成 `本地镜像库（data/fund_insight.db）` / `本地 sqlite 文件（不是镜像库）：…` / `内存 sqlite（不落盘…）`，`purge_junk_funds.py`（S6 那把硬删）的 `[target]` 同步；src 侧与守卫侧两份 `machine_name` 不能互相 import，故钉"逐条相等"（10 个样品）。**③ 文档的第二把账＝出处**（A-MAJOR-1 / B-MINOR-6）—— `audit_doc_claims.py` 认中文数字，并新增"`docs/*.md` 每行 `数据源：` 必须认得出具体目标，或明写未记录 + 给复现命令"；4 行旧报告（L3 两处 / L1 / 校准表）已改成承认未记录；`--fix` 现在还要打印"它没改哪些账"。**④ 两个 L3 脚本补 argparse**（B-MINOR-5，与第 39 轮同源）：判据是"跑完 `--help`/坏旗子之后 `docs/` 一个字节都没变 + 坏旗子退 2"。**⑤ 受检集合改由 `git ls-files` 定义**（A-MINOR-3），并配"被排除的文件必须全是草稿/守卫本体"的控制断言。**⑥ 前端那族**（A-M2 / A-MINOR-1 / B 的 class 洞）：博主榜失败横幅搬进"列表非空"那一支、`bloggersStale` 数出挂着几行、`deleteBlogger` 四种结局改由真实状态驱动（先证明那条分支可达再断言它说话）、新增"页面用到的 class 必须定义过"闸。**实测**：`tests/unit` **1019/16/0**（262.94s）、`tests/` **1028/16/0**（260.19s）；前端变异体检全套 104 处逐条跑过（CONTROL-GREEN + 101 RED + **3 处 ANCHOR-MISS**）—— 那 3 处是**我自己的锚点烂了**（翻页条早已从 `total \|\| 0` 换成 `numOrDash(...)`、`setup()` 导出名单加了 `bloggersStale`），修锚点后各自复跑为 RED；本批另抓到尺子一处误报：`backtest_l1_weighting.py` 被切成 `test_l1_weighting.py` 判成"收集不到的文件" ⇒ 名字加了标识符边界并配一条"真承诺仍认得出"的控制。**浏览器核验**：博主榜失败横幅在真页面看过（注入 `axios.get` 拒绝；横幅 color `rgb(212,107,8)`、`stale=16`），上一批"没亲眼看到"的诚实边界已作废 |
| 第 43 轮两份独立复评 | **A=81 / B=66 ⇒ 取低分 66**（轨迹 39:79 → 40:78 → 41:77 → 42:82/78 → 43:81/66）。A 席首次上 80 但 B 席大跌，两边共同指回同一族：**守卫信号仍能被"说明文 + 样板句"白送**（A-MAJOR-1：docstring 就是 `ast.Constant`，`raise SystemExit(main())` 是入口样板 ⇒ 三个识别器一起点头，而被绕过的那条判据自己的 docstring 写着"写在 docstring 里不算"）；B 席则把"落笔能力只按 SQLAlchemy 建模"列成 **12 类里 10 类三道判据一条不响**（`create_all` / 裸 SQL DML / `sqlite3.connect` / `to_sql(replace)` / 覆盖 `.db`·`.env` / 子进程借道 alembic / `requests.request('DELETE')` / 别名 `create_engine as ce` / 函数体内 `SessionLocal` 纯读），并给出 BLOCKER：`run_migrations.py` 没有 argparse，`--help` 就是真发迁移。
**⚠ 一次真实的生产触碰（评审员自述，我当场只读核对）**：B 席在读源码之前跑了 `python scripts/run_migrations.py --help` 与 `--totally-bogus-flag`（当时**没有** `ALEMBIC_DATABASE_URL`）⇒ 按 `.env` 连上生产 Supabase 并执行了 `upgrade head`。事后只读复核：`select version_num from alembic_version` = `add_sector_mapping_keywords` ＝ 仓库唯一 head（`python -c` 反查 `down_revision` 图，只有这一支不是别人的父）⇒ **那两次是 no-op，没有任何 DDL 落地**；它取放了一次 advisory lock。同时刻生产账目复算：映射 118、`fund_info` 162、活预测 1616、已判 1057、末条净值 2026-09-13 —— 与本文档 §1/§6 记的数字逐字一致。命令：`PYTHONIOENCODING=utf-8 python scripts/q.py --production "select version_num from alembic_version"`。
| 第 43 轮返修（本批，待复评） | ① 说明文买不到守卫：`_docstring_consts` 把模块/类/函数 docstring 从 `consts` 里剔掉，"会拒跑"改成**条件分支里**"印 `[abort]` 并且停下来"（raise 或 `return 4` 都算，只认 raise 会误伤 `sync_db_columns.py` 这类真护栏）；`test_prose_cannot_buy_a_guard_signal` 三个说明文样品必须判"没守卫"+ 两个真护栏对照组必须判"有"。② 能力按类别枚举（`capabilities` 表 + 每类合成样品，含"只读查询不许被判成能写"的过宽对照）；别名还原到 `sqlalchemy`（`create_engine as ce` 不再隐身）；读侧触发从 `engine_from_env` 扩到 `engine_from_env or direct_db`（函数体内拿 `SessionLocal` 的纯读脚本从此要答连哪儿）。③ `run_migrations.py` 加 argparse + `--dry-run`（实测：`--help` 退 0、坏旗子退 2、`--dry-run` 只报名字后退 2，**无参数仍照旧迁移**，所以 `render.yaml` 那支不动 —— 把默认改成要口令是老板的决定项 #57）。④ `_db_guard` 导入时把 stdout/stderr 改成行缓冲（B-M4：块缓冲时"我要动谁"排到 DDL 后面）；三把生产工具与 `src/__main__.py` 的手写剥口令换成尺子，剩下的 4 处钉进**只许变短**的棘轮 `test_no_new_hand_rolled_credential_stripping_appears`（第一版这条按文本 grep 会被我自己的 docstring 点红、按 AST 又因 `[-1]` 是 `UnaryOp` 而恒空 —— 两个错都被它自己的控制断言抓出来）。⑤ 守卫侧 `read_only_connect` 的自报不再把任何 sqlite 文件叫"本地镜像库"（B-M3），两份尺子改喂**笛卡尔积样品**并比**整句**；镜像身份从"后缀匹配"改成 `realpath` 比对（B-MINOR-1：`C:/backup/.../data/fund_insight.db` 以前与真镜像一字不差）。⑥ `push_sector_mappings_to_prod.py` 的 `[目标]` 按 host 判措辞（B-M5：`--base http://127.0.0.1:9/` 以前印"线上生产库"）。⑦ 新增 `scripts/drop_probe_residue.py` 并**已清掉真镜像里那张 `_db_guard_probe2`**（0 行、全仓 0 处引用；工具默认只报告退 3，`--apply --confirm DROP-PROBE` 才删，只碰 SQLite、只删"0 行 + 模型没声明"）；复核：`python scripts/drop_probe_residue.py` → `[ok] 没有探针残渣表`。⑧ 两份 L3 报告的"日期"改成 `_today_beijing()`；`[目标]`/`[target]` 两种拼法两边都认；`audit_doc_claims` 的 `missing` 分支补判据（"指向收不到的文件"必须算进退码，配"同一形状但收得到 ⇒ 退 0"的控制）；变异日志改放随仓库走的 `docs/迭代计划/run-20260924-mutation/round43-frontend-mutations.txt`；`test_script_db_guards.py` 补模块级 `import pytest`（以前 git 不可用时是 `NameError` 不是 skip）。
| 第 44 轮两份独立复评 | **A=76 / B=73 ⇒ 取低分 73**（轨迹 40:78 → 41:77 → 42:82/78 → 43:81/66 → 44:76/73；七轮都在 66-82 之间）。两席共同的诊断仍是同一族，但这次给了**可判的形状**：守卫信号只要"在同一文件里共存"就能买通，而**不是要求它们出自同一个决定**（A-M-1 / B-BL-1：把 `postgres` 写进 argparse 的 help、再写一条与目标无关的 `[abort]` 分支，就冒充了"见远程就拒跑"），另外三条各自命中一处：B-M-4"赋 `DATABASE_URL` 不区分方向也算守卫"、B-M-3"只 import 一个 service 就隐身"、B-M-6"`del sys.modules` 一个动作就让钉库自证清白"。B 席还数出两处我没上闸的正面账：`alembic/versions/*.py` 完全不在扫描范围（`run_migrations.py` 每次 Render 启动都发 `upgrade head`，而 env.py 那道方向闸只管"能不能连过去"）、以及 `purge_junk_funds.py` 的 `[target]` 硬写着"本地镜像库"（`LOCAL_DB_URL` 指到副本时也说自己是镜像）。A 席数出 8 条"改了却没被任何用例盯着"的零覆盖分支（`--fix`、`declared_tables()`、git 不可用的退回那一支、`_today_beijing()` 有没有真被用…）。 |
| 第 44 轮返修（本批，待复评） | **① 守卫信号改成"同一条分支做到了"**：拒跑要**条件看方向 + 印 `[abort]` + 停下来**（`raise` / `return 非 0` / `sys.exit(非 0)` 都算 —— 不认 `sys.exit` 会误伤仓库里真在用它的三个脚本），`if False:` 那一支算死代码；名字表加关键字参数与 `sep=`/别名（`env_written` 只在**方向能证明**是 SQLite 时才算守卫，方向不明另记一键只用于"它动过连接串"）；helper 返回的 `[目标]` 只有被 `print` 过才算自报。**② 读侧触发补一次间接**：只 `import src.services.x`（它顶层 import 了 ORM）也算"碰得到一个活的连接"，判据走 src 顶层 import 图，对照样品是**现造的一棵临时 src 树**。**③ 钉库前先问"还有谁连着别处"**（B-M-6）：绑在非 SQLite 上的 `engine`/`SessionLocal` 散落在任一仓库模块 ⇒ `[abort]` 退 4。**④ `alembic/versions/*.py` 进扫描**（B-m4）：必须有 `upgrade`+`downgrade`，**往上那一支**删结构要登记（今天 9 支迁移的删除全在 `downgrade`）。**⑤ 两份尺子不再回显 key=value 连接串**（A-m6：`host=… password=…` 以前被原样印进日志/`docs/`），只留 `host/port/dbname`，认不出 scheme 时也不回显原串；样品加进笛卡尔积，并新增"摘口令但必须留下 host 与库名"的双向判据。**⑥ 只读门补 `PRAGMA query_only=ON`**（B-m1：`mode=ro` 只锁主库，`ATTACH` 一个可写文件再往**那个库**写以前是允许的 —— 当场跑通）；顺序排在探针**之后**并 `dispose()` 池里那条旧连接，否则 fail-closed 探针被 pragma 自己"通过"。**⑦ 措辞与口径**：`purge_junk_funds.py` 的 `[target]` 改印 `db_kind()`；`push_sector_mappings_to_prod.py` 认生产改成**逐字等于**已知主机（旧的是子串匹配 ⇒ `onrender.com.attacker.example` 自称生产；我第一版放宽成"或它的子域"又过头 ⇒ Render 上别人的应用也不是我的生产）。**⑧ 六条零覆盖分支上闸**：写死"老板已确认"的授予点棘轮（三种拼写都认 + 搬运已有值不误伤）、L3 报告日期必须现算、`audit_doc_claims --fix` 只当代改条数且幂等、`declared_tables()` 与 AST 读到的 `__tablename__` 逐字对表、`_target_line` 必须真被 print、git 不可用时退回磁盘那一支自己跑一次。**本批我自己制造了两次故障，都由新用例当场抓红**（记下来因为它们比修好的东西更有价值）：(a) 新守卫对 `sys.modules` 每个值取 `vars()` ⇒ Windows 上那里混着 `ctypes` 的 `kernel32.dll`，`ffi.error: symbol not found` 让 `pin_local_sqlite()` 崩、**12 条** in-process 用例一起红（分布当场从失败清单读：`test_audit_fund_info_identity` ×4 + `test_sector_seed_route_honesty` ×2 + `test_seed_owner_proxies_gate` ×4 + `test_snapshot_prod_mappings` ×2）；(b) 第一版按"有没有 url 属性"过滤 ⇒ `sqlalchemy` 包自己有个叫 `engine` 的**子模块**，每个导入过 sqlalchemy 的进程都被判"绑在远程"。两条现在各有一道对照（怪对象必须放行、真散落连接必须仍拦住）。复核：`pytest tests/unit -q` → **1041 passed / 16 skipped / 0 failed**（471.06s）；`pytest tests/ -q` → **1050 passed / 16 skipped / 0 failed**（448.64s）；`python scripts/audit_doc_claims.py` 见下面结论行。 |
| 第 45 轮两份独立复评 | **A=74 / B=76 ⇒ 取低分 74**（轨迹 41:77 → 42:82/78 → 43:81/66 → 44:76/73 → 45:74/76；九轮没过线，两席再度咬得很近＝同一族）。两席共同把上一轮那条诊断又推进一层：守卫信号**已经**要求"出自同一条分支"，却仍然**不要求那条分支可达、也不要求它排在危险动作之前**（A-M-1 / B-BL-1：把 `[abort]` 写在 `upgrade(...)` 之后、或写在 `if False and 远程:` 里，判据照样点头）；另一半是"方向来自**名字**或**说明文**而不是**值**"（A-M-2：`def local_url()` 返回生产串仍算"自设 SQLite"）。A 另数出：文档条数那把尺子**逐行**扫 ⇒ 文件名在行末、数在行首的软换行藏得住话（`前端与接口层.md` 那句过时的"16 条判据、35 处变异"就是这么躲了三轮）；B 另数出 `db_kind()` 看见 `postgres` 就印"线上生产库"（`127.0.0.1` 也算）、`purge_junk_funds.py --production` 是**装饰性旗子**（删掉它行为一个字不变），以及授予点棘轮仍按"文件里出现过"数、`__init__.py` 整族免检。A 席还报告了**两次注入尝试**（一句伪装成"记忆水印"的指令、一段伪造的 `[SYSTEM NOTIFICATION]`），它拒了；全仓 grep 查无出处 ⇒ 报告在案，不当代码事实用 |
| 第 45 轮返修（本批，待复评） | **① 死路不算守卫**：`if False:` / `X and False` 恒假分支里的 `[abort]` 认成死代码；**② 顺序**：同一分支里 `upgrade`/`commit`/`drop` 在前、拒跑在后 ⇒ 不算守卫（触发词表 `DANGEROUS_CALLS`）。**③ 方向要来自值**：只有函数**自己每条 `return`** 都返回 sqlite 字面量（`_returns_sqlite` 不钻嵌套函数）、赋的字面量、或 `pin_local_sqlite` 才算"自设方向"。**④ 危险迁移一份实现**：新增 `scripts/migration_policy.py`（pytest 与 `run_migrations.py` 读同一份），`run_migrations()` **第一行**就问它 —— 在建连接、在 `import src.models.database` 之前；登记处 `alembic/destructive-upgrades.json`（键＝迁移名、值＝为什么非丢不可），复核 `python -c "import sys; sys.path.insert(0,'scripts'); import migration_policy as m; p,s=m.audit(); print(s,p)"` → 今天 `9 []`。**⑤ 报"哪个库"改三档**：`本机 PostgreSQL（…，不是线上生产库）` / `线上生产库（…）` / `远程 PostgreSQL（…）—— 不是本项目那台 Supabase 生产库`；生产那一档只认**注册域精确相等或以 `supabase.com\|co\|in` 结尾**（`notsupabase.evil.example` 买不到），IPv6 `[::1]` 的主机切法单独钉；两份尺子同步改，样品池新增**主机**这一维（回环、私网 IP、带括号 IPv6、大写 scheme、带端口、`@` 前后带口令）。**⑥ `--production` 有牙**：`purge_junk_funds.py` 新增 `_target_agrees_with_the_flag()`（旗子与实际连接不一致 ⇒ 在第一次写之前退 4），AST 判据钉它排在 `commit` 之前。**⑦ 授予点棘轮补全**：形状清单（属性/字典/`setattr` 与 `object.__setattr__`/批量 `.values()`/模块常量/三目/字段名与授予值成对的元组）、名单值改成**条数**、`__init__.py` 不再免检、动态字段名落 `IMMUNITY_OPAQUE_SITES` 并必须写明依据；第 5 条来源 `sweep_sector_mappings.py --restore-owner-immunity` 默认**不**还原老板免疫（新增 `test_sweep_restore_owner_immunity.py`）。**⑧ 文档那把尺子改按段扫**（跨行只许一处换行且不跨句末标点，配"同一行形状仍然认得"的对照），`_db_guard` 导不进来从 `pytest.skip` 改成**不通过**（A-m3）。**本批自己被打脸的两处**（比修好的东西更值得记）：(a) 上一轮我写的"`del sys.modules['src.models.database']` 这条绕法作废"**当场是假的** —— 散落连接扫描在顺父包属性那一层按 `__name__` 跳过，而 `del` 之后那个对象的 `__name__` 恰好还是它 ⇒ 我要堵的路自己留着门；现在只有"确实还挂在那个键下"才交给 `already_built_url()`（`test_pinning_also_refuses_when_a_stray_module_holds_a_remote_engine` 第二格抓着）。(b) 我这轮新写的用例用**子串**断言"不许说生产"，而"（…，不是线上生产库）"里就含那四个字 ⇒ 对的话被读成谎话，改判"这一档的开头是什么"。⇒ 通则还是那条：**"作废"这种词要说得出**凭什么作废、以及还剩什么没堵**。数跟着表走。复核：`pytest tests/unit -q` → **1054 passed / 16 skipped / 0 failed**（532.33s）；
`pytest tests/ -q` → **1063 passed / 16 skipped / 0 failed**（570.75s）；`python scripts/audit_doc_claims.py` 退 0 |

| 第 46 轮两份独立复评 | **A=76 / B=72 ⇒ 取低分 72**（轨迹 42:82/78 → 43:81/66 → 44:76/73 → 45:74/76 → 46:76/72；**十轮没过线**，分数在 66~82 之间来回、没有趋势）。两席这轮咬在同一条线上，而且咬的是**我自己上一轮写在"当前测试基线"里的那三句撤谎话**：上一轮宣称"方向只认值"，实现却是"表达式里**出现过** `sqlite` 字样" ⇒ 一条三目（`= "sqlite:///…" if a.local else os.environ["PROD_URL"]`）或 `os.environ.get(k, "sqlite:///x.db")` 就白买到守卫（A-M1/B-M3）；宣称"每条 `return` 都是 sqlite"却漏掉 `except` 里那条出口（A-M2）；宣称"同一条分支要可达且排在危险动作之前"，而"可达"只问到"函数名有没有被别处写过"、"顺序"只比同一个最内层函数里的行号 ⇒ 把 DDL 抽进 helper 就整条绕过（A-M4）。B 另给出两条独立的结构性账：`migration_policy` 的名单在测试里是**另一份**（登记进 JSON 会让运行期放行、让 pytest 变红）、扫描用 `os.listdir` 不递归（alembic 自己用 `os.walk` ⇒ 子目录里一支 `drop_table` 会被 apply 却从未被核对，B-M1/M4/M5）；A 另有两条：授予点棘轮只认字面量 `True`（`if getattr(row,'owner_locked',None)` 读的是**真值**，A-M6/B-M10）、`--production` 只问"远不远"不问"是不是那台"（A-m3/B-M12）。**本轮两处报告瑕疵（记在案，不当代码事实用）**：A 席自述用 glob 删掉了**另一个会话**的临时草稿文件；A-m6（"报错分支回显口令"）我拿当前代码复现**不成立** ⇒ 按"评审报告先复现再动手"记为不适用，只补了同族真存在的两条（`sqlite:///u:pw@/db`、`postgres:///password=…` 落在路径段） |
| 第 46 轮返修（本批，待复评） | **① 守卫判据又深一层**（四条各配样品 + 反向对照）：方向改走 `_proves_sqlite`（字面量**以** sqlite 开头 / 多臂表达式**每一臂**单独证明 / 式子里有运行时子式就整体不证明，**解析顺序本身是判据** —— 先认 helper 出口再问运行时子式，反序会打死 `url(bool(os.environ.get("L")))` 这种诚实写法，我第一版就被对照抓红）；`_returns_sqlite` 出口含 `except`，而同一文件 `_guard_stmts` 反过来**不钻** `except`（两个问题共用一个开关才是 bug）；死路改**求值**（`and 1 == 0` / `if not True` / `if ()` / `while 0:`）；顺序与可达走**调用图**（行号元组字典序，第一版用累加小数把顺序弄反过、把一条真护栏判红）。危险语句识别从"必须是一整句"改成"看所有调用"（赋值/条件/推导式/`with` 里的 `execute` 以前全部隐身且不标"看不清"），`sa.text(默认值)` 仍不误伤。**② 迁移名单真的只剩一份**：测试改为消费运行期那份 JSON（行为判据），`audit()` 改递归 + 名单类型归一 + **一支都没看到不许报"干净"**。**③ 报"哪个库"改四档**（B-M6/A-m1）：第 45 轮的三档有个致命默认 —— **看不见主机就当本机**，而 `postgresql:///postgres?host=…supabase…`（主机只在 query 里，SQLAlchemy 正规写法）就这样被两把尺子**一致地**说成本机；**一致地错不会被"两边逐条相等"那条判据抓到**，因为唯一盯着它的用例正是那条相等检查 ⇒ 现在按 `,`/空格切候选、任一命中即生产、全无候选落"认不出主机"档。**④ 口令不变式不分位置不分方言**（`_redact_secrets` 两边都过），反向钉"合法文件名里的 `@` 不许被剥坏"。**⑤ `--production` 问"是不是那台"**：必须命中已知生产域，否则退 4 并把"要怎么改"说清（加尺子的域名，不是在删数据的脚本上调串）。**⑥ `sweep_sector_mappings.py --restore-from` 今天才有门**（B-M8/M9，而这条函数正是我上一轮刚编辑过的）：以前绕过 `--apply`、无确认词，直接 `setattr` → `commit`，末尾还 `FundHistory.delete()`；且 `created_at` 缺失时"只清本轮新建"退化成**清这只基金全部历史**（净值不可再生）⇒ 现在默认 dry-run、真写要 `--apply --confirm RESTORE-SWEEP`、检查排在 `pin_local_sqlite()`/`SessionLocal()` **之前**、拿不到下界一行都不清、清单字段过 `MANIFEST_FIELDS` 白名单。**⑦ 授予点棘轮改真值语义**（A-M6/B-M10）：`= 2`、`= not False`、`= x or True`、带类型标注赋值、`def grant(row, lock=True)` 隔一层默认值、`setdefault`/`row['reviewed_by']='owner'`、裸 SQL `SET owner_locked = true` 从此都认，每条各配"搬运已有值不许误伤"的反向对照；**边界写明**：值的来路看不见（跨模块常量、`{**payload}`）**不算**写死授予值也不另开登记通道 —— 第一版开了，当场把 5 处正常代码变成"待解释"，那张 `IMMUNITY_OPAQUE_SITES` 就从"要依据"退化成"盖章"。**⑧ 文档那把尺子**：B-M2 建议的"按句判"**实测驳回**（照做后 AGENTS 基线那段立刻假红：文档写 3、当场 35），只保留它要的另一半 ⇒ "因流水段而没判"的条数必须印出来；尺子拿不到时 `skip` 而不是 `fail`（B-M7）；并发 pytest 抢不到锁时印一行警告（A-m7）⇒ "跑不动"与"跑不绿"分得开。**⑨ 我自己这轮跑出来的卫生问题**：两条 `write_manifest` 用例把回滚清单写进**仓库里那份真清单的同名目录**，断言中途失败就不清 ⇒ 本机漏了两份 `sweep-manifest-pytest-realign*.json`；现在 `OUT_DIR` 钉到 `tmp_path`，并加一条只认 `pytest` tag 的闸（真实演练清单不误伤、自带"现造一份泄漏必须被点名"的控制；撤掉重定向复跑 ⇒ 2 条一起红）。**复核**：`pytest tests/unit -q` → **1067 passed / 16 skipped / 0 failed**（445.73s）；`pytest tests/ -q` → **1076 / 16 / 0**（357.13s，两个口径**串行**跑）；`python scripts/audit_doc_claims.py` 退 0；`python -c "import sys; sys.path.insert(0,'scripts'); import migration_policy as m; p,s=m.audit(); print(s,p)"` 印 `9 []`（2026-09-25 16:5x 北京，默认 cp936 口径）|

| 第 47 轮两份独立复评 | **A=68 / B=80 ⇒ 取低分 68**（十一轮没过线；同一天两席相差 12 分，差在权重表不在发现）。A 席（40 分押守卫可绕过性 / 30 押用例可证伪 / 30 押产品现实）：A1 `if/else` 两条赋值合成"方向已证明"（文件级 OR，与上一轮修掉的三目同语义）、A2 写死一句 `print("[目标] 本地镜像库（sqlite）")` 买到自报、A3 `dbapi_direct` 那一类**恒空**（条件要求 callee 既等于 `connect` 又等于模块名；全仓 0 命中，"每类一个合成样品"里那一类其实靠 `raw_sql_write` 兜底）、A4 我上一轮加的并发警告在默认 cp936 下让第二个 pytest 会话 `INTERNALERROR`、A5 授予点棘轮的裸 SQL 腿只认"整整一句就是一次调用"、A6 迁移闸门对解析不出的调用静默放行（与它自己 docstring 的"看不清要报"矛盾）、A7 `os.environ.update({...})` 这种诚实钉库反被判"没方向"。B 席（30/25/15/20/10 押在攻击面、fail-open、泄露、可红性、可复现数）：B-1 迁移闸门仍对四种普通形状报干净（类体里的 helper、`op.rename_table`、跨模块 helper、`alter_column`）而 `run_migrations.py` 每次 Render 启动**先问它**再对 `.env` 那个库发 DDL、B-2 `alembic/env.py` 是**第三把**手搓剥口令尺子（`?password=` 原样进 stderr → Render 日志）而那条棘轮只扫 `scripts`/`src` ⇒ 结构上看不见、B-3 `is_correct`/`fund_code` 两条"唯一入口"根本没有裸 SQL 这一腿（本仓真这么写：`src/api/main.py:463`、`:556`）、B-4 散落连接扫描整趟失败时只印警告继续钉库（那一支 0 覆盖）、B-5 `sweep --restore-from` 印完 `[abort]` 退 0 且 `--apply` 照写映射行、B-6 `线上生产库（postgresql:///postgres）` 把主机丢了、B-7 `audit_doc_claims` 结论行不重复"不判 8 条" |
| 第 47 轮返修（本批，待复评） | 十一条逐条复现后各自修 + 各自配"现造一处违规必须被点名"的控制：**① 方向**：同文件里存在任何一处方向不明的赋值 ⇒ 整篇不计分（`if/else` 样品红、两臂都 sqlite 的样品仍绿）。**② 自报要来自值**：f-string 挖空 / `%` 插值 / 调函数才算；写死一句不算（`backup_database.py` 因此被抓，改印 `db_kind(源)`，实测 `本地镜像库（data/fund_insight.db）` 与 `本地 sqlite 文件（不是镜像库）：…` 两种都对）。**③ DB-API 能力**：认模块属性 / `from X import connect [as y]` / `import X as z` 三种拼写，且"开了连接 + 发一条看不见的语句或 `commit()`"才算能写，`execute("select …")` 不误伤。**④ 并发警告不许崩会话**：`pytest_configure` 开头把**当时那个**流设 `errors='replace'`（顶层那次不够，`wrap_session` 之后 pytest 会换掉 `sys.stdout`），判据真起子进程并显式剥掉 `PYTHONIOENCODING`，两头都钉（不许 INTERNALERROR + 警告必须印出来）。**⑤④′ 授予/唯一入口的裸 SQL 腿**：三处共用一个位置无关的 `_raw_sql_write_hits`，只认 SET 子句 / INSERT 列清单；`UPDATE … SET status = 1 WHERE is_correct = true` 与 `sum(1 for m in rows if m["owner_locked"])` 是两条反向对照（第一版我把后者判成授予，当场被自己的对照抓红）。**⑥⑤ 迁移闸门**：调用图跟属性出口（`Legacy().wipe()`）、删除方法按前缀取类别（`rename_table`/`rename_column` 从此算删除类）、裸名 `drop_table(...)` 算、解析不出的调用记"看不清"、`alter_column` 归"看不清"；9 支真迁移仍回 `9 []`。**⑦ B-4**：整趟扫描失败 ⇒ `[abort]` 退 4（那一支以前 0 覆盖，现在这条判据就是它的覆盖）。**⑧ B-5**：`[abort]` 必须挡流程 ⇒ 返回 4；顺带纠正我上一轮写的那条**把旧行为钉成规矩**的用例（`assert restore(...) == 0`）—— 判据写错时绿色用例会替错误行为作保。**⑨ B-2**：`env.py` 改用共用的 `machine_name`，棘轮 bases 加 `alembic` + "写在 alembic/ 下的手抄必须被点名"控制，另加一条不 import env.py 的 AST 取样判据（旧写法在这四串里必泄 ⇒ 控制非空判）。**⑩ B-6**：两把尺子都把 `?host=` 里的主机补进自报；**第一版按"有没有冒号"猜主机，把裸 IPv6 `postgresql://::1/db` 弄出假主机**，是那条"逐条相等"的判据抓红的（一致性判据本轮第一次替我挡了我自己）。**⑪ B-7**：`audit_doc_claims` 结论行带上"另有 N 条看得见但不判"。**⑫ 我自己造的一条虚假记录（记下来防复发）**：压缩上下文后我凭印象重列 B 席条目，写成"只读门 `PRAGMA` 侧门 + 真往生产写进 3 行 + 镜像里有 `_load_legacy_probe` 残渣" —— B 席原文只有 B-1~B-7 且明写"生产未连接"。实测：镜像 28 张表无残渣表、`drop_probe_residue.py` 回 `[ok] 没有探针残渣表`；只读门在副本上连开 5 条连接 / 走 session / `ATTACH` 三条路全被挡（对照：不经门 27 行博主被删空）⇒ 三条**作废**，任务记录按原文重写。**复核**：`pytest tests/unit -q` → **1070 / 16 skipped / 0 failed**（453.57s）；`pytest tests/ -q` → **1079 / 16 / 0**（419.45s，串行）；`python scripts/audit_doc_claims.py` 退 0；`migration_policy.audit()` 印 `9 []`（2026-09-25 18:2x 北京，默认 cp936）|

| 结论 | **门禁未达**：第 47 轮取低分 **68** < 80 ⇒ **不推 GitHub、不部署、不写生产**；本批**没动生产数据、没动 `render.yaml`、没动 `web/`**（改的是守卫、判据、`sweep`/`backup`/`env.py` 三处脚本行为，全部只在本地生效），所以前端那 104 处变异不需要重跑。第 48 轮两份独立复评待派出。**十一轮没过线这件事要如实报给老板**：分数在 66~82 之间来回（这轮同一天两席差 12 分＝权重表差异，不是发现差异），被反复点名的始终是"判据自己能不能被骗 / 会不会 fail-open"这一族，而不是业务逻辑坏了 —— 老板那句"体验完美"里属于页面的部分（#58 三个列表取点铺 `withWakeRetry`、#51 生产区间没有可跑命令、#29 生产回写链路）全部堵在门禁后面 |

```bash
git log --oneline -3          # 只允许本地前进；origin/main 不动
git rev-list --count origin/main..HEAD   # 实测 83（2026-09-23 02:44；每提交一笔就 +1，别把它当常数）
```

---

## 1. 部署前必须存在、现在还不存在的东西

| # | 事实 | 今天的证据 | 缺什么 |
| --- | --- | --- | --- |
| 1.1 | 生产跑旧构建，回写接口不存在 | dry-run 返回 **HTTP 404**「生产尚无审计回写接口：先把本轮代码部署上去再回写」；另一次证据：prod OpenAPI 只有 76 条路径 | 部署 |
| 1.2 | 生产侧身份体检**没有任何可执行路径** | `sweep_sector_mappings.py:489` 硬钉 `pin_local_sqlite`（行号会漂，看那句 `pin_local_sqlite` 本身）；`sector-mappings` 的 11 条路由里**没有 sweep** | 任务 **#29** |
| 1.3 | 回写清单有 3 行指向"生产取不到净值"的基金 | 清单 145 行；生产 `562700/515170/511100` ⇒ `fund_info` **0**、`fund_history` **0** 行。**比这更难看的两条**（第 27 轮 C 实测，我原先说成"只有生产定价不了"）：① `562700` 在**镜像也是 0 行净值**（档案有、名字对），而它 `reviewed_by='owner'` + `owner_locked=1` ⇒ 它是镜像 4 行 owner 锁定里唯一一只定价不了的，而 owner 豁免正好挡住身份体检去说这句话；② 清单里 `515170/511100` 的 `is_fetchable` 写着 **True**（那是在镜像上算的）⇒ 在客户端加"能不能取到净值"的闸会把这三行全放成"可服务"，闸门必须在**生产侧**跑 | 任务 **#28** |
| 1.3b | "3 行"那句早已过时，但**真实数取决于你拿哪一份清单去量** | 两份都实测过：拿 **09-21 那份**（指纹 `1ed8e42a49858ea6`）量 → `可服务 112 / 净值偏薄 2 / 无净值 0 / 无档案 31`、`不可服务 31 行牵动 249 条`；拿 **09-23 15:14 重导的**（指纹 `230767a8d3c95a7f`）量 → `可服务 114 / … / 无档案 29`、`不可服务 29 行仍牵动 249 条`。差异来自 12 处标的变更里 **5 个板块出了"无档案"桶（中科三环/华为概念/应用/德明利/紫紫）、3 个新进来（低空经济/区块链/拓维信息）**。复现：`python scripts/prod_writeback_preflight_readonly.py`（只读，退码 5；第一行会印它用的是哪份清单与指纹）|
| 1.3c | 推 GitHub 前**没有**一条机器闸在核"tracked 文件里没有密钥" | 我今天手工扫过一次（`api_key\|password\|secret\|token\|eyJ\|postgres(ql)://` 对 `git ls-files`，命中只有 `.env.example`、三处测试假连接串、一份 spec.md 的格式说明；`.env` 从未入库）。`src/api/main.py:207` 的 fail-closed 是真的（`os.getenv("ACCESS_PASSWORD","")` + 未配置就拒答）。缺的是把它变成用例 —— **任务 #46** |
| 1.4 | 生产 `verified_at/evidence` 全 NULL ⇒ 可服务判据与 ⚠ 徽章在生产**没有输入** | 118 行整表前像核过 | #29 完成后才有 |
| 1.5 | 冷启动：老板第一次点开可能直接失败 | `/api/health` 冷态 **45s 超时**；唤醒后同一接口 **1.5~2.5s 200** | 前端重试**已实现**（#27 关闭：`withWakeRetry` + 状态码分档 + 每个列表页的失败态，判据 `tests/unit/test_frontend_cold_start.py`）；**生产上的真实唤醒仍需部署后复核一次**，本机测不到 |
| 1.6 | **生产现在没有 `tzdata`** ⇒ 页面"截至 <日期>"可能差一天 | `grep tzdata requirements.txt` 本地有（第 33 行，第 25 轮加的）；`git show origin/main:requirements.txt \| grep -c tzdata` = **0** | 部署即修好；部署后要**当场核** `as_of` 是不是北京日期 |
| 1.7 | **生产每日跑批 10 天没动**（净值与末次验证都停在 2026-09-13，**141** 条到期未判） | `q.py --production` 实测；`fund_sync_retry` **0 行**、`system_config` 最后写入 08-06、最后新帖 09-11。天数与条数以 **§7.1b**（09-23 15:06 重量）为准 —— 这一行原先写"9 天 / 136 条"，与同文件 §7.1b 自相矛盾，第 36 轮 B 抓到 | 任务 **#31**：要老板在 Render 控制台看 Cron 状态/日志（`.env` 里没有 `RENDER_API_KEY`，我自己查不了） |

1.7 的关键补充（决定修起来多大工程）：**缺的那些天（09-14 起）源端有数据**，不是取不到——
`fund_api.get_fund_history_range('512480', 2026-09-14, 2026-09-22)` 返回 **7 条**
（含 09-22：1.0506）。所以 #31 是"**调度没跑**"而不是"数据没有"，重启 Cron + 一次补数即可；
补完那 141 条到期预测才能回到正常水位（补数属写生产，按第 3 节流程走，且**先把差异清单给老板过目**）。

1.6 的复核方法（部署后）：`curl -H "X-Access-Password: …" .../api/stats/evidence` 里 `as_of`
必须等于当天**北京**日期；Render 跑 UTC，缺 tzdata 时 `zoneinfo('Asia/Shanghai')` 抛异常，
`prediction_lifecycle.py:48` 那圈 `except` 会**退回系统时钟**并只打一条 WARNING —— 也就是说
**页面会安静地显示错一天的截止日**，没人报错。这类"降级不响亮"的点，见到就该要求它至少把
降级状态显示到用户看得见的地方。

---

## 2. 只读体检（现在就能跑，不写任何东西）

```bash
# 2.1 静态表：判据两根轴，退码 0 才算干净
PYTHONIOENCODING=utf-8 python scripts/audit_static_sector_map.py \
    --fixture tests/fixtures/sector_map_roster_snapshot.json
#   期望/今天实测：R 99 / E 10 / A=D1=D2=D3=0 / 标签不符 0，退码 0
#   线上全名册同口径也一致（--roster-cache data/_roster_full.json）

# 2.2 历史提交里的对照（"82 条"那句查无出处，可复现的只有这一行 ⇒ 82 已撤回，见 §五十.4）
git show c7cbc25:"docs/迭代计划/run-2026-09-20/static-map-audit.csv" | cut -d, -f1 | sort | uniq -c
#   今天实测：A 1 / B 72 / C 23 / D1 11 / D2 10 / D3 5（122 条，改码之前的表）

# 2.3 映射表层：只读体检，不写库
PYTHONIOENCODING=utf-8 python scripts/sweep_sector_mappings.py
#   今天实测：145 行；not_a_fund 0 / code_is_other_fund 0 / wrong_code 0 / not_fetchable 0
#            降级候选 0；relevance_low 1（硬件，待 LLM 语义判断）

# 2.4 结论没被我的数据动作偷偷推动
PYTHONIOENCODING=utf-8 python scripts/replay_verifications_on_copy.py --limit 200 --offline
#   今天实测：164 条重放，差异 0、未解释 0；证据指纹 (17543, 32642.2341, 47) 与源库 sha256 前后一致
#   跑完记得删 %TEMP% 里的副本（脚本自己删不掉，见任务 #9 ⑨）

# 2.5 垃圾码：只读预检（不加 --apply）
PYTHONIOENCODING=utf-8 python scripts/purge_junk_funds.py            # 镜像
PYTHONIOENCODING=utf-8 python scripts/purge_junk_funds.py --production   # 生产只读
#   今天实测：生产 6 个码各挂 **1 行映射**、净值 0、活预测 0；603758 回收站 1 条软删预测
#            ⇒ 预检**整批拒绝**（这是设计行为，别绕）
#   17:35 复量同值；"删 + 一键还原"已在镜像副本上真跑过、顺序结论见 §7.2b / §7.2c

# 2.6 生产数字（q.py 每次都真试一次写、被数据库拒绝才算只读成立）
PYTHONIOENCODING=utf-8 python scripts/q.py --production \
  "select count(*) rows, sum(case when reviewed then 1 else 0 end) reviewed, sum(case when owner_locked then 1 else 0 end) locked from sector_fund_mapping"
#   今天实测：118 / 104 / **0**；且 reviewed_by、match_source **全部 NULL**
PYTHONIOENCODING=utf-8 python scripts/q.py --production \
  "select count(*) judged, sum(case when is_correct then 1 else 0 end) correct from predictions where is_deleted = false and is_correct is not null"
#   今天实测：1057 / 591（=55.91%）。**含软删是 1192**，跨库比较只能用未删口径（镜像 1110/573）
```

```
# 2.7 镜像"到期未判"到底有多少条能立刻判（这条纠正我 09-22 晚口头报的数）
PYTHONIOENCODING=utf-8 python scripts/triage_unverifiable_predictions.py
#   2026-09-23 01:32（北京）实测：到期队列 88 条 ——
#     exact_target 50 / endpoint_lag_unproven 19 / no_source_history 10 /
#     insufficient_points 5 / same_nav_endpoint 2 / end_nav_too_old 2
#   ⇒ **立刻能出结论的是 50 条，不是 62 条**。我先前报的"62 可判、26 等净值"用的是自己
#      随手写的判据"这只基金本地末条净值 ≥ 目标日"，而它把"净值只在目标日**之后**有行、
#      窗口里面缺"的 12 条也算成了可判 —— 验证服务自己的判据（防未来函数 + 端点陈旧度
#      ≤10 天 + 窗口内点数）会把那 12 条退回去。
#   通则：报"系统能不能做 X"之前，判据要用**做那件事的那段代码**，不能用我另写的一条 SQL。
```

`q.py` 在 GBK 控制台下会因它自己打印的 `⇒` 崩掉（只读探针本身已经成功）⇒
曾要求"永远带 `PYTHONIOENCODING=utf-8`"；**已根治**（第 28 轮：`q.py` 启动时把 stdout
重设成 UTF-8 + `errors='replace'`，实测 `PYTHONIOENCODING=gbk python scripts/q.py "select '⇒'"`
正常出结果、退码 0）。带不带那个环境变量都能跑了。

---

## 3. 部署之后、回写之前（顺序不能换）

1. `git push` 前先拿第三份评审的分数（取低分 ≥90 才算过）。
2. Render 部署完成 → `curl https://fund-insight.onrender.com/api/stats/evidence` 必须 200
   （现在 404），且返回里 `database` 字段要写"生产"而不是"本地镜像库"。
3. 补生产缺的 3 只标的档案/净值（**写生产**：先只读预检 → dry-run → 显式确认 → 逐行回执，老板点头）。
4. 跑 #29 的生产身份体检（同样只读预检先行），拿到逐行回执与 `run_id`。
5. **那一刻**重导清单：`python scripts/export_repaired_mappings.py`
   —— 别用隔夜指纹（现在这份是 `1ed8e42a49858ea6`，生成于 2026-09-21T16:44）。
6. `push_sector_mappings_to_prod.py --limit 5` 试批 → 核回执 → 再 `--confirm WRITE-TO-PROD`。
7. 删 6 个垃圾码（生产，同样 dry-run → 确认）。
8. 回写后当场提醒老板：生产 `owner_locked=0`，`audit-import` 会剔掉载荷里的 owner 免疫
   ⇒ 镜像那 4 行有意代理在生产**不免疫**，要他在页面重新点"老板已确认"。

---

## 4. 已核实、不必重做的

- **APK 无需重打包**：`dist/Fund-Insight-{arm64,armv7}-v1.1.apk`；全包只有 1 处
  `https://fund-insight.onrender.com`（在 `lib/arm64-v8a/libapp.so` 偏移 306573），**无局域网地址**。
- 本地镜像备份：`data/backups/fund_insight-before-map-sync-20260922-224441.db`（补净值前）、
  `backup/fund_insight_20260922T114139Z.sqlite3`。
- 生产前像（118 行 × 19 列，含审计列）：`docs/迭代计划/run-2026-09-20/prod-mappings-full-20260922-205428.json`。
- **S6 第①步"本地重跑 AI"已经做完，且有据可查**（2026-09-23 15:10 北京只读重量，不是凭记忆）：
  镜像 `sector_fund_mapping` 145 行里 `reviewed=true` 123（其中 `reviewed_by='agent'` 28、`='owner'` 4），
  剩 **22 行待审查 = 全部 `match_source='agent'` 且 `evidence` 无一为空**
  ⇒ AI 那一轮确实逐行跑过并留了证据，这 22 行是"等老板在页面上看过"，不是"没人管"。
  最后一次写这表的时间戳：`2026-09-22 21:38:21`。复核命令（两条都是只读）：
  ```bash
  python scripts/q.py "select count(*) total, sum(case when reviewed then 1 else 0 end) reviewed_true, sum(case when reviewed_by='agent' then 1 else 0 end) by_agent, sum(case when reviewed_by='owner' then 1 else 0 end) by_owner, max(updated_at) from sector_fund_mapping"
  python scripts/q.py "select coalesce(match_source,'(null)') src, count(*) n, sum(case when evidence is null then 1 else 0 end) no_evidence from sector_fund_mapping where reviewed=false group by 1"
  ```
- **`no-cache` 之后每次开页全量重下"贵不贵"——已经量过，不是问题**（第 35 轮 A 把它列成"需老板决策"）：
  自有资源 `index.html 51.8KB + common.css 11.2KB + 三个 manager 14.9KB`（gzip 后，
  `python -c` 逐文件 `gzip.compress(b,9)` 加总）= **一次开页上限约 79 KB / 未压 400 KB**。
  按 Render 免费层每月 100 GB 出带宽算，要 **约 130 万次开页**才用完 —— 这App是老板一个人每天点几次，
  跟"旧脚本配新页面静默缺数"的代价完全不在一个量级。A 另测到"带 `If-None-Match` 仍回 200"
  （uvicorn 那条路没走 304），所以这个 79 KB 就是真实值，不是乐观值。
- 测试基线：**这一节只留历史值，当前值以 `AGENTS.md` 的"当前测试基线"为准**（它随每批复测更新）。
  历史：`tests/unit` 835 / `tests/` 844（2026-09-22 23:39）→ 898 / 907（09-23 11:02，后被判为**红**：
  默认 cp936 locale 下有一条用例响）→ 本批见 §0 最后一行。**报数必须带日期、时刻与 locale。**

## 5. 我还没做/没验的（别当成已完成）

- 手机端像素级显示（截图能力本机不可用：Playwright 内核未装、内置浏览器要求前台可见窗口）。
- ~~逐页点击遍历全站~~ 第 34 轮做了：`serve_mirror.py` 起在 8017/8018，五个视图逐个真点
  （点完读 `document.body.innerText` 与卡面 DOM），并且**把服务停掉再点一遍**看失败态。
  这一趟照出两个新缺陷（洞察四张卡里三张挂着旧数、观点页分页条没有失败分支），均已修 + 配变异。
  仍然没验到的：**TOP 空榜那一支**（镜像里就有上榜博主，造不出空榜；只有文本与结构判据钉着）、
  手机窄屏下的排版。
- #28/#29/#30 的实现与它们的"改前会红"用例。
- 12 条结构性不可验、003033 那 3 条预测、合并导入是否加闸（#25）——**都是老板决定项**。

---

## 6. 现在等你点头的事（一次列全，附我的建议）

按"不点头就卡住什么"排序。每条我都给了建议，但你说不通就不做。

| # | 要你决定什么 | 我的建议 | 不点头的后果 | 工作量 |
| --- | --- | --- | --- | --- |
| 1 | **第 27 轮三份复评回来后**，分数没到 90 要不要继续迭代 | 继续迭代（前 26 轮都是这么过来的，从没到过 90 也没出过事故） | 一切都停在"本地已改好、线上还是旧版" | 每轮约 1~2 小时 |
| 2 | **生产冷启动**怎么解决（#27）：加"保温"定时任务（要改 `render.yaml`），还是前端做冷启动重试 | **前端重试已做完**（不改部署配置）；若你希望"永远秒开"再加保温 Cron —— 那要改 `render.yaml`，仍是你的决定项 | 你早上第一次打开、或手机第一次连，可能白屏/转圈失败 | 重试小；Cron 要改部署配置 |
| 3 | **生产跑批停了 10 天**（#31）：你去 Render 控制台看 Cron 状态/日志，还是给我一个 `RENDER_API_KEY` 我自己查 | 你先看一眼最快（只需确认那条每日任务还在不在、有没有 paused）；或给令牌 | 线上净值停在 09-13，**141** 条到期没判，你看到的准确率是 10 天前的（天数/条数以 §7.1b 的命令现量；原先这格写"9 天/136 条"，是同文件自相矛盾，第 36 轮 B 抓到） | 你 2 分钟 |
| 4 | **补生产数据 + 定向回写**这一串（#28 净值预检、#29 生产身份体检、回写、删 6 个垃圾码）是否授权我做 | 建议按检查单第 3 节顺序做，每步先只读预检 + dry-run + 给你清单再落笔 | 生产数据与页面能力都停在旧版；⚠ 徽章在生产不亮 | 我半天 |
| 5 | **T3 的 70 分及格线**（#22）：金标集你来标，还是授权我按证据代标（我会写成"AI 判定"不当成人审结论） | 你只标高分段那 **43 行**里你**一眼能判**的十几行就够，剩下我来。（这一格原来写 33 行：那份种子表把分数配到了**别的候选**上——T3 是一行多条、逐个候选评估，`t3s[0]` 未必是本行那只基金的记录。按本码配对后 52 行全在、≥70 分是 43 行，命令：`python scripts/emit_t3_goldset_seed.py`） | 语义判据继续"没标定就在用"，可能给不合适标的打高分 | 你 10 分钟 |
| 6 | **（已做完，第 29~31 轮）博主榜一行里两个口径同时展示**（#30）：`命中率`（现算，判对/已验证）与 `加权评分`（存储列 `accuracy_rate`，含归档、按 verify_score 加权）。~~"16 个博主存储列与现算不符"~~ 已撤回：按生产代码自己的口径比，0 人对不上（第 27 轮两份复评各自实测）。要点头的是：一个数还是两个数、以及 `grade` 只由后者算而页面上看不见它 | 建议"以现算命中率为准，存储列只当缓存"，并把两件事在页面上分开写明（手机上没有 hover，不能只靠 `title`） | 榜单上"准确率"与"加权评分"并排出现却不解释，你最关心的"谁可信"被打折 | 我 1~2 小时 |
| 7 | 三件旧的决定：`003033` 那 3 条预测（本地净值停在 2020-12-08）留还是删；合并导入能不能带 `is_correct`（#25）；结构性不可验的 12 条要不要移出到期队列（#8，要加列＝生产迁移） | 我的倾向：003033 **留**并如实显示"取不到净值"；合并导入**加闸**（只挡 `is_correct`，不动你正常的备份恢复）；#8 **先不加列**，维持"留在队列里但如实说明" | 都属"不致命但会一直硌着"的小尾巴 | 各 10 分钟~1 小时 |
| 8 | **`POST /api/config/sector-mappings/seed` 这条路由要不要直接删**（第 36 轮 B-MAJOR-1）。我这批只做到"默认关 + 确认头 + 看退码 + 只补缺不覆盖"，路由还在 | 建议**删掉路由、只留命令行脚本**：`web/index.html` 不调它、`docs/模块总览/板块与基金匹配.md` 本来就写"不要用"；但删接口属公共接口变更 ⇒ 你点头我才动 | 留着就多一条"能改映射表"的 HTTP 路（现在至少要说不动话了：退码非 0 或缺回执一律报失败） | 你 1 分钟；我 10 分钟 |
| 9 | **`603758` 那条回收站预测怎么处置**（它是生产删 6 个垃圾码的唯一堵点：`predictions.id=3009`、`is_correct` 为空、2026-09-10 进回收站） | 建议**先备份再硬删这一条**，然后整批删 6 个码；备选：只删另外 5 个、`603758` 的档案留着 | 不处理就删不到底，`fund_info` 上继续挂着"股票名当基金" | 你 1 分钟；我 15 分钟（含回执） |
| 10 | **APK 要不要换项目自有签名钥**（#55）：v1.1 实测是 `CN=Android Debug` 签的，同机重打可覆盖安装，但 debug keystore 重建或换机器打包就要你先卸载重装 | 下次真重打时顺手换成自有 upload 钥（钥文件不入库、写清备份位置）。**不急**——现在这包能用 | 只影响"以后升级顺不顺" | 你 1 分钟；我 20 分钟 |
| 11 | **6 个垃圾码里有 3 个的"替代码"在生产没有档案 ⇒ 这 3 个板块不能按原顺序删**（第 40 轮 B 现场只读实测、第 41 轮复核：`秦安股份→562700`、`矿泉水→515170`、`美债→511100` 三个替代码在**生产 `fund_info` 查无此码**（尺子：`python scripts/q.py --production "select code, (select count(*) from fund_info f where f.code=s.fund_code) from sector_fund_mapping s where s.fund_code in ('562700','515170','511100')"`），而你选的是 B（预检自动剔掉不可服务的行）⇒ 这三行回写**不会落地**；随后 `purge_junk_funds.py` 会删掉它们现在的映射行 ⇒ 这三个板块在生产变成"没有任何映射"。另外 3 个（`510300`/`512760`/`513100`）档案与净值都齐，没问题。）<br>**空窗的真实爆炸半径（第 41 轮量，镜像只读）**：这 3 个板块现在只挂着 **2 条活预测**（`矿泉水` 1、`美债` 1、`秦安股份` 0；尺子：`python scripts/q.py "select m.sector_name, count(*) from predictions p join sector_fund_mapping m on m.fund_code=p.fund_code where m.sector_name in ('秦安股份','矿泉水','美债') and coalesce(p.is_deleted,false)=false group by m.sector_name"`）。<br>**⓪ 还有一层没人写在这里：A 今天执行不了** —— 生产净值停在 **2026-09-13**（第 41 轮只读实测：`fund_history` 9541 行、`max(nav_date)=2026-09-13`，也就是**落后 11 天**；尺子 `python scripts/q.py --production "select max(nav_date), count(*) from fund_history"`）。你说过净值/验证一直是你手动跑的 ⇒ 就算今天建好档案，那 3 个码在生产仍是"无净值 ⇒ 预检照样剔掉"。A 的前提是**你先在生产跑一次基金净值更新**，不是我先补档案 | **已于 2026-09-24 定了：C** —— 只删确定安全的 3 个（`北美→513100`、`海力士→512760`、`场内第一持仓→510300`），这 3 个板块的映射一行都不动，等档案 + 净值补齐再处理剩下 3 个。<br>**D（等你点头的第二步）**：先只给这 3 个替代码在生产**建 `fund_info` 档案**（不删任何旧映射 ⇒ 不空窗），你手动跑一次净值更新，回写那 3 行落地后，再删剩下的垃圾码 | 选 C 的代价：这 3 个板块继续挂着**股票码/非基金码**当映射（`600189 泉阳泉`、`152788 21罗美债`、`603758 秦安股份`），也就是"只允许基金不允许股票"这条规矩在生产上还剩 3 个洞 | 选 A/D 需要你先在生产点一次"更新基金净值"；我这边的活约 40 分钟（只读预检 → dry-run → 逐行回执） |

第 4 条**今天重新量过**（§7.2）：六个垃圾码在生产上都还在，工具与回滚路径都齐了，
只剩一句授权。里面多出一个需要你表态的小岔口：`603758 秦安股份` 回收站里还挂着 1 条软删预测，
默认闸门会因此**整批不动**。我的建议是选 A：**先只删另外 5 个**（`--codes` 显式列出来），
`603758` 等那条回收站记录被处理后再删 —— 宁可留一只名字古怪的行，也不要造出一条指向空档案的可恢复行。

需要你做的其实只有三件事：**去 Render 看一眼那条 Cron（3）、给一句"授权按第 3 节顺序动生产"（4）、以及复评回来后你说要不要继续磨（1）**。其余我按建议推进，涉及写生产的都会在动手前把清单摆给你。

---

## 7. 生产侧回写预检（2026-09-23 02:06 北京，只读实测）—— **§1.3 那句"3 行"报少了，真实数是 31 行**（这一节量的是 `1ed8e42a` 那份旧清单；15:14 重导后的数是 29 行，见 §7.4）

```bash
python scripts/prod_writeback_preflight_readonly.py   # 只读：复用 q.py 的引擎级只读 + "真试一次写"探针；退码 5 = 有不可服务行
```

回执（生产 Supabase，清单 `prod-writeback-sector-mappings.json` 的 145 行，`sha256 1ed8e42a49858ea6`）：

| 结论 | 行数 | 说明 |
| --- | --- | --- |
| 可服务 | **112** | 生产有 `fund_info` 档案且 `fund_history` ≥30 行 |
| 净值偏薄（<30 行） | 2 | `158038 粮食ETF富国` 2 行 / 最新 09-11；`011399 汇添富数字未来混合A` 24 行 / 09-11 |
| **生产查无此基金档案** | **31 行 / 30 个代码**（`亚太` 与 `亚太精选` 同用 159687） | 这 30 个代码里**只有 `159877` 在生产有净值行**（8 行、最新 2026-06-30），其余 29 个连净值都一行没有。大头：`创新药 159992`（该板块生产活预测 **72** 条）、`光伏 159857`（48）、`医疗 159877`（40）、`券商 159842`（37）、`传媒 159805`（18）、`中药 159647`（8）、`储能 159305`（7）、`A股 515440`（6） |
| 有档案但零净值 | 0 | — |
| **合计牵动生产活预测** | **249 条** | 31 行不可服务那批 |

对照：生产现有 `sector_fund_mapping` 118 行，清单 145 行里有 **118 个板块名在生产已存在**（是更新不是新增），`owner_locked` 0 行。

**为什么我只报过"3 行"**：因为我当时只查了三个看着可疑的板块（矿泉水/美债/场内第一持仓），
**没做全量**。这跟第 27 轮 D 抓到的那条是同一个病的完整版——清单自带的 `fund_info_needed` 字段是
**空的**，因为那一项是**在镜像上算的**（而我已经在镜像补过 26 只档案）：
所以"这份清单说缺档案"这句话在生产上根本不成立。⇒ 教训：**跨库的"可服务性"必须在目标库上量，
抽样不能报成总数。**

**这对 S6 顺序的影响（动手前必须先定）**：整份推过去 = 把 31 条"定价不了的映射"写进生产，
而这 31 个板块上有 249 条活预测——今天它们还能按静态表/现有映射解析，
被这些新行盖过去之后就取不到净值了。两条路：
  A. **先补生产**：在生产建这 31 只的 `fund_info` + 回补 `fund_history`（另一笔生产写，要单独 dry-run + 逐行回执），再推 145 行；
  B. **只推可服务的 112 行**（+ 那 2 行净值偏薄的先补净值），31 行留在镜像，页面/映射表继续用现状。
我的建议是先走 B（写面最小、可回滚、不动预测），把 A 单独排一批；两条都不许顺手合并成"一键全推"。

另两条顺量出来的事实（同一脚本、同一份清单）：
- **[只允许基金] 104 个代码在全量基金名册（27905 只）里查无此码的：0 个** ⇒ 这份清单没有把股票当基金推过去。
- **[字面这根轴] 核心词命中 43 / 只共一个汉字 20 / 字面看不出关系 82（其中 `reviewed=True` 65 行）**。
  这 82 行**不等于错配**：`存储→芯片ETF`、`CPO→通信ETF`、`面板→消费电子ETF` 语义上是对的，
  说的是"字面这根轴说不出话"——要的是 T3 语义判据 + 金标集（任务 #22 仍卡在那 43 行没人标）。
  唯一一眼可疑的是 `核聚变→562350 电力ETF银华`（`reviewed=False`），它也在 #32 那批里。

工具：`scripts/prod_writeback_preflight_readonly.py`（只读，已纳入版本库）。

### 7.1 生产身份体检：今天跑不动的**原因**已经量出来了（2026-09-23 02:49 只读实测）

```bash
python scripts/prod_writeback_preflight_readonly.py --identity-audit
#   [行] 生产映射 118 行；evidence 为空 **118 行**；被判不可服务 0 行；相关性旗标 0 行
#   [结论分布] （无身份结论） 118
```

结论不是"体检说生产干净"，而是**体检在生产上没有输入**：身份结论存在 `evidence` JSON 里
（第 21 轮起零新列、零迁移），而生产 118 行的 `evidence` 全是 NULL ⇒ 任何判据都无从解析。
这同时纠正任务 #29 的旧说法（"脚本钉死镜像、服务端没有体检路由"只是表层原因）。

⇒ **S6 顺序必须是**：部署带体检的代码 → 回写清单（顺带把 `evidence` 带过去，
`AUDIT_FIELDS` 已含这一列）→ 才谈得上"在生产跑一次身份体检"。
跑之前/之后都用上面这条命令对照，别再拿"0 行被判不可服务"当"生产没问题"。

### 7.1b 生产跑批停摆：今天重量的天数与条数（2026-09-23 15:06 北京，只读）

```bash
python scripts/q.py --production "select (select max(nav_date) from fund_history) as last_nav, (select count(*) from fund_history) as nav_rows, (select max(verified_at) from predictions where is_deleted=false) as last_verified, (select count(*) from predictions where is_deleted=false and is_correct is null and target_date <= current_date) as due_unjudged, (select count(*) from predictions where is_deleted=false) as live_predictions"
# 这条连接由数据库自己判只读：[guard] 探针写临时表被数据库拒绝 ⇒ 这条连接确实只读
```

| 事实 | 实测值 |
| --- | --- |
| 生产最后一行净值 | **2026-09-13**（今天 09-23 ⇒ 停摆 **10 天**，不是原先写的 9 天） |
| 生产 `fund_history` 行数 | 9541（与 09-22 记录一致 ⇒ 这十天一行都没进） |
| 最后一次预测验证 | 2026-09-13 15:23 |
| 到期未判的活预测 | **141** 条（上次记 136，随日期推进变大） |
| 生产活预测总数 | 1616 |

⇒ 这一条同时卡着目标 ① 与 ④：净值不往前推，验证队列就是空的，"多少结论证据已失效"在生产
也只能报 09-13 那批。工具、只读预检、`run_scheduled_tasks.py` 的"[库] …"日志都已就位，
**剩下的是 Render 控制台里那一次点击**（老板自己看，或给一个 `RENDER_API_KEY` 我来查）。

### 7.4 回写载荷（manifest）今天重新生成过：它比 09-21 那份多带 12 处标的变更，但**牵动的生产预测是 0 条**

```bash
python scripts/export_repaired_mappings.py        # 钉镜像、只读镜像，重写 run-2026-09-20/prod-writeback-sector-mappings.json
python scripts/push_sector_mappings_to_prod.py    # 不带 --confirm = 只出计划
```

- 旧 manifest 生成于 **2026-09-21T16:44:33**（指纹 `1ed8e42a49858ea6`），而镜像最后一次写这张表是
  **2026-09-22 21:38**（§4 那条 AI 重跑）⇒ 拿旧清单回写会把 09-21 的状态盖回去。今天重新生成：
  `145 行（不可服务 0、老板锁定 4），需补基金档案 0 只，指纹 230767a8d3c95a7f`。
- 逐字段比对（`git show HEAD:<manifest>` vs 现文件）：145 行的 `evidence`/`verified_at` 全部刷新，
  22 行多出 `llm_reason`/`verify_message`（就是 §4 那 22 行待审查），18 行 `confidence` 变化，
  **12 行的 `fund_code` 与 09-21 那份不同**（逐对取自 `git diff` 后的清单文件，不是从控制台抄的）：
  `核聚变 562350→159525`、`中科三环 562800→516780`、`德明利 012650→512760`、`紫紫 159998→159996`、
  `华为概念 159509→159732`、`应用 021252→516160`、`拓维信息 159995→159998`、`哈三联 512010→512170`、
  `海力士 159801→512760`、`北美 513500→513100`、`低空经济 159507→159530`、`区块链 159898→159890`。
  **顺带照出 ① 还没做完的一半**：这 12 个键里有 6 个根本不是板块而是**个股名/昵称**
  （`中科三环`、`德明利`、`哈三联`、`拓维信息`、`海力士`、`紫紫`）—— 它们挂在 `sector_fund_mapping`
  的 `sector_name` 上，就意味着"按板块找标的"这条路上有一批行的键压根不是板块。
  这是判断项不是 bug 修复（改键会牵动这些行自己的历史预测解析），列进 §6 等老板定。
- **爆炸半径已量（生产只读，`[guard] 探针写临时表被数据库拒绝`）**：把这 12 个板块名拿去
  `predictions.sector` 里数，**一行都没有**（含软删）⇒ 回写这 12 处标的变更**不会改动任何一条
  已判结论所依据的标的**。生产板块的头部是 `半导体 122 / 科技 83 / 黄金 78 / 大盘 74 …`，
  与这 12 个不重叠。
- 为什么这 12 行碰不到预测，一句话就能看懂：**镜像 145 行映射里有 50 行的 `sector_name` 今天挂着 0 条活预测**
  （`python scripts/q.py "select count(*) mapping_rows, sum(case when (select count(*) from predictions p where p.sector = m.sector_name and p.is_deleted=false)=0 then 1 else 0 end) keys_zero_live from sector_fund_mapping m"`
  → `145 / 50`）。这 50 个键"管不着任何结论"，所以改它们的标的是低风险；反过来 95 个键承载着全部流量，
  那才是 R1（任务 #38）与"无档案那一批"要小心的地方。
- 仍然要分开看的是**另一件事：清单换了一份，"无档案"的行数就变了**，两份回执都在这节里：
  `1ed8e42a`（09-21 清单）→ `可服务 112 / 无档案 31`；`230767a8`（今天 15:14 重导）→
  `可服务 114 / 无档案 29`（5 个板块出桶：中科三环/华为概念/应用/德明利/紫紫；3 个进桶：
  低空经济/区块链/拓维信息）。**两份都印 `牵动生产活预测 249 条`**（进出相抵）。
  所以 §7 表里那个 31 不是算错，是另一份清单的状态；真到回写那一刻，以 `push` 工具第一行印的指纹为准。
  这一批行 `push_sector_mappings_to_prod.py` 的预检会**剔掉不发**（硬发要显式 `--allow-unservable`）
  ⇒ 回写要定的不是"要不要改那 12 只标的"，而是"这 29 行先补档案还是先跳过"。

### 7.2 六个垃圾基金码：生产侧只读预检（2026-09-23 14:53 北京，`--production` 不带 `--apply` = 只出计划）

```bash
python scripts/purge_junk_funds.py --production      # 只 SELECT；没有 --apply 就在打印计划后 return
```

生产实况（**六个码都还在**，各挂 1 行映射、0 条活预测、0 行净值、0 行重试）：

| 代码 | fund_info 里的名字 | 映射板块 | 备注 |
| --- | --- | --- | --- |
| 603758 | 秦安股份 | 秦安股份 | **回收站里还挂着 1 条软删预测** ⇒ 闸门拒绝整批 |
| 600189 | 泉阳泉 | 矿泉水 | 股票名混进基金档案 |
| 152788 | 21罗美债 | 美债 | 非公募基金代码 |
| HYNX | 二倍做多海力士ETF-T-R | 海力士 |  leveraged 海外份额，净值源取不到 |
| SBSP76 | 第一创业基础设施10号 | 场内第一持仓 | 私募/资管计划，不是公募 |
| ign | 北美多媒体网络etf-ish | 北美 | 拼错的代码 |

⇒ 这一批的形状正好说明 ① 为什么要"只允许基金不允许股票"：`603758 秦安股份`、`600189 泉阳泉`
是**股票名挂在 `fund_info` 上**，`SBSP76`/`152788` 是非公募品种。
⇒ **删的动作仍堵在两处**：一是门禁（两份复评 ≥90 前不写生产），二是 `603758` 那条软删预测 ——
三个选法（我推荐 ③，理由见 §7.2b）：
① 老板点头加 `--allow-dead-predictions`（`predictions` 无外键，删完它就是一条指向空档案的可恢复行）；
② 只删另外 5 个，`603758` 的档案留着；
③ 先硬删那条回收站记录（备份后删）再整批删 —— 它 `is_correct` 为空，从来没判过。
工具侧已就位：备份先落盘再删、`--restore-from` 逐列写回、
11 条用例钉着（含"按业务键去重""不许白送老板免疫"；条数以 `pytest tests/unit/test_purge_junk_funds.py -q` 末行为准，我原先手写的 13 是错的）。

### 7.2b 删除 + 一键还原已经在**镜像的一次性副本**上真跑过（2026-09-23 17:32–17:33 北京）

为什么用副本不用镜像本体：这条演练会真写库，而同一时间评审在读镜像。副本姿势可复现：

```bash
cp data/fund_insight.db data/_rehearse-purge.db
LOCAL_DB_URL="data/_rehearse-purge.db" PYTHONIOENCODING=utf-8 python scripts/purge_junk_funds.py            # 出计划
LOCAL_DB_URL="data/_rehearse-purge.db" PYTHONIOENCODING=utf-8 python scripts/purge_junk_funds.py --allow-dead-predictions --apply --confirm PURGE-JUNK
LOCAL_DB_URL="data/_rehearse-purge.db" PYTHONIOENCODING=utf-8 python scripts/purge_junk_funds.py --restore-from backup/purge-junk-<stamp>.json --apply --confirm RESTORE-JUNK
```
（`LOCAL_DB_URL` 在 `pin_local_sqlite` 里优先级最高，见 `scripts/_db_guard.py`；跑完删副本。
真镜像全程未动：`fund_info` 242 行、文件 mtime 仍是 09-22 22:52。）

实测四件事：
1. 不带 `--allow-dead-predictions` ⇒ 退码 5、**整批不动**（`603758：回收站里还有 1 条软删预测`）；
2. 只删那 5 个（`--codes 600189,152788,HYNX,SBSP76,ign`）⇒ 删 5 行 `fund_info`，
   `fund_history` 17543 / `predictions` 2178 一行没动；
3. 硬要 6 个全删 ⇒ 副本上"指向不存在档案的软删预测"从 **0 变 1**（正是 `predictions.id=3009`，
   `post_id=751 / blogger_id=34`）。**这条门不是保守过头，它挡的就是这一行**；
4. `--restore-from` 还原两次（5 行、6 行）都 `[完成] … 失败 0`，且六个码**逐列 SHA 与镜像一致**、
   `fund_info` 回到 242、孤儿回到 0 ⇒ "先备份可回滚"这句从此有实测支撑，不再是读代码读出来的承诺。

### 7.2c 顺序结论：**先回写、后删垃圾码**（2026-09-23 17:35–17:38 北京，两次生产只读实测）

```bash
PYTHONIOENCODING=utf-8 python scripts/purge_junk_funds.py --production            # 只读预检，退码 5
PYTHONIOENCODING=utf-8 python scripts/q.py --production "select m.sector_name, m.fund_code, m.reviewed, m.reviewed_by, m.owner_locked, (select count(*) from sector_fund_mapping s where s.sector_name=m.sector_name and s.fund_code<>m.fund_code and s.is_active) siblings from sector_fund_mapping m where m.fund_code in ('603758','600189','152788','HYNX','SBSP76','ign')"
PYTHONIOENCODING=utf-8 python scripts/prod_writeback_preflight_readonly.py         # 只读，退码 5
```

量出来三点（都带当时输出）：
1. 生产那 6 行映射 **全部 `reviewed=False`、`reviewed_by=NULL`、`owner_locked=NULL`、`siblings=0`**
   ⇒ 没有一行是老板盖过章的，也没有同板块的另一行兜着；
2. 回写清单（指纹 `230767a8d3c95a7f`，145 行）里**这 6 个板块键全都有对应的新标的**：
   `北美→513100 纳指ETF国泰`、`美债→511100 国债ETF华夏`、`海力士→512760 芯片ETF国泰`、
   `矿泉水→515170 食品饮料ETF华夏`、`场内第一持仓→510300 沪深300ETF华泰柏瑞`、
   `秦安股份→562700 汽车零部件ETF华夏`；清单里**没有任何一行还指向那 6 个垃圾码**；
3. 同一份预检的对照行：`生产现有映射 118 行；清单里已有同名板块的行 118；owner_locked 0`
   ⇒ 145 行里 118 行是**按板块名原地更新**（`_audit_apply_row` 先按 `sector_name` 找行，
   找不到才新建），27 行才是新建，且这 27 个板块名在生产**不存在** ⇒ 本批不会撞
   生产那条 `sector_fund_mapping_sector_name_key UNIQUE(sector_name)`（AGENTS"后果二"的现量版）。

⇒ 所以顺序应当是 **回写在前、删垃圾码在后**：回写会先把这 6 行映射改指向真基金，
之后删档案时就只剩 `fund_info` 那 6 行（映射行不再是"板块唯一的入口"，不会被顺手删掉）；
反过来先删，等于先把"北美""美债"这两个板块在生产**变成没有映射行**，白留一段空窗。
