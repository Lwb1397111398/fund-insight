# 评分记录 · 板块→基金智能匹配迭代

计划案：`2026-09-20-板块基金智能匹配-总体计划-v1.md`
门禁：计划 ≥90 分才开工；代码 ≥90 分才算完成；<90 按 MUST-FIX 修订后重评。

## 开工前实测事实（作为计划的证据，非评审结论）

| 项 | 结果 | 证据 |
| --- | --- | --- |
| 全量测试基线 | `431 passed, 3 failed, 16 skipped`（151s） | `python -m pytest tests/ -q` @ HEAD 138ab0a |
| AGENTS.md 记载的"123 个测试" | **已过时**，需一并修正 | 同上 |
| 失败 1 | `test_prediction_change_log.py::test_manual_verification_creates_change_log` 期望 `status='verified'`，代码写 `success`（a7d2057 废弃观望后的语义漂移） | `tests/unit/test_prediction_change_log.py:97` |
| 失败 2 | `test_services.py::...failed_predictions_as_verified` 期望 `accuracy==0.5`，代码返回 `50.0`；代码注释明确 accuracy 口径是 0–100（`src/services/prediction_service.py:209`）→ **测试口径错** | `tests/unit/test_services/test_services.py:181` |
| 失败 3 | `test_database_url_routing.py::test_explicit_sqlite_database_url_is_honored` 单跑通过、全量跑失败：`result.stdout` 为 `None`。根因是 `subprocess.run(text=True)` 用 GBK 解码子进程输出，路径含中文时解码异常被吞，stdout 变 `None`（同批 warning 里能看到 `UnicodeDecodeError: 'gbk' codec ...`）→ 必须显式 `encoding='utf-8'` | 复现：`UnicodeDecodeError` in pytest warnings |
| 本机中文输出 | PowerShell/Git-Bash 下 Python `print` 中文乱码（GBK 控制台），脚本一律写文件不回显中文 | 多次观察 |
| APK 工具链 | **可今天出包**：Flutter 3.47.3（`E:\flutter`）+ JDK17（`C:\Users\李文彬\java\17.0.13+11`）+ `flutter doctor` Android 全绿、许可已接受；SDK 真身 `E:\Android\Sdk`（platforms 35/36、build-tools 36、aapt2/adb/sdkmanager 齐）；`ANDROID_HOME` 环境变量指向不存在的 `E:\androidstudioSDK`（需修正或用 `local.properties`）；生产站点 `https://fund-insight.onrender.com` `/api/health` 200 | 子代理实测 |
| 数据基线 | 118 映射（14 待审查）、68 条板块名与基金名零字面重合、`sector_alias` 0 行、162 只基金 `fund_type` 全 NULL、预测 2178（pending 986 / success 648 / failed 544） | `docs/迭代计划/baseline-2026-09-20.md`（由 `scripts/audit_export_baseline.py` 生成） |
| 本地库现状 | 落后于导出（本地 1476 预测 / 90 映射 vs 导出 2178 / 118）→ S1 必须导入 | `sqlite3 data/fund_insight.db` |

## 计划评审

### 第 1 轮（v1）

| 评审代理 | 视角 | 分数 | 结论 |
| --- | --- | --- | --- |
| Reviewer-A | 根因覆盖 / 可行性 / 风险 | 待补 | 待补 |
| Reviewer-B | 数据安全 / 测试可证伪性 | 待补 | 待补 |

MUST-FIX 与处置：待补。
