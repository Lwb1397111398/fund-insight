# 评分记录 · 板块→基金智能匹配迭代

计划案：`2026-09-20-板块基金智能匹配-总体计划-v1.md`
门禁：计划 ≥90 分才开工；代码 ≥90 分才算完成；<90 按 MUST-FIX 修订后重评。

## 开工前实测事实（作为计划的证据，非评审结论）

| 项 | 结果 | 证据 |
| --- | --- | --- |
| 全量测试基线 | `431 passed, 3 failed, 16 skipped`（151s） | `python -m pytest tests/ -q` @ HEAD 138ab0a |
| AGENTS.md 记载的"123 个测试" | **已过时**，需一并修正 | 同上 |
| 失败 1 | `test_prediction_change_log.py::test_manual_verification_creates_change_log` 期望 `status='verified'`，代码写 `success`（a7d2057 废弃观望后的语义漂移） | `tests/unit/test_prediction_change_log.py:97` |
| 失败 2 | `test_services.py::...failed_predictions_as_verified` 期望 `accuracy==0.5`，代码返回 `50.0`；代码注释明确 accuracy 口径是 0–100（`src/services/prediction_service.py:209`）→ **测试口径错** | `tests/unit/test_services/test_services.py:181` |
| 失败 3 | `test_database_url_routing.py::test_explicit_sqlite_database_url_is_honored` 单跑通过、全量跑失败：`result.stdout` 为 `None`。根因是 `subprocess.run(text=True)` 用 GBK 解码子进程输出，路径含中文时解码异常被吞，stdout 变 `None`（同批 warning 里能看到 `UnicodeDecodeError: 'gbk' codec ...`）→ 必须显式 `encoding='utf-8'` | 复现：`UnicodeDecodeError` in pytest warnings |
| 本机中文输出 | PowerShell/Git-Bash 下 Python `print` 中文乱码（GBK 控制台），脚本一律写文件不回显中文 | 多次观察 |
| APK 工具链 | **可今天出包**：Flutter 3.47.3（`E:\flutter`）+ JDK17（`C:\Users\李文彬\java\17.0.13+11`）+ `flutter doctor` Android 全绿、许可已接受；SDK 真身 `E:\Android\Sdk`（platforms 35/36、build-tools 36、aapt2/adb/sdkmanager 齐）；`ANDROID_HOME` 环境变量指向不存在的 `E:\androidstudioSDK`（需修正或用 `local.properties`）；生产站点 `https://fund-insight.onrender.com` `/api/health` 200 | 子代理实测 |
| 数据基线 | 118 映射（14 待审查）、68 条板块名与基金名零字面重合、`sector_alias` 0 行、162 只基金 `fund_type` 全 NULL、预测 2178（pending 986 / success 648 / failed 544） | `docs/迭代计划/baseline-2026-09-20.md`（由 `scripts/audit_export_baseline.py` 生成） |
| 本地库现状 | 落后于导出（本地 1476 预测 / 90 映射 vs 导出 2178 / 118）→ S1 必须导入 | `sqlite3 data/fund_insight.db` |

## 计划评审

### 第 1 轮（v1）

| 评审代理 | 视角 | 分数 | 结论 |
| --- | --- | --- | --- |
| Reviewer-A | 根因覆盖 / 可行性 / 风险 | 待补 | 待补 |
| Reviewer-B | 数据安全 / 测试可证伪性 | 待补 | 待补 |

MUST-FIX 与处置：待补。

---

## 第 1 轮（v1）实际分数

| 评审代理 | 视角 | 分数 | 结论 |
| --- | --- | --- | --- |
| Reviewer-A | 根因/可行性/风险/可证伪 | **68** | 15 条 MUST-FIX，全部写进 v2（见 v2 §八 对照表） |
| Reviewer-B | 数据安全/统计完整/测试可证伪 | **64** | 12 条 MUST-FIX，全部写进 v3（见 v3 §八 对照表） |

其中被代码/数据复核证实的关键纠正：迁移必须走 Alembic（`init_db` 不给已有表加列）；
`.env` 的 `DATABASE_URL` 指向生产 Supabase（门禁命令差点直接写线上库）；
`_find_fund_in_fundinfo` 才是错配自毒回路；真实工单是 169 板块（69 个无映射行、辐射 1646 条预测）；
`update_mapping` 无条件 `reviewed=True` 会让"待审查归零"门禁可作弊；S4 的预测重映射逻辑
`PredictionMaintenanceService.sync_sector_mappings` 早已存在，应复用而非新写。

## 第 2 轮（v3.1）

| 评审代理 | 分数 | MUST-FIX 条数 | 处置 |
| --- | --- | --- | --- |
| Reviewer-C（架构+可证伪，复核 v3 全部引用） | **74** | 10 | 全部落到代码/计划：见下表 |

| C# | 问题 | 已做的处置（可复核） |
| --- | --- | --- |
| 1 | 证据化 SQL 门禁被 NULL 绕过（`confidence < 0.85` 对 NULL 结果是 NULL） | 判据改为 `confidence IS NULL OR ... OR reviewed IS TRUE`；S4 执行时用 `run-2026-09-20/` 下的 SQL 落盘留证 |
| 2 | `is_fetchable/match_kind/evidence` 等被 G4/§十 引用但 0007 没建 | 新增 alembic **0008**：`is_fetchable`、`evidence`、`reviewed_by`、`owner_locked`（已在真实镜像库 `PRAGMA table_info` 验证 19 列齐全） |
| 3 | proxy 与置信算式冲突：有意代理永远达不到 0.85，会被排除在覆盖率之外 | 拆双阈值（direct 0.80 / proxy 0.68）+ `direct_exhausted` 前置条件；`SectorDecision.auto_reviewable` 实现并有单测 |
| 4 | `name_sim<0.35` 淘汰会杀掉正确答案（半导体→芯片ETF 字面零重合） | 拆成 `claim_sim`（LLM 声称名 vs 官方名，只作降级+打折）与 `sector_overlap`（仅排序），实测 512660/159813 不再被误杀 |
| 5 | `seed_sector_mappings.py` 会把 122 内置板块直接标 `reviewed=True` | S4 步 0 不调用它，改为写 `match_source='seed_builtin'` + `reviewed=false`，并加前后 `reviewed` 计数断言 |
| 6 | 金标集自指（25 条来自未审查的内置字典） | 加 no-op 基线对照，要求 `agent − no-op ≥ +0.15` 且 ≥8 条金标答案与内置字典不同 |
| 7 | `run_migrations.py` 无库守卫；promote 会抹掉 `alembic_version` | 迁移前必须贴连接串；`import_export.py --promote` 之后立即跑迁移（本轮实测：漏跑导致 `no such column: is_fetchable`，已补跑并写进流程） |
| 8 | S4 门禁里的数字（252/435）是跑批前快照，跑完必然不等 | 改为"从最终 reviewed 映射集重算期望值，再断言脚本输出==重算值" |
| 9 | 收敛清单仍漏 `llm_analyzer.py:1174`、`sector_fund_service.get_fund_by_sector` 降级读未审查行 | `_fill_fund_from_sector` 第 4/5 层已整体改走 agent；`get_fund_by_sector` 调用点保持但 T0 只把未审查行当候选（`source_prior=0.6`） |
| 10 | 无法区分老板挑定的行与 agent 写的行 | 0008 的 `owner_locked`/`reviewed_by`；`apply_decision` 遇锁定行直接拒绝并返回 `locked`（有单测） |

## 实现期实测（S1–S3）

- S1：`fund_insight_export_2026-09-20.json` → 影子库 → `--promote`，15 表计数全等、`PRAGMA foreign_key_check` 违规 0、字段级抽样 0 差异；备份 `data/backups/fund_insight_pre_import_20260920_234429.db`。
- S2：`tests/unit` **451 passed / 16 skipped / 0 failed**；真实 LLM+抓站实测：半导体→159995 芯片ETF华宝、债券→511010 国债ETF、白酒→161725 白酒LOF、日股→513880 日经225ETF、存储→159813（proxy）、军工→512680；SpaceX 因预算超时诚实落到 `needs_review`。
- S3：`POST /ai-match` 端到端 200（8.8s，写入 mapping_id=121，置信 0.759 < 0.80 故 `reviewed=false`）；浏览器实测板块匹配页 AI 按钮渲染、证据链面板显示 T0→T3 全过程、console 无报错；发现并修掉 `ai-batch` 的锁重入死锁（`Lock`→`RLock`，加回归测试）。


---

## S4a 映射表身份体检 —— 计划评分轮次（老板要求的"每步计划 AI 评分 ≥90"）

| 轮 | 版本 | 分 | 打回的关键理由 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | v1 | 62 | `_mapping_eligible` 的历史分支（reviewed=1 且 confidence IS NULL 无条件放行）没被关闭；`is_etf` 的 `15[0-9]{3}` 是 5 位死分支；"feeder 置信 ≤0.80"在算术上不可达（KIND_PRIOR 权重只有 0.15）；降级不挡读路径（`get_fund_by_sector` 有降级分支会捞回未审查行）；一键"全部标记已审查"可复活被洗掉的行 | 全部落进 v2，并逐条实测复核 |
| 2 | v2 | 68 | 体检依赖 `verify_fund_fetchable(probe_stock=True)` 判股票——**实测 145 行 0 条被判 stock**；官方名回填会误伤 33 行（含 SpaceX→159206）；`is_fetchable != False` 的三值 NULL 会让 105 条历史映射静默全灭；只改了 1 条读路径，还有 3 条裸查表；"搜索唯一"不成立（传媒有 3 只同名 ETF）；金标门禁 uplift 恒为 0 不可能通过 | v3 换判据、补读者、删自动 reviewed |
| 3 | v3 | 66 | 名字来源 `search_fund` **本身是混合证券搜索**（000938→紫光股份），"官方名"根本不代表基金域；push2 股票域在本机 ProxyError，"reviewed 的股票降为 0"这个指标会因工具坏掉而空过；二字 Dice 会把 `华宝中证医疗ETF` vs `医疗ETF华宝`（同一只）判成 0.33 错行 | v4 彻底放弃股票域，改 NFKC + 字符集 Jaccard |
| 4 | v4 | 79 | `wrong_code` 被自己写死的先决条件弄成**不可达**（"本行码不是基金域任何品种"——填错的码按定义仍是基金），导致 4 条历史假阴性（核聚变/华为概念/应用/中药）仍放过；`match_kind` 是 `String(12)` 且属 agent，塞 verdict 会在 Postgres 溢出；phase-evidence 写 `match_source` 会把 145 行"证据齐备"集体点亮、解除审查门禁 | v5 新增 `code_is_other_fund`、列使用纪律、幂等四元组 |
| 5 | v5 | 84 | 站点负证据判定写的是文本 `'页面未找到' in text`，实测 404 页是 HTTP200 + 3924B 的 GBK 乱码 title，**永不匹配**；`is_fetchable` 只表达"能不能抓"导致 QDII 慢更新行被无声踢出服务；漏了第 6/7/8 条读者（含 `prediction_maintenance_service:151` 这条真正写预测的通道）；"≥55 板块仍合格"指标不可达 | v6 全部按行号点名修正 |
| 6 | v6 | 88 | 404 字节 needle 仍写错（"页未" 0/6 命中）；`not_fetchable` 没进不可服务集合；id 51 的正确归宿是 unknown 而非 ok；相关性判据实测命中 10 条而非 8 条；拒绝集若按码建会误伤（159819 对"应用"错、对"AI"对）；`_load_cache` 是缓存优先，不加 servable 到 dict 则 8 个门禁里 6 个空转 | v6.1 附录（B.1-B.10 + D 节）逐条回填 |
| 7 | v6.1 | **90** | 门禁通过，允许执行 | 按 §二 步骤实施 |

### S4a 实测结果（2026-09-21，本地镜像库）

- **根因坐实**：6 位代码在股票域与基金域撞码。`000725` 映射表存"京东方Ａ"（深市股票），
  基金域同码是"大成添利宝货币B"（货基，能正常抓到净值）；`000938`→"华商稳固添利债券C"、
  `000530`→"招商丰盛稳定增长混合A"。旧口径 `if ok: return 'fund'` 让这些行**全部验证通过**，
  于是"京东方看涨"的预测被一只货币基金净值验证——这就是老板说的"差十万八千里"的最底层机制。
- **体检结果**：145 行 → `ok 123 / not_a_fund 17 / code_is_other_fund 4 / unknown 1`，
  金丝雀全程在线、0 条因站点抖动误判；reviewed **119 → 106**（降级 13 条：9 只股票名 + 4 条同码无关基金）。
- **4 条历史假阴性本轮判出**（v4/v5 都会放过）：核聚变→159886 实为机械ETF富国、
  华为概念→159565 实为汽车零部件ETF、应用→159819 实为人工智能ETF、中药→004529 实为长盛盛通纯债C。
  离线名册给出建议标的：中药→**159647 中药ETF鹏华**、应用→**021252**（只建议，未采信）。
- **相关性存疑 9 条**（身份正确但标的八竿子打不着，只报告不降级）：保险→纳斯达克ETF、
  信创→农业ETF、储能→养殖ETF、养殖→智能汽车ETF、Ai应用→软件ETF 等。
- **改标源变干净**：`run_sector_sync.py` dry-run 从"112 映射 / 535 预测"变为"100 映射 / 530 预测"，
  预览 530 条中**目标落在不可服务映射上的行数 = 0**，被降级的 13 个板块全部不再出现在改标源里。
- **可逆性**：幂等二次跑 demote 0；`--restore-from` 演练把 reviewed 还原回 119、
  `is_fetchable=False` 还原回 0，再 apply 回到 106 —— 来回两次，字段逐项相等。
- **测试**：`tests/unit` **498 passed / 16 skipped / 0 failed**（新增 27 条，其中 3 条旧口径测试按新契约改写）。
- **前端实测**：板块匹配页出现「体检不可服务 21」「相关性存疑 9」两枚筛选，
  点开只显示这 21 行且每行带一句中文原因（如"「报 喜 鸟」在基金域查不到同名产品，检索到的是股票市场品种"），
  console 无报错。截图 `run-2026-09-20/ui-s4a-identity-filter.png`。

### ⚠ 部署阻塞（本轮实测撞出来的）

用 `python -m uvicorn src.api.main:app` 直起服务会读 `.env` 的 `DATABASE_URL`（**生产 Supabase**），
实测报 `psycopg2.errors.UndefinedColumn: column sector_fund_mapping.match_source does not exist`
——生产库还没有 alembic 0007/0008 的 10 个证据列。
**结论：本版代码部署到 Render 之前必须先跑 `python scripts/run_migrations.py`，
否则板块匹配相关接口全部 500。** 已写进 S6 的部署步骤。
本地验证一律显式贴 `DATABASE_URL=sqlite:///...`（脚本侧由 `scripts/_db_guard.py` fail-closed 兜住）。
