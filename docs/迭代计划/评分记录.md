# 评分记录 · 板块→基金智能匹配迭代

计划案：`2026-09-20-板块基金智能匹配-总体计划-v1.md`
门禁：计划 ≥90 分才开工；代码 ≥90 分才算完成；<90 按 MUST-FIX 修订后重评。

## 开工前实测事实（作为计划的证据，非评审结论）

| 项 | 结果 | 证据 |
| --- | --- | --- |
| 全量测试基线 | `431 passed, 3 failed, 16 skipped`（151s） | `python -m pytest tests/ -q` @ HEAD 138ab0a |
| AGENTS.md 记载的"123 个测试" | **已过时**，需一并修正 | 同上 |
| 失败 1 | `test_prediction_change_log.py::test_manual_verification_creates_change_log` 期望 `status='verified'`，代码写 `success`（a7d2057 废弃观望后的语义漂移） | `tests/unit/test_prediction_change_log.py:97` |
| 失败 2 | `test_services.py::...failed_predictions_as_verified` 期望 `accuracy==0.5`，代码返回 `50.0`；代码注释明确 accuracy 口径是 0–100（`src/services/prediction_service.py:209`）→ **测试口径错** | `tests/unit/test_services/test_services.py:181` |
| 失败 3 | `test_database_url_routing.py::test_explicit_sqlite_database_url_is_honored` 单跑通过、全量跑失败：`result.stdout` 为 `None`。根因是 `subprocess.run(text=True)` 用 GBK 解码子进程输出，路径含中文时解码异常被吞，stdout 变 `None`（同批 warning 里能看到 `UnicodeDecodeError: 'gbk' codec ...`）→ 必须显式 `encoding='utf-8'` | 复现：`UnicodeDecodeError` in pytest warnings |
| 本机中文输出 | PowerShell/Git-Bash 下 Python `print` 中文乱码（GBK 控制台），脚本一律写文件不回显中文 | 多次观察 |
| APK 工具链 | **可今天出包**：Flutter 3.47.3（`E:\flutter`）+ JDK17（`C:\Users\李文彬\java\17.0.13+11`）+ `flutter doctor` Android 全绿、许可已接受；SDK 真身 `E:\Android\Sdk`（platforms 35/36、build-tools 36、aapt2/adb/sdkmanager 齐）；`ANDROID_HOME` 环境变量指向不存在的 `E:\androidstudioSDK`（需修正或用 `local.properties`）；生产站点 `https://fund-insight.onrender.com` `/api/health` 200 | 子代理实测 |
| 数据基线 | 118 映射（14 待审查）、68 条板块名与基金名零字面重合、`sector_alias` 0 行、162 只基金 `fund_type` 全 NULL、预测 2178（pending 986 / success 648 / failed 544） | `docs/迭代计划/baseline-2026-09-20.md`（由 `scripts/audit_export_baseline.py` 生成） |
| 本地库现状 | 落后于导出（本地 1476 预测 / 90 映射 vs 导出 2178 / 118）→ S1 必须导入 | `sqlite3 data/fund_insight.db` |

## 计划评审

### 第 1 轮（v1）

| 评审代理 | 视角 | 分数 | 结论 |
| --- | --- | --- | --- |
| Reviewer-A | 根因覆盖 / 可行性 / 风险 | 待补 | 待补 |
| Reviewer-B | 数据安全 / 测试可证伪性 | 待补 | 待补 |

MUST-FIX 与处置：待补。

---

## 第 1 轮（v1）实际分数

| 评审代理 | 视角 | 分数 | 结论 |
| --- | --- | --- | --- |
| Reviewer-A | 根因/可行性/风险/可证伪 | **68** | 15 条 MUST-FIX，全部写进 v2（见 v2 §八 对照表） |
| Reviewer-B | 数据安全/统计完整/测试可证伪 | **64** | 12 条 MUST-FIX，全部写进 v3（见 v3 §八 对照表） |

其中被代码/数据复核证实的关键纠正：迁移必须走 Alembic（`init_db` 不给已有表加列）；
`.env` 的 `DATABASE_URL` 指向生产 Supabase（门禁命令差点直接写线上库）；
`_find_fund_in_fundinfo` 才是错配自毒回路；真实工单是 169 板块（69 个无映射行、辐射 1646 条预测）；
`update_mapping` 无条件 `reviewed=True` 会让"待审查归零"门禁可作弊；S4 的预测重映射逻辑
`PredictionMaintenanceService.sync_sector_mappings` 早已存在，应复用而非新写。

## 第 2 轮（v3.1）

| 评审代理 | 分数 | MUST-FIX 条数 | 处置 |
| --- | --- | --- | --- |
| Reviewer-C（架构+可证伪，复核 v3 全部引用） | **74** | 10 | 全部落到代码/计划：见下表 |

| C# | 问题 | 已做的处置（可复核） |
| --- | --- | --- |
| 1 | 证据化 SQL 门禁被 NULL 绕过（`confidence < 0.85` 对 NULL 结果是 NULL） | 判据改为 `confidence IS NULL OR ... OR reviewed IS TRUE`；S4 执行时用 `run-2026-09-20/` 下的 SQL 落盘留证 |
| 2 | `is_fetchable/match_kind/evidence` 等被 G4/§十 引用但 0007 没建 | 新增 alembic **0008**：`is_fetchable`、`evidence`、`reviewed_by`、`owner_locked`（已在真实镜像库 `PRAGMA table_info` 验证 19 列齐全） |
| 3 | proxy 与置信算式冲突：有意代理永远达不到 0.85，会被排除在覆盖率之外 | 拆双阈值（direct 0.80 / proxy 0.68）+ `direct_exhausted` 前置条件；`SectorDecision.auto_reviewable` 实现并有单测 |
| 4 | `name_sim<0.35` 淘汰会杀掉正确答案（半导体→芯片ETF 字面零重合） | 拆成 `claim_sim`（LLM 声称名 vs 官方名，只作降级+打折）与 `sector_overlap`（仅排序），实测 512660/159813 不再被误杀 |
| 5 | `seed_sector_mappings.py` 会把 122 内置板块直接标 `reviewed=True` | S4 步 0 不调用它，改为写 `match_source='seed_builtin'` + `reviewed=false`，并加前后 `reviewed` 计数断言 |
| 6 | 金标集自指（25 条来自未审查的内置字典） | 加 no-op 基线对照，要求 `agent − no-op ≥ +0.15` 且 ≥8 条金标答案与内置字典不同 |
| 7 | `run_migrations.py` 无库守卫；promote 会抹掉 `alembic_version` | 迁移前必须贴连接串；`import_export.py --promote` 之后立即跑迁移（本轮实测：漏跑导致 `no such column: is_fetchable`，已补跑并写进流程） |
| 8 | S4 门禁里的数字（252/435）是跑批前快照，跑完必然不等 | 改为"从最终 reviewed 映射集重算期望值，再断言脚本输出==重算值" |
| 9 | 收敛清单仍漏 `llm_analyzer.py:1174`、`sector_fund_service.get_fund_by_sector` 降级读未审查行 | `_fill_fund_from_sector` 第 4/5 层已整体改走 agent；`get_fund_by_sector` 调用点保持但 T0 只把未审查行当候选（`source_prior=0.6`） |
| 10 | 无法区分老板挑定的行与 agent 写的行 | 0008 的 `owner_locked`/`reviewed_by`；`apply_decision` 遇锁定行直接拒绝并返回 `locked`（有单测） |

## 实现期实测（S1–S3）

- S1：`fund_insight_export_2026-09-20.json` → 影子库 → `--promote`，15 表计数全等、`PRAGMA foreign_key_check` 违规 0、字段级抽样 0 差异；备份 `data/backups/fund_insight_pre_import_20260920_234429.db`。
- S2：`tests/unit` **451 passed / 16 skipped / 0 failed**；真实 LLM+抓站实测：半导体→159995 芯片ETF华宝、债券→511010 国债ETF、白酒→161725 白酒LOF、日股→513880 日经225ETF、存储→159813（proxy）、军工→512680；SpaceX 因预算超时诚实落到 `needs_review`。
- S3：`POST /ai-match` 端到端 200（8.8s，写入 mapping_id=121，置信 0.759 < 0.80 故 `reviewed=false`）；浏览器实测板块匹配页 AI 按钮渲染、证据链面板显示 T0→T3 全过程、console 无报错；发现并修掉 `ai-batch` 的锁重入死锁（`Lock`→`RLock`，加回归测试）。


---

## S4a 映射表身份体检 —— 计划评分轮次（老板要求的"每步计划 AI 评分 ≥90"）

| 轮 | 版本 | 分 | 打回的关键理由 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | v1 | 62 | `_mapping_eligible` 的历史分支（reviewed=1 且 confidence IS NULL 无条件放行）没被关闭；`is_etf` 的 `15[0-9]{3}` 是 5 位死分支；"feeder 置信 ≤0.80"在算术上不可达（KIND_PRIOR 权重只有 0.15）；降级不挡读路径（`get_fund_by_sector` 有降级分支会捞回未审查行）；一键"全部标记已审查"可复活被洗掉的行 | 全部落进 v2，并逐条实测复核 |
| 2 | v2 | 68 | 体检依赖 `verify_fund_fetchable(probe_stock=True)` 判股票——**实测 145 行 0 条被判 stock**；官方名回填会误伤 33 行（含 SpaceX→159206）；`is_fetchable != False` 的三值 NULL 会让 105 条历史映射静默全灭；只改了 1 条读路径，还有 3 条裸查表；"搜索唯一"不成立（传媒有 3 只同名 ETF）；金标门禁 uplift 恒为 0 不可能通过 | v3 换判据、补读者、删自动 reviewed |
| 3 | v3 | 66 | 名字来源 `search_fund` **本身是混合证券搜索**（000938→紫光股份），"官方名"根本不代表基金域；push2 股票域在本机 ProxyError，"reviewed 的股票降为 0"这个指标会因工具坏掉而空过；二字 Dice 会把 `华宝中证医疗ETF` vs `医疗ETF华宝`（同一只）判成 0.33 错行 | v4 彻底放弃股票域，改 NFKC + 字符集 Jaccard |
| 4 | v4 | 79 | `wrong_code` 被自己写死的先决条件弄成**不可达**（"本行码不是基金域任何品种"——填错的码按定义仍是基金），导致 4 条历史假阴性（核聚变/华为概念/应用/中药）仍放过；`match_kind` 是 `String(12)` 且属 agent，塞 verdict 会在 Postgres 溢出；phase-evidence 写 `match_source` 会把 145 行"证据齐备"集体点亮、解除审查门禁 | v5 新增 `code_is_other_fund`、列使用纪律、幂等四元组 |
| 5 | v5 | 84 | 站点负证据判定写的是文本 `'页面未找到' in text`，实测 404 页是 HTTP200 + 3924B 的 GBK 乱码 title，**永不匹配**；`is_fetchable` 只表达"能不能抓"导致 QDII 慢更新行被无声踢出服务；漏了第 6/7/8 条读者（含 `prediction_maintenance_service:151` 这条真正写预测的通道）；"≥55 板块仍合格"指标不可达 | v6 全部按行号点名修正 |
| 6 | v6 | 88 | 404 字节 needle 仍写错（"页未" 0/6 命中）；`not_fetchable` 没进不可服务集合；id 51 的正确归宿是 unknown 而非 ok；相关性判据实测命中 10 条而非 8 条；拒绝集若按码建会误伤（159819 对"应用"错、对"AI"对）；`_load_cache` 是缓存优先，不加 servable 到 dict 则 8 个门禁里 6 个空转 | v6.1 附录（B.1-B.10 + D 节）逐条回填 |
| 7 | v6.1 | **90** | 门禁通过，允许执行 | 按 §二 步骤实施 |

### S4a 实测结果（2026-09-21，本地镜像库）

- **根因坐实**：6 位代码在股票域与基金域撞码。`000725` 映射表存"京东方Ａ"（深市股票），
  基金域同码是"大成添利宝货币B"（货基，能正常抓到净值）；`000938`→"华商稳固添利债券C"、
  `000530`→"招商丰盛稳定增长混合A"。旧口径 `if ok: return 'fund'` 让这些行**全部验证通过**，
  于是"京东方看涨"的预测被一只货币基金净值验证——这就是老板说的"差十万八千里"的最底层机制。
- **体检结果**：145 行 → `ok 123 / not_a_fund 17 / code_is_other_fund 4 / unknown 1`，
  金丝雀全程在线、0 条因站点抖动误判；reviewed **119 → 106**（降级 13 条：9 只股票名 + 4 条同码无关基金）。
- **4 条历史假阴性本轮判出**（v4/v5 都会放过）：核聚变→159886 实为机械ETF富国、
  华为概念→159565 实为汽车零部件ETF、应用→159819 实为人工智能ETF、中药→004529 实为长盛盛通纯债C。
  离线名册给出建议标的：中药→**159647 中药ETF鹏华**、应用→**021252**（只建议，未采信）。
- **相关性存疑 9 条**（身份正确但标的八竿子打不着，只报告不降级）：保险→纳斯达克ETF、
  信创→农业ETF、储能→养殖ETF、养殖→智能汽车ETF、Ai应用→软件ETF 等。
- **改标源变干净**：`run_sector_sync.py` dry-run 从"112 映射 / 535 预测"变为"100 映射 / 530 预测"，
  预览 530 条中**目标落在不可服务映射上的行数 = 0**，被降级的 13 个板块全部不再出现在改标源里。
- **可逆性**：幂等二次跑 demote 0；`--restore-from` 演练把 reviewed 还原回 119、
  `is_fetchable=False` 还原回 0，再 apply 回到 106 —— 来回两次，字段逐项相等。
- **测试**：`tests/unit` **498 passed / 16 skipped / 0 failed**（新增 27 条，其中 3 条旧口径测试按新契约改写）。
- **前端实测**：板块匹配页出现「体检不可服务 21」「相关性存疑 9」两枚筛选，
  点开只显示这 21 行且每行带一句中文原因（如"「报 喜 鸟」在基金域查不到同名产品，检索到的是股票市场品种"），
  console 无报错。截图 `run-2026-09-20/ui-s4a-identity-filter.png`。

### ⚠ 部署阻塞（本轮实测撞出来的）

用 `python -m uvicorn src.api.main:app` 直起服务会读 `.env` 的 `DATABASE_URL`（**生产 Supabase**），
实测报 `psycopg2.errors.UndefinedColumn: column sector_fund_mapping.match_source does not exist`
——生产库还没有 alembic 0007/0008 的 10 个证据列。
**结论：本版代码部署到 Render 之前必须先跑 `python scripts/run_migrations.py`，
否则板块匹配相关接口全部 500。** 已写进 S6 的部署步骤。
本地验证一律显式贴 `DATABASE_URL=sqlite:///...`（脚本侧由 `scripts/_db_guard.py` fail-closed 兜住）。


## S4a 代码质量评审（对执行产生的代码打分，老板要求的 ≥90 门禁）

| 轮 | 分 | 主要打回理由 | 处置 |
| --- | --- | --- | --- |
| 1 | 72 | 静态表 `SECTOR_FUND_MAP` 三处回落没过拒绝集（降级在这三处等于没降）；agent 把**抓取**语义写进语义已改为"可服务"的 `is_fetchable`；`update_mapping` 换标的后不清旧结论；体检把 `status='error'` 写进续跑缓存（一次抖动永久化）；`--tag` 日粒度会让二次 apply **覆盖唯一回滚清单**；单测越界打 3.1MB 名册；两条测试缺 `assert` | 逐条落地：共享 `static_fund_for_sector`；agent 不再拥有该列（改记 `{'stage':'FETCH'}` 证据）；error 不入缓存；tag 带时分秒 + 清单撞名加后缀；autouse fixture 断网；补断言 |
| 2 | 78 | `update_mapping` 绕过自家门禁（只翻状态就能复活不可服务行）；`apply_decision` 换码仍留 `is_fetchable=False` 且把 evidence 覆写成列表；`llm_analyzer` 异常兜底直接放行未过滤静态表；`rejected_codes` 归一分支查了**另一个库**；`servable` 与 `row_unservable` 两个真值来源不一致；"读者测试"只查源码字符串（未使用的 import 也算过） | 逐条落地：改码即清结论 + 只翻状态时拒绝；兜底走 `_audited_static_map()`；`db=` 一路传到底；行级判据收敛为 `row_unservable`；读者测试改为行为测试 + 新增**镜像不变量测试**（verdict 不可服务 ⇒ 列必为 False） |
| 3 | 85 | 归一分支的 `db=` 还是没传（且这两个函数零测试）；**只改名字**就能复活降级行（实测）；写库方仍把手上的行塞进类级缓存；`_cache_loaded` 永不过期 → Cron 里的体检对 Web 进程是**空操作**；同码重判既不清标记又抹掉身份证据却回报 applied=True；`POST /sector-mappings` 把门禁拒绝说成"更新成功" | 逐条落地：`changed` 只认代码；写库出口统一 `refresh_cache()`；缓存加 60s TTL 且写类属性；同码重判返回 `audit_rejected`；PUT/POST 拒绝都返回 success=False + 人话；补 6 条行为测试（含改标写通道 `sync_sector_mappings`） |
| 4 | 88 | 3 条 MUST-FIX：两条修复无回归覆盖（revert 后测试仍全绿）；生产可能残留"False 且无 verdict"的旧语义行会黑洞；`mark_reviewed=False` 的名字编辑让 SQL 读者与 Python 读者结论相反 | 补 2 条 strict 包装测试；体检新增 `repair_legacy_fetchable_flag`（写库前自动复位并计数）；`update_mapping` 写库出口重物化镜像；`_cache_loaded`/`_cache_at` 统一类属性；`nulls_last` 补齐；文案修正 |
| 5 | **91** | 通过。剩余 3 条为测试/文案补充：归一化那一支要用真会变化的键（半导体板块→半导体）；repair 与镜像两处仍无覆盖；两处"需重启"文案与 60s TTL 矛盾 | 全部补齐：`test_rejected_codes_never_falls_back_to_the_process_cache` 改用 `'半导体板块'` 真走归一分支；新增 `test_repair_resets_flags_that_have_no_verdict`、`test_retag_writer_refuses_unservable_mapping`、`test_sweep_never_blacklists_an_owner_locked_row`；文案改 60s TTL |

第 5 轮评审另外点出两个 S4b 真陷阱，已在本轮一并修掉：

- **owner 行被盖"不可服务"**：`apply_results` 以前对老板锁定行也写 `is_fetchable=False`，而审查门禁连 `owner_confirm` 都拒绝 → 老板自己选的代理会被永久关在门外。现在 owner 锁定行只记证据、不打不可服务章（有单测）。
- **改标源只查列**：agent 换标的会把列清成 NULL，`sync_sector_mappings` 的映射源于是可能拿"从没做过身份体检的新标的"去改几百条预测。`_mapping_eligible` 现在同时认 verdict（有单测）。

### 门禁结论

- 计划案：v1→v6.1 = 62 / 68 / 66 / 79 / 84 / 88 → **90**（≥90 放行执行）。
- 代码质量：72 → 78 → 85 → 88 → **91**（≥90 放行）。
- `pytest tests/unit`：**509 passed / 16 skipped / 0 failed**；`python -m src --init-db` 通过；
  本地镜像复检：145 行、reviewed 106、不可服务 21、镜像断裂 0、owner 两只代理（id 25/147）健在；
  体检二次跑降级 0 条（幂等）。

### 交给 S4b 的硬规则（评审点出，防再次踩坑）

1. **顺序**：任何 AI 批量（`ai-batch`）之后必须先跑一次 `sweep_sector_mappings.py --apply`，
   再决定是否 `run_sector_sync.py --apply --run-id`；因为 agent 换标的会清掉身份标记。
2. **部署顺序**：`alembic upgrade head` → 部署 → 立刻跑一次体检（会顺带复位旧语义遗留的
   `is_fetchable=False`）。缺列未迁就上线会让所有映射读接口 500。
3. `get_fund_by_sector` 命中缓存时不查库：跨进程结论传播受 60s TTL 限制，验证时要等或重启。


## S4b（存量数据改标）本轮实测结论 2026-09-21

### 先补一个真 bug：回滚工具自己跑不起来

`scripts/restore_prediction_batch.py` 的 `main()` 里写
`from src.models.database import snapshot_prediction` —— 那个函数住在审计服务模块里，
所以**回滚脚本一进 main 就 ImportError 崩掉**，而既有单测只 import 常量与 `coerce_value`，
全套测试照样绿。也就是说"改预测之前以为自己能回滚"是假的。已修（正确模块），
并实测：`--run-id` 回滚 530 条预测、重算 22 个博主统计与 70 只基金计数，
`pred 7` 的标的从 162412 精确退回 512170。

### 顺带查出：映射表里还有"联接基金冒充 ETF"

回滚预演时发现 医疗 板块的映射是 **162412 华宝医疗ETF联接A**（场外联接，身份体检判 `ok`
因为它确实是那只基金），而 remap 会把 40 条医疗预测从正确的场内 ETF 改成它。
老板规则是"选基金最好是 ETF，因为最纯粹"，所以体检新增 `--upgrade-etf`：

- 只在"原标的不是场内 ETF **且** 名册里存在官方名含板块核心词的场内 ETF **且** 该 ETF 严格可抓"时替换；
- 同指数往往有多只 ETF（医疗：512170 / 159828 / 158010 / 159877），名字长度分不出好坏，
  排序信号用**本系统已存的净值条数**（零网络、有区分度，且保证选中的是现在就验证得了的那只）；
- 老板锁定的行、以及身份体检已判不可服务的行一律不动（各有单测）；
- 新标的先补 `fund_info` 档案再写映射（`fund_code` 有外键，不补就 IntegrityError——实测踩到）。

落地 3 条：医疗 162412→**159877 医疗ETF南方**、传媒 010677→**159805 传媒ETF鹏华**、
亚太精选 050015→159687 亚太精选ETF南方。

### 改标结果（本地镜像库）

- `run_sector_sync.py --apply --run-id rematch-20260921b`：**改标 530 条预测、重置 351 条验证结论**，
  全部写进 `prediction_change_logs`（带 run_id，可整批回滚，已实测）。
- 改标预览里 `new_fund_code` 落在"不可服务映射"上的行数 **0**；被降级的 13 个板块不再出现在改标源里。
- 改标前后对比：可改映射 112→**100** 条（把股票/错基金从源头摘掉），预测改动 535→530 条，
  无映射预测 1000→1006 条（宁可不改也不能改错）。
- 351 条被重置的预测现在是 `pending`，等预测验证任务重跑（这一步只用净值数据，不需要 LLM）。

### 待验证状态

`tests/unit` **511 passed / 16 skipped / 0 failed**；`python -m src --init-db` 通过。
剩余待办：351 条 pending 预测的重新验证、约 60 个 gap/待审查板块的真实匹配（需要 LLM，
当前 `403 AccountOverdueError`）、生产定向回写、APK 与 GitHub 推送。


## S4a-v7.3/v7.4：不相关标的确定性纠正（代码质检轮）

| 轮次 | 分数 | 结论 |
| --- | --- | --- |
| 计划 v7 → v7.1 → v7.2 → v7.3 | 83 → 87 → 88 → **91** | v7.3 定稿（3 个一行级规格漏洞并进步骤 1-3，免再审） |
| 代码第 1 轮 | **81** | 12 条缺陷 + 5 条缺失用例；详见 `2026-09-21-S4a-v7.4-代码评审修订.md` |
| 代码第 1 轮处理 | 采纳 8 条 | 报告口径、性能（分钟级→13.4s）、evidence-only 门禁、理由文案、公司名剔除顺序、金标进 fixtures 并去掉静默 skip、+5 条用例 |
| 代码第 1 轮驳回 4 条 | 附证据 | 子串匹配（最长认领已挡，加边界会复现 v7.1 记录的假阴性）、`_realign_*` 并非死代码、`core_variants` 已按 upper 查表、realign 行**实测在 `/sector-mappings` 里可见**（231 行含 5 行 `realigned`） |
| 代码第 2 轮 | **87** | 残留 8 条已全改（见 v7.4 §五）；另由端到端实测抓到 agent 换标的撞外键的真 bug（§六） |
| 代码第 3 轮 | 89 |
| 代码第 4 轮 | **88** | 两条 BLOCKER（升级行仍显示旧标的的"已审查/已自证"）+ 五条 MAJOR，已全修，见 v7.4 §十 |
| 代码第 5 轮（复审） | 89 | D1~D14 部分落地，另抓到我自己造成的 **verify_message 数据损坏**（39 行理由被刷平），已修代码 + 从备份返修 |
| 代码第 6 轮 | **84** | 3 BLOCKER + 6 MAJOR：回写脚本 11/13 字段被静默丢弃且会把行永久锁成 owner 行、5 个连生产的 `_tmp_` 脚本即将随 push 发布、`_db_guard` 逃生口不设防、可服务判据两套（SQL 服务 / Python 隐藏）、0.80↔0.85 死区让 34 条预测仍按错标的打分、机器换标的的章挂在会被盖掉的 `match_source`、人工改码是唯一无"基金非股票"证明的写入口、回滚会吞 209 行历史净值。逐条复核**全部成立**，见 v7.4 §十四 |
| 代码第 7 轮 | **83** | 我修第 6 轮的 B1 时，把 M1/M4 在更大的写入口上重开了：回写端点逐字段照搬、不验身份、还能从线上获得 owner 免疫。四条已全部堵回，见 v7.4 §十四/§十五 |

落库事实（本机 SQLite，备份+清单可回滚，已各验证一次）：纠正 5 行（中药 004529→560080、
保险 159632→512070、信创 516810→562570、储能 159867→159305、养殖 516590→159865），
`code_is_other_fund` 4→3、`ok` 123→124、相关性存疑 9→0、二次跑 0 变更；
单测 538 → **575 passed / 16 skipped**；LLM（火山引擎，主力 doubao-seed-2-1-pro-260915 / 辅助 glm-5-2-260617）已接通，首批 6 个待审查板块 agent 实测 4 条落库。

| 代码第 8 轮 | **87** | 审查按钮伪造老板署名（秦安股份 0.7975、硬件 0.755 靠它绕过门槛驱动改标）、新建行接受垃圾数据、回写清单过期 41 处、run_id 串批、"生产前像"其实是假证据；并推翻了我"剩 80 条是本地数据限制"的错误结论（真因是补拉只看最早一天）。见 §十六 |
| 代码第 9 轮 | **88** | 无名行是身份体检的盲区（体检入口 unknown=不指控）→ 缺名创建/回写两条路都能产出"可服务+已审查"的股票映射；批量取消审查留下"未审查却老板锁定"的僵尸行；我上一轮的密度门槛会要求窗口物理上放不下的条数（373 条预测空转）。见 §十九 |
| 代码第 10 轮（S7 系列复评，编号与上一行末尾那条"进行中"是同一次） | **58** | 2 BLOCKER + 5 MAJOR，我逐条自复现后**全部成立**：① 一次网络抖动被写成"源端确实没有"的凭据并 7 天免检（真数据永远回不来）；② 多次探测做区间并集，把从没问过的空洞谎称问过；③ 目标日就是今天、净值未发布被误标"永久不可验"，提示语还建议把目标日往后挪＝未来函数；④ 清理脚本从"今天的门"取候选，看不到窗口空着的历史 0% 假结论；⑤ 补拉后无条件 commit 把 `_warm_cache` 作废、批量退化成 N+1 且替调用方提交事务；⑥ `start_nav_date`/`end_nav_date` 两套日期口径；⑦ 不限 id 的毁灭式回溯仍被前端按钮一键可达。逐条修法见 §廿五，提交 `1b4737b` |
| 代码第 11 轮 | **74** | 复评 `1b4737b` 的修复本身。1 BLOCKER + 8 MAJOR 我逐条复现成立：`Data:null` 仍被当成"问过且没有"（并用真请求确认了线上"真无数据"的形状是 `LSJZList:[]`+`TotalCount:0`）；上一轮的提交条件因为 flush 后 `Session.new/dirty` 已空而**永远不成立**（注释声称的"立刻提交"是假的）；非空答复仍被并段，破掉"单次探测完整包含"；凭据上限按 start 截尾丢掉的恰是每天要重问的老窗口（秒级时间戳并列还会裁错人）；"缓存为空才回落"只堵了一半（部分命中照样少算点数）；清理脚本 print 参数不匹配（一有活就崩）；前端回溯按钮根本没有定向入口、200 + `success:false` 被当成成功；PG 唯一约束冲突会把整批验证打断；老基金一类永远拿不到凭据 ⇒ 无限重试。修法与用例见 §廿六 |
| 代码第 12 轮 | **79** | 复评 `3c9ad92`。2 BLOCKER + 6 MAJOR：信封校验只查"键存在"（`LSJZList:null`、`ErrCode≠0`、整页解析失败仍产出 `[]`→写负凭据）；`TotalCount` 缺省回落成当页行数 ⇒ 翻页保护整体失效、**比改前更差**；`same_nav_endpoint` 的第二条证据是纯日历推断 ⇒ S7-b 的硬约束被自己开后门；我为修 PG 并发引入的 SAVEPOINT 在 SQLite 上等于提前提交（被自己新加的回滚用例当场证伪）；`get_nav_history` 仍采信部分命中的缓存 ⇒ 过程指标可让 Cron 与手动判出相反结论（上一轮只修了点数那一半）；回溯真写没有 run_id、`verify_history` 不留痕、前端 `ids` 取自不限 id 的全量预览（"定向"是空保护）；分诊脚本 `--json` 引用了已删变量必崩、自检被写成字面量 `'OK'`。逐条处置与"S7 整体质检结论"见 §廿七 |
| 代码第 13 轮 | **83** | 复评 `cd475dd`。我上一轮"合并取较旧时间戳"被证伪：今天刚问到的凭据当场作废 ⇒ 从第 3 天起每天重问、结构性归因天天翻脸（评审按真实顺序模拟 92 天：空答复窗口发问 86 次、凭据只覆盖 9 天）；预热缓存存 ORM 实例 + 每条预测一次 commit ⇒ 预热翻转成 N+1 放大器（0 → 720 条 SELECT）；墓碑写在前端不读的位置、还会被渲染成"验证失败/0 分/错误"的假历史；详情抽屉 19 处读 `verify_history[0]` 而后端是正序 ⇒ 451 条多轮验证的预测打开都看到最早那轮；分诊自检仍是恒等式。处置见 §廿八 |
| S5 交叉回归（首次覆盖 S7 之外的模块） | **58** | 两条 BLOCKER 都是真数据问题：① 板块匹配的静态表排在**已审查映射之前**，命中即 return ⇒ 新预测直接挂错标的（15 个板块实测可复现），下一轮改标又抹掉结论；② `end_nav_date > target_date` 的已验证结论 **94 条 / 1168**，是 S7-1"休市顺延"用目标日之后净值判出来的，违反本仓库防未来函数硬约束，一直留在准确率里。处置见 §廿八（含"94 条撤掉重判后判对 608→598"的实测差值） |
| 代码第 14 轮 | **87** | 复评 `f4ec931`。**0 BLOCKER**，且评审把我上轮每条声明都自己复现（凭据发问 86→31 次／可归因 9→92 天、`end_nav_date>target_date` 全库 0 条、判对 598 判错 569、清零字段逐字段一致、博主统计 0 不一致）。6 MAJOR 全成立，最刺眼的还是"把意图当事实"：① `owner_confirm:false` 在真实数据上是纯空操作（23 行未审查全部有机器证据，传真传假都照样盖 owner + 锁定），而弹窗写的是"不会冒充老板署名、不会给免疫"；② 新加的覆盖导入确认头 UI 从不发 ⇒ 覆盖模式成必然 403 的死控件，catch 还读 `message` 而 FastAPI 在 `detail` ⇒ 指引全丢，且这道安全边界零测试；③ `get_fund_by_sector` 的 reviewed 记忆永不过期、重灌时也拒绝降级，而本轮刚把它提到匹配链最前端；④ 出口倒序后"本次验证详情"把已撤销的结论当现状显示（11 行）；⑤ `_ttl_for` 自己调 `date.today()` 破坏 `today` 注入契约；⑥ `SNAPSHOT_FIELDS` 不含 `verify_history` ⇒ "run_id 可整批还原"还原后自相矛盾。处置见 §廿九 |
| 代码第 15 轮 | 进行中 | 复评对象 = 第 14 轮的修复；当前全量 **695 passed / 16 skipped** |

**我自己这轮造成的事故（也记在闸门里）**：所谓"库副本重放"其实写进了镜像库
（engine 在 import 时就绑死 URL），88 条已验证预测被清掉重算，已从老板 09-20 导出快照
逐字段还原 + 9 条走正常路径重验，判对 608 / 判错 560。详见 §廿四 与脚本
`scripts/repair_replay_side_effects.py`（`run_id=replay-repair-20260921a`）。

**当前门禁状态**：最高 88 分 < 90（第 10 轮回落到 58，修完待第 11 轮复评），**未推 GitHub**；
生产迁移与定向回写**未执行**（等老板授权）。S7 已完成 S7-1（撤销）/S7-2/S7-b，
剩 S7-c（持久"不可验"状态，需新列＝生产迁移，等老板点头）：
`2026-09-21-S7-休市顺延与结构性不可验-计划案.md`。

