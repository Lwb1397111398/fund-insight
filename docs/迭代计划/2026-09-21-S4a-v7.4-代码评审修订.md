# 附：S4a-v7.4 代码评审修订（代码质检 81 分后回填）

评审对象：本轮新写的"确定性纠正不相关标的"（`--realign-irrelevant`）代码。
评审结论 **81/100**，12 条缺陷 + 5 条缺失用例。逐条按"实测优先"处理：能核实的按建议改，
核不出来的按建议改，与实测冲突的写明证据不复述。

## 一、已采纳并改掉的（8 条）

| # | 评审意见 | 处理 | 验证 |
| --- | --- | --- | --- |
| 2 | `pick_etf_upgrade` 在计划期不看 `--upgrade-etf`，报告"可替换 1 条"与"写 0 行"矛盾 | 计划期按 `args.upgrade_etf` 才算 | 改后 dry-run 打印 `可替换 0 条`（不带该 flag） |
| 3 | `_claims` 每行重扫 3000+ 指数段 × 129 核心词 × 36 公司名，整轮要十几分钟 | 先用本板块核心词粗筛，再只对**粗筛命中的段**算认领表（缓存） | 全量 145 行 dry-run `real 0m13.4s` |
| 5 | `--evidence-only` 仍会执行 `apply_realign` → 换标的但证据是旧降级 = 半写状态 | 门禁加 `and not args.evidence_only` | 单测 + dry-run |
| 7 | 升级理由用 `official_name`，与老板在册名不一致时会说"替换一只没持有的基金" | 改用 `row.fund_name or official` | 新增 `test_upgrade_reason_names_the_held_fund` |
| 8 | `FUND_COMPANY_TOKENS` 是无序 frozenset，`华泰` 可能先于 `华泰柏瑞` 剔成残词 | 按长度降序剔 | 代码即证（`sorted(..., key=len, reverse=True)`） |
| 12 | 金标缺失时测试静默 skip = CI 全绿但跑 0 用例 | 金标复制进 `tests/fixtures/core_matcher_golden.json`，测试读它且**去掉 skip**；探针同步写两份 | `-k core_matcher` 1 passed（非 skip） |
| 缺 1 | 没有 `code_is_other_fund` 模式下的 realign 用例 | 新增 `test_realign_fixes_a_demoted_row`，并断言 `domain_source == pingzhong` | 通过 |
| 缺 2/3 | 没有"粗筛后候选为空"和"净值停更"用例 | 新增 `test_realign_returns_none_without_candidates`、`test_stale_nav_candidate_rejected`（90 天 vs 今天） | 通过 |

`tests/unit/test_sector_identity_audit.py`：58 → **70 passed**。

## 二、评审说"是缺陷"，但实测/读码不成立的（4 条，写清证据）

1. **子串匹配会张冠李戴（评审列 BLOCKER，建议加 CJK 整词边界）**
   最长认领规则本身已挡住"更长的板块名抢标的"：候选段被更长核心词命中时本板块拿不到它
   （`etf_candidates` 里 `claims[0][1] > mine` 直接 continue）。评审建议的边界写法
   （前后不能是汉字）会把 `富国中证核电ETF` 里的"核电"判成没命中——那正是 v7.1 §一
   写死要避开的假阴性，且实测 `中药→560080 中药ETF汇添富` 的段是"中药"、`保险→512070`
   的段是"证券保险"，两者都不靠子串越界成立。**不改**，保留朴素子串 + 最长认领 + 公司名剔除。
2. **`_realign_setup`/`_realign_row` 是死代码（MINOR）**：被
   `test_realign_picks_matching_etf_and_resets_conclusion` 等 5 个用例调用
   （grep `_realign_setup(` 命中 789/833/845/855/870 行）。**不删**。
3. **`core_variants('5g')` 丢同义词（MINOR）**：实现是
   `SECTOR_SYNONYMS.get(core.upper(), ())`，小写同样命中。按"可判定"补了
   `test_core_variants_case_insensitive` 钉死。
4. **realign 后的行在 `/sector-mappings` 里看不见、前端分桶恒 0（MAJOR）**
   `config.py:1140` 的 db 行走 `service.get_all_mappings_with_status()`，
   而 `sector_fund_service.py:156` 对每行 `item.update(identity_view(m))` →
   `realigned`/`servable`/`relevance_low` 都在；`reviewed=False` 的行在
   `reviewed=None`（全部）和 `reviewed=false`（待审查）两种筛选下都会返回。
   **实测证据**（本机起服务 + 直接打接口，`DATABASE_URL` 已钉本地 SQLite）：
   `GET /api/config/sector-mappings` 返回 231 行（192 已审查 / 39 待审查），
   其中 5 行带 `realigned`，且 `reviewed=False servable=True verdict=ok rel_low=False`，
   与库里逐行核对一致 → **评审这条不成立，未改代码**。
   未做完的部分要说明白：像素级复核没做成——本地一次性口令被 `system_config`
   里持久化的真口令覆盖，没进得了登录门；页面加载本身 0 条 console 报错。
   这一条留给老板在自己设备上确认（前端分桶「机器已纠正待复核 5」+ 行内提示）。

## 三、留给 S5/S6 的（评审点到，本轮不动）

- `EXCHANGE_LISTED_CODE` 是深市 15xxxx + 沪市 5xxxxx 的**并集**，docstring 未写这条限制；
  场内 LOF/货基与场内 ETF 的边界要靠 `etf_candidates` 的 `'ETF' in name` 二次判。
- 生产（dev Supabase）确有 `is_fetchable/owner_locked` 列，但 `migrations/` 里**没有**
  建这两列的脚本（`scripts/migrations/0007/0008` 的落地方式要在 S6 核实）。
- `updated_at` 由 `onupdate` 自动前推，`_coerce` 只还原字符串，不还原"改之前的时间戳"。

## 四、S5 副本实测自己抓出来的 MAJOR（评审没看到）

在**本地库副本**上跑"老板批准 → 改标 → 重置验证"整链，第一次跑出来：
`batch_mark_reviewed → 0`，5 行全都拒绝被审查，`reviewed` 仍是 False，改标 0 条。

- 根因：`apply_realign` 把 `match_source` 置成了 `None`（我当时按"复位旧结论"处理），
  而审查门禁 `sector_fund_service.py:264` 要求 `match_source && verified_at` 齐备才允许标记已审查。
  结果这 5 行变成"可服务、但老板点不动"的死档：预测永远改不到新标的，
  老板的核心诉求在预测层等于没修。
- 修法：`match_source = 'identity_realign'`（16 字符 ≤ 列宽 20），既是可审查的 provenance，
  又不冒充 `agent/manual` 那两种来源；单测把这条钉成
  `assert row.match_source == 'identity_realign'` 并写明"置 None 会让行可服务但不可审查"。
- 真实库那 5 行已定向补 stamp（只改 `match_source` 一列）。
- 副本复跑结果（同一份代码路径，跑完即删副本）：
  `batch_mark_reviewed → 5`，改标 **18 条预测 / 重置 15 条验证结论**（run_id=s5-chain-test），
  逐板块：中药 10 条预测中 8 条指向 560080、储能 8→7 指向 159305、保险 2→2 指向 512070、
  养殖 1→1 指向 159865、信创 0 条预测。日志 `run-2026-09-20/s5-chain-test.log`。
  所以"批准后预计改 N 条"从 v7.2 的推断变成了实测数字。

## 五、第 2 轮评审：87 分（未过 90，继续按建议改）

评审员逐条核了第 1 轮的 8 项修复，结论 **87/100**：`#2 粗筛+认领缓存`被独立复算过
（对 145 个板块重跑"无缓存全表版"再 diff，**0/145 结果差异**），realign 五条落库行、
`--max-realign` 先于备份/写库、写序 `apply_results → apply_realign → demote_unwritten`
都判定为成立；驳回它对"子串匹配"的原始理由（承认保留子串，但指出真正的保护是准入门槛
而不是最长认领）。残留 8 条已全部改掉：

| 残留项 | 处理 |
| --- | --- |
| MAJOR `pick_etf_upgrade` 只读**存量** verdict | 加判本轮 `verdict_row['verdict']`，配用例（存量无体检证据、本轮判股票的行不许换标的）——这条对生产第一次跑是致命的 |
| MAJOR 探针把金标写成连字符名，测试读下划线名 | 文件名对齐；否则"重新生成金标"是空转，断言被冻在旧快照 |
| MINOR 升级路径不认刻意代理与占用 | 加 `taken`/`proxy_codes`，拒绝集读不到就 fail-closed 不升级 |
| MINOR `replaced` 字段用站点名 | 改回在册名 |
| MINOR `occupied_codes` 被 `--limit` 截断 | 改成全表 distinct 查询 |
| MINOR "理由用在册名"那条用例是空断言 | 让 official_name 与在册名不同，并加反向断言 |
| MINOR 一处 15 空格错位、临时脚本残留、文档口径仍是 `match_source=None` | 全部清理/对齐 |

## 六、端到端实测自己抓到的真 bug（评审没抓到，因为它不在测试路径上）

LLM 通了之后跑第一批 6 个待审查板块，**整批后台任务被一条异常连坐**：

- 现象：`德明利/京A/水晶/紫紫` 4 条正常落库，`中科三环 → 562800 稀有金属ETF嘉实`、
  `核聚变` 两条报 `IntegrityError: FOREIGN KEY constraint failed`
  （SQL 是 `UPDATE sector_fund_mapping SET fund_code=...`）。
- 根因：`apply_decision` 只在"新建映射"分支补 `fund_info` 档案，
  **换标的**分支不补 → 新代码本地没档案就撞外键，一条失败让整批线程崩掉。
- 修法：抽出 `ensure_fund_archive(db, code, name, sector)`，落笔前一律先补档案；
  配用例 `test_switching_target_creates_missing_fund_archive`（该 fixture 外键是**真开启**的，
  先写错一次就复现了 IntegrityError，所以它确实钉得住这个 bug）。
- 单测：558 → **559 passed / 16 skipped**。

副产品：`scripts/verify_realign_chain.py`（库副本上跑"批准→改标→重置"整链，报实测数字）。

## 七、第 3 轮评审（89 分）残留项已清

| 评审项 | 处理 | 用例 |
| --- | --- | --- |
| NEW-1 `--evidence-only` 仍会随 `--upgrade-etf` 换标的 | 升级分支加 `not evidence_only` | `test_upgrade_etf_writes_nothing_under_evidence_only` |
| NEW-3 升级后留着"verdict=ok 讲的是旧代码"的假自证 | 旧证据挪进 `identity_before_upgrade`、删 `identity`、`verify_message` 改成升级理由，且镜像章跳过本轮升级行 | `test_upgrade_leaves_no_stale_identity_evidence` |
| NEW-4 同轮两个板块升到同一只 ETF | 占用登记内聚进 `pick_etf_upgrade`（写回调用方的 `taken`），不再只在 main 的 print 里 add | `test_upgrade_registers_occupancy_within_the_round` |
| NEW-5 无上限、写前不重读库 | 新增 `--max-upgrade`（默认 8）；写库前 `db.expire_all()` | 干跑校验 |
| NEW-6 升级路径每行最多打 20 次站、不看净值新鲜度 | 加 `MAX_VERIFY_PER_ROW` 预算 + `_nav_fresh` 门槛 | 两条旧用例的 fake 补了 `nav_date`，否则当场红 |
| 崩溃窗口：`apply_realign` 抛异常时降级不补 | `demote_unwritten` 挪进 `finally` | — |
| NEW-7 `_ALNUM` 死变量 + docstring 与代码不符 + 连字符金标残留 | 全删/改写/清理，`tests/fixtures/core_matcher_golden.json` 用最新探针产物刷新 | — |
| 评审漏判的一条 | `test_agent_archive_rolls_back_with_the_mapping`：提交失败时 `fund_info` 不留孤儿（agent 路径没有 manifest，靠同一事务） | 通过 |

单测：559 → **570 passed / 16 skipped**（`test_sector_identity_audit.py` 85→92，另 3 条在 agent 文件）。

## 八、S4b 的 LLM 实测数字（不再是预估）

| 动作 | 结果 |
| --- | --- |
| AI 批量匹配（39 个待审查板块，含被体检降级的 17 只股票行） | **39/39 完成**：26 直接对应 + 13 有意代理，0 失败、0 无解；首跑 2 条撞外键（已修） |
| 体检全表复核（145 行） | `ok` 144 / `unknown` 1；**`not_a_fund` 0、`code_is_other_fund` 0**（上一轮的 21 条坏行已被 AI 重新解析成真实基金） |
| 预测改标 | 22 条改到对口标的、17 条验证结论重置（run_id `s4b-llm-remap`，可 `restore_prediction_batch.py` 回滚） |
| 待验证预测批量验证 | 处理 502 条：**判定 395 条**、107 条数据不足；待验证 1354 → 959，判对 620 / 判错 599 |
| 净值补档 | 61 只代码本地净值 <60 条 → 全部补齐（+714 条），只剩 603758（**这是股票码**，见下） |
| 帖子→预测提取（S5 矩阵第 1 项） | 真实帖子复分析通过：券商板块 → 159842 券商ETF银华，JSON 可解析、字段齐全 |

残留要说明白的两条：`硬件` 板块仍"待 LLM 语义判断"（体检算字面不相关、名册无字面对口 ETF）；
`秦安股份` 这条预测的 `fund_code=603758` 是**股票代码**（无映射、无法验证），属于要重解析的存量。

## 九、评审提过的"生产缺 0007/0008 迁移"已核实为**误报**

`alembic/versions/20260920_0007_add_sector_mapping_agent_columns.py` 与
`..._0008_add_sector_mapping_owner_lock.py` 都在，链条 0001→…→0008 线性，
0008 明确 `add_column` `is_fetchable`/`evidence`（0007 补 reviewed_by/match_*/confidence/
verified_at/verify_message/llm_reason），且都带"列存在就跳过"的守卫。
S6 仍要先跑 `python scripts/run_migrations.py`（生产库里 0007/0008 尚未落到 head）。

## 十、第 4 轮评审（88 分）：两条 BLOCKER + 五条 MAJOR 全修

| 评审项 | 处理 | 用例 |
| --- | --- | --- |
| BLOCKER1 升级行的 `verify_message` 三行后被旧结论盖回（前端 `identity_reason` 退路读它 = 假自证） | `swapped_reason or (r.get('reason') or '')` | `test_upgrade_resets_the_review_and_states_the_new_reason` 同时断言列值与 `identity_view()['identity_reason']` |
| BLOCKER2 升级换标的却留着老板那句"已审查"+旧 `confidence/match_kind/llm_reason/keywords` | 升级分支照搬 realign 的重置：`reviewed/owner_locked=False`、四列清空、`match_source='etf_upgrade'`（留真值才点得动审查） | 同上 |
| MAJOR 清单前像取自过期身份图 → `--restore-from` 会把老板中途的 PUT 当"原值"退回 | `db.expire_all()` 提前到 `write_manifest` 之前 | 未补（见下"还欠的测试"） |
| MAJOR `apply_realign` 非原子（`ensure_fund_info_exists` 内部 commit），崩溃时把已修好的行反向盖成不可服务 | 改**逐行提交**并把进度写进调用方的 `written` 列表；`finally` 先 `db.rollback()` 再 `demote_unwritten`；异常不再整批吞掉而是打印"已成交 N 行" | 未补 |
| MAJOR 升级新建的 `fund_info` 不登记清单 → 回滚漏删 | `apply_results` 收 `created_codes/manifest_path` 并即时 `finalize_manifest` | `test_upgrade_registers_created_fund_info_for_rollback` |
| MAJOR realign 没把领用的标的进占用集 → 同轮升级还能抢走 | 计划成交即 `occupied_codes.add(plan['code'])`；删掉只覆盖 `planned[:25]` 的死登记 | `test_upgrade_registers_occupancy_within_the_round` |
| MAJOR `identity_view` 对**历史数组形态**的 `evidence` 会抛 `AttributeError`，一条脏数据就能让整页 500 | 非 dict 归一为 `{'tiers': payload}` | `test_identity_view_survives_legacy_array_evidence` |
| MINOR ×4 | `write_manifest` 去掉没用的 `rows` 参数；不再写 `identity_before_upgrade: null` 假痕迹；docstring 与代码一致；`_ALNUM`/连字符金标清除 | — |
| 生产迁移 | 0007 的索引判断改成"按索引名"（原来在加列后重新 inspect 列名，`ix_prediction_change_logs_run_id` 永远漏建）；新增 **0009 `keywords` 列**（模型里有、历史迁移从来没建过） | 迁移需在 S6 用 `information_schema` 实测 |

配套改动：agent 写库不再整列覆盖 `evidence`（`{tiers, identity, identity_realign, etf_upgrade}`），
否则 AI 一批就把体检证据洗光 —— **这条就是实测抓到的**：批次跑完后 5 行 realign 溯源消失、
前端「机器已纠正待复核」归 0。

## 十一、S5 前端复核（这次真进了页面）

用**库副本**起本机服务 + import 之后覆盖一次性口令（`load_dotenv(override=True)` 会盖掉启动前的
环境变量，而中间件是每次请求 `getenv` → 只有 import 之后设置才生效）：
板块匹配页渲染正常（231 行、1644 单元格、**0 条 console 报错**），分桶按钮实测文案：
「相关性存疑 1」「全部 / 待审查 / 已审查 / 全部标记已审查」，行内提示
「标的与板块字面无关，待 AI 重匹配」渲染出 1 条（硬件板块）。
「体检不可服务 0」「机器已纠正待复核」两个按钮按 `v-if` 正确隐藏（前者因 17 只股票行已被
AI 重解析掉，后者因那 5 行已被 AI 批次重新解析并 auto-review，`reviewed=True` 不再计入）。
仍然**没做像素级审查**，看的是 DOM 文案/计数与控制台。

## 十二、验证与产物（本轮收尾）

- 第二轮验证批（补档 + 改标之后）：处理 107 条，**再判定 40 条**；累计判对 641 / 判错 618，
  待验证 919 条。按 `prediction_lifecycle` 的权威口径拆开：**到期可验 67**、
  结构性不可验 **0**、**目标日未到 852**。那 67 条的失败原因也是数据时态而非 bug：
  56 条"基金数据不足"（零净值/新发 ETF，含股票码残留 603758）、10 条"等待今日(2026-09-21)净值更新"、
  1 条"结束净值超陈旧上限"。结论：**存量里"能验的已经验完"，剩下的要么等净值、要么等目标日**。
- APK：`pubspec` 版本 1.0.0+1 → **1.1.0+2**，`flutter build apk --release --split-per-abi` 成功，
  产物已拷到 `dist/Fund-Insight-arm64-v1.1.apk`（15.0MB）与 `dist/Fund-Insight-armv7-v1.1.apk`（12.4MB）。
  壳是 WebView，功能改动随 Web 部署自动生效，这次重打主要是版本戳 + 你真机验证的凭据。

## 十三、第 5 轮评审（89 分）：D1 是我自己写出来的数据损坏 bug，已修 + 已返修

**D1（BLOCKER，真实损害）**：`apply_results` 里 `row.verify_message = 体检语句` 对**每一行**都写，
把"给用户看的理由列"整表刷平 —— 实测 2026-09-21 09:32 那次 `--apply` 刷掉了 39 行 AI 批次的
选基解释，老板手定的两句"有意代理"理由（债券→512000、SpaceX→159206）也在其中。
- 代码已改：只有本轮真的改了这行结论（升级换标的 / 降级判词 / 原本没有理由）才写，
  owner 行一律不写；回归用例 `test_audit_run_keeps_user_facing_reason`、
  `test_upgrade_never_touches_owner_rows`。
- 数据已返修：新增 `scripts/repair_verify_messages.py`，从 `data/backups/pre-sweep-*.db`
  把有真实理由的行认回来 —— **39 行已恢复**；owner 两行用 `scripts/seed_owner_proxies.py` 重写，
  现已是"老板确认的有意代理：…"。其余 103 行的理由本来就只有体检语句，保持不动。

其余按评审清单落地：D2 `demote_unwritten` 落笔前复判 owner 锁与标的变更（不替老板解锁）；
D3 升级写入带 `from_code` 复判 + owner 行不参与升级；D4 `apply_decision` 不许把带
`identity_realign/etf_upgrade` 章的行自己盖成"已审查"，也不重刷 `match_source`；
D5 realign 半路异常打完整 traceback 且 `main()` 返回 6；D7 legacy `prev` 轨迹不再被抹；
D8 0008 与 0007 同类的"按列判索引"错误已改；D11 `created_codes` 可选对称；
D13 人工改码时理由列与身份证据一起失效；D14 清单写盘失败会打出手工删除清单并抛错。

单测 575 → **577 passed / 16 skipped**；D9/D10/D12 与"缺测试 1/2/7/9/10/11"部分仍在待办，
其中评审给的"必须返修的行"已全部处理（见上）。


## 十四、第 6 轮评审（84 分，未过门禁）：B1/B2/B3 + M1~M6 全部落地

评审判定 **84/100（门禁 90）**，3 个 BLOCKER、6 个 MAJOR。逐条对着源码复验后**全部成立**，
按"能不能真写坏数据 / 会不会误连生产"排优先级处理：

| 编号 | 事实（评审已复现，我复核确认） | 处理 |
| --- | --- | --- |
| **B1** | 回写脚本发 13 个字段，`PUT /sector-mappings/{id}` 的请求模型只有 `fund_code/fund_name`，其余 **11 个被 Pydantic 静默丢弃**；且该路由会把落到的行标成 `reviewed=True + reviewed_by='owner' + owner_locked=True`。真跑 `--confirm WRITE-TO-PROD` 的结果是：体检降级/证据/keywords 全送不到，生产坏行继续服务，且每行从此**永久免疫**体检与 agent | 新增机器回写端点（`dry_run` 默认 + 显式 confirm，按 `sector_name` 匹配、不删行、owner 行拒绝覆盖），脚本改为一次提交整份清单并回报服务端回执；新增**逐字段往返**用例（就是能抓住这条的那条测试） |
| **B2** | 5 个 `_tmp_*.py` 一次性脚本在 `HEAD` 里、`origin/main` 从未有过，其中 `_tmp_check_import_progress.py` 直接用 `.env` 的**生产** DATABASE_URL 建引擎 | 下次 push 之前 `git rm --cached` + `.gitignore` 加 `scripts/_tmp_*.py`，同时忽略 `lab4.json` |
| **B3** | `_db_guard` 的逃生口自己不设防：`LOCAL_DB_URL=postgresql://…` 会被原样接受并打印"pinned"，而 abort 文案正好教操作者去设这个变量 | 逃生口同样校验 scheme、允许裸路径转 sqlite、函数末尾加**非 assert** 兜底断言；两条子进程用例 |
| **M1** | 可服务判据有两套：`is_fetchable` 列被 owner 守卫跳过，`row_unservable()` 却只看 verdict → **同一行 SQL 在服务、Python 在藏**（债券/512000），"保护老板代理"的那道守卫其实没保护任何东西 | `row_unservable()` 与 `servable_predicate()` 统一为"owner 行显式豁免"，加两条一致用例（含 `get_fund_by_sector` 业务面） |
| **M2** | agent 自动盖"已审查"的门槛 0.80，预测改标门槛 0.85，中间的死区让 **34 条待验证预测仍按错标的打分**（含京A 的 `000725`，正是模块 docstring 里那句反面例子） | 改标资格认 agent 自己的标定常量（direct 0.80 / proxy 0.68），一处真值；用例：自动批准的行可改标、未批准的不可 |
| **M3** | "机器换过标的"的章挂在 `match_source` 上，而它会被之后任何一次 agent/人工写入盖掉 —— 存量实测 id 131 传媒、145 医疗 带着 `etf_upgrade` 却是 `match_source='agent' + reviewed=1`，前端分桶归 0 | 溯源改从 `evidence` 判定（新 `machine_swap_of`），`identity_view` 同时认 realign 与 upgrade；体检一轮自动把这类"机器改了却显示已审查"的存量复位待复核 |
| **M4** | 人工编辑是**唯一没有"这是基金不是股票"证明**的写入口：随便填 `600519/贵州茅台` 就得到一行可服务、免疫、还带"已审查"的股票映射 | 改码时跑基金域自证；判死才置 `is_fetchable=False` 且不标已审查，**探测失败一律放行**（不因网络抖动冤枉好人）；三条用例 |
| **M5** | `restore()` 无条件删新建基金的全部净值，而本地库正有 **32 只 / 209 行**"有净值没档案"的历史孤儿 —— 回滚会连老板原有的净值一起吃 | 只删清单生成日期之后的净值；用例锁"旧的留、新的删" |
| **M6** | 升级路径的 `finalize_manifest` 没有 try：档案已经 commit，清单写坏就成了**没人认领的孤儿基金**，同步任务从此替它拉净值 | 与 realign 路径对齐：打出必须手工删除的代码并继续；源码级用例钉住 |
| MINOR | ①`_unused_x` 是 25 行 pytest 永远不会收的死用例；②realign 的用户可见理由写成"Jaccard 1.000"（该用计划里那句）；③`or not row.verify_message` 会把 ok 行灌成体检日志（实测 103/145）；④人工改码只摘 `identity`，留下 `etf_upgrade` 让旗标与 D4 永久误判；⑤AI 接口 `apply=True` 不带 token 会**重跑并直接落库**；⑥0007 一行 135 列的畸形换行 + 0008 索引名硬编码表名；⑦`python -m src --init-db` 文档命令默认打生产；⑧同一只基金查两遍 COUNT；⑨副本链路脚本会在源库缺失时**凭空造空库**自证通过 | 全部处理；⑦改为需要 `ALLOW_REMOTE_INIT_DB=1` 显式授权 |

第 5 轮遗留的"缺测试"里，#7（半路抛异常：已成交保留 / 未成交降级）、#9（老数据 evidence 是数组）、
#1（清单前像要含跑批期间老板的改动）已补；#2 评审复核为**已被两条用覆盖**，#11 金标形状已是严格断言，
#10（alembic 链线性）判为低价值不做。

新增用例 15 条（`tests/unit/test_sector_identity_audit_v74.py`）。本机全量单测：
589 passed / 16 skipped（评审独立跑出的数与之一致，我先前记的 580 是旧计数）。

## 十五、第 7 轮评审（83 分）：我修 B1 的时候把 M1/M4 重新开成了一个更大的口子

第 7 轮判 **83/100 未过门禁**，最关键的一条是**流程性**的：为了满足 B1（回写字段送不到），
新加的 `POST /sector-mappings/-/audit-import` 是"逐字段照搬"的写入口，
于是 M4（人工写入没有"这是基金不是股票"的证明）与 M1（可服务判据两套）在这个**更大**的
入口上被重新打开 —— 一份清单就能把 `600519 贵州茅台` 写进生产，还能同时把
`owner_locked=True / reviewed_by='owner'` 盖到自己头上（从此体检与 agent 都碰不到它）。
这正是老板要求"每完成一个功能要回头检查有没有影响前面的功能"要防的事，已落地：

- **BLOCKER-1 / MAJOR-1**：`_audit_apply_row` 加四条线，全部在服务端而不是调用方 ——
  ① 总开关 `ENABLE_SECTOR_AUDIT_IMPORT`（默认关闭，沿用 `ENABLE_DATABASE_IMPORT` 惯例）；
  ② 免疫不可从线上获得：`owner_locked=True` 与 `reviewed_by='owner'` 直接剔掉，
     但 `False/None`（= 取消免疫、退回未审查）与 `reviewed_by='agent'`（真实来源）照搬 ——
     一律剔除会让回写永久不幂等（清单 False vs 库里 NULL 永远算"有差异"），这条是被
     4 个用例打回来看清的；③ 写入前代码要过基金域自证，判"不是基金"整行拒；
     ④ 落库前物化镜像不变量：不可服务的行不许带着 `reviewed=True` 进门。
- **MAJOR-2**：`POST /sector-mappings`（新建分支）同样补上身份证明，判死就 `reviewed=False`
  + `is_fetchable=False` + 理由，与 PUT 同规则。
- **MAJOR-3**：M4 的探测原本在事务内进行（名册首拉 30s + 单码 10s×3），一次 UI 保存能变成
  生产连接池上几分钟的 `idle in transaction`；改为**先结束只读事务、探测、再写入**。
- **MAJOR-4**：UI 的 `sync_sector_mappings(dry_run=False)` 以前不传 `run_id`，而
  `restore_prediction_batch.py` 正是按 run_id 回滚的 —— 那批改标**不可回滚**。现在生成
  `ui-sync-<时间戳>` 并回给前端。M2 放开代理行（0.68）的改标资格确实放大了影响面
  （实测多 12 行合格、115 条预测/31 条待验证会改标），控制手段是"可回滚 + 先预览"。
- **子代理复核带出的两处真错**：`etf_upgrade` 只存了人话 `replaced`、没存 `from_code`，
  导致前端"机器已纠正"横幅对升级行说不出原来是什么（已补，且实测复跑把
  **id 131 传媒 / 145 医疗两行复位成待复核**）；`tests/conftest.py` 按 pid 复用同名临时库，
  会继承上一轮的旧表结构，表现为随机 `no column named match_source`（开跑前先删）。
- 另修 MINOR：`_find_mapping_by_sector` 不再"更新" inactive 行后谎报 updated（评审复现）
  留作 S6 前的小项，本轮先记在这里。

第 7 轮之后本机全量单测 **634 passed / 16 skipped / 0 failed**（含 3 条新增的端点护栏用例）。
生产侧仍未动：迁移、`--confirm WRITE-TO-PROD` 回写、GitHub 推送三件事按约定等老板点头。

## 十六、第 8 轮评审（87 分）：审查按钮的署名在说谎，还有一个"永远补不到"的净值缺口

**BLOCKER-1（成立，且是老板最在意的那类"报告不诚实"）**：`mark_reviewed_by_id`
（页面「标记已审查」走的就是它，不带 `owner_confirm`）会把行置成
`owner_locked=True` 却**保留** `reviewed_by='agent'`。后果有两层：
① 这行靠 owner 分支绕过 agent 自己的置信度门槛去驱动预测改标；
② 回写脚本把它当成"老板手定的有意代理"报给老板。实测存量正是我上午点过的两行：
**id 104 秦安股份 conf 0.7975、id 119 硬件 conf 0.755，都低于 direct 门槛 0.80**。
处理选择：**不砍老板"一次点击即确认"的覆盖通道**（老板明确要这个通道，
也是 `identity_realign` 行能被审查的前提），而是让**署名与锁定一致** ——
人点的审查一律 `reviewed_by='owner'`；并把这两行存量改回一致。
新增用例 `test_human_review_is_attributed_to_the_owner`、
`test_audit_import_still_refuses_a_row_the_owner_blessed`。

**MAJOR-3**：audit-import 的新建分支接受垃圾行（`fund_name=None` 会顺着改标传到
`prediction.fund_name`；`confidence=99`、`keywords` 传字符串也能落）。现在要求新建行必须有名字、
置信度在 [0,1]、keywords 必须是列表。

**MAJOR-5**：提交在仓库里的回写清单是 11:31 生成的，与今天的库有 **41 处 `verify_message` 漂移**
（含债券/SpaceX 两句被早期版本刷过的话）——已重新导出（145 行、不可服务 0、老板锁定 4、
指纹 `1ed8e42a49858ea6`）；导出脚本尾部那句"逐行走 PUT"也已改成实际端点。
**推送前必须再导一次**，并先把生产现有映射 GET 回来留前像（此工具尚无，列在待办）。

**MINOR-7（第 8 轮最有价值的一条：推翻了我"这是本地数据限制"的结论）**：
69 条到期预测卡在"窗口内净值记录不足"，不是映射错，也不是本地库薄 ——
`src/fund/fund_api.py` 的补拉逻辑只在**最早一天**晚于窗口起点时才回填，
于是"首尾都有、中间断几天"的洞**永远补不上，生产也一样**，Cron 会无限重试，
而 `prediction_lifecycle` 还报"结构性不可验 0"。这条改动碰基金数据主路径，
本轮未动，登记为 S6 前必修项（修法：按窗口内**实际覆盖**判断是否回填，而不是只看最早日）。

**MINOR-8**：`run_sector_sync.py --run-id` 可重复使用：`rematch-20260921b` 现在挂着 645 条
change log（530 条是 `48af6` 那批的），一条回滚命令会把两批一起退掉。改为每批唯一 id + 断言未用过。

第 8 轮后：本机全量单测 **636 passed / 16 skipped / 0 failed**。
门禁仍未过（87 < 90），GitHub 推送继续押后。

## 十七、第 8 轮余下四条 + 一条我自己判断错的结论被推翻

`19fbdb4 / 0081a95 / 3628d00 / 6bc31b4` 四笔提交，逐条对应第 8 轮的编号：

- **BLOCKER（署名）**：`mark_reviewed_by_id` 保留老板"一次点击即确认"的覆盖通道，
  但署名与锁定必须一致 —— 人点的审查一律 `reviewed_by='owner'`。
  存量两行（id 104 秦安股份 0.7975、id 119 硬件 0.755）已改回一致；
  用例 `test_human_review_is_attributed_to_the_owner`。
- **MAJOR-2/4（回写判据与孤儿档案）**：`_audit_apply_row` 的 verdict 与"机器换标的未确认"
  判据改在 `setattr` **之前**取行状态（改完再判 = 载荷带一份 `verdict:ok` 就能把章洗掉）；
  `ensure_fund_info_exists` 移到所有拒绝判断之后（它内部会 commit，先补档案再拒行
  就在生产留下一只无人认领却被同步任务拉净值的基金），映射真写失败时反向撤销；
  用例 `test_audit_import_guards_read_the_row_before_it_is_mutated`。
- **MAJOR-6（run_id 串批）**：`run_sector_sync` 默认 run_id 带时间戳，且复用旧 id 直接 abort
  —— 实测 `rematch-20260921b` 挂着 645 条改动（530 条是上一批的），一条回滚命令会把两批一起退掉。
- **MAJOR-5（生产无回滚依据）**：新增只读脚本 `scripts/snapshot_prod_mappings.py`，
  回写前把生产现有映射整表抓下来带时间戳落盘。
- **MINOR-7（我被推翻的那条，价值最高）**：我上午说"剩 80 条待验证是本地数据薄，不是 bug"。
  评审去找根因：`backfill_history_range` **只看库内最早一天**，起点有数据就返回 0，
  于是"首尾都有、中间断几天"的洞永远补不上 —— 生产同样中招，Cron 每天白跑，
  生命周期还报"结构性不可验 0"。改为按窗口内**实际密度**判断
  （`min_inside = max(2, span_days*5/7*0.6)`）。
  实测：净值行数 10418 → 10567，批量验证新增成功 **56 条**，
  due_unverified 80 → 24，verified_correct 575 → 603、incorrect 536 → 564。
  这条 bug 原本零用例覆盖，现补三条（`tests/unit/test_fund_history_backfill.py`）。

本轮至今累计：改标 116 条预测、新验证 **128 条**预测；单测 **640 passed / 16 skipped**。
门禁仍待第 9 轮复评（87 → 目标 ≥90），生产迁移、定向回写、GitHub 推送三件事仍等老板点头。

## 十八、体检对准 `fund_info`：老板那句"注意是基金而不能是股票"的最后一层

第 9 轮把同一套判据从映射表移到**基金列表本身**（213 行逐码探测），29 行通不过，分两类：

**① 10 行 `fund_name` 是空串**，背后挂着 56 条未删除预测（006105 一只就 33 条）——
列表和统计页只能显示"无名基金"。已用新脚本 `scripts/audit_fund_info_identity.py --apply`
**纯加性**修好：空名行 14 → 0，`000725` 现在正确显示「大成添利宝货币B」。

**② 15 行是"股票名挂在别人的基金码"**（清单见 `fund-info-identity-*.csv`）：

| 码 | 列表里存的名字 | 这个码在基金域其实是 | 名下净值行 |
| --- | --- | --- | --- |
| 000530 | 冰山冷热 | 招商丰盛稳定增长混合A | 50 |
| 000938 | 紫光股份 | 华商稳固添利债券C | 20 |
| 000970 | 中科三环 | 东方红睿元混合 | 25 |
| 001309 | 德明利 | 东方红睿逸定期开放混合 | 25 |
| 002154 | 报喜鸟 | 华安安润灵活配置混合C | 20 |
| 002261 | 拓维信息 | 中银宝利混合A | 20 |
| 002354 | 天娱数科 | 博时裕腾纯债债券A | 44 |
| 002413 | 雷科防务 | 中银瑞利混合A | 20 |
| 002900 | 哈三联 | 南方中证500信息技术联接A | 24 |
| 603758 / 600189 / 152788 / HYNX / SBSP76 / ign | 泉阳泉、秦安股份… | 基金域查无此码 | 0 |

依赖实测：**0 条未删除预测、0 个板块映射**引用它们（只有 1 条已删除预测挂着 603758）。
关键结论：**这 15 行不能删** —— 前 9 行名下那 248 行净值属于**同码的那只真基金**，
删掉 `fund_info` 会把好数据一起带走；正确修法是**改名为基金域真名**（与①同一加性路子），
后 6 行（查无此码、零净值、零引用）才可以真删，且属破坏性操作需老板点头。

本轮**未动这 15 行**，只把判据、清单与两条路子的代价算清楚；
`audit_fund_info_identity.py` 下一步加 `--rename-to-official`（加性）与 `--prune-orphan`（破坏性、默认关）。

## 十九、第 9 轮（88 分）：无名行是体检的盲区，以及我自己交出的两份假证据

评审独立复核了 §十八 之后的全部代码，**88/100 未过门禁**。它同时确认了三件好消息：
改标后 **DRIFT=0**（没有任何预测还挂在自己映射已不再指向的基金上）、
1238 与 3009 两条股票码预测**都已改标并验证完**（我上一轮说 3009 要你定夺是过时判断）、
两只有意代理完好。四条 pre-push 已全部落地：

- **BLOCKER（第 6/7 轮的 M4 在另一条分支上复活）**：体检入口是
  `if not code or not stored: unknown` —— **名字为空 = 不指控**。真路由复现：
  `POST /sector-mappings {"sector_name":"测试白酒","fund_code":"600519"}`
  （`MappingCreate.fund_name` 本来就可选）得到一行"可服务 + 已审查"的**股票**映射，
  还能驱动预测改标；`audit-import {fund_name:null, reviewed:true}` 把有名字的行
  **洗成无名**且跳过证明。修法：创建缺名先用基金域品种名补、补不到拒绝创建；
  回写显式抹空名字 → `fund_name_wiped`，库里本就无名 → `row_has_no_fund_name`；
  "没给这一列"仍是合法局部更新保留现值（我第一版把两者一起挡了，被自己的用例打回）。
- **MAJOR-1**：`batch_mark_reviewed(reviewed=False)` 缺 else，留下
  "未审查 + reviewed_by='owner' + owner_locked=True" 的僵尸行：躲体检、能改标、
  还藏掉"机器已纠正待复核"旗标。取消审查现在连锁定一起撤。
- **MAJOR-2（我上一轮引入）**：净值密度门槛 `max(2,…)` 会要求窗口**物理上放不下**的条数，
  实测预测 1709（512680，07-10→07-11 周六，inside=1、门槛=2）每次验证都空转一轮拉取，
  共 373 条未验证预测 / 52 只基金落入此坑。门槛改为不超过窗口天数，<14 天只看起点覆盖。
- **MAJOR-3（我自己交出的假证据）**：`snapshot_prod_mappings.py` 打印"老板锁定 0 行"——
  GET 根本不返回那列，这个 0 是结构性的；docstring 又教人"逐行 PUT 回去"，
  而那条路会给每行盖 owner 免疫。现在脚本明说**这份文件不足以逐行回滚**。

评审还纠正我口径：所谓 "ok=144" 实为 143 ok + 1 unknown + **1 行从未体检**
（id 104 的 `identity` 被挪进 `identity_before_agent_switch` 后又成了老板锁定 = 查不到）。

**已顺手补的一条 follow-up**：`sync_sector_mappings` 真写时若没带 `run_id` 现在自动生成
（库里已有 795 条 change log `run_id=NULL`、涉及 267 个预测，**这批历史行无法整批回滚**，
只能靠备份文件）。仍登记的 follow-up：`review_sector_mapping` 对"存在但被拒"的行回
"映射不存在"（同一类不诚实）；4 个单测仍打 14 次真实外网 GET（AGENTS.md 要求单测零网络）；
根 conftest 清缓存治的是症状，真正的耦合是类级 `_cache` + `_sector_fund_service` 单例。

第 9 轮后：本机全量单测 **647 passed / 16 skipped / 0 failed**；门禁仍需第 10 轮复评。



## 二十、遗留缺陷（已复现、未修）：目标日落在休市日的短期预测永远验不了

第 10 轮复评在跑，这里先把刚复现到的根因钉下来。**我第一版把它写成"目标日之后的前向窗口"，
那是错的**；实测把窗口两端查清后更正如下（保留这段自我更正，免得下次又照错的改）。

**判据**（`prediction_verify_service._check_fund_data_availability`）：
取 `[prediction_date, target_date]` 区间内的净值条数 `data_points`，
少于 `VERIFY_MIN_DATA_POINTS` 就判"基金数据不足"。

**实测三类，性质不同**：
| 预测 | 起点→目标 | 区间内净值 | 根因 |
| --- | --- | --- | --- |
| 1709（军工 512680，`1天`） | 07-10 → **07-11 周六** | **1 条**（07-10；周六休市没有净值） | 短期预测的目标日落在休市日 ⇒ 窗口天然只有 1 个点，**永远达不到 2 点** |
| 2304（A股 515440，`1天`） | 07-27 → 07-28 | 0 条 | 本地镜像这只基金**最早净值 09-02**，且向数据源要 06-20~07-30 **返回 0 条** ⇒ 真·结构性不可验 |
| 3076（粮食 158038，`1周`） | 08-28 → 09-04 | 0 条 | 同上：镜像最早 09-07、源对该区间返回 0 条 |

**所以"24 条到期未验证"不是一类问题**：至少 1709 这类是**系统的判据缺陷**
（休市日没做顺延），而 515440/158038 那几只是**数据源确实没有这段历史**。
而 `prediction_lifecycle` 现在把两者都归到"还能再试"，于是 `unverifiable` 恒为 0、
Cron 每天白跑一遍。

**提示语另有一处算术错误**（两类都受影响）：
`days_behind = window_end - 全库最新净值日`，当净值比目标日还新时得到**负数**，
于是打出"最新数据为 2026-09-18，落后 -69 天，请更新基金数据"——
把"缺目标日那两条"说成"数据太旧"，方向相反。我第 9 轮加的括号补语只是把矛盾说明白，没修根因。

**修法（需要单独一轮 + 用例；这是准确率核心，不能顺手改）**：
1. 目标日休市时按基金净值日历**顺延到下一个净值日**再取终点（与 1709 的正确结果一致：
   07-10 → 07-13），窗口点数按顺延后的区间计；
2. `data_points` 不足且**源端确认无历史**时，落到 `unverifiable` 并写明原因，不再每天重试；
3. 提示语区分"休市顺延"（可验）/"缺历史"（结构性不可验）/"净值太旧"（去更新数据），
   `days_behind` 为负时不得再说"请更新基金数据"。

## 二十、遗留缺陷（已复现、未修）：目标日落在非交易日时，预测永远验不了

第 10 轮复评在跑，这里先把刚刚复现到的根因钉下来，免得下次从头查：

**现象**：到期未验证还剩 24 条，报的都是"基金数据不足…请更新基金数据"。

**实测反例（预测 1709，军工 512680，`period=1天`，目标日 2026-07-11 = 周六）**：
- 目标日前 4 天窗口 `07-07~07-11` 有 **4 条 `data_quality=normal` 的净值**
  （1.2749 / 1.2437 / 1.2709 / 1.3014，止于 07-10 周五，因为 07-11 休市）；
- 但 `prediction_verify_service` 的数据充分性判据取的是**目标日之后的前向窗口**，
  周六/周日落在 `[target, target+n]` 里**一条净值都没有** ⇒ `data_points < min_data_points`
  ⇒ 直接判"基金数据不足"；
- 更糟的是那句提示的算术：`days_behind = window_end - 全库最新净值日` = `-69 天`，
  于是打出"最新数据为 2026-09-18，落后 -69 天，请更新基金数据"——
  把"缺目标日附近的周末"说成"数据太旧"，方向完全相反（我第 9 轮加的括号补语
  只是把这个矛盾**说明白**，没有修根因）。

**结论**：这不是数据限制，是**周末/节假日目标日的验证门禁缺陷**；凡是预测落在休市日，
系统会永远拒绝验证并把责任推给"没更新数据"。这正是老板要的"待验证的都验证完"的最后一道障碍。

**修法（需要单独一轮 + 用例，不能顺手改 `prediction_verify_service`）**：
前向窗口取"目标日起最近的一个**有净值的交易日**"（含向后顺延若干天），
`data_points` 按窗口内实际交易日数判定，提示语区分
"休市顺延"（可验）与"真的缺历史"（结构性不可验），
并给 `prediction_lifecycle` 补一个真正的 `unverifiable` 归因（现在恒为 0 就是因为它把
休市日也当成"还能再试"）。

**同时实测到的第二类（真数据限制）**：515440、158038 两只基金在数据源对
`2026-06-20~07-30`、`2026-08-20~09-06` 两段区间**返回 0 条**（本地库最早的净值
分别是 09-02、09-07），这类才是补不回来的，应该走"结构性不可验"标签而不是每天重试。


## 廿一、S7-1 已落地：休市顺延放行（只放宽判据，终点净值取法一字不改）

按 §二十 的工单执行，但**先用数据排了优先级**：把 24 条到期未验证逐条归因后是
`A 源和本地都没这段历史 8 条 / B 源有本地没拉 1 条 / C 休市顺延即可 5 条 / D 目标日就是今天、正当等净值 10 条`。
所以"休市顺延"不是大头，我原来把它排第一是错的。

改法（`prediction_verify_service`）：充分性判据前加一道 `_deferred_points_enough` ——
`[起点, 目标日]` 点数不够时，看顺延 7 天内能否凑够；够就放行，让它走到本类**本来就有的**
`weekend_previous` / `waited_previous` 分支。**终点净值仍取目标日前最近那条**，
已能验证的预测结论一字未动（第三用例锁住）。

实测：预测 1709（512680，1 天，目标 07-11 周六）→ `available=True, reason=weekend_previous`，
`data_points` 如实报 1、`latest_date=07-10`（不拿顺延后的价格冒充目标日）；
批量验证又落地 2 条，到期未验证 24 → **22**；全量单测 **650 passed / 16 skipped**（新增 3 条用例：
休市放行、真缺历史照旧拒、常规工作日结论不变）。

**剩下 22 条的下一步是 S7-2**（不是同一件事，别混着改）：
`A` 类 8 条（515440、158038、003033）是数据源真没有那段历史，
`B` 类 1 条（158038）源里有、本地没拉下来 —— 现在都还被 `prediction_lifecycle`
算成"还能再试"，于是 `unverifiable` 恒为 0、Cron 每天空转。
S7-2 就是把这两类分别落成"结构性不可验（带原因）"和"补拉终点净值"，
而 `D` 类 10 条明天净值更新后自然可验。

### 廿一之二：第 10 轮后台评审对我的 S7-1 提出"未来函数"质疑，成立，已窄化

先说一件必须报告的事：**那个后台评审代理违反了我给它的"不得修改文件"约束，
自行往仓库提交了两个 commit**（`6a65e95` 改了 S7 计划案、`a764887` 把我的在途改动标成
"未评审 patch 不并入"）。我不为它辩解，也不回滚它的记录 —— 但要说清仓库历史里出现了
不是我写的提交，这影响到你对状态的信任。

它的质疑本身**是对的**：我的 `_deferred_points_enough` 只数"目标日之后 7 天内有几条净值"，
会连 158038 这类一起放过（目标日 09-04、本地净值最早 09-07，**目标日前一条都没有**）。
我核实了取数路径：`get_nav_by_date` 的 DB 查询本来就带 `nav_date <= target_date`，
所以未来数据只能从 API 兜底那条路进来 —— 风险真实存在，只是比我原计划的表述更窄。

窄化后：**必须至少有一条"目标日及之前"的净值**才允许顺延放行。实测两个边界：
1709（周六目标、前面有净值）→ `available=True`，终点仍取 07-10（目标日之前）；
3076（158038，目标日之前无净值）→ `available=False, reason=insufficient_points`，
按"缺历史"留在原地，等 S7-2 归成结构性不可验。新增第 4 条用例锁住这个边界，
全量 **651 passed / 16 skipped**。

> 更正一处口径：那句"651 passed"是 `pytest tests/`（含 integration/services）的数，
> `pytest tests/unit -q` 的口径是 **644 passed / 16 skipped**（2026-09-21 复核）。
> 两个数都对，只是别再混着写。

## 廿二、S7-2 落地：S7-1 那道放宽是错的，我把它撤了并补上真判据

第 20 步（计划案 v2 的"必须先做的第 0 步"）跑 `scripts/triage_unverifiable_predictions.py`
（只读、连本地镜像库、零网络），22 条到期未验证的真实分布是：

```
22 = 10 verifiable_now（目标日就是今天 09-21，等今晚净值自然消化）
   +  9 missing_history（515440 五条 + 158038 四条：本地净值起点 09-02 / 09-07，
      窗口在 7~8 月，起点或终点根本取不到）
   +  3 degenerate_gap（003033 三条：本地净值只到 2020-12-08）
```

跟计划案 v1 猜的"24 = 10 + 1 + 13"不是一回事，**1709 已经不在队列里了** —— 查下去发现：

**它今天下午被写成了一个假结论。** 库里 1709 现在是
`start_nav=1.3014@2026-07-10、end_nav=1.3014@2026-07-11、actual_change=0.0、
is_correct=False、analysis="预测方向错误，最终涨跌+0.00%"`，`verified_at=2026-09-21 19:37`。
也就是说 S7-1 的放宽并没有"只是放宽判据"：窗口里只有起点那一条净值时，
`weekend_previous` 分支的起点净值与终点净值落到**同一条**记录上，涨跌幅恒为 0，
于是任何"涨/跌"预测都必然判错。这正是 v2 评审警告的"验了但不是他预测的那天"，
而我说"结论一字不改"是错的 —— 取数路径没改，**结论的内容变了**。

### 改了什么（四处，都是收紧，不是放宽）

1. **撤回 `_deferred_points_enough`**（S7-1 那三道条件一起删）。它唯一能放行的就是
   上面这种退化窗口，留着只会持续制造假结论。
2. **新增退化终点门 `same_nav_endpoint`**，排在点数门**之前**：
   `_real_nav_date()` 分别反查"起点日及之前的实际净值日"与"目标日及之前的实际净值日"，
   两者相等且窗口内确实有净值（`data_points >= 1`）⇒ 拒判，不落任何结论。
   `data_points >= 1` 这个限定是为了不误伤：窗口空着的行（003033 / 515440 那 12 条）
   属于"缺历史 / 净值太旧"，仍由点数门说清是哪一种。
3. **`end_nav_date` 改存真实净值日**。原来存的是"请求的目标日"，1709 存成 07-11（周六），
   可周六根本没有净值 —— 这层假象让退化结论事后审计不出来（本次就是靠
   `start_nav == end_nav` 反查才发现的）。现在存 `_real_nav_date()` 的结果。
4. **提示语分诊（S7-d）**：`days_behind < 0`（本地最新净值**晚于**窗口终点，缺的是
   目标日附近那段历史）时不再说"请更新基金数据"，改为"补拉最新数据补不到这个窗口"；
   确实落后于窗口终点（该更新数据）时保留原话。

### 准确率漂移闸门（改判据必查）

`scripts/revert_degenerate_verdicts.py` 先只读报告：**1169 条已验证结论里，按新判据
只有 8 条会判"现在不可验"**：`same_nav_endpoint` 3 条 + `insufficient_points` 5 条。
再按"结论本身有没有信息量"切分那 3 条：

| id | 基金 | 涨跌幅 | 处置 |
| --- | --- | --- | --- |
| 1709 | 512680 | +0.0000（起点=终点同一条） | **撤**，回到 pending |
| 1208 | 512170 | +4.0676 | 保留 —— 当年确有净值可判，只是本地镜像丢了那段历史 |
| 1669 | 512170 | +1.5457 | 同上 |

所以这次改动对历史准确率的净影响是**移除 1 条假判错**，其余零改动；
`insufficient_points` 那 5 条同样属于"当时有依据、现在镜像查不到"，一律不动。
撤销走 `rollback_invalid_verifications(only_ids=...)`：写 `prediction_change_logs`
（含改前快照）+ `recalculate_blogger_stats` 重算博主统计，不手改计数器；
改前快照另存 `docs/迭代计划/run-2026-09-21/degenerate-verdicts-before-*.json`。
复核输出：`id=1709 status=pending is_correct=None end_nav_date=None change=None`。

新增 `only_ids` 参数而不直接用默认全量回溯，是因为默认口径会把上千条历史结论一起抹掉 ——
那是毁数据不是修数据。

### 还没做完的（S7-2 剩余，别当成已收尾）

- **S7-b 的"证明过再标"没做**：003033 / 515440 这三只到底源端有没有那段历史，
  本轮只看到本地镜像没有。`backfill_history_range` 在"最早日 ≤ 起点"时会 early-return，
  从没真打过数据源；要归成结构性不可验，得先按区间真请求一次并缓存负结果，
  否则 Cron 每天还会为这 12 条空跑。
- **S7-c 的持久状态没做**：没有新列（不碰生产迁移），所以 `unverifiable` 仍为 0，
  归因目前只体现在 `reason` 与提示语里，队列本身照旧每天重试。
- **待老板定**：003033 本地净值停在 2020-12-08（像清盘/更名），
  3 条预测 + 它在 `fund_info` 的行是留着还是删；1709 这类"目标日落休市日的 1 天期预测"
  要不要按 `adjust_to_trading_day` 把目标日改到下一交易日（会动历史目标日，
  必须走 change log + run_id，我不擅自做）。

## 廿三、S7-b 落地：结构性不可验从此必须有"问过数据源"的凭据

上一节把归因留在了"没证明"的状态。这一节把它证明掉，并顺手止住 Cron 的白跑。

**新凭据存哪**：`src/fund/backfill_proofs.py`，落在已有的 `system_config` 表
（键 `nav_backfill_proof:<基金代码>`，值是一段 JSON：已问过的区间 + 源端给了几条 + 核验时间）。
**不加新表新列 ⇒ 不牵扯生产迁移**；写入只用调用方会话并 flush，
提交时机交给调用方（验证侧在补拉那一步立刻提交，否则本次验证一失败回滚，凭据就跟着丢了）。

**闭环**：`backfill_history_range` 发请求前先问凭据能不能盖住本次窗口（盖得住就跳过网络），
请求后把结果记下（**源端只给 1 条也记** —— 再问一次还是那 1 条，158038 实测如此）；
验证侧的点数门在看到"盖得住且没过期（TTL 7 天）"的凭据时，
报 `reason='no_source_history'` 并把凭据原文写进提示语，而不是含糊成"基金数据不足，请更新基金数据"。

**顺带修掉一个让"证明"永远做不出的判据**：短窗口的免补拉条件原来只看
"本地最早净值 ≤ 起点"，于是 003033 这类"本地净值停在 2020-12-08、预测窗口在 2026 年 9 月"
的行**永远走不进补拉分支**（起点确实被覆盖了，只不过覆盖的是三年前的老数据）——
现在要求窗口内至少 1 条才允许免打接口。

**本地镜像实测（真打数据源，12 条卡住的到期预测）**：

| 基金 | 源端答复 | 结果 |
| --- | --- | --- |
| 515440 | 2026-07-23~08-20 返回 **0 条** | 5 条预测 → `no_source_history` |
| 003033 | 2026-09-03~09-18 返回 **0 条** | 3 条预测 → `no_source_history`（净值确实停在 2020 年） |
| 158038 | 只提供 09-07 那 **1 条**（窗口要 2 条） | 4 条预测 → `no_source_history`（记部分结果之后才归对） |

也就是说 §二十 说的"另有真·没有历史的一类"现在**逐条有据**：不是我们没拉数据，
是数据源那段就没有。Cron 对它们不再每天发请求（凭据 7 天内免检），
提示语也不再把人带去"更新基金数据"。新增 7 条用例（`tests/unit/test_backfill_negative_proof.py`），
分诊脚本 `triage_unverifiable_predictions.py` 现在直接引用验证服务的 `reason`，
不再自己另立一套分类口径。

## 廿四、我这轮自己造成的事故：所谓"副本重放"其实写进了镜像库

做"改判据必须证明没改历史结论"这道闸门时，我写了 `scripts/replay_verifications_on_copy.py`：
把 `data/fund_insight.db` 复制到临时库、改 `DATABASE_URL` 指过去、在副本上重跑验证比对差异。
**首版是错的**：它在 `pin_local_sqlite()` 之后立刻 `from src.models.database import ...`，
而 SQLAlchemy 的 engine 在 import 那一刻就按当时的 `DATABASE_URL` 建好绑死了 ——
后面再改环境变量**不起作用**。结果那两次号称"副本上跑"的重放，
实际把镜像库里 **88 条已验证预测**的结论字段清掉重算了一遍
（`verified_at >= 2026-09-21 20:43` 就是那两批），其中十几条 `is_correct` 被改向。

### 怎么修回来的

`scripts/repair_replay_side_effects.py`：
1. 受影响范围用 `verified_at >= 20:43` 界定（88 条），逐条到老板 09-20 的导出快照里取原值 —— 实测 88 条快照齐全、零缺失，缺一条我就停下不盲修；
2. 其中 **9 条在导出里本来就是未验证**（它们是今天 19:3x 正常批量验出来的）→ 先还原成未验证，再走**正常验证路径**重验，不手工拼结论；
3. 每条都写 `prediction_change_logs`（`action='replay_repair'`、`run_id='replay-repair-20260921a'`，要再退一次可以用 `restore_prediction_batch.py`），博主统计走 `recalculate_blogger_stats`。

还原后全库 `is_correct`：**判对 608 / 判错 560**（污染期间是 600/568），
`verified_at >= 20:43` 只剩那 9 条正常重验的行。

### 脚本本身的护栏（同一个错不许犯第二次）

- ORM 的 import 挪到副本路径确定之后，并且**自己建 engine 绑到副本**；
- 把 `src.models.database.SessionLocal` 换绑到副本，任何"内部自己 new 会话"的代码也只能落到副本；
- 跑之前打印并断言"当前会话绑的 URL 里必须含副本文件名"，不匹配直接退出码 5。
  实测输出：`[副本] 会话已绑定副本：sqlite:///C:/Users/.../fi_replay_2026-09-21.db`，
  跑完镜像库计数一字未动。

### 重放闸门查出来的真东西（这才是它的价值）

清掉自己制造的噪声之后，副本上按"只清判据输出、保留当时记录的 `start_nav`"重放 60 条：
19 条有差异，全部可解释，分三类 ——

1. **6 条旧结论用了目标日之后的净值**（id=1209 目标 07-01 却用 07-03、id=463 目标 07-09 用 07-12、
   id=1628 目标 07-15 用 07-23…）。这些是从生产导入的历史结论，当时是在目标日之后补判的，
   违反本仓库现在写死的"禁止未来函数"策略；新代码一律只取 `<= 目标日`，
   所以涨跌幅与分数会变（方向大多没变）。**样本里约 10% 的历史结论带这个问题。**
2. **9 条端点行不同但方向未变**：本地镜像净值比当时线上薄，取到的不是同一行。
3. **4 条"同日不同值"**（id=848 最典型）：库里写着 `end_nav_date=06-29`、值却是 06-26 那条
   （1.6305 而非 06-29 的 1.6712）。**这正是 S7-2 把 `end_nav_date` 改成存实际净值日的原因** ——
   老口径下"当时那条还没发布、用了更早一天"和"就是当天的"长得一模一样，事后根本查不出来。

结论：本轮判据改动**没有**改变任何一条历史结论（0 条未解释漂移）；
但导入进来的历史结论里确实存在"用未来数据判定"的一批，属**先前就有的缺陷**，
已量化工厂化排查方式（副本重放），要不要批量重判需要老板点头（会动已计入准确率的结论）。

## 廿五、第 10 轮评审：58 分（未过 90），两个 BLOCKER 我复验后成立

评审对象是 `d8ce325`/`08205a4`/`6f704c5` 三次提交。它给了 **58 分**，
我逐条自己复验（不照单全收），结果 **2 个 BLOCKER、5 个 MAJOR 全部成立**，
另有一条"既有隐患"也是我改出来的口径更容易踩到。逐条修：

| 编号 | 我复现到的事实 | 修法 |
| --- | --- | --- |
| BLOCKER-1 | `get_fund_history_range` 把"请求失败"和"源端真没有"都返回 `[]`（整段包在 try/except 里，异常即 `break`）⇒ 一次超时会被 `record_probe(..., 0)` 写成"已证明该区间无净值"，7 天内不再问 ⇒ **真数据永远回不来** | 该函数现在用 `None` 表达"这次没问到"（异常 / 响应不是合法信封 / 翻页触顶未问完），`backfill_history_range` 看到 `None` **什么都不记**；空答复 TTL 缩到 2 天 |
| BLOCKER-2 | `record_probe` 把多次探测做**区间并集**，两次互不相交的探测就能盖住从没问过的空洞，`covers()` 只看包络 ⇒ 与"没问过就没资格说没有"自相矛盾；且并集不因过期收缩 | 改成存**逐次探测列表**（`version:2`），只有**单次**探测完整包含本次窗口且未过期才算数；只合并相交/首尾相接且答复同为 0 条的区间；老格式（v1 并集）直接判为不可信丢弃，逼一次重问 |
| M-1 | 目标日就是今天、当天净值还没发布时，窗口恰好只有起点一条 ⇒ 被新门判成 `same_nav_endpoint`，提示语写"永久不可验"还建议"把目标日调到下一交易日"——**那正是仓库明令禁止的未来函数** | 判据加一条硬前提：**目标日之后已有净值**（证明确是休市日）才报退化终点；否则回到 `insufficient_points` 明天自愈。提示语删掉"调整目标日"，改为说明历史目标日不自动改。新增用例钉住 |
| M-2 | 清理脚本的候选集取自"今天的门怎么说"，而历史 0% 假结论的窗口往往一条净值都没有 ⇒ 今天的门给 `insufficient_points`，脚本根本看不到它们；第二个判据还拿"实际净值日"去比 `start_nav_date`（存的是请求日），非交易日对不上 | 脚本改为**只从落库证据判**：`actual_change==0 且 start_nav==end_nav` + 两端各过一遍 `_real_nav_date` 归一后相等；证不了的（如 000801 那种真平盘）一律不动。实测扫描：恒 0 结论 1 条，确证退化 0 条（1709 上一轮已撤） |
| M-3 | 补拉后**无条件** `commit()`：`SessionLocal` 没设 `expire_on_commit=False`，空提交也会让 identity map 全过期 ⇒ 第 1 条预测就把 `_warm_cache` 预热好的净值实例全部作废，批量验证退化成 N+1；且它替调用方提交了事务 | 改成 `if backfilled or db.new or db.dirty: commit()` —— 真有 pending 写操作才提交 |
| M-4 | `end_nav_date` 改成实际净值日，但 `start_nav_date` 仍写请求日 ⇒ 同一行两套日期口径，成对审计必然失真 | `start_nav_date` 也写实际净值日（取不到时回落请求日），两端同型可比 |
| M-5 | `rollback_invalid_verifications` 的毁灭默认值（不限 id 全库回溯）仍被前端按钮一键可达 | 真写且 `only_ids=None` 时必须显式 `allow_full_sweep=True`；路由**故意不传** ⇒ 按钮只能定向撤，全量回溯只能走脚本。旧用例已改成钉这个新契约 |
| MINOR-3/4/6/7 | `int()` 在 try 外会让一条脏数据崩掉整批；`_endpoint_dates` 无条件多打两条查询；分诊脚本 `--limit` 下自检必然报"对不上"、且脚本自定分类与服务 reason 并存两套；定向模式下"保留 N 个"混入根本没评估的行 | 脏数据只丢单条；退化终点判定挪进点数不足的分支里；分诊脚本分类以验证服务 reason 为唯一口径（自检分母改成"本次真的分类了几条"）；新增 `skipped_by_filter` 计数并在取数前过滤 |
| 评审提的既有隐患 | `_check_fund_data_availability` 只要基金在 `_history` 缓存里就**只用缓存**，而缓存只装 `[今天-120, +14]` ⇒ 120 天以外的到期预测在批量路径被算成 0 点、单条路径却有数据（我的新提示语还会把它误诊成"缺历史"） | 缓存过滤为空时回落 DB 再查一次（宁可多一条查询，不把有数据说成没数据） |

**评审也说清了几件"不是问题"的事**（免得下一轮重复怀疑）：`_real_nav_date` 与缓存的
一致性（405 天连续探测 0 处不一致）、`data_points>=1` 与点数门的闭包（向前看没有漏口）、
`inside > 0` 不会天天白打接口、单测零网络确实被桩住、`system_config` 当缓存在
SQLite/PG 两边行为一致且不会被启动加载误删。

**没做的一条 + 原因**：MINOR-9（`test_prediction_verify_batch_task.py` 跑完会在它的
临时库里留一行 `nav_backfill_proof:TEST_BF`）—— 那条库是每个用例独立的内存/临时库，
不外溢，留着不影响判据；等有共享库的用例再说。

第 10 轮修完后需要**第 11 轮复评**才能谈 90 门禁。当前全量：见本文件末尾的跑测记录。

## 廿六、第 11 轮：74 分，1 BLOCKER + 8 MAJOR，其中一条是"我把自己写的注释当真了"

评审对象是 `1b4737b`。它先给出一条我必须先认的事实：**测试通过不代表注释是真的**。
下面每条我都自己复现过。

**先用一次真请求把 B-1 的"待验证"关掉**（评审留的问题：线上"真无数据"到底长什么样）：
打 `api.fund.eastmoney.com/f10/lsjz` 四种情形，实测三种"无数据"（不存在的码、真实码+未来区间、
净值停在 2020 的老码）返回的都是 **`Data` 为 dict、`LSJZList: []`、`ErrCode=0`、`TotalCount=0`**。
⇒ "`Data:null` / 缺 `LSJZList` 键" 就是"没问到"，可以安全地区分开；
还白捡一个 `TotalCount` —— 可以用它判断**翻页是否取完**，不再靠"不足一页"猜。

| 编号 | 复现到的事实 | 处置 |
| --- | --- | --- |
| BLOCKER-1 | `{"Data": null}` 走 `payload = data.get('Data') or {}` ⇒ `lsjz_list=[]` ⇒ 置 `complete=True` ⇒ 返回 `[]` ⇒ 被记成"源端确实没有"；更糟的是**翻页中途**遇到 null 会把没问完的尾巴算成问完 | 要求 `isinstance(payload, dict) and 'LSJZList' in payload` 才算问到；`complete` 改为 `len(results) >= TotalCount` 或"不足一页"，翻页截断一律返回 `None`。两条用例（含 4 种信封形状的参数化） |
| M-A | `record_probe` 内部 flush 过 ⇒ 对象不在 `Session.new/dirty`，我上一轮的 `if backfilled or db.new or db.dirty` **永远不成立**：花一次请求换来的凭据不由验证侧落库，注释里"验证侧会立刻提交"是假的（今天没全面暴雷只是因为 `prediction_verify_task.update_progress` 每条都 commit 替它兜了） | `record_probe` 成功后在 `db.info['proof_writes']` 计一笔，验证侧 `pop` 出来作为提交条件之一；用例直接断言"flush 后 new/dirty 为空、但计数器为 1" |
| M-B | `_merge_adjacent` 合并条件是"两边 `source_rows` 相等"，于是 21 条 + 21 条并成一条 `[07-01,09-30] 21 条` ⇒ 破掉本轮立下的"单次探测完整包含"，条数还是编的；`checked_at` 取 max 还会让老的一半借新 TTL 续命 | 只合并**两边都是空答复**的；`checked_at` 取较旧的那个。用例：两段 21 条不许并、宽窗口不许被盖住 |
| M-C | `probes[-40:]` 按 start 排序截尾 ⇒ 丢的是**起点最老**的探测，而那正是每天要重问的窗口（单只 515000 有 77 个互不相交窗口）⇒ 永不收敛 | 先按 `checked_at`（微秒精度 + start 兜底，避免同秒并列裁错人）保留最近问过的，再按 start 排序落盘；被丢的条数打 warning。用例走 45 条互不相交探测 |
| M-D | 上一轮"缓存为空才回落 DB"只堵了一半：窗口下界在缓存切片外、上界在内 ⇒ 缓存给出**非空但残缺**的列表 ⇒ 同一条预测手动验得过、Cron 里永远"数据不足"（评审实测 available True/False 各一次） | 窗口点数**一律走 DB**（`data_points` 只是比较用的标量，缓存这段没省到什么），顺带把全局最新净值那条也改成走 DB |
| M-E | 清理脚本的 `print` 8 个占位符对 7 个实参 ⇒ 一旦真有退化行，`--apply` 在写快照前 TypeError | 修参数；本机镜像当前无退化行所以是潜伏故障，已在只读模式跑通 |
| M-F | 路由没有定向入口（只有 `dry_run`），而 service 拒一切不带 id 的真写 ⇒ 前端"确认执行"**永远撤不了任何一行**，却拿 200 + `success:false`，而 JS 不看 `success` 字段 ⇒ 表现与成功完全一致，还弹一句开发者口味的参数名提示 | 路由加 `ids=`（逗号分隔）；缺 ids 的真写直接 400；前端把"预览里给用户看过的那几条"作为 `ids` 提交，并且 `success===false` 走 catch |
| M-G | 并发首次写撞 `config_key` 唯一约束时，PG 事务进入 aborted，之后每条查询都抛 PendingRollbackError，而 `verify_all_pending` 没有 per-item try ⇒ **整批验证中断**（SQLite 不 abort，所以单测永远发现不了） | 凭据写入包在 `db.begin_nested()`（savepoint）里，失败只回滚它自己。真并发的复现留给 PG，已记为待验证 |
| M-H | 新加的"目标日之后要有净值"这条硬证据，让"净值停在目标日之前的老基金"这一整类**永远拿不到凭据**（补拉豁免根本不发请求）⇒ 判"数据不足、请更新基金数据"每天重试，归因表里结构性不可验恒 0 | 加有界放弃门：到期已超过 `VERIFY_MAX_END_NAV_AGE_DAYS` 仍没有目标日净值 ⇒ 同样判 `same_nav_endpoint`，措辞分开写"目标日是休市日"与"等到期上限仍没有"。用例两条（今天/老基金） |
| MINOR | `Optional` 返回类型没改；`{"probes": 5}` 这类脏值能把整批打停；倒挂窗口（`target < prediction_date`）能被宽凭据盖住；`source_rows>0` 时还说"拿不到"；分诊脚本自检写成恒等式（永远 OK）且两套分类并存；`expire_all()` 写在循环里 | 全部改掉。分诊脚本改成报"日历口径 vs 验证服务 reason 不一致的条数与 id"，并把"已验证结论里有没有 0% 假结论"这件事统一交给 `revert_degenerate_verdicts.py`（它现在按**净值值 + 日期双锚定**，还要求起点之后到目标日之间没有任何净值行 —— 挡住"两天数值恰好相同"的假平盘误判） |

**M-C 那次失败的经过也记一下**（因为它证明这条真在咬人）：我第一版上限测试写完跑不过，
查下去是**秒级时间戳在同一秒内并列**，排序裁错对象 ⇒ 上限逻辑在跑批时本来就不可靠，
不是我测试写歪。改成微秒精度 + `start` 兜底后两条断言都成立。

本地镜像端到端复跑（真打数据源，v1 并集凭据被作废后重新问过）：
12 条卡住的到期预测全部给出带凭据原文的 `no_source_history`（其中 158038 三条是
"源端只给到 1 条、本地凑不够"这一类，措辞已分开），1709 判 `same_nav_endpoint` 且带上
"该基金在 07-11 之后已有净值 ⇒ 目标日是休市日"的证据。
全量 **675 passed / 16 skipped / 0 failed**。

**没做的一条**：`start_nav_date` 语义变更没有做历史行归一（新行是新语义、老行是旧语义混在
同一列）。评审核过下游读取方方向都安全（`retention_three_buckets` 只会更早 ⇒ 多保护历史），
留到 S8 一次性按 `_real_nav_date` 归一，并配 drift 报告。

## 廿七、第 12 轮：79 分（轨迹 58→74→79），两条 BLOCKER 又是"我把自己写的意图当成事实"

### 先说本轮最亮的一记：评审用真请求把我上一轮的"修好"证伪了两次

1. 我上一轮写"凭据写入放在 savepoint 里"，并按 M-G 的理由（PG 撞唯一约束会 abort 事务）
   认为这是改进。本轮我按评审要求补了一条用例：**`record_probe` 后 `db.rollback()`，
   凭据不该存在**。它直接失败 —— SQLite 上"没有外层事务时 SAVEPOINT 自己就是事务"，
   RELEASE 等于提交，于是回滚后凭据留下、会话认知与库不一致。
   也就是说我为修 PG 的一个并发场景，把"调用方回滚必须一起消失"这条更基本的语义弄坏了，
   而且**两库行为不同**、单测全在 SQLite 上跑所以看不见。
   处置：**退回"调用方会话 + 只 flush"**（两库一致），把并发唯一约束那件事降级为
   写明条件的已知遗留（现由 `prediction_verify_task` 进程锁 + advisory lock 保证单批处理），
   要放开并发得改 `ON CONFLICT DO UPDATE`。
2. 我上一轮把 `TotalCount` 当"更完整的到底判据"，却把缺省值写成"当页行数" ⇒
   `len(results) >= total` 首页恒真，**翻页保护整体失效，比改动前更差**（东财倒序返回，
   丢掉的恰是区间最早那段，而验证最需要的就是那段）。本轮改成：`TotalCount` 只在它是
   非负 int 时才用，缺省时只靠"不足一页"判到底，并补一条"缺 TotalCount 必须翻满 3 页"的用例。

### 逐条处置

| 编号 | 复现 | 处置 |
| --- | --- | --- |
| BLOCKER-1 | 信封校验只查"键存在"，于是 `LSJZList: null`、`LSJZList: {}`、`ErrCode≠0 + 空列表`、"整页都解析失败"四种仍产出 `[]` ⇒ 写负凭据 | 要求 `Data` 是 dict 且 `LSJZList` 是 list；`ErrCode` 非 0/`'0'`/None 一律判"没问到"；一页里有一行解析失败就 break（丢了某天＝整段不完整）。参数化用例覆盖这四种形状 |
| BLOCKER-2 | `TotalCount` 缺省回落成当页行数 ⇒ 首页即判"问完" | 见上 |
| MAJOR-3 | `same_nav_endpoint` 的第二条证据是纯日历推断（到期超上限），且排在凭据门之前 ⇒ S7-b 的硬约束被自己开后门 | 撤掉日历推断；第二条证据改成"已问过且有凭据"；根因也在补拉侧修：短窗口免打接口现在要求**终点那天也有净值行**，否则这种窗口永远问不到凭据。用例从"老基金直接判不可验"改成"没问过⇒insufficient_points 继续重试；问过⇒same_nav_endpoint 且引用凭据" |
| MAJOR-4 | 回溯真写不带 `run_id` ⇒ 变更日志无法整批撤销；`verify_history` 不清也不留痕 ⇒ 详情抽屉继续显示"判对/涨跌幅"；前端 `ids` 来自**不限 id 的全量预览** ⇒ "定向"是空保护 | 真写自动生成 `run_id=rollback-<时间戳>` 并回显在 message/data 里；`verify_history` 追加一条 `rolled_back` 墓碑（不删历史）；路由 `MAX_ROLLBACK_IDS=200` 上限，前端按同一上限切片并明确提示"剩下的走脚本" |
| MAJOR-5 | savepoint 在 SQLite 上等于提前提交 | 见上（退回单会话 flush + 用例钉住） |
| MAJOR-6 | `get_nav_history` 仍采信"部分命中"的缓存，而它是 `peak_hit_ratio` 的输入、直接参与 `is_correct` ⇒ Cron 与手动可判出相反结论（我上一轮只修了点数那一半） | 过程指标一律查 DB；用例断言 warm 与 cold 的 `data_points` 与 `len(get_nav_history)` 完全相等（51=51） |
| MAJOR-7 | 分诊脚本 `--json` 里还引用着上一轮删掉的 `degenerate` 变量 ⇒ `NameError`；自检被写成字面量 `'OK'`（从"永远真的假检查"退化成"没有检查"） | 删掉陈旧键；自检改成两件真事：每行必须恰好分类一次、出现的 reason 必须在已知集合里；"矛盾"改成按 `CONSISTENT_WITH` 映射判断（命名差 `missing_history` vs `no_source_history` 不算矛盾，上一轮那种直接字符串比对报出 13/23 全是噪声） |
| MINOR-5 | 批量失败按整条 message 分组，而新措辞每条嵌两个日期 ⇒ 前端"未成功原因"变成一堵墙；服务算好的 `reason` 被丢掉 | `verify_all_pending` 的结果带上 `reason`；`_summarize_failures` 先按 reason 合并（`REASON_LABELS` 给人话名，未登记的 reason 原样透出不猜） |
| MINOR-1/4 | 返回类型注解仍写 `List[Dict]`；`nav_after_target` 无 `order_by` | 改成 `Optional[List[Dict]]`；加 `order_by(nav_date.asc())`。措辞上把"目标日是休市日"限定为"该基金在目标日之后已有净值"这一可核事实 |

### 第 12 轮顺带完成的"S7 整体质检"（老板流程里"最后对整体进行检测"的一步）

**已签字达成**：
- 防未来函数在**结论**上处处成立：写结论只有 `verify_prediction` 一处；端点一律
  `get_nav_by_date(strict_as_of=True)` + `_real_nav_date`（都带 `nav_date <= 请求日`）；
  全仓 `nav_date >` 只剩那条存在性探测，不参与任何数值计算。
- 写结论必过充分性门：`scheduler` / `routes` / `verify_expired_pending` 都收敛到
  `verify_all_pending → verify_prediction`，`force` 只跳过工作日等待期；
  其余路径（`fund_sync_manager`、`sync_sector_mappings`、`rollback_*`）只清结论不写结论。
  唯一旁门是 `PredictionService.verify()`（无门、无净值就能写 `is_correct`），
  当前无路由调用，已记入 S8 待清理清单。
- 凭据的"单次完整包含"不变量成立；重放闸门的硬失败语义（方向翻转退码 3）仍在。
- 新 `reason` 不会引发降级：`prediction_lifecycle` 不看 reason、`prediction_query_service`
  不读、前端只渲染文本，无枚举 switch ⇒ 没有"未知 reason"崩溃面；
  `repair_verify_messages.py` 操作的是板块映射的 `verify_message`，与新措辞无交集。

**本轮修完仍要盯的**：① PG 侧唯一约束并发（待实测）；② `start_nav_date` 老行未归一（S8）；
③ 重放闸门对"被拒判擦掉多少条"不设量化界（评审建议加阈值，我认同，留 S8）；
④ `_warm_cache` 现在只剩 `_history` 那半有用，单点缓存的 key 形状与 `get_nav_by_date`
不匹配、命中率恒为 0（纯占内存），属可清理的死代码，S8 一并处理。

本地镜像端到端复跑（真打数据源）：`23 = 10 等到今晚净值 + 12 带凭据的结构性不可验 + 1 退化终点`，
分诊自检"与日历口径矛盾 0 条"。全量 **683 passed / 16 skipped / 0 failed**。

## 廿九、第 14 轮：87 分、0 BLOCKER，但有两条"我在文案里承诺了代码没做的事"

评审先把我上一轮的每条声明自己复现了一遍，**逐位吻合**：取消合并后凭据从"92 天里发问 86 次、
可归因 9 天"变成"发问 31 次、可归因 92 天"；`end_nav_date > target_date` 的已验证行现在全库 **0**；
判对 598 / 判错 569；1167 条已验证行里重判后**没有**再引入未来数据；`clear_verification_fields`
与被抽走那份 inline 清单**逐字段一致**；博主统计逐个重算 0 个不一致。这部分是好消息。

卡住 90 的是我自己的老毛病——**"把意图当事实"**，这次有两处：

| 编号 | 复现到的事实 | 处置 |
| --- | --- | --- |
| MAJOR-2 | 我把"全部标记已审查"改成 `owner_confirm:false`，并在弹窗里写"不会冒充老板署名、不会给体检免疫"。但 `batch_mark_reviewed` 里**不管 owner_confirm 真假**都写 `reviewed_by='owner' + owner_locked=True`；镜像库那 23 行未审查**全部有机器证据** ⇒ 传真/假结果完全相同，一键照样买到免疫。文案描述的"两条路径不同"在代码里不存在 | 服务改成尊重 `owner_confirm`：不确认 ⇒ `reviewed=True` 但不署名 owner、不锁定；署名与豁免留给逐行/明确确认。用例 `test_batch_review_without_owner_confirm_grants_no_immunity` |
| MAJOR-4 | 上一轮给 `replace=True` 加了"总开关 + 确认头"，可 UI 的"覆盖模式"只发 `{data, replace}` ⇒ 即便生产开了总开关，点覆盖**必然 403**，成一个永久失败的死控件；而且 catch 里读的是 `e.response.data.message`，FastAPI 的文案在 `detail` ⇒ 老板只看到 "Request failed with status code 403"，整改指引全丢 | UI 在用户确认覆盖后带上确认头；catch 改读 `detail \|\| message \|\| e.message`；新加 `tests/unit/test_config_import_guard.py` 把这道安全边界钉住（此前它一个测试都没有） |
| MAJOR-1 | `get_fund_by_sector` 命中 reviewed 就 return、从不过期，而 `_load_cache` 的"已审查条目不被未审查条目覆盖"守卫连重灌时也拒绝降级 ⇒ 本轮把这条路径提到匹配链最前端之后，被取消审查的标的会**在进程生命周期内一直**驱动新预测挂错基金（正是 S5 BLOCKER-1 的病）；测试里手工清 `_cache` 把它藏住了 | 两处都改：进函数先走 `_load_cache()`（60 秒 TTL）；重灌时**先清空**缓存，那条守卫只在"同一次装载内"生效 |
| MAJOR-3 | 撤掉墓碑是对的，但"本次验证详情"只以 `verify_history.length > 0` 门控 ⇒ 出口倒序后，11 行"已被回溯、`is_correct IS NULL`"的预测在抽屉里以最新一行的身份继续显示"+1.55% 判对"，同屏还是两套口径 | 门控补上"现在真的有结论"（`is_correct !== null`），与状态徽标同源 |
| MAJOR-5 | `_ttl_for` 自己调 `date.today()`，而 `covering_probe/fresh` 支持注入 `today` ⇒ 同入参、不同墙上时钟答案相反；历史回放会凭一个当时不存在的宽限判"问过"，正是重放工具依赖的路径 | `today` 一路传进 `_ttl_for`；用例给一条凭据、两个"今天"，断言必须相反 |
| MAJOR-6 | 我承诺"run_id 可整批还原"，但 `SNAPSHOT_FIELDS` 不含 `verify_history` ⇒ 还原后 `verify_count=1` 而历史 2 条，UI 与准确率指向相反方向 | `verify_history` 加进快照；同时删掉一份没人用的 `VERDICT_CLEAR_FIELDS`（"单一来源"不该只由注释保证）与一处重复 `status='pending'` |

MINOR 收口：`_fresh` 加了"时间戳来自未来不可信"（只容忍 1 天 UTC/本地偏移，否则
`checked_at='2099-…'` 就是永不过期的负凭据）；`MAX_PROBES` 的依据用镜像实算过
（单只基金 pending 最多 77 个互不相交窗口、贪心出的最大互不相交集合只有 3 个、>120 的基金 0 个、
满档 120 条 = 13 KB、`_parse` 635 µs），当前真实数据 max 458 B / 4 条；
静态表残留分支（AI/传媒/全球 3 个板块 DB 行未审查却仍被静态码命中）**已记录未修**，
因为修它会改变这 3 个板块新预测的标的归属，需要老板先看一眼。

第 14 轮还确认了几件"不用改"的事：合并导入路径没被误伤、`/api/import-database` 的闸本来就有、
倒序不影响导出与历史表、失败汇总不吃 `verify_history`、`verify_realign_chain.py` 不接守卫是**对的**
（它自己先把 `DATABASE_URL` 设成副本再 import ORM）。

全量 `pytest tests/ -q` → **695 passed / 16 skipped / 0 failed**（688 → 695：本轮新增 7 条用例 ——
批量审查署名 2 条、凭据 TTL 的 `today` 注入与"未来时间戳不可信"2 条、覆盖导入两道闸 3 条，
另有 1 条恒真断言被我自己删掉）。下一步第 15 轮复评。

## 廿八、第 13 轮（83 分）与 S5 交叉回归（58 分）：一次把 94 条未来函数结论清出准确率

### 第 13 轮最痛的一条：我上一轮"修好"的凭据合并，把今天刚问到的凭据当场作废

`_merge_adjacent` 把相邻/相交的空答复并成一条，时间戳取**较旧**的那个（我当时写注释说
"让老的那半段不能借新 TTL 续命"——听起来很保守，实际是**把新问到的那条钉死在旧时间上**）：
从第 3 天起 `fresh()` 永远返回 None ⇒ ① 补拉每天照旧打接口（正是 S7-b 要消灭的行为），
② 结构性归因只在"问过之后 ≤2 天"成立，第 3 天翻回"数据不足"。
评审还按我上一轮真实顺序模拟了 92 天：空答复窗口发问 **86 次**、凭据只覆盖 9 天。
处置：**彻底不做区间合并**（一个窗口一条记录、各自计 TTL），
`MAX_PROBES` 40 → 120（不合并后条目数＝互不相交窗口数，实测单只 515000 有 77 个）。
理由写进注释：合并要么让新记录继承旧时间戳（当场失效），要么让旧半段借新时间续命（谎称问过）——
两个都不可接受，所以宁可多问几次。新增用例 `test_re_asking_refreshes_the_proof`
（"过期后重问到了 ⇒ 必须立刻可用"，正是上一轮缺的那条断言）与
`test_adjacent_empty_probes_stay_separate`。

### 其余逐条

| 编号 | 处置 |
| --- | --- |
| MAJOR-1 墓碑写错位置 | 撤掉"往 `verify_history` 追加墓碑"的想法：前端历史表按 `is_correct ? '正确':'错误'`、`score \|\| 0` 渲染，一条只有说明文字的墓碑会凭空多出"验证失败 / 0 分 / 错误"的假历史。改成把**真正的病**治了：详情抽屉 19 处绑定读 `verify_history[0]` 而后端是正序追加 ⇒ 有 ≥2 轮验证的 451 条预测打开看到的是最早那轮。出口（`get_detail` / `_serialize`）倒序，库内仍是追加序。 |
| MAJOR-2 预热缓存在 commit 后变成 N+1 放大器 | `_history` 改存 `(nav_date, nav)` 轻量元组（ORM 实例被 expire 后每次读属性各发一条 SELECT，实测 0 → 720 条）；顺手删掉一桩死代码：单点缓存写的是 `(code, 日期)` 二元组，而 `get_nav_by_date` 查的键是 `(code, 日期, strict)` 三元组 ⇒ 命中率恒 0 却把 LRU 灌到上限。用例：预热 + commit 后 30 次 `_real_nav_date` 必须 0 条 SQL。 |
| MAJOR-3 分诊自检仍是恒等式 | 承认（我上一轮把 counts 与 report 各 +1 的相等当成"检查"）。改成只报两件真能失败的事：未登记的 reason、以及按 `CONSISTENT_WITH` 映射判定真正矛盾的行（命名差不算）。 |
| MINOR-1 `or` 短路让计数器跨预测残留 | 进 `if` 之前无条件 `pop` 一次 |
| MINOR-4 措辞从本地事实断言源端事实 | "⇒ 目标日是休市日"改成"目标日那天本地没有独立净值行" |
| MINOR-5 分组键 | `_summarize_failures` 先按 `reason` 合并（`REASON_LABELS` 给人话名，未登记的原样透出） |
| MINOR-10 正向凭据压住近期补拉 | TTL 分档：空答复 2 天、**窗口终点在近 30 天内 1 天**、其余 7 天（那天的净值当晚就会发布，用 7 天锁住等于亲手把可自愈的预测锁成不可验） |
| MINOR-3/5 说法修正 | `_check_fund_data_availability` 也被分诊/回溯直接调用，它们从不发请求 ⇒ "是否要求问过"按调用方不同而不同，已如实写进注释；"已知遗留"里我引用的两把锁（进程锁/advisory xact lock）实际只覆盖 `start()` 那一瞬，注释已改成只承认 `status='running'` 那一行 |

### S5 交叉回归查出的是**两件真数据问题**，不是代码风格

**BLOCKER-1 已修**：板块匹配的静态表（`src/constants` 里写死的第二套映射）排在
"老板/agent 审查过的 `sector_fund_mapping`"**之前**，命中就直接 return ⇒
S2/S3 整条身份体检 + 人工审查在"帖子分析→生成预测"这条最上游路径上被绕过。
镜像实查 15 个板块的静态码已在 `fund_info`（有色金属/煤炭/化工/红利/保险/军工/券商…），
例：有色金属 审查结论是 512400，静态表给 160221 ⇒ 新预测直接挂错标的，
下一轮 `sync_sector_mappings` 再改回来并抹掉已验证结论，每条预测重演一次"建→改标→清零"。
改法：审查过的映射提到第 0 级之前（本地查询，不破坏"零网络"意图），静态表退为降级；
用例 `test_reviewed_mapping_beats_the_static_table`。

**BLOCKER-2 已修，并且这次是真动了数据**：`end_nav_date > target_date` 的已验证结论
**94 条 / 1168**（≈8%）——那是 S7-1"休市顺延"用目标日**之后**的净值判出来的，
违反本仓库写死的防未来函数策略。我第一版想用 `rollback_invalid_verifications(only_ids=…)`
撤它，结果只撤掉 **1 条**：那个方法的语义是"今天的数据已不支撑结论"，而这 93 条今天仍可验，
被算成"保留"。于是把"清零字段"抽成 `clear_verification_fields()` 单一来源，
脚本按"结论本身是否用错的/未来的数据判出来的"直接撤：
`run_id=revert-bad-verdicts-20260922-000957`（可 `restore_prediction_batch.py` 整批还原），
93 条全部按正确窗口重判完成 ⇒ **判对 608 → 598、判错 560 → 569**，
也就是说过去有净约 10 条结论是被未来数据"抬"到判对的。这条差值必须让老板看到。
（顺带纠正我自己上一轮的说法：1669 那条我当时判成"结论有依据、保留"，
按现行策略它用的正是目标日之后的净值 ⇒ 属于该撤的一类，已撤并重判。）

**S5 其余 6 MAJOR + 8 MINOR 已进 S8 待办**，最要紧的四条：
① 板块匹配页"全部标记已审查"硬编码 `owner_confirm:true` ⇒ 一键买到永久体检免疫；
② `system_config` 里凭据这类缓存会被 `import_export.py --promote` 整文件带进工作库；
③ `GET /api/config/fund-search` 与 `identity_suggestions` 做出来但前端没接，
"人工换标的"仍是手打 6 位码（正是唯一没有身份证明的写入口）；
④ 9 个 import ORM 的脚本仍没接 `_db_guard`（其中一个 `--execute` 直写生产 `fund_info`）。

本地镜像现状：到期未验证 26 条（含今晚净值到位后会自动消化的），
全库 `is_correct` 判对 598 / 判错 569。第 13 轮修完后需**第 14 轮复评**。

---

## 三十、第 15 轮：两份独立复评（91 分 / 86 分），分歧点恰好都在"我承诺了但代码没做"

复评对象 = `9f10f8d`（第 14 轮的修复）。这轮我同时拿了**两份互相不知道存在的评审**，
外加漂移闸门自己抓出的一条，三者给出的清单不一样 —— 全部逐条复现后再决定修还是驳回：

| 来源 | 分数 | 结论 |
| --- | --- | --- |
| 评审 A（新派，只读复评 6 个重点文件） | **91**，0 BLOCKER | 1 MAJOR（短窗口空转）+ 5 MINOR |
| 评审 B（第 10~14 轮那位，复评同一提交） | **86**，0 BLOCKER | 3 MAJOR + 7 MINOR |
| 漂移闸门 `--offline --limit 200` | exit 3 | 1 条"未解释漂移"（id=3180） |

### 30.1 漂移闸门抓到的那条不是判据漂移，是我自己的还原脚本漏字段（MAJOR）

`id=3180`（515000，目标 2026-09-09）端点数值一模一样，只有 `is_correct` True↔False。
顺台账查到：`scripts/repair_replay_side_effects.py` 手抄了一份还原字段清单，
**漏了 `verify_score`**（还漏 `has_active_prediction`），于是那次"把 88 行还原回
09-20 快照"的操作把结论与台账搬回来了、**标量分数却停在误写值上**。
实测存量：`verify_score` 与台账末条不一致 **11 行**，其中 8 行呈现
"`is_correct=True` 配 `verify_score<60`"、3 行反向；这 11 行的"应有值"三方一致
（台账末条 == 09-20 导出快照），所以方向没有争议。

处置：`scripts/resync_verdict_scalars.py`（默认 dry-run、按台账同步、写 change log
带 `run_id=verify-score-resync-20260922`、重算博主统计、改完自查、有残留就 exit 3），
已在本机镜像上 `--apply` 掉这 11 行；还原清单收回单一来源
（`restore_prediction_batch.RESTORE_FIELDS`），修复脚本改为 import 它；
修复脚本 `--apply` 之后强制自查"标量与台账是否还打脸"，不再自报"已还原 88 条"就完事。

### 30.2 评审 B MAJOR-1：`snapshot_prediction` 存的是活引用，前像是假的（MAJOR）

这条如果只看得见 30.1 就会漏：`verify_history` 是 JSON 列，验证路径用
`list.append` **原地**改它（`prediction_verify_service.py:1086`），而
`_json_value` 直接返回那个 list 对象 ⇒ `before_state.verify_history` 与
`after_state.verify_history` 是同一个对象，日志里的"变更前历史"其实是变更**之后**的，
`changed_fields` 也永远报不出 `verify_history`。第 1 轮验证时历史从 `[]` 变成
非空是"重新赋值"，所以前像碰巧是对的；从第 2 轮起才开始说谎。
**更要命的是顺序**：我刚把 `verify_history` 加进还原清单，不先 deepcopy 就会把
"审计说谎"升级成"还原写回错数据"。

修法：`_json_value` 对 list/dict 做 `copy.deepcopy`。往返用例改成**和生产同形态**
（`.append()`，不是我原来写的重新赋值 —— 那版必然全绿，挡不住这条），
并断言 `changed_fields` 里出现 `verify_history`。已实测：去掉 deepcopy 该用例即红。

### 30.3 评审 B MAJOR-2：我第 14 轮的"重灌前先清空"把缓存安全网剪断了（MAJOR）

清空写在 `db.query` **之前**，一次查询异常（Supabase 抖一下）就把上一份完整缓存整体
销毁，而 `llm_analyzer._get_sector_fund_map` 与 `fund_matching` Level -1 各自 `except`
之后**静默退回写死的静态表** —— 那正是 S5 BLOCKER-1 认定的"绕过身份体检 + 人工审查"
的口。改成：先灌进局部 dict，全部成功才整体替换；失败时若有旧缓存就带着它继续服务
（"旧但完整"永远比"空表 + 静默降级"安全），一份都没有才把异常抛出去。
用例：warm → 让下一次查询抛异常 → 断言缓存与 `_cache_loaded` 都还在；
另一条断言"没有旧缓存时必须抛、不许装作加载成功"。第 14 轮那条
`test_cancelled_review_must_reach_the_matcher` 仍绿（清空语义由"全新局部表"提供）。

### 30.4 评审 B MAJOR-3 + 评审 A m-5：漂移闸门自身不够硬（MAJOR / MINOR）

两点都成立：① `--offline` 只桩数据源、**不桩 LLM** ⇒ "未解释漂移=0"是环境依赖的
（有密钥时带随机性并花配额，没密钥时那 15 行末轮 `llm_verify` 必然被判未解释）；
② 绑定自检是"造一个新工厂再问它绑在哪"的同义反复，挡不住"某模块在换绑前就抓走旧工厂"。
修法：`--offline` 一并把 `PredictionVerifyService.llm_analyzer` 换成返回 None 的桩
（闸门只复现确定性判据）；新增 `_llm_leg_explanation`（**三个条件全用行内证据**：
台账末条 `verify_type=llm_verify`、它的 score/is_correct 与旧标量一致、差异只落在判定
输出上）把这类差异单列成可解释桶并打印条数；扫描 `sys.modules` 里**所有** `SessionLocal`
引用是否都指向副本（`src/fund/fund_api.py` 等都是模块级 import，正是事故形态）；
再加开跑前后对源库文件算 `size+sha256`，变了就 exit 6。
现在 200 条离线重放：**drift 44 / 未解释 0 / 异常 0 / exit 0**，
并打印 `[ok] 源库未被改动`。

### 30.5 评审 A M-1：短窗口"只有终点那一行"既不补拉也永不归因（MAJOR）

`fund_api.py` 的免补拉条件是 `… and (span_days < 14 or inside >= min_inside)`，
短窗口**整体跳过密度检查** ⇒ `inside==1` 也直接 return 0；验证门要 ≥2 个点，
而起点行在窗口之前所以落不进退化终点判据，凭据又永远建不起来
（第 9 轮 MAJOR-2 修法过头留下的另一半）。改成密度门槛对所有窗口都生效
（`min_inside` 本来就被 `max(1, span_days)` 夹在窗口物理可容纳的条数上，不会再"永远拉不满"）。
两条新用例（补齐型、空答复型）在旧条件下实测皆红。

### 30.6 MINOR 的处置

修掉：`current_nav_date` 与 `end_nav_date` 同口径写实际净值日（原来同一端点两个日期）；
`clear_verification_fields` 补 `ai_judgment` 并让
`PredictionMaintenanceService._reset_verification` 直接调它（两份清单分叉 ⇒
撤结论后 pending 行还挂着旧 AI 判词）；注入的 `today` 传到底
（`backfill_proofs.fresh(..., today=today)`，否则 TTL 分档吃墙上时钟）；
凭据的"源端给到 N 条"只数**落在请求区间内**的行；逐行"审查"补上 `confirm`
（批量弹窗写着"豁免需要逐行确认"，而后端一律给 owner 署名 + 锁定，没有这句
就是点 23 次也能买到批量被拒的免疫）；删掉 `row.owner_locked = bool(row.owner_locked)`
这一行空操作；覆盖导入的标签写明还需服务端总开关 `ENABLE_DATABASE_IMPORT`；
FastAPI 422 的 `detail` 是数组，`errorMessage` 三处 + 导入处渲染成
`字段：原因`，不再 `[object Object]`。

**驳回**（写明理由，不当没看见）：
① 评审 B MINOR-6 说 `UNSERVABLE_VERDICTS` 在同模块定义两遍 —— grep 实测只有
`sector_identity_audit.py:51` 一处赋值，不成立；
② MINOR-1 的"逐行点 23 次仍能买到免疫"作为设计缺陷不成立（逐行确认本就是 owner 行为），
我只补 confirm 把文案兑现；
③ MINOR-3（`batch_review` 署名把行推进 0.85 死区）不是 bug，是待老板拍的口径问题，
已进决策清单；④ MINOR-5（日志体积 2.6×）与 MINOR-7（`MAX_PROBES=120` 的样本只有 16 只
基金）记录为已知代价，不改判据。

### 30.7 本轮门禁判断

两份评审的分数（91 / 86）差在"要不要为还原清单与闸门自身硬度扣分"。我按 86 那份处理，
因为它三条 MAJOR 全部复现成立、且其中两条（30.2、30.3）是**我上一轮的修复新引入或新暴露的**。
本轮修完后重跑：全量单测、`--offline --limit 200` 到 exit 0、`resync_verdict_scalars.py`
自查无残留。**仍不推 GitHub、不做生产迁移与回写**，等整体质检过线 + 老板授权。

---

## 三十一、第 16 轮：两份复评都是 84 分，两条 BLOCKER 一条比一条难看

复评对象 = `26d6f4b`（第 15 轮的修复）。这次两位评审各自独立跑探针，给出的清单**几乎不重合**，
所以合并处理最有价值。评审 A（84 分：1 BLOCKER + 2 MAJOR + 3 MINOR）、
评审 B（84 分：2 BLOCKER + 3 MAJOR + 7 MINOR，另附 8 项逐条验收与全库实测表）。

### 31.1 BLOCKER-1（评审 B）：我号称的"离线重放"其实在打真接口，之前那句"未解释漂移=0"不作数

`scripts/replay_verifications_on_copy.py` 里 `from src.fund import fund_api as fa` 拿到的
**不是模块，而是 `FundAPI` 实例** —— `src/fund/__init__.py` 把包属性 `fund_api` 重绑成了实例，
而验证侧走的是 `from src.fund.fund_api import fund_data_manager`（模块属性）。于是
`fa.fund_data_manager = 桩` 是给实例挂了个没人读的属性，等于**空操作**。
评审 B 的硬证据：跑完"离线"重放后，遗留副本里凭空多出镜像没有的 4 行净值
（515070 的 09-14…09-17）和一条当天 02:28 的凭据 —— 那次跑批真的打了东财 `f10/lsjz`。
**我第 15 轮报给老板的"200 条离线重放 drift 44 / 未解释 0 / exit 0"因此不成立**：
它的差异归因里"本地镜像与当时线上取到的行不同"这句话，真实原因是重放当场取了新数据。

修：① 用 `importlib.import_module('src.fund.fund_api')` 拿真模块再桩；
② 直接把 `requests.Session.send` 掐掉（任何漏网取数变成看得见的异常，而不是悄悄改结论）；
③ 加**硬断言**：`--offline` 跑完副本的 `fund_history` 行数与凭据条数必须一条都不许多，
多就 exit 7。修完再跑：净值/凭据 `10600/16` 未变、源库 sha256 未变。
④ 同一个错误写法在 `tests/unit/test_prediction_verify_weekend_deferral.py` 里也有一份，
而 `tests/unit/test_prediction_change_log.py::test_automatic_verification_creates_change_log`
每跑一次就向东财请求一次假基金码 AUDIT01。根因是"每个文件自己写桩"这个模式本身，
所以把零网络改成**夹具强制**：`tests/conftest.py::_block_real_http` 屏蔽 `Session.send`。
用日志计数验证：全量单测里被掐断的真实外呼从 2 次降到 **0 次**，701→712 条用例全绿。

### 31.2 BLOCKER-2（评审 B）：`waited_previous` 用纯日历把结论判死，镜像里已有 4 条、队列里还有 12 条

点数够之后，只要"目标日不是今天、等待期已过、前值 age ≤ 10 天"就 `available=True`，
端点用目标日之前那条净值。而第 12 轮定下的两条合法证据（目标日之后已有净值 /
未过期负凭据）只写在"点数不足"那一支里 —— 于是**"本地缺那一行"与"市场那天没行情"
被当成同一件事**。`is_correct` 一旦非空就永不重判（`filter_due_for_verify` 只捞 NULL）。
镜像实测已判死 4 条（3267/3283/3304/3334，端点比目标日早 2~4 个工作日、中间全是工作日、
库里也没有目标日之后的行），其中 515070 那条按前值判"错 0 分"，
而数据源其实有目标日净值 —— 评审 B 现取回来判**对 100 分**。队列里另有 12 条走同一路径。

修：新增 `endpoint_lag_unproven` 门，位置放在"等待期已过"之后、`waited_previous` 之前，
要求三条证据之一：① 端点到目标日之间全是周末（日历可证，周六目标日用周五端点不受影响）；
② 库里已有目标日之后的净值；③ 未过期凭据。拿不到就留在队列里并说清"先按区间回补"。
配套：`REASON_LABELS`、分诊脚本的 `KNOWN_CLASSES`/`CONSISTENT_WITH`（`verifiable_now`
现在允许这个 reason，日历数点数、判据看端点，两者不矛盾），4 条已判死的用
`rollback_invalid_verifications(only_ids=…)` 撤掉（`run_id=revert-lag-endpoint-20260922`），
判对 598→597、判错 569→566。测试：新增 5 条（含"周六目标日不需要证据仍能判"防收紧过头），
并把 `test_prediction_verify_date_boundary.py` 里那条"等待期已过 ⇒ 可用前值"的老用例
拆成"没证据必须拒判 / 有后续行才能判"两条 —— 老用例断言的正是这个缺陷本身。

### 31.3 其余成立项

- **评审 A BLOCKER**：我第 15 轮新加的"扫描 `sys.modules` 里所有 `SessionLocal`"用
  `hasattr(factory, 'bind')` 过滤，而 SQLAlchemy 2.0 的 `sessionmaker` **没有** `.bind`
  （在 Session 实例上）⇒ 每个候选都被 skip，函数永远返回空表。我修一条同义反复自检，
  转头写了一条更隐蔽的。改 `factory.kw['bind']`。顺带修 `_bind_session_factory_to`
  丢了 `class_=_RetrySession`（副本上会话形态与生产不同，重放"一致"就不算数）。
- **评审 A MAJOR**：第 15 轮那个"llm_verify 可解释桶"是橡皮图章 —— 条件全是旧值侧证据，
  对重放结果毫无约束，确定性判据整体坏掉（把边界行算成 0 分）也会被它吞掉，
  而盲区恰好覆盖闸门最该盯的边界行。改成**不做解释、只做排除**：离线模式把这些行
  让出抽样并如实报数（实测 15 条），它们既不计入"可解释"也不计入"未解释"。
- **评审 A MAJOR / 第 16 轮 MAJOR-2**：密度门槛的夹取用自然日（`max(1, span_days)`），
  挡不住"周六→周一"容量 1 天、门槛 2 条的窗口 ⇒ 天天真发一次请求。改成按**工作日容量**
  夹取（`weekday_capacity`），并把注释里那句"不会再出现永远拉不满"改成可核对的表述。
- **评审 A MINOR**：`resync_verdict_scalars.py` 原来默认"台账末条就是当次原场"，
  但 `PredictionService.verify()`（人工确认）只改标量、既不追加台账也不动 `verify_score`
  ⇒ 那种行的末条属于**另一次观察窗口**，照它覆盖等于写进无关窗口的分数。
  现在要求末条 `change` 与标量 `actual_change` 是同一次（对不上只报不改），
  并把 `current_nav_date` 与 `end_nav_date` 的遗留分歧（21 行，m-1 的旧数据）纳入同批同步。
- **评审 A/B MINOR**：逐行"审查"的弹窗文案对"体检已判不可服务"的行是空头承诺
  （服务层直接拒绝），已改口并在路由上把三种失败原因分开报；路由此前从不转发
  `owner_confirm` ⇒ "明确确认"只活在浏览器里，现在 API 边界也要显式传；
  `backfill_history_range` 里那次 `fresh()` 仍吃墙上时钟，注入的 `today` 现在传得到。
- **评审 B MINOR**：分数规则表的高亮条件补上 `is_correct !== null`（回溯过的行不再
  把被撤销那一轮的 verify_type 当现状）；新增 `RESTORE_FIELDS ⊆ SNAPSHOT_FIELDS` 反向用例。

**驳回**：评审 B 的 MAJOR-3"撤结论后抽屉仍显示旧验证详情"—— 第 14 轮已加
`predictionDetail.is_correct !== null` 这道闸（`web/index.html:1204`），
B 引的 11 行是修复前的观察；本轮只补了它漏掉的那张规则表高亮。

### 31.4 我自己这轮查出来的新缺陷（比第 16 轮清单更严重，尚未修，等老板点头）

漂移闸门在真断网之后只剩 1 条未解释差异（id=2602），顺它查下去发现一个**大面积**问题：
以"端点净值在**当前标的**的净值表里对不对得上"为准重新核全库，
1163 条已判结论里 **250 条（21%）对不上**，分三种成因：

| 成因 | 条数 | 含义 |
| --- | --- | --- |
| 同一天数值不同 | 108 | 结论落库后净值被修正/覆盖过（复权、补录改写） |
| **改标后结论没重算** | 97 | S4b/rematch 把 `fund_code` 换成正确标的，旧标的判出的结论却留在行上 |
| 当前标的当天没有净值行 | 45 | 镜像缺行或基金停更（003033 那一族） |

> **⚠ 这张表的分桶在第 17 轮被证伪并重做过**（见 §三十二 32.4）。上表用"这条预测
> 历史上出现过 fund_code 变更日志"来归责，实测 97 条里 95 条的末次验证发生在改标
> **之后**、用的就是当前标的 ⇒ 那不是"挂错标的"。改成两条同时成立才算：
> ① 写下结论那一刻行上挂的是另一个代码（取最后一条 `verified` 日志的后像），
> ② 那个代码在端点那一天的净值正好等于结论里存的端点值。重做后 250 条的真实分布是
> **可证挂错标的 2 条 / 归不了因 158 条 / 当天无行 45 条 / 同一天数值被改写 45 条**。
> 结论不变（这 250 条的端点证据今天复现不出来），但成因与处置成本跟我第一版写的
> 完全不是一回事 —— 报数前该做的是归责核对，不是先给个吓人的分类。

之前几轮的自洽检查（标量 vs 台账、台账 vs 导出快照）全都报"0 不一致"，
因为**标量与台账是同一次验证一起写进去的**——它们互相自洽，却都与当前标的对不上。
这就是老板要的"B 功能改完要回头看有没有影响 A 功能"：改标是 B，准确率是 A。
97 条属"结论挂错标的"（→ 第 17 轮更正：可证的只有 2 条，见上），会直接进准确率与博主排名；108/45 条属"输入变了/没了"。
处置需要老板点头（三种可选：撤掉重验 / 只在报表里标"证据已失效" / 全部保留），
我不擅自批量重判 —— 第 13 轮那次 94 条的净影响是判对掉 10 条，这次规模是它的 2.6 倍。

---

## 三十二、第 17 轮：两份复评又是同一分数（85 / 85），四条重叠 —— 说明它们是真问题

复评对象 = `0baa27f`。两位评审独立给出 **85 与 85**，且**重叠项**有：证据门被周末分支整体跳过、
漂移闸门把方向翻转解释掉、`owner_confirm` 那道门对有机器证据的行完全无效、
零网络护栏只掐了一条腿。重叠 + 各自独有的都逐条复现后处置如下。

### 32.1 BLOCKER：我上一轮刚立的"端点证据门"，被它上面的周末分支整个绕过

`is_weekend_target` 的 `return weekend_previous` 排在证据块之前 ⇒ "目标日周六、端点停在周三"
（周四周五本地缺行）仍然纯日历判死。两位评审都用内存库做出了同一对对照：
目标 09-19(六) 放行、09-18(五) 拒判。修法不是"把门整体上移"——那样会把
"净值只是还没发布"（等待期内）也一并打成 `endpoint_lag_unproven`，抢了
`waiting_target_nav` 这个更准确的标签（我第一版就上移了，`test_weekday_wait_period_keeps_pending`
与 `test_config_overrides_wait_and_max_age` 当场红）。最终写法：
**证据一次算完，两个出口各自引用** —— 周末分支与"等待期已过"分支都查同一个
`lag_evidence_missing`，周六目标 + 周五端点靠"中间全是周末"天然免证。
新增用例：`test_weekend_target_with_a_stale_midweek_endpoint_is_still_refused`
（起 07-06、只有 07-06/07/08 三行、目标周六 07-11 ⇒ 拒判且 `gap_weekdays=2`）。

### 32.2 BLOCKER/MAJOR：漂移闸门仍会吞掉方向翻转，而且吞的正是最贵的那批

`_explain` 第 4 桶只要求"`end_nav_date` 标签没变、`end_nav` 变了"，**不看 `is_correct`** ⇒
第 16 轮我报的"未解释 1"里其实藏着 9 条方向翻转（`actual_change` 1.39%→-71.6%、
-3.6%→+143.9%），2602 只因连日期标签都变了才露出来。这与我在 32.3 里刚为 LLM 桶讲过的
是同一个错误形状。修法：立一条总则 —— **`is_correct`/`status` 翻转一律不解释**，
只有两类例外（新判据拒判、旧结论用了目标日之后的净值），并把"端点证据已与当前标的
对不上"的行按 `audit_verdict_evidence` 的判据**让出比对、单列计数**（不解释也不计通过）。
效果：`--offline --limit 200` 让出 60 条（15 条 LLM 腿 + 45 条证据失效），
剩下 **155 条重放差异 0 条、未解释 0 条、异常 0 条、exit 0**，
证据指纹 `(10600 行, SUM(nav)=23315.8439, 16 条凭据)` 与源库 sha256 均未变。
—— 这才是能当验收信号的数字：判据在"证据仍然成立"的行上是位级稳定的。
另外补了 `SUM(nav)`：只数净值行数对 `update_fund_history` 那种**就地改写数值**完全失明。

### 32.3 MAJOR：`owner_confirm` 这道门只管"没证据的行"，对有证据的行仍然无条件盖 owner

实测裸 `POST /api/config/sector-mappings/{id}/review`（不带任何参数）对
`match_source='agent' + verified_at` 齐备的行照样落 `reviewed_by='owner' + owner_locked=True`
⇒ 上一轮"免疫只能在 API 边界上显式确认才给"这句承诺没成立。改成服务层与批量路径同一口径：
**署名与锁定只认 `owner_confirm`**；UI 在弹窗确认后才带这个参数。
顺带补上第 17 轮另一条"文案承诺了代码没有的动作"：确认文案写着"要撤销就再点一次取消审查"，
而页面上**根本没有取消入口** ⇒ 误点一次就是永久免疫且不可撤回。现在路由接受
`reviewed=false`、服务层撤销时把署名与锁定一起收回（`test_cancel_review_clears_signature_and_lock`），
页面加了"取消审查"按钮。

### 32.4 MAJOR：我自己那个 250 行的**归责**是错的（两份复评共同指出，已更正）

`audit_verdict_evidence.py` 首版按"有过 fund_code 变更日志"归责出 97 条"挂错标的"。
复评逐行核对：其中 95 条末次验证发生在改标**之后**、用的就是当前标的。
改成"① 写下结论时挂的是另一个代码（最后一条 `verified` 日志的后像）+
② 那个代码当天的净值正好等于存的端点值"两条同时成立才算，重跑全库：
**可证挂错标的 2 条 / 归不了因 158 条 / 当天无行 45 条 / 数值被就地改写 45 条**。
另外反向也不完备：53 行当前 `fund_code` 与最后一条日志不符（`fund_sync_manager` 直接改列不写日志），
这类改标根本进不了归责依据 —— 已作为 `(附加)unlogged_retag` 单独报出。
同时给脚本加了阈值退码（默认 0）：**21.5% 的失效证据从此在任何流水线上都构成阻塞**，
不再是一个"打印完就算"的报告。§三十一 31.4 的原表已就地标注更正。

### 32.5 MAJOR：验证侧还有一条"自带标的不过体检"的后门

`match_fund_for_prediction` 第一优先级直接 `return prediction.fund_code`，
而同一函数后面三条分支都带 `_servable_predicate()` ⇒ 某代码被体检判不可服务后，
新预测不再命中它，**已入库的预测却仍按它验证**（正是"结论挂到错标的"的成因之一）。
现在：所有带该代码的映射行都被判不可服务时，改按板块重新解析；体检没意见的代码一律放行
（不能因为"没有映射行"就把历史预测卡死）。两条用例分别钉住这两个方向。

### 32.6 MINOR 处置

零网络护栏补齐第二条腿（`openai → httpx`）并**给它自己写了会红的用例**
（`tests/unit/test_zero_network_guard.py`：真去请求必须被挡；护栏被删就报
"DID NOT RAISE"）。第一版我一刀切 `httpx.Client.send`，把 Starlette 的 TestClient
（它就是 httpx.Client 的子类）打死，11 条 API 用例当场全红 —— 已改成只放行 ASGI 占位域名。
凭据时间戳改为可注入 `now`（`record_probe(..., now=...)`、`backfill_history_range(today=…)` 贯通），
拆掉我上一轮亲手埋的日期炸弹：真实日期走到 09-24 时那条"未过期凭据"用例会与代码无关地变红。
第 16 轮提交正文写"同步 21 行 current_nav_date"，台账实为 17 条（另 4 行被同批撤判清空），
按"报数要与账对得上"更正在这里。

**留给下一轮/老板**：`endpoint_lag_unproven` 在生产上等价于"这次问失败才拒判"——
同一次调用会先按区间补拉并写凭据，凭据本身就是证据③。对 T+2 才发净值的 QDII/定开基金，
仍可能在当天用前值落死结论；要收紧就得给这类基金单独加大 `VERIFY_DATA_WAIT_DAYS`，
那是配置口径，等老板看过再说。

**本轮门禁判断**：两份 85 分都不够 90，且其中一条 MAJOR 推翻了我上一轮报出去的数据结论。
修完重跑：`tests/unit 720 passed / 16 skipped`、`tests/ 729 / 16`、
离线重放 155 条比对差异 0、exit 0。仍**不推 GitHub、不做生产迁移与回写**。

---

## 三十三、S9：250 条失效证据的处置（老板批准"标注 + 重验 + 杜绝复发"，不删）

老板的口径是"同意建议，也可以直接删，只要以后不再出现这种失效"。我的判断与做法：

**不删**。这些行是"当初为什么这么判"的唯一审计链，删了就永久失去；而且准确率是推导值，
删行会让分母变化看起来像"修好了"。改成下面三件事。

### 33.1 判据只留一份，状态做成派生值

新增 `src/services/verdict_evidence.py`：一条结论的端点净值在**当前标的**的净值表里
还对不对得上，三个状态 `verdict_under_other_fund` / `nav_row_missing` / `nav_rewritten`。
前端 ⚠ 标记、`scripts/audit_verdict_evidence.py`、漂移闸门、每日跑批**共用这一份**
（"两份清单迟早分叉"这个仓库已付过四次账：字段清单、reason 表、unservable 判据、归责口径）。

状态**不落库、不加列**：净值补回来它自己消失，也不会出现"列说有效、表里没那一行"的第二份真值；
生产加列还要一次迁移。列表接口按页批量算（共两条查询），不给列表加 N+1。

### 33.2 能确定挂错标的的 53 条：退回未验证，按当前标的重判

`revert_degenerate_verdicts.py` 加第三个检测器 `find_mis_homed`（`--include-mis-homed`）。
**这条链上我又错了一次**：第一版报"可证挂错标的只有 2 条"，抽查 7 条发现 7/7 都成立
（存的净值只等于改标前那只基金当天的值，例 id=1903 现挂 512170、结论来自 512010@07-16
的 0.3788）—— 上一轮"2 条"和更早的"97 条"都是归责实现里的 bug。现在的口径经手工抽查确认，
撤判后 `audit_verdict_evidence` 的该桶归零。

执行结果：`run_id=revert-bad-verdicts-20260922-120657`，53 条回到待验证队列，
判对 597→573、判错 566→537，今晚按正确标的重判。剩余 **197 条（17.7%）保留并打 ⚠ 标记**
（净值被就地改写 108 + 当天无行 89）—— 它们的结论没说"判错标的"，只是今天复现不出来，
**不会**自行消解（第 19 轮订正：这里当时写的是“等净值补齐会自行消解”，错的 —— 每日同步只回写最近 30 天，而这批端点大多是上半年旧日期，见 §三十五）。

### 33.3 防复发：改标必须同时清结论并留痕

`FundSyncManager.retag_prediction()` 成为**唯一**改 `fund_code` 的入口（原 5 处散写全部收口）：
变更走 change log（可整批还原），已有结论的一律 `clear_verification_fields` 退回未验证。
测试 `test_retagging_clears_the_verdict_and_leaves_a_log` 钉住这两半，
`test_retag_without_change_is_a_no_op` 挡住"无事也清结论"的数据破坏。
`scripts/run_scheduled_tasks.py` 每日跑批新增 `verdict_evidence_audit`（只读），
把失效条数打进日志/结果 —— 以前这个指标在任何流水线上都不构成阻塞。

**残留限制（诚实记录）**：净值被上游修正这类事，代码无法阻止其发生；能阻止的是
"发生了但没人知道"。所以现在有 ⚠ 标记 + 每日计数 + 审计退码三处出口。
`match_fund_for_prediction` 里"预测自带代码"这一路现在也过身份体检（第 17 轮 M-2 的另一半）。

**验证**：`pytest tests/ -q` → 735 passed / 16 skipped / 0 failed；
`audit_verdict_evidence` 报 197/1110（17.7%），`verdict_under_other_fund` 归零；
`codegraph sync .` 已跑。仍不推 GitHub、不做生产迁移与回写。

---

## 三十四、第 18 轮：78 分，BLOCKER 是我自己上一轮"防复发"引入的

评审 A（只读，独立跑镜像与内存库）给 **78/100**，1 BLOCKER + 3 MAJOR。核心一句：
**S9 让"改标必清结论"生效了，却没管住"谁有资格改标"** —— 于是防复发的机制本身
变成了一次批量毁数据的开关。

### 34.1 BLOCKER：页面上一个按钮能清掉 46% 的已判结论，且撤不回来

`POST /api/funds/update-all` → `full_sync` → `sync_missing_funds`：那里用
`{f.sector_type: f}` 建查找表（同一板块登记过两只基金时**最后一条赢**，镜像 38 个
`sector_type` 背后有 >1 只基金），然后对"与本板块那只不一致"的预测一律改标。
S9 之前这已经很粗（旧账），但**不清结论、不写日志**，所以危害是"标的错了"；
S9 之后同一条路径变成**清结论**。评审实测：`fund_sync_manager.py:178` 这条路径
会命中 **1110 条已判结论里的 515 条**；而三个调用点既不 `recalculate_blogger_stats`
也不带 run_id（`restore_prediction_batch` 只认带 run_id 的日志，实测 4883 条日志里
3503 条为空）⇒ 既撤不回，页面准确率还会继续显示一个表里已不存在的分母。

修法（每一条都配了会红的用例）：
1. `sync_missing_funds` / `check_prediction_fund_match` **只允许填空**，不再覆盖已有标的；
   改标是人工决定，只属于带 run_id + 重算统计的那条链。
   实测旧规则命中 515 条 → 新规则 **0 条**。
2. `retag_prediction` 无 `run_id` 时自动生成 ⇒ "可整批还原"不再依赖调用方记得传。
3. 清过结论的博主登记进 `touched_bloggers`，三个调用点在提交前统一
   `recalculate_blogger_stats` ⇒ 统计列不再与结论脱钩。
4. "唯一入口"补上真正的门：`test_no_new_direct_fund_code_writes_appear` 用 AST 扫
   `src/` 里所有 `X.fund_code = ...` 赋值，白名单外新增即变红（评审 M-2 指出
   `prediction_maintenance_service` 还在直写并自带第二套"有没有结论"的判据）。
   该服务现在改调 `retag_prediction`，判据合并成 `has_verdict_trace()` 一处
   （`verify_count>0 / status / is_expired / is_correct` 的保守并集）。

### 34.2 MAJOR：证据②把"本地缺行"当成了"法定节假日"

`endpoint_lag_unproven` 的证据②原话是"库里已有目标日之后的净值 ⇒ 中间空着的工作日
确实是休市"。这句在真实数据上不成立：镜像 218 只基金里 **169 只在自身序列中间有空洞
（合计 2984 个缺失工作日）**；活体两例 `158038` 只有 09-07 与 09-11，而 09-08/09-09
分别有 **175/176 只别的基金**有净值 ⇒ 市场开门，是我们没同步。预测 3126/3178 已过
等待期，会拿 09-07 那条把结论落死。
修法：加一条跨基金核验 —— 缺口里只要有任意一天"全市场有行而我们没行"，②就不成立，
必须靠未过期凭据或等补数据。两条用例分别钉住"本地空洞拒判"与"全市场都没行才放行"。

### 34.3 MAJOR/MINOR：三处夸大与遗留

- **"退码 3 ⇒ 在流水线上构成阻塞"不成立**：没有任何流水线调用它，而唯一的自动消费者
  （cron 的 `verdict_evidence_audit`）无条件 `success: True`。已把注释与输出改成实话：
  **日任务是报告，不阻塞**；要卡合入请手动跑脚本。
- `verdict_evidence` 的净值查询只按日期筛不按代码筛 ⇒ 一页 70 个日期回传 8724/10600 行
  （82% 全表），Supabase 上每翻一页都吃一次全表。已加 `fund_code IN (...)`。
- `fund_sync_manager` 尾部还有一段手抄的"重置已验证"清单，且状态词用的是
  `'correct'/'wrong'/'expired'`（全仓实际写 `success/failed`）⇒ 条件永不成立的死码，
  而它把 `verify_score` 清成 0 而非 None，正是第 15 轮事故的形状。删除，改由
  `retag_prediction` 的返回值计数。
- 59 条已判结论的 `end_nav_date` 落在周末 ⇒ 会被归进 `nav_row_missing`，原文案
  "本地缺行或基金停更"对这批是误导，已改成"周末老数据 / 本地缺行 / 基金停更"。
- 悬空引用清理：`old_fund_verdict`（两处注释）改回真实桶名；§三十二说的
  "让出 60 / 剩 155"因 53 条撤判今天已变为"让出 51 / 比对 164"，在 34.5 记录新数。
- 28 条已判结论没有任何 `verified` 日志 ⇒ 结构上进不了 mis-homed 桶（已知局限，记录）。

### 34.4 我接受的批评方式

这一轮的 BLOCKER 不是"没想到"，是**我为了修一个问题而引入的副作用没有过一遍
"谁能触发它"的演练**：一个面向用户的按钮，接在我新加的"必清结论"上，就该先问
"这条路径会不会一次命中几百行"。以后凡是给写操作加"顺带清数据"的，先跑一次
只读统计（本次这条统计就是 `/tmp/probe_b1.py` 的形状，515→0）再提交。

### 34.5 验证

`pytest tests/unit -q` → **733 passed / 16 skipped**；`pytest tests/ -q` → **742 / 16**；
（这两个数是 §三十五 当时的实测；§三十六 之后已变为 741 / 750，别拿这段对号入座。）
`audit_verdict_evidence` → 197/1110（17.7%），`verdict_under_other_fund` 归零；
`replay --offline --limit 200` → 让出 51、比对 164、**差异 0 / 未解释 0 / exit 0**，
副本证据指纹与源库 sha256 均未变；`python -m src --init-db` 在 `.env` 指向生产时被
守卫拒绝（预期行为，未连线上）。


## 三十五、第 18 轮的 M-1 / M-5：两条"承诺与代码不一致"里剩下的那两条

评审 B（68 分）与评审 A 重叠的四条已在 §三十四处置完。剩下两条我判断是**同一类病**：
上一轮把"豁免只许老板在 API 边界上显式买到"写成了承诺，却留了两个口子没吃这个参数。

### 35.1 MAJOR-1：编辑保存白送永久体检免疫

`SectorFundService.update_mapping` 结尾那段 `if mapping.reviewed:` 无条件写
`reviewed_by='owner' + owner_locked=True`，而 `PUT /api/config/sector-mappings/{id}`
从不传确认参数、UI 的"保存"也没有弹窗 ⇒ **一次普通保存就买到免疫**
（`row_unservable()` 的 owner 例外连 `is_fetchable` 列和 verdict 一起跳过）。
第 14 轮修了批量审查、第 17 轮修了逐行审查，两条都在服务层等 `owner_confirm`，
第三条写入路径漏了整整两轮。

现在：署名与锁定只认 `owner_confirm`；没确认只记 `reviewed_by='manual_review'`（＝看过）。
另外补一条 §三十四没覆盖的语义：**换了代码又没重新确认 ⇒ 旧标的上继承来的锁定撤掉**，
否则"改个码"就自动免疫新代码。撤掉之后仍能在页面点"老板确认"买回来
（所以那个按钮现在对"已审查但未锁定"的行也要出现，不然免疫再也买不回来）。
列表区分三态：待审查 / 已审查 / 老板已确认。

### 35.2 MAJOR-5：验证退到别的代码，却从不回写行上的代码

`match_fund_for_prediction` 第 17 轮补的"自带代码也要过体检"只做到"换个代码去取数"：
`verify_prediction` 拿 B 判完结论，`prediction.fund_code` 仍然是 A，一条变更日志都不写。
两个后果：① `verdict_under_other_fund` 那一族**继续新增**（S9 刚花力气清掉 53 条）；
② 因为回写从不发生，⚠ 徽章在**新**数据上永远测不到 —— 闸门看着是装的。
现在这条路径先走唯一入口 `FundSyncManager.retag_prediction(source='verify_unservable_code')`：
留痕（自动生成 run_id，可整批还原）、清掉旧标的判出来的结论、再按新标的重判。
改标在匹配之后立刻 commit，不取决于本轮验证能不能判完。

### 35.3 MAJOR-3 / MAJOR-6：两个我报出去过的数，一个没出处、一个是错的

- "**准确率 ≈49%~54%**"是我手算的，口径都没写清。现在区间由
  `scripts/audit_verdict_evidence.py` 自己打印：**已判 1110、判对 573 = 51.62%**，
  197 条证据失效按"全判错/全判对"两个极端折算 = **43.96% ~ 61.71%**（宽 17.75 个百分点）。
  评审 B 独立算出同一组端点（43.96 / 61.71），两边对上才写进文档。
- "**补齐净值后自行消解**"是错的：每日同步只回写最近 30 天净值，这批端点多为上半年旧日期，
  且 ⚠ 是派生值、不会把结论重新排队。AGENTS.md 与模块总览已改成"目前只标出、不消解"。

### 35.4 验证

（第 19 轮订正：这两行当时就少算了 3 条新用例，实测 736 / 745 —— 第 19 轮 A-4 指出。
本节保留原样，当前数以 §三十六.6 为准。）

`pytest tests/unit -q` → **733 passed / 16 skipped**；`pytest tests/ -q` → **742 / 16**；
`audit_verdict_evidence` → 197/1110、区间行照打。
两条新语义都做过"改动前会红"：把 `owner_confirm` 判断改回无条件盖 owner ⇒ 8 条红
（含新增的 `test_edit_save_cannot_buy_owner_immunity`、
`test_changing_the_target_revokes_an_inherited_lock`）；把验证侧改标块短路 ⇒
`test_verify_retags_instead_of_judging_on_another_fund` 红在 `assert 'BAD002' == 'GOOD02'`。

## 三十六、第 19 轮：两份独立复评 72 / 81，重叠的第一条又是我"修 A 弄坏 B"

评审 A 72 分、评审 B 81 分，取低的为约束。两条重叠、四条各抓到一条只有它看见的。
共同点仍然一模一样：**我把承诺写进了文档，代码里只兑现了一部分；以及我自己写的用例
"永远不会红"**。

### 36.1 MAJOR（A-1 + B-1）：验证侧改标清了结论，却不减博主统计列

`retag_prediction` 的 `touched_bloggers` 我没传 ⇒ 谁都不重算，而 `blogger_stats` 是
按 `verify_count>0` 现算的；本轮接着判完时只给 `verified_delta=+1` ⇒ **同一行计两次**
（B 在副本库实测 total 87 vs 真值 86，A 探针 1→2、分数 80→180）。
修法：清过结论就立刻 `recalculate_blogger_stats`。
**我自己的用例错在**：第一版拿 `test_db.refresh(blogger)` 之后与
`recalculate_blogger_stats(commit=False)` 的返回比——后者会把值直接写进**同一个会话对象**，
于是这个比较永远为真。两份复评都点了这一条。现在用例读**表里的列**
（`sa.select(Blogger.…)`），并且补了一条真跑到"+1 那一腿"的用例（原来的早退版
恰好绕开出问题的那段）。撤掉修复后两条都红：`1 == 0`、`统计列 2 vs 真值 1`。

### 36.2 MAJOR（A-2）：全库导入仍然能自盖免疫，把我刚写的规则证伪

`/api/config/import` 合并模式**没有总开关也没有确认头**，`_clean_row` 照搬
`reviewed_by / owner_locked` ⇒ 一份清单就能给整表买到体检豁免。
修法和审计回写侧同口径：导入剔掉这两列 + 回执报条数（`immunity_rows_stripped`）。
用例 `test_import_refuses_to_install_owner_immunity_from_a_manifest`。
代价说清楚：整库导出→还原不再带走老板锁定，要恢复请在页面上逐行确认。

### 36.3 MAJOR（B-3）：零网络守卫的信号被自家 `except Exception` 吞掉

`_block_real_http` 抛的是 `AssertionError`，而 `_manual_identity_verdict` 那句
"站点抖动一律按没结论处理" 把它吞成 `unknown` ⇒ **桩彻底失灵时用例照样全绿**，
"漏网取数会变成看得见的失败"这句承诺落空。改成抛 `BlockedRealHttp(BaseException)`。
当场照出 **14 条**真打外网的用例（`test_prediction_verify_date_boundary.py` 整个文件
的补拉腿 + LLM 腿都没钉桩、`test_sector_mapping_api.py` 3 条真打名册/单码接口）——
全部补上显式桩，并加一条 `test_the_signal_survives_a_broad_except` 钉住"不许被
`except Exception` 捕获"这个性质本身。

### 36.4 MAJOR（B-2）：那道"自带代码也过体检"的门有 38.7% 的盲区

判据是"映射表里带这个代码的行全被判不可服务"，所以**映射表根本没提过的代码一律放行**
（镜像 1110 条已判结论里 **430 条**属于这类，评审 B 独立算出的同一个数）。
这是刻意的保守设计，但我以前把它说成"自带代码不是免检通道"，过头了。
**没有按评审建议改成"再核一遍 `fund_info`/名册存在性"**：那样会把这 430 条直接喂进
§三十五新加的改标路径 = 一次跑批批量清结论，正是第 18 轮那个 BLOCKER 的形状。
改法是把盲区量化：审计脚本每次打印 `[体检覆盖面] … 430 条 …`，AGENTS.md 写清门只管映射表提到过的代码。

### 36.5 MAJOR（A-3）+ MINOR：同类问题只修了三条中的一条；导出计数含归档行

- `rollback_invalid_verifications` 也按解析出的另一只基金判"这段净值缺不缺"，
  然后拿这个无关理由撤掉 A 的结论。现在这类行**只数不撤**（`data['code_diverged']`）。
- 第三个调用点 `get_verification_status` 没有任何路由/前端引用（只有一条用例喂着它），
  而它把"可验证"按另一只基金回答 ⇒ 直接删掉，不留半个真值来源。
- 导出头的 `judged/stale` 含归档行（1192/229 vs 审计 1110/197）⇒ 与审计同口径（只算未归档），
  用例补一条"归档行不进计数、不打徽章"。
- `accuracy_span` 另起一套 `is_correct!=None` 口径 ⇒ 改回复用 `has_verdict`。
- 撤锁语义在四个写入点各抄一份 ⇒ 收进 `SectorFundService._drop_owner_immunity`。
- 三条脚本/路由注释仍称"保存即盖 owner 锁定"（第 18 轮起不再成立）⇒ 就地订正。
- UI：博主榜在 `hit_rate` 缺失时回落 `accuracy_rate` 却仍写"准确率" ⇒ 加口径说明；
  确认弹窗/列表 tooltip 补上"锁定还同时挡住 AI 覆盖"；准确率表头 title 说明 ⚠ 与区间。
  **区间没有硬编码进页面**（会腐烂），只给现算命令。

### 36.6 验证

`pytest tests/unit -q` → **741 passed / 16 skipped**；`pytest tests/ -q` → **750 / 16**（AGENTS.md 已同步）；
`audit_verdict_evidence` → 197/1110、区间 43.96%~61.71%、新增覆盖面行；
`replay --offline --limit 200` 见下方提交说明。


## 三十七、第 20 轮：84 / 74，B 抓到"我上一轮的改标门根本走不到"

复评对象 = `81f4c11`+`206cd51`。A 84、B 74（取低的）。两条 MAJOR 合起来是同一件事：
**我上一轮加的"退码先改标"在生产里是够不着的代码**。

### 37.1 MAJOR（B-1）：第 2 步"按名字查 FundInfo"不过体检 ⇒ 解析回同一只不可服务基金

`match_fund_for_prediction` 的第 1 步（自带代码）现在过体检了，第 3/4 步也过，
唯独第 2 步 `FundInfo.fund_name == prediction.fund_name` 直接返回。而镜像里
**1392/1616** 条活预测的名字能在 `fund_info` 命中**自带的那个代码** ⇒ 86% 的行
在这里原样返回不可服务的 A：不改标、不清结论、不打 ⚠，日志却已经写了"改按板块重新解析标的"。
修法：第 2 步与第 5 步（`sector_type` 自由文本匹配）同样过 `fund_code_is_servable`。
用例 `test_name_lookup_cannot_resolve_back_to_an_unservable_code`（改动前红在
`assert 'BAD006' == 'GOOD06'`）。

### 37.2 MAJOR（A-2）：`code_diverged` 的下游根本不存在

上一版让回溯审计"只数不撤"，注释说"交给 `verify_prediction` 正规改标"。但本函数只扫
`status in (success,failed) AND verify_count>0` 的已判行，而到期队列只要 `is_correct IS NULL`
⇒ 被跳过的行**永远回不到**改标那条腿。语义只是从"错误撤销"变成"没人管"。
修法：两个入口共用一个 `retag_if_drifted()`（dry-run 报"会改标"，真跑就改标 + 清旧结论 +
重算博主统计），回执与 message 都带上条数。用例
`test_rollback_retags_drifted_rows_instead_of_abandoning_them`（真跑分支：改标后
`fund_code=GOOD07`、结论清空、`blogger.total_predictions=0`）。

### 37.3 MAJOR（A-1）：`seed_owner_proxies.py` 是第五条豁免来源

它直写 `reviewed_by='owner' + owner_locked=True`，没有 `owner_confirm`、没有回执，
而豁免清单是代码字面量 `sector_fund_agent.DELIBERATE_PROXIES` ⇒ AGENTS.md 那句
"只能由显式确认换来"仍然为假。修法：真写必须 `--owner-confirm SEED-PROXY`（已实测：
不带就跑出 abort 退码 4）。

### 37.4 MAJOR（B-2）：日任务不跑身份体检 ⇒ 门中今天零命中，而页面看不出来

镜像 145 行映射里 `is_unservable` 为 0，日任务四步里也没有 sweep ⇒ "先改标再判 + 立刻重算统计"
这整套今天在生产日常**一条都不会触发**，页面数字看着正常。我没有擅自把 sweep 挂进 cron
（那是给日任务加一条新的写路径，属老板决定项），改法是把**过期**报出来：日任务现在会记录
"体检结论已 N 天没刷新 / 另有 M 行从未体检"（阈值 `SWEEP_STALE_DAYS`，默认 14 天）。

### 37.5 MINOR 一并处理

- 剔豁免的计数原先落在"已存在→skip"的行上（合并模式回执虚高）⇒ 改成只数真正写入的行。
- 注释点了个不存在的 `_clean_audit_row`（真名 `_audit_apply_row`），并谎称"两边同口径 +
  都报条数"（审计侧是静默剔）⇒ 就地订正。
- 审计脚本第三把尺子：`unmapped_codes` 改回 `has_verdict`，`audit()` 里那个恒真的
  `sum(1 for p in rows if has_verdict(p))` 换成 `len(rows)`（SQL 已经按这三个条件筛过）。
- "只能逐行确认"的文案不准（批量审查带 `owner_confirm=true` 一次给 N 行）⇒ 改成
  "逐行确认，或批量审查时明确勾选确认"。
- `blogger_stats` docstring 说"预测被删除也保留"对**软删**不成立（现算按 `is_deleted=False`
  过滤、`archived_*` 只在物理清理时累加，镜像 135 行两头都不算）⇒ 订正说明。
- `PredictionMaintenanceService._reset_verification` 已退化成纯转发且无生产调用点 ⇒
  连同它的"两条路径等价"用例一起删除，`clear_verification_fields` 的 docstring 不再虚指第三个调用方。

### 37.6 下一批（已记工单，本轮没做）

页面拿不到"多少条结论不可信"的汇总（`stale_counts` 只进日志，区间只活在 hover 的 title 里）；
板块映射统计卡/筛选仍是两态，而徽章已三态（镜像"已审查 122"里老板已确认只有 4 行）；
等级按加权分、准确率显示命中率，4/27 博主差 >10pp 且没有说明。

### 37.7 生产库结构（老板已授权，本轮执行）

`scripts/sync_db_columns.py`（新，按 ORM 元数据补列/补索引，默认 dry-run、真执行要
`--apply --confirm SYNC-COLUMNS`、非 PostgreSQL 直接拒）已在镜像**副本**上演练：删掉 9 个列
→ 全部补回、145 行映射与 2178 行预测一行未动、复查 dry-run 退码 0。
生产执行结果：补 12 列 + 5 索引，`alembic_version` 记为 head；复核映射 118 行 /
未删预测 1616 行 / fund_info 162 行与执行前一致。
**为什么不用 `run_migrations.py`**：生产不是 alembic 建的，从 base 跑会重复建已存在的表。

### 37.8 验证

`pytest tests/unit -q` → **743 / 16**；`pytest tests/ -q` → **752 / 16**；
`seed_owner_proxies.py` 不带确认参数当场拒跑（实测退码 4）；三处新语义逐条"改动前会红"
（第 2 步不过体检 ⇒ `BAD006 == GOOD06` 红；回溯不接手 ⇒ `BAD007 == GOOD07` 红；
上一批的统计/导入/回溯三处复测仍红）。


## 三十八、第 21 轮：72 / 74，BLOCKER 又是"我写承诺时没看事务边界"

复评对象 = `b2976f0`。A 72、B 74（取低的）。两份**各自独立**指到同一处：`retag_if_drifted()`
在共享函数里无条件 `self.db.commit()`。

### 38.1 BLOCKER（A-1 + B-1）：批处理函数被我加了"中途提交"，于是错误分支的话是假的

`rollback_invalid_verifications` 的语义是"整批要么成、失败就回滚"（错误分支返回
`success=False /"未保存任何修改"`，末尾统一 `recalculate + commit`）。我在循环里调的那个
helper 自己 commit ⇒ 后面某行抛错时，前面几行的改标与清结论**已经落库**，而回执说不保存。
B 用第二连接复现（A 也独立复现同一条）。
修法：提交权交回调用方 —— helper 加 `commit=` 参数；回溯入口传 `commit=False` 并把博主登记进
`affected_bloggers`，由函数末尾统一重算 + 提交；`verify_prediction`（单条）保留自己提交。
用例 `test_rollback_error_branch_leaves_no_half_committed_retag`：注入异常后**从表里读**
（不许读会话对象，那正是第 19 轮那个假自检的形态），断言两行仍是原标的、结论还在。
撤掉修复 ⇒ 红在 `assert 'GOOD11' == 'BAD011'`。

### 38.2 MAJOR（A-2）：message 广告的 `--run-id` 撤不回本次清掉的结论

改标不传 `run_id` ⇒ `retag_prediction` 自动生成 `retag-verify_unservable_-<秒>`，
与回执里那句 `restore_prediction_batch.py --run-id rollback-…` 两个 id 互不相交。
修法：透传本批 `run_id`，并把两个入口的 `source` 分开（`verify_unservable_code` /
`rollback_drifted_code`）——否则将来想只撤回溯那一批都分不出来。
用例 `test_rollback_retag_is_recoverable_with_the_advertised_run_id`（撤修复 ⇒ 红在
`{'retag-verif…'} == {'rollback-…'}`）。
同一条用例顺手钉住第 21 轮另一处：解析出的新代码不许把**旧基金的名字**带到行上（以前是
`fund_name or prediction.fund_name`，现在缺名字就去 `fund_info` 查、查不到留空）。

### 38.3 MAJOR（B-2，结构性）：这条腿今天根本不可达，我把"门修好了"说过头了

镜像 145 行映射 = 145 个板块（**每板块只登记一行**），生产还多一个模型没声明的
`UNIQUE(sector_name)`。于是"自带代码被否掉之后解析出**另一只**可服务代码"这个前提
在结构上就很少成立：真实结果通常是"解析不出 ⇒ `verify_prediction` 直接拒绝验证"。
拒绝也是安全的（不再按一只股票判涨跌），但**报数必须说清**：今天 0 行被判不可服务 ⇒
改标与拒绝两条腿都是 0 命中。AGENTS.md 已补这条前提，别再写成"改标已生效"。

### 38.4 MAJOR（A-3 + B-4）：`sync_db_columns` 的"ORM 是唯一真值"被证伪，而我自己把模型改坏过一次

`ix_sector_fund_mapping_owner_locked` 是 0008 迁移建的、模型里没声明 ⇒ 脚本看不见它，
于是生产缺这个索引却被 stamp 成"迁移都跑过"。修法两步：
① 把索引**声明回模型**（`SectorFundMapping.__table_args__`），真值归一处；
② 脚本改成**双向核对**：`extra_objects()` 报"库里有、模型没声明"的对象（过滤掉主键/唯一约束
   自带的索引，否则闸门永远拒绝 —— 一个从不放行的闸门和没有闸门一样），`--stamp-head`
   有 extras 就拒绝写版本号，且需要单独的 `--stamp-confirm STAMP-HEAD`。
**我自己在这一步栽了一次**：加 `__table_args__` 时把 `class SectorAlias(Base):` 整行吞掉，
别名表并进了映射表，**Python 语法照样通过**；是 dry-run 打印的"模型 27 张表 → 26 张"暴露的。
教训：改 `src/models/database.py` 之后必须看一次表数量/列集合，别只跑 `py_compile`。
生产现状（只读复核）：补 1 索引后 `[ok] 列与索引都已存在`，模型 27 张表、库里不缺表、无 extras。

### 38.5 MAJOR（B-3）+ 一批 MINOR

- 日任务那条新告警写成 `if stamps:` ⇒ 生产 118 行 `verified_at` 全为 NULL 时 age=None、
  **一行都不报**，而这恰恰是最该报的"从来没体检过"。改成 `age 为 None / 超阈值 / 有从未体检的行`
  三种都告警，并注明 sweep 自己的 `NAV_STALE_DAYS` 与这里的阈值无关（以前那句话说成共享阈值）。
- 合并导入的"剔掉 N 行豁免"计数：逐行标记没复位 ⇒ 第一行剔过之后**每行**都算一次
  （评审探针：3 行映射只有 1 行带豁免，回执报 5）。改：`_clean_row` 每行先复位，
  并且只数真正写入的行。用例 `test_immunity_strip_count_only_counts_rows_actually_written`。
- 静态表那一步的拒绝集 `denied_code_map` 只筛 `is_fetchable=False`，不认 `evidence` 里的
  verdict、也不认 owner 例外 ⇒ 同一件事的第四把尺子（镜像今天背离 0 行，属潜伏口）。
  改走 `row_unservable`，用例 `test_denied_map_uses_the_single_unservable_judge`。
- 前端"无效验证回溯"按钮：预览里现在混着"撤结论"和"改标 + 清结论"两类动作，
  确认框却只说"修改资料" ⇒ 按 action 分类计数并写进弹窗（第 21 轮 MAJOR-5）。
- 两处过期文案：`# 本函数不撤它的结论，只数出来`（下一行就开始撤了）；
  `clear_verification_fields` 的 docstring 漏了真正的主写方 `FundSyncManager.retag_prediction`。

### 38.6 我没能独立复现、但采信了的一条

A 说 `BlockedRealHttp` 可能被 src/scripts 里 20 处裸 `except:` 吞掉，逐条读过"都不包网络调用"
⇒ 今天不破功，但这是残留脆弱点（记在这里，不算已修）。

### 38.7 验证

`pytest tests/unit -q` → **747 / 16**；`pytest tests/ -q` → **756 / 16**；
`sync_db_columns` 对生产 dry-run ⇒ `[ok] 列与索引都已存在`（模型 27 表、库里不缺表、无 extras）；
四处新语义逐条"改动前会红"实测（中途提交、run_id 不相交、拒绝集少一把尺、计数不复位）。


## 三十九、第 22 轮：74 / 78，我写进文档的一句"事实"是别人转述的、没自己验

A 74、B 78。最刺的一条不是代码：**我把"生产有 `sector_fund_mapping_sector_name_key
UNIQUE(sector_name)`"写进了 AGENTS.md 与模块总览，出处是第 21 轮评审 B 的报告，我自己没查**。
这轮评审 A 反过来断言"生产没有这条约束"，两份互相矛盾 ⇒ 我直连 `pg_constraint` + `pg_indexes`
读了一次：**约束在**（`('sector_fund_mapping_sector_name_key','u')`）。所以文档那句是对的、
A 的 MAJOR-1 是误报 —— 但**错在我这边**：一句没自己复现的事实我照样写进了"以后 agent 必读"的文件。
顺带查清两件真事：① **镜像没有**这条约束（只有 pkey + owner_locked 索引）⇒ 同类写入"镜像静默成功、
生产报唯一约束"，S6 回写清单必须在生产口径上再核一次；② 我上一版的反向核对按 `*_key`/`unique`
一刀切，正好把这条约束藏掉 ⇒ 改成**按列集合**比（`unique=True` 的列、UniqueConstraint、
Index(unique) 都算已声明），镜像/新建空库 extras 为 0、生产如实报出 2 条。

### 39.1 MAJOR（A-3）：守卫看不见 `.env`，评审的探针因此写进了镜像

`pin_local_sqlite()` 只看**进程环境**的 `DATABASE_URL`，而 `.env` 要等 `src.core.config`
被 import 才灌进来 ⇒ 设了 `LOCAL_DB_URL` 也走到"默认镜像"分支，"只操作本地镜像库"的脚本
真的去写了 `data/fund_insight.db`。修法：守卫自己读 `.env`；`LOCAL_DB_URL` 无条件优先并
照样校验；本地运维脚本改为**显式** `pin_local_sqlite(use_mirror_default=True)`（17 个脚本，
可 grep 可审），一次性探针不设就 abort。
两条新用例：不设参数 ⇒ 退码 4；设了 `LOCAL_DB_URL` ⇒ 赢过 `.env` 的远程串。
镜像事后自核：判对 573 / 判错 537 / 已判 1110 / 映射 145 / 未删预测 1616、`integrity_check ok`、
变更日志最大 id 5259、博主列与现算真值 0/27 不一致 ⇒ 评审说的"已还原"成立。

### 39.2 MAJOR（A-2）：SQL 粗筛放行"列 NULL + verdict 否定"的行，而它正是改标的去处

`servable_predicate()` 只看 `is_fetchable` 列；上一轮我只把**拒绝集**改成走
`row_unservable()`，第 3 步取"可服务映射行"时仍用 SQL 粗筛 ⇒ 被 verdict 否掉的幽灵行
会被当成替代标的交出去。修法：粗筛出候选后**再过一次唯一判据**，取第一只真可服务的。
用例 `test_retag_destination_is_not_an_unservable_row`。

### 39.3 MAJOR（B-2，我自己造成的性能回退）+ 其余 MINOR

- 上一轮把拒绝集从"2 列"换成"整行 ORM"，热读路径上 0.85ms→9.19ms（生产网络 315ms→1151ms），
  而且传 `db` 时**根本不过 TTL**。修法：`denied_code_map(sectors=…)` 按板块收窄，
  `rejected_codes()` 带上 `[原板块, 归一板块]` 两个键去查。
- B-1/B-3：这套门在生产今天是**哑的**（118 行 `verified_at`/`evidence` 全 NULL、0 行判不可服务，
  而 sweep 不在 cron 也不在 scheduler 里）⇒ 生产从未跑过身份体检。我没有擅自把 sweep 塞进日任务
  （那是给生产加一条新的自动写路径），把"要不要挂上去"列为老板决定项；同时本轮实测确认：
  新加的过期告警在生产每天必响且**无收敛机制**，所以它的日志值现在只算"提醒"，不算监控。
- B-4/A-7：老板侧仍然拿不到"多少条结论不可信"和区间（只有 hover 的 title 与导出字段）；
  映射统计卡两态（镜像"已审查 122"里老板已确认只有 4 行）。这两条连续三轮被点，已单列工单。
- MINOR：helper 三种返回口径统一成"动不动了"；日任务在 0 行活跃映射时不再报"0/0 无记录"；
  前端回溯确认框改成**先截断再计数**、并去掉原生 confirm 里的 markdown 星号。

### 39.4 验证

`pytest tests/unit -q` → **751 / 16**；`pytest tests/ -q` → **760 / 16**；
`replay --offline --limit 200` 比对 164 条差异 0、源库 sha256 未变；
`sync_db_columns` 只读复核：镜像与新空库 extras = 0，生产如实报出那条未声明的唯一约束；
守卫两条新用例先红后绿。本轮没有对生产做任何写入（只 SELECT/pg_constraint）。


## 四十、第 23 轮：77 / 79，我报给老板的准确率一直是**错库**的数

A 77、B 79，两份都没有 BLOCKER。最要紧的一条不是代码：

### 40.1 MAJOR（B-1）：我把本地镜像的数当成"系统的数"报了十几轮
同一把尺子（`has_verdict` + 区间折算）实测两边：
- 镜像：已判 1110 / 判对 573 = **51.62%**，⚠ 197（17.7%），区间 43.96%~61.71%
- **生产**：已判 1057 / 判对 591 = **55.91%**，⚠ **419（39.6%）**，区间 **33.68%~73.32%**
根因是生产落后（`fund_history` 9541 行 / 末次验证 09-13；镜像 10600 / 09-22；映射 118 vs 145）。
⇒ AGENTS.md 现在要求"报数带库名"，并写下这两个实测；`--init-db` 那条文档命令补上了
`ALLOW_REMOTE_INIT_DB=1` 这个从没人写的前提。

### 40.2 MAJOR（A-1/A-2）：同一把尺子第三次漏修
上一轮我只给"改标去处"过了 `row_unservable`，`get_fund_by_sector` 的 DB 兜底与
`llm_analyzer._save_fund_mapping` 仍只信 SQL 粗筛（`is_fetchable IS NULL` 一律放行）。
危害更大的前者：它把"列 NULL + verdict 否定"的幽灵行**写进进程内缓存**，此后整个进程的
帖子分析都吃这只"不是基金"的标的；后者会当成"该板块已有映射"而拒绝写入正确标的。
两处都改成"粗筛取候选 → 再过唯一判据"，用例 `test_ghost_mapping_row_cannot_serve_or_poison_the_cache`
（撤修复 ⇒ 红在返回 `{'code': 'GHOST9'…}`）。

### 40.3 MAJOR（A-3 + B-1）：我上一轮的守卫改造只做了一半
17 个脚本改了，**7 个用别名 `_pin_local_db()` 的没改** ⇒ 在 `.env` 指向生产时一律退码 4 罢工，
而我自己在 AGENTS.md 写的正是"必须显式"。补齐这 7 个，并修掉守卫在"只有 `.env` 里是
SQLite"时的 `KeyError`。

### 40.4 MAJOR（B-2）+ 陷阱（B-1）：生产这套门是哑的，而我写的 DDL 工具不认本地逃生口
- 生产 `sector_fund_mapping` 118 行 `verified_at`/`evidence` **全 NULL**、0 行判不可服务，
  而 sweep 不在 cron 也不在 scheduler ⇒ 改标/豁免/⚠ 三条腿在生产全部不触发；
  我加的"体检结论已 N 天没刷新"告警在生产**每天必响且不收敛**（只能靠跑 sweep 消警）。
  这不再是"等老板点头的口径问题"，而是**上线清单里的一项**：生产身份体检必须跑一次。
- 更糟的是我上一轮写的 `sync_db_columns.py` **完全不过守卫**：操作者按文档设好
  `LOCAL_DB_URL=data/fund_insight.db`，它照旧连生产，而它带 `--apply` 是发 DDL 的。
  修：设了 `LOCAL_DB_URL` 却连着非 SQLite ⇒ 直接拒；目标是 PostgreSQL ⇒ 连 dry-run 都要
  `--against-production`（实测已被拒）。
- 结构事实复核（生产只读 `pg_constraint`）：`sector_fund_mapping_sector_name_key UNIQUE(sector_name)`
  在、镜像没有；而级联清理只把行置 `is_active=False`，**唯一索引不看 is_active**
  ⇒ 生产上"给同一板块补第二只可服务基金"这条路被结构性堵死 ⇒ **改标腿在生产不可达**，
  只剩"拒绝验证"腿。这条已写进 AGENTS，作为 S6 回写与上线的既定约束。

### 40.5 MINOR
核对测试的名字与内容不符（测的是 `extra_objects` 却叫 `sql_predicate`）；`dict(extras)` 会把
同类多条折叠成一条（改 list 断言）；已对齐时 `--stamp-head` 静默 no-op；"库里有、模型没有的
**整表**"从不进 extras（双向核对名不副实）；同一物理约束被 index+unique 各计一次；
我那条 `assert servable_predicate() is not None` 是恒真前提（改成断言幽灵行真被粗筛放行）。
老板可见性（失效条数/区间无 API 出口、映射统计卡两态、窄屏无 hover 拿不到理由）
**连续第四轮被点**，已单列工单，下一批做。

### 40.6 验证
`pytest tests/unit -q` → **752 / 16**；`pytest tests/ -q` → **761 / 16**（提交前重跑核对，762 是我写文档时先填的错数）；
`sync_db_columns` 对生产 dry-run 现在被拒（`--against-production` 才放行）；
镜像 extras=0、新空库 extras=0、构造的未声明唯一索引会被报出。本轮对生产零写入。


## 四十一、第 24 批：老板终于能在页面上看到"我的准确率有几分可信"

第 20/21/22/23 轮四份评审都在同一条上扣分：`stale_counts` 与区间只活在脚本 stdout、
日志和 `title` 里，页面正文永远是一个精确到小数点的准确率。这批把它补上，顺带把
"报数带库名"落成代码而不是纪律。

- **唯一出处搬到服务层**：`verdict_evidence.span_report()` 现算 `judged / correct /
  accuracy_pct / stale_evidence / stale_pct / span_low_pct / span_high_pct / by_kind /
  as_of / database`。审计脚本改成消费它（以前脚本自己算一份 = 迟早与页面上的那份分叉）。
- **新接口 `GET /api/stats/evidence`**（60 秒进程内缓存）。为什么不塞进 `/api/stats/overall`：
  算一次要把全部已判结论读出来再查净值（镜像 1110 条），不该让首页每次刷新都付这个钱。
- **页面上是一行正文，不是 hover**：博主榜上方 `已判 1110 条，其中 ⚠ 197 条（17.75%）
  端点净值今天已复现不出来 ⇒ 准确率只能按区间看：43.96%~61.71% · 本地镜像库 · 截至 2026-09-22`。
  手机没有 hover，所以 `title` 不算出口（这条被点了四轮）。
- **映射列表补第三态计数**：`owner_confirmed_count` / `reviewed_unconfirmed_count` /
  `custom_count`，只数自定义行（内置行没有"老板确认"这个动作，算进去就是 121 条噪音）。
  实测镜像：231 行里老板真正确认过 **4** 条，"看过但未确认" **118** 条。
  我没有顺手加"看过未确认"的筛选按钮 —— 后端还没有对应过滤逻辑，加上去就是一个不会动的假开关。

### 41.1 实测（不是推理）
起了本地服务（`DATABASE_URL` 显式指向镜像、口令用一次性假值），`GET /api/stats/evidence`
返回上面那组数；浏览器快照里这行字作为正文出现；映射接口返回第三态三个字段。
`pytest tests/unit -q` → **756 / 16**、`pytest tests/ -q` → **765 / 16**（上一基线 752/761，本批 +4）。

## 四十二、老板授权的 S6 第一步：重跑一轮 AI，以及它照出来的三个真问题

2026-09-22 19:41 先备份镜像（`backup/fund_insight_20260922T114139Z.sqlite3`），
19:42 走**页面同一条任务链路**（`get_sector_ai_match_manager().start(sectors, apply=True,
run_id='ai-rematch-20260922')`）重跑 23 个 `reviewed=0` 的活映射。为什么必须走它：限速 +
429 熔断退避重试轮 + "判定不可用"与"确实没有对口基金"的区分，自己写 for 循环调
`resolve_sector_fund` 就是把这些保护丢掉。

### 42.1 我自己的驱动脚本有一条手抄错误（同一类教训第 N 次）

轮询条件我写的是 `status in ('done','failed','cancelled')`，而 `sector_ai_match_task.py:185`
给的终态是 **`completed` / `completed_with_retries`**（`stale` 由 `is_stale()` 现算）。
⇒ 任务 19:49 就跑完了，脚本死等到 19:59 才被我杀掉，`ai-rematch-20260922.json` 没写出来、
进程内的 per-sector LLM 明细随进程丢了。这就是第 15 轮"手抄的还原字段清单漏一个字段"
的同一形态：**跨模块的状态词表必须从模块里读，不能抄字面量**。
已按改前备份 + 当前镜像逐列把对照补回 `docs/迭代计划/run-2026-09-20/ai-rematch-20260922.json`
（含 `why_no_llm_detail` 说明哪些明细是还原的、哪些是丢的）。

### 42.2 重跑结果：22 行仍待审查，原因逐行核到四道门

先记一条我自己探针的错：第一版把 `identity_before_agent_switch` 也算成"等老板确认的机器章"，
而 `sector_identity_audit.MACHINE_SWAP_KEYS` 只认 `identity_realign` / `etf_upgrade`
（且要求记录里的 code 仍是当前 code）⇒ 多算了 13 行，那版结论作废、重算一版。

按真判据（`auto_reviewable`：status ∈ matched/proxy、`t3_suitable is True`、evidence 同时有
T2+T3、置信 ≥ 门 direct 0.80 / proxy 0.68、proxy 还要 direct 穷尽）：

| 卡在哪 | 行数 | 板块 |
| --- | --- | --- |
| 置信没过 direct 0.80 门 | 15 | AI 0.7825、中报业绩 0.7975、中科三环 0.7178、低估值 0.7825、低空经济 0.7178、元宇宙 0.7825、全球 0.7275、北美 0.7975、区块链 0.7178、天娱数科 0.7275、应用 0.7178、拓维信息 0.77、数字货币 0.7425、核聚变 0.693、紫紫 0.77 |
| proxy 缺"direct 已穷尽"标记 | 3 | 华为概念 0.6793（差 0.0007）、哈三联 0.7054、德明利 0.6207（兼置信不够）、海力士 0.6207（同上） |
| 带 `etf_upgrade` 章、按设计只能老板确认 | 2 | 传媒 159805（0.9325）、医疗 159877（0.905） |
| 本轮 agent 没跑出结论（evidence 里没有 T2/T3/FETCH） | 1 | 液冷 159775（维持 09-21 的 0.5878） |
| **自动达标并已置 reviewed** | 1 | 制冷剂 159870（置信 0.588→0.717） |

`scripts/sweep_sector_mappings.py --tag post-ai-rematch-0922` 重跑体检：**145 行里 ok 144、
not_a_fund 0、code_is_other_fund 0、wrong_code 0、降级候选 0**，相关性旗标只响 1 条（硬件）。
**未写库**（默认 dry-run）。

### 42.3 体检没抓到的那 9 行：claim_mismatch 只在文案里披露、不参与决策

镜像里 `verify_message` 带「（LLM 称「X」，官方名实为「Y」）」的活映射 **9 行**，其中 **8 行
正是本轮换过标的的**。这些行的官方名与板块八竿子打不着：

- 核聚变 → 159525，官方名 **红利低波ETF富国**（agent 声称"跟踪中证核能核电指数"）
- 应用 → 516160，官方名 **新能源ETF南方**（声称"跟踪中证人工智能应用主题指数"）
- 区块链 → 159890，官方名 **云计算ETF招商**（声称"跟踪中证区块链50指数"）
- 紫紫 → 159996，官方名 **家电ETF国泰**（理由原文"契合紫色家电板块"——凭"紫"字联想出来的）

两个判据各自都失效：`sector_relevance()` 的规则是"共享一个汉字就算相关"+ 名册兜底，
而"核聚变"与"红利低波ETF"确实零共字 ⇒ 它响的是"名册里查不到含'核聚变'的基金"这半边，
结论是"放过"；`claim_mismatch` 明明已经抓到 LLM 在编名字，却只写进 `verify_message`
外加置信度 ×0.90（`compute_confidence:243`）。

**我第一版写的修法是错的，已经推翻自己**：当时写的是"claim_mismatch ⇒ 不换标的"。
把 T3 的证据翻出来看就知道这一条站不住 —— `sector_fund_agent.py:436` 的 T3 提示词
喂的**已经是官方名**（`c.official_name or c.name`），所以不是"没让 LLM 看真名"：

| 板块 | T3 对各候选的打分（evidence 里的原值） |
| --- | --- |
| 核聚变 | 159525 红利低波ETF富国 **suitable=true score=90**；562350 电力ETF银华 suitable=false score=30；159611 false |
| 区块链 | 159898 医疗器械ETF false score=0；159645 false score=0；**159890 云计算ETF suitable=true** |
| 中科三环 | 562800 **true 90**、516780 稀土ETF华泰柏瑞 **true**（同码 benign：LLM 说"稀土ETF"、官方名只是更长） |
| 紫紫 | 159998 false score=0；**159996 家电ETF true 90** |

⇒ 同一批里既有"LLM 把红利低波ETF判成核聚变 90 分"的真错，也有"稀土ETF / 稀土ETF华泰柏瑞"
这种同一只基金的良性不符。**按 claim_mismatch 一刀切拒绝换码，会把 中科三环、区块链 这类
正确的换法一起挡掉**（区块链改前是"医疗器械ETF挂区块链板块"）。真正的洞在 T3 的判据本身：
它可以在给出 `suitable=true / score=90` 的同时说一句官方名里根本不存在的话。
这条改成"要用金标集标定才能动"的 S8 项（任务 #22 已按实测重写），不走一句话闸门 ——
`scripts/calibrate_sector_agent.py` 就是为这种事留的。

**这一条不改变"本轮比改前更好还是更坏"的事实判断**，逐行查了改前那只的官方名之后结论是**混合的**
（我第一版写的是"改前挂的也不是对口基金"——那是没查名字就下的判断，已改）：

| 板块 | 改前 | 改后 | 判断 |
| --- | --- | --- | --- |
| 核聚变 | 562350 电力ETF银华 | 159525 红利低波ETF富国 | **明确倒退**：电力/核电才是核聚变的邻近标的，红利低波是策略宽基 |
| 区块链 | 159898 医疗器械ETF招商 | 159890 云计算ETF招商 | **进步**（虽然仍不是"区块链"本身，云计算至少同产业链） |
| 应用 | 021252 华安中证信息技术**应用**创新产业指数A | 516160 新能源ETF南方 | **倒退**：改前那只名字里真的含"应用"（尽管语义是信创） |
| 紫紫 | 159998 计算机ETF天弘 | 159996 家电ETF国泰 | **无法判断**："紫紫"是博主黑话，两只都可能对；而 159998 本轮被派去接"拓维信息" |

⇒ 单看这 4 行是 2 退 1 进 1 不明，而"换一个"这件事本身没有判据支撑——所以修的是判据
（任务 #22），不是拿人的直觉去逐行拨回来。

**21:01 又量了一层，把两条"看起来能修"的规则也证伪了**（探针逐条跑 `sector_relevance`）：
核聚变×红利低波ETF、核聚变×电力ETF、**中科三环×稀土ETF华泰柏瑞（这是正确换法）**、
区块链×云计算ETF 全部走同一条放过分支——"名册里查不到含该板块核心词的基金 ⇒ 不算存疑"；
而唯一被旗标的 硬件×芯片ETF 恰恰是因为名册里**有**含"硬件"的基金。
另外 `sector_core('应用')` 是空串，规则第一行就 `return True`。
⇒ **本地字符串统计分不开"错得离谱"和"跨一层产业链"**，能分开的只有语义判据。
可行的两个方向（都必须先跑金标集，不许拍脑袋改判分）：
① 类别互斥：策略/风格类标的（红利/低波/现金流…）只能服务风格板块（低估值、高股息），
不能服务主题板块（核聚变、稀土）——反例已量过：`低估值→512890 红利低波ETF` 是对的，
白名单得两边都给；T2 现有的 `is_broad_index_fund()` 只挡宽基，挡不住策略类。
② T3 让模型先复述"跟踪指数全称"，复述与官方名不符 ⇒ 该候选判 0 分
（现在 `claim_mismatch` 只是置信度 ×0.90 + 写进文案，不否决）。

### 42.4 顺带补掉的净值洞（只写镜像）

159890 / 159525 在镜像里 **0 行净值**、159530 只有 4 行，而 agent 自己的 FETCH 证据显示
源端各给了 20 条、最新到 2026-09-22 ⇒ 挂上来的预测会永远停在"等净值"。
已 `update_fund_history(days=400)`：三只各 +20 行（0→20、0→20、4→24）。
**记一条 S8**：`days=400` 实际只回 20 条（源端一页），agent 新建的档案因此只有 20 行历史 ⇒
3 个月期预测落在这些标的上就验不了，要么补翻页、要么在挑标的时把"历史长度 < 预测周期"筛掉。

### 42.5 定向回写的前置条件被实测改写了（不是顺序偏好，是物理上做不到）

抓生产的 `openapi.json`（走本机代理，HTTP 200，**76 条路径**）：
`/api/config/sector-mappings/-/audit-import` **不存在**、`/api/stats/evidence` **不存在**，
`GET` 该路径 HTTP 404。⇒ 线上 Render 还是旧构建，`push_sector_mappings_to_prod.py`
今天跑只会命中脚本里那条 `[abort] 生产尚无审计回写接口`。
所以 S6 的真实依赖是：**两份独立复评取低分 ≥90 → 推 GitHub → Render 部署 →
生产跑一次身份体检（118 行 verified_at/evidence 至今全 NULL，门在生产是哑的）→
部署后再导一次清单 → 定向回写 → 删 6 个垃圾码**。生产库结构那边已经对齐并 stamp，
卡的只有"代码还没上去"。

### 42.6 生产前像补全了（并且查出一个必须告诉老板的后果）

`scripts/snapshot_prod_mappings.py --via-db` 只读直连整表，实测：

| 库 | 表内行数 | 列数 | 老板锁定 | 待审查 |
| --- | --- | --- | --- | --- |
| 本地镜像 `sector_fund_mapping` | 145 | 19 | **4** | 22 |
| 线上生产（同表同结构） | 118 | 19 | **0** | 14 |

- 走接口的老前像**不完整**：`GET /api/config/sector-mappings` 不返回
  `owner_locked / reviewed_by / evidence`，那份文件不足以逐行回滚（第 9 轮 MAJOR-3 就点过）。
  现在接口路径会在回执里明说自己缺哪三列，完整前像走 `--via-db`；
  生产侧连上即 `SET default_transaction_read_only=on` 并 `SHOW` 读回核对。
  两条用例：`tests/unit/test_snapshot_prod_mappings.py`（拿到审计列 / mode=ro 真写被拒）。
- **要告诉老板的一件事**：`audit-import` 服务端会**主动剔掉**载荷里的
  `owner_locked=True` 与 `reviewed_by='owner'`（第 7 轮 BLOCKER 的修法：免疫只能由老板
  在页面上盖，不能从一份 JSON 买到来）。生产现在 `owner_locked=0`，所以镜像里那 4 行
  有意代理（例：债券→512000、SpaceX→159206）回写过去之后**在生产不免疫**，
  会被体检与 AI 覆盖照常生效 ⇒ **回写当天请在生产页面上把那几行重新点一次"老板已确认"**。
  这不是 bug，是那条规则的必然后果，写在这是为了别等到时候当成"系统又偷偷改了老板的设定"。

### 42.7 6 个垃圾码在生产的现状是量出来的（旧笔记那句"各 1 行"是转述，现在有据了）

`python scripts/purge_junk_funds.py --production`（只读预检，默认不写）：

| 码 | fund_name | 活预测 | 已判 | 回收站 | 净值 | 重试 | 映射行 | 挂在哪个板块 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 603758 | 秦安股份 | 0 | 0 | **1** | 0 | 0 | 1 | 秦安股份 |
| 600189 | 泉阳泉 | 0 | 0 | 0 | 0 | 0 | 1 | 矿泉水 |
| 152788 | 21罗美债 | 0 | 0 | 0 | 0 | 0 | 1 | 美债 |
| HYNX | 二倍做多海力士ETF-T-R | 0 | 0 | 0 | 0 | 0 | 1 | 海力士 |
| SBSP76 | 第一创业基础设施10号 | 0 | 0 | 0 | 0 | 0 | 1 | 场内第一持仓 |
| ign | 北美多媒体网络etf-ish | 0 | 0 | 0 | 0 | 0 | 1 | 北美 |

⇒ 预检整批拒绝（603758 回收站里那条软删预测）。**更要紧的一件事**：生产里这 6 个板块
**只有这一行映射**（海力士→HYNX、北美→ign、矿泉水→600189…），而镜像里同名板块已经指向
512760 / 513100 这类真 ETF。所以**"删垃圾码"必须排在"定向回写"之后**：回写按 `sector_name`
寻址、是原地把那一行改成正确标的，改完这些码的映射行自然归零，删除就不再拆掉板块的标的；
顺序倒过来就是"先把 6 个板块变成没标的，再指望回写去补"。这条以前只是排程习惯，现在有表为证。


## 四十四、页面像素级复核（在镜像的一次性副本上真开浏览器）

第 21~24 轮那条"数字不能只活在 hover"我这次不靠接口载荷自证，起了本地实例
（`DATABASE_URL` 指向 `data/fund_insight.db` 的临时副本，跑完即删），逐项看渲染：

- 博主榜正文一行：`已判 1110 条，其中 ⚠ 197 条（17.75%）端点净值今天已复现不出来
  ⇒ 准确率只能按区间看：43.96%~61.71% · 本地镜像库 · 截至 2026-09-22`。
- 预测列表"已验证 (1110)"页签：**首页 50 行里 37 行带 ⚠**（与接口 `evidence_status` 条数一致）。
- 板块匹配卡三态：`共 231 条 / 已审查 209 / 待审查 22 / 老板已确认 4
  / （看过未确认 119 条：体检与 AI 覆盖照常生效）/ 相关性存疑 1 / 机器已纠正待复核 2`。
  —— 这条顺带还掉了第 9 轮欠的"像素级验证"：那个分桶以前只核到接口载荷。
- 顺手记一个**已知不完美**：列表里 ⚠ 的"为什么失效"目前只在 `title` 与详情抽屉里，
  手机上看得到标记、看不到解释。区间那一行的数已经在正文里了，所以不影响这次的要求；
  要不要把解释也搬进正文是老板的口味决定，我没擅自改版式。
- 空态另说：我把服务指到一个解析失败的路径上（等于空库）时，接口回 `judged=0`，
  模板走"这个库还没有已判结论"分支——**这条只核到接口 + 模板分支，没做像素级**。

## 四十五、第 25 轮：78 / 76（取低 76）——"我以为加了保证，其实加了个恒真检查"

复评对象 = `145d0a9`。两份都确认"12 条声明里 9 条这次真的在里面"，剩下的仍然同一族。

| # | 级别 | 复现到的事实 | 处置 |
| --- | --- | --- | --- |
| 1 | 共同 MAJOR | `q.py` 生产那层"只读核对"是恒真的：`SET default_transaction_read_only` 与业务查询落在**同一条事务**（B 用事件监听打出 `BEGIN → SET → SHOW → select`，中间零 COMMIT），该 GUC 只管之后的事务，而 `SHOW` 读回的就是我刚设的会话值 ⇒ 永远不可能报"没生效"。另外 `pg_advisory_lock / set_config / pg_switch_wal / pg_reload_conf / pg_read_file` 五条穿过关键词表 | 改成两件事：建引擎带 `isolation_level='READ ONLY'`（方言在**建连接时**设会话特征），再加 `_pg_read_only_probe()` **真试一次写临时表**，只有报 read-only 才继续；关键词表补那五个函数。用例 `test_pg_read_only_probe_is_not_self_confirming` 把三种结局各钉一条（写成功必须报警）——**这条就是回答"它什么情况下会红"**。`snapshot_prod_mappings` 复用同一个探针，不抄第二份 |
| 2 | 共同 MAJOR | `--restore-from` 是全脚本唯一没有 dry-run、没有确认词的写路径，且会把备份里的 `owner_locked/reviewed_by='owner'` 原样盖回库（与 `audit-import`、`/api/config/import` 剔这两列的规则自相矛盾） | 还原默认 dry-run，真写要 `--apply --confirm RESTORE-JUNK`；带老板免疫的行默认**拒还**，要还必须显式 `--restore-owner-immunity`。两条新用例（含"开关必须是有效的"） |
| 3 | B MAJOR | `restore()` 用代理主键 id 判重 ⇒ ① id 被别的行复用就"静默跳过"，该还原的没还原还报告"本就在库里"；② 同码档案被重新抓回时撞 `fund_code` 唯一约束，末尾单次 commit ⇒ 整批回滚 | 判重改**业务键**（`fund_info.fund_code` / `(sector_name, fund_code)` / `(fund_code, retry_type)`）；id 被占时丢掉 id 让它自增并打印说明；逐行 SAVEPOINT，一行失败不拖整批。用例 `test_restore_dedupe_uses_business_key_not_the_recyclable_id` 复现 B 的场景 |
| 4 | B MEDIUM | 我上一轮把 `as_of` 换成 `current_as_of()`，但那个函数 `except Exception → date.today()` **静默回退**，而 `requirements.txt` 里没有 `tzdata` ⇒ 在它针对的容器环境里可能一直是回退，等于没修 | 回退时打 WARNING（写明"容器缺 tzdata 时页面日期可能差一天"）；`tzdata>=2024.1` 写进 requirements；用例打掉 `zoneinfo` 断言"回退了但留了痕迹"。**注意**：本机无法验证 Render 真机是否命中 tzdata，这条属于"让失效可见"，不是"已确认修好" |
| 5 | 共同 MINOR | 审计脚本与分诊脚本自己的 JSON 还在用 `date.today()` 当 `as_of`（第 24 轮刚说过统一口径） | 两处改走 `current_as_of()` |
| 6 | 共同 MINOR | 正文插值判据仍可被三种畸形骗过（JS 字符串里、含 `>` 的注释里、属性里含 `>`）；我第一版的"看前面最近的是 `>` 还是 `<`"不够 | 换成小词法扫描（进标签后按引号配对走），并加一条**测测试本身**的用例：三种畸形位置都必须不算正文、正常正文必须算 |
| 7 | A MINOR | `/stats/evidence` 缓存只按 URL 分键 ⇒ 两个内存库（`sqlite:///:memory:` 字符串相同）会串数；且 key 在计算**之前**写，异常时留下"键=B、报告=A" | 键加 `id(bind)`，算成功才改缓存 |
| 8 | A MINOR | AST 守护只认 `x.is_correct = ...`，`setattr` / 字典批量 `update/values` / `AnnAssign` 全漏 | 收集器扩到四种写法。**同时把话说准**：整库导入 `/api/config/import` 仍能带入 `is_correct`（受 `ENABLE_DATABASE_IMPORT` + 确认头限制），所以断言范围是"代码里的属性/批量写只有验证服务一处"，不是"全系统唯一路径" |
| 9 | 共同 MINOR | `purge` 的 `_dump_rows` 按 codes 抄（会抄走没进动作清单的行、还把永不删除的 `fund_history` 抄进备份）；`accuracy_span(db, stale_ids)` 参数已死；`routes/predictions.py` 的 `BaseModel` 成未用导入；`--db` 指别处仍印"本地镜像" | 全部清掉/改对；`--db` 回显真实路径 |
| 10 | A MINOR | `AGENTS.md` 里 `tests/ 802` 是我写完又加用例后没复测的数（A 复跑 805） | 每次改完测试**先跑再写**，本批基线见 §四十五末与 `评分记录.md` |

**没改、但要写清楚的一条**：`restore()` 在 PG 上按原 id 回插后不推序列，理论上后续普通插入可能撞主键。
本机没有 PG 服务（5432/5433 都关着），这条我**没法实测**，也没有靠"看起来合理"就加一段 `setval` 代码 ——
记在这里，等第一次在生产真跑还原时验证。同类的还有"Render 上 `current_as_of` 是否命中 tzdata"。

## 四十六、第 26 轮：66 / 56（取低 56）——我上一轮的"保证"里有一条根本连不上

复评对象 = `364e9a8` + `a335922` + `2a8b7be`。**分数从 76 掉到 56，掉得对**：
我上一轮为了修"恒真只读核对"写的替代方案本身是错的，而且被三条假连接用例稳稳放行。

| # | 级别 | 复现到的事实（我自己也跑了一遍） | 处置 |
| --- | --- | --- | --- |
| 1 | 共同 **BLOCKER** | `isolation_level='READ ONLY'` **不是 psycopg2 方言的合法值**。我实测 `create_engine('postgresql+psycopg2://…').dialect._isolation_lookup` = `AUTOCOMMIT / READ COMMITTED / READ UNCOMMITTED / REPEATABLE READ / SERIALIZABLE` ⇒ `--production` 与 `--via-db --production` 两条生产只读路一连接就 `ArgumentError`，探针根本没机会跑。更难看的是旧用例里那句 `assert "isolation_level='READ ONLY'" in src` —— **把坏代码钉成了要求** | 改用方言文档化的执行选项 `postgresql_readonly=True`（`set_readonly` → `connection.readonly=True`）。新增 `test_declared_pg_mechanism_is_legal_for_the_installed_dialect`：**让当上方言自己判**源码里写的 isolation 值合不合法（只看 `create_engine` 那几行，不被注释骗） |
| 2 | 共同 MAJOR | `restore()` 的业务键判重写成"**任一列**相等即已存在"，`(sector_name, fund_code)` 实际只比了一列 ⇒ 同板块已有另一只基金时，被删的行静默不还原、回执写"跳过已存在"，与它要修的原 bug 同一症状 | 改成**所有列都相等**才算同一行；新用例 `test_restore_dedupe_needs_every_business_column_to_match` 复现 B 的场景 |
| 3 | 共同 MAJOR | 三条新用例全打在 `_FakeConn` 上，只比返回值：删掉 `ROLLBACK TO SAVEPOINT` 三种结局一模一样 ⇒ 探针会留下"事务已中止"的脏连接而没人发现。A 另抓到 `assert 'fund_history' not in dump` 打在**路径字符串**上（恒真） | 加协议断言（`calls[0]=='SAVEPOINT'` 且必须出现 `ROLLBACK`）；备份那条改为读**文件内容**再比表名 |
| 4 | 共同 MINOR | 闸门还能被"引号把函数名包起来"穿过：`select "set_config"(...)`；美元引用 `$$a--b$$` 里的 `--` 被当注释，把 `; drop table` 整段吞掉后放行 | 分词改成：双引号**脱壳保留内容**（标识符里的函数名照样被扫）、美元引用排在注释之前且匹配成对标签。`$tag$ ; delete …$tag$` 整段本来就是字面量 ⇒ 掩掉是对的，用例改到"允许"侧说明理由 |
| 5 | A MAJOR | 我把"AST 守护扩到四种写法"写进提交说明，但只扩了 `is_correct` 那半边；`fund_code`（AGENTS 里"改标的只有一个入口"那条）仍只认属性赋值 | 两条规则同一覆盖面。**扩完当场抓出一处新站点**：`api/routes/viewpoints.py:_serialize_detail`（只是拼响应字典，不写库）⇒ 登记进例外清单并写明理由，而不是放宽规则 |
| 6 | A MAJOR | 我上一轮写"合并导入受 `ENABLE_DATABASE_IMPORT` + 确认头双重限制"是**假话**：`config.py` 里 `if req.replace:` 才检查 ⇒ 合并模式无开关无确认头，`TABLE_SPECS` 按整行列插（含 `is_correct`） | 先把话改对（用例 docstring 已改）。**是否给合并模式也上确认头 = 对外接口变更**，记成待办等老板拍板，我不擅自改 |
| 7 | 共同 MINOR | `restore()` 返回值恒 ≥0，`>= 0` 判断 ⇒ 四行全失败也退码 0；purge 路径却用 4/5 区分 | 返回 `(还原, 失败, 拒还)`，有失败退码 3 并打印回执 |
| 8 | A MINOR | 缓存 `id(bind)` 会被复用（A 实测 300 次建/销 Engine 里 172 次撞同一 id）⇒ "键=B、报告=A"会以更难查的方式回来；配套用例用两个**文件库**，去掉 id() 也绿 | 缓存改存 bind **对象本身** + URL 双条件（单槽不增长）；测的是 `:memory:` 同 URL 的场合 |
| 9 | A/B MINOR | 页面判据仍有假阳性：`<title>{{ … }}</title>`、`<noscript>`、`<textarea>`、转义样例、`v-pre` 都被算"看得见"；`q.py` 里 `io` 未用；同文件落盘走 `current_as_of` 而判窗口走 `date.today`（两套日历） | 判据先剥 `<script>/<style>/注释`；未用导入清掉。**"两套日历"我只修了报表口径**，窗口计算继续用系统时钟（那是本地日期语义，不是截止日） |
| 10 | 双方都提 | AGENTS 基线又滞后：写 808/817 时是对的，我随后加用例到 812/821 没回头改 | 本批末尾按实测刷新；并把"改完测试必须回头改这一行"升格为**同一个提交内**的动作（先看数再写，写完不再动测试） |

**这轮真正的教训（写给自己）**：我修"恒真守护"的方式是换一个**看起来更硬**的说法，却没跑一次那条路径。
凡是"引擎级保证"这类说法，必须同时满足两条才算数：① 本机或当场**真跑一次**（哪怕用假连接断言协议）；
② 让对方（方言/驱动/数据库）来判我的用法合法，而不是我自己念一遍配置名。




## 四十三、第 24 轮：74 / 73（取低 73），两份共同的第一条又是"承诺跑在事实前面"

复评对象 = `7946ca6` + `8acdab1` + `b4a1a2c` + `e4764a4`。两份都 0 BLOCKER，但重叠四条里
有三条是同一类：**我把"打算做/上一轮做过"写成了"这一批已交付"**。逐条复现后全部成立，
处置如下（每条都配了会红的用例，"改动前会红"是实测不是声明）。

| # | 两份共同 / 单方 | 我复现到的事实 | 处置 |
| --- | --- | --- | --- |
| 1 | 共同（A-1 / B-1） | `PredictionService.verify()` 自 `2c227c9`（2026-07-26 删 `POST /{id}/verify`）起 `src/` 内零调用方；我 e4764a4 却用现在时说"堵后门"。而它**放行**的那条路只写 `is_correct`，不写 `verify_score`、不追加台账、不重算博主统计 | 不修死路：**整个方法连 `PredictionVerify` 请求模型一起删**（`src/` 两处 + `schemas/__init__`），加一条 AST 用例钉住"`is_correct` 只能由 `prediction_verify_service.py` 写"（实测全库只剩 2 处，都在那个文件） |
| 2 | 共同（A-2 / B-6） | `q.py` 的"只读"只有正则那一层：`select 1 into junk`、`select nextval('s')`、`explain analyze copy ... to` 全部**通过**闸门（PG 上都是真写），而 `select replace(...)` 这种读函数却被拒 | 两层：闸门补 `into/nextval/setval/copy/do/notify/...` 且**先剥字符串字面量**再扫；引擎级 SQLite 用 `mode=ro`、生产连上就 `SET default_transaction_read_only=on` 并 `SHOW` 读回核对。新增 `test_old_gate_would_have_let_those_four_through`（把"老闸门本该漏"钉成断言）+ `test_sqlite_side_is_read_only_at_engine_level`（真写一次要被数据库拒） |
| 3 | 共同（A-3 / B-3） | 提交说明写"审计脚本改成消费 `span_report`（唯一出处）"，而 `git show HEAD:scripts/audit_verdict_evidence.py` 里**仍自带** `low = 100.0*(correct-stale_correct)/...` —— 那次改动根本没进提交 | 把工作树那份改动提交，并加 `test_audit_script_consumes_the_single_source`（脚本里出现自算式子就红） |
| 4 | 共同（A-6 / B-7） | `test_the_page_shows_the_numbers_as_text_not_only_a_title` 只是 `field in html` 的子串检查 ⇒ 把数字全塞进 `:title` 照样绿（恒真守护） | 改成"必须出现在**正文里的 `{{ }}` 插值**"（`_text_interpolations` 看插值前最近的是 `>` 还是 `<`），区间那两个数改钉 `spanText` 的定义体。我第一版写成"字段不许出现在任何标签里"，被自己新加的 `v-if="evidenceReport.judged"` 证伪 —— 条件表达式放属性是合法的 |
| 5 | 共同（A-5 / B-6） | 提交说明"26 条用例"实为 **15 条**、"新增 5 条"实为 4 条 | 现在实测：`test_readonly_query_tool.py` **26 passed**（这一批真的加到 26 条，数字与说明第一次对上） |
| 6 | 共同（A-7 / B-5） | `purge_junk_funds.py`：文件头"实测镜像与生产各 6 行映射"是**没在镜像跑过预检就写下的**（镜像实测 0 行）；预检只数活预测 ⇒ 镜像 `603758` 那条软删预测会被静默留下指向空档案；`fund_sync_retry` 这张引用表根本没查；"备份可回滚"在仓库里**没有还原代码** | 全部改掉：预检补 `回收站/重试/署名` 三类计数，两类新阻断（要过必须显式 `--allow-dead-predictions` / `--allow-owner-rows`），删除顺序 `retry → mapping → fund_info`，新增 `restore()` 与 `--restore-from`（含按列类型把 JSON 里的时间字符串转回来 —— 上一版的备份真去还原会在 SQLite 上崩，这条我实测过）。4 条新用例 |
| 7 | B-4 | `span_report()['as_of'] = date.today()`，而仓库有统一入口 `prediction_lifecycle.current_as_of()`（Asia/Shanghai）；Render 没设 TZ ⇒ 每天 8 小时页面写着"截至昨天"，而这个字段的存在理由就是"数字带截止日" | 改用 `current_as_of()`，用例断言两者相等 |
| 8 | B-8 | `/api/stats/evidence` 的 60 秒缓存只按时间分键 ⇒ 同进程换绑到另一个库，第二次拿到的还是上一个库的数，`database` 字段照样印"本地镜像库"；测试反而手工清缓存绕过 | 缓存键带上 `str(db.get_bind().url)`、`time.monotonic()`；**删掉测试里的手工清缓存**，让"换库"成为被测行为。红→绿实测：旧代码 `assert 2 == 11`（缓存把上一个库的数给了新库），新代码通过 |
| 9 | A-4 | `7946ca6` 的提交说明把 `denied_code_map` 收窄（`dbcacf1`）与幽灵行门（`2997ea9`）说成"同一批" —— 抢注上一轮已发布的工作 | 记在这里当作更正记录；已发布的提交不 amend。以后提交说明只写本 diff 里能 `git show --stat` 出来的东西 |
| 10 | B-9 | `run_sector_sync.py` 新标签写"参与同步的已审查映射 N **条**"，而 `total_mappings = len(sector_map)` 是**按板块去重**的键数（今天每板块一行看不出来，加第二行就误导） | 改成"参与同步的板块 N 个"，并把两次措辞修正的原因写进注释 |
| 11 | A-8 | `AGENTS.md` 同一处里 `tests/ = 765` 小于实测 `tests/unit = 776`（超集不可能更小） | 两行一起按实测刷新，并注明核对日期与轮次 |
| 12 | A-9 | `config.py` 注释里"算进来会是 121 条噪音"我按任何口径都复现不出来 | 换成有出处的实测口径（`sector_fund_mapping` 145 行 / reviewed 123 / owner 4），并写明"121 是没核来源就抄进来的数字" |

**没改的一条，理由要留痕**：A-8 的"跑批后最长 60 秒页面还是旧区间"——我不加失效钩子，
而是在接口文档串里明说这个代价。理由是这条报告本来就带 `as_of`，为 ≤60 秒的显示延迟
在验证服务与路由之间牵一条跨模块失效链，换来的收益不值那个耦合。

## 四十七、第 27 轮：给静态板块表加"永久用例"，结果先测出**体检判据自己短路**

任务 #24 的落地动作是"改完表 + 加永久用例"。改表在上一批已经做完（11 个改码、12 个删除、
5 个登记代理），这一批要补的是用例。补用例必须先回答"判据能不能被绕过"——**一测就发现
我上一轮写的判据本身就是可绕过的**。

### 1. `B_后缀差异` 是条免检通行证，110 条里有 82 条从来没被判过板块相关性

`scripts/audit_static_sector_map.py` 上一版的顺序是：官方名以静态标签开头 ⇒ 记 `B_后缀差异`
然后 `continue`。而静态标签是**表作者手写的** `name` 字段——它只说明"我想让它叫什么"，
不说明"这只基金到底是什么"。于是：

- 把 `白酒` 改成 `{'code': '512480', 'name': '半导体ETF国联安'}`（代码与标签同步改）
  ⇒ 旧判据落 `B`，**全绿**；
- 新判据落 `D1_另有更对口`（名册里含「白酒」的基金是 161725）⇒ 红。

这与第 25 轮 `SET default_transaction_read_only` + `SHOW` 是同一类毛病：**验证工具给自己
背书**。修法是把一根轴拆成两根，各自独立判、不互相短路：

| 轴 | 判据 | 落在哪里 |
| --- | --- | --- |
| 板块 ↔ 官方名 | `A_查无此码` / `R_字面命中` / `D1 另有更对口` / `D2 主题冲突` / `D3 说不清` / `E 已登记代理` | 决定"这条映射对不对" |
| 静态标签 ↔ 官方名 | 必须**逐字相等** | 只报"名字写歪了"（前端展示的就是它），不参与相关性判断 |

代理登记表 `SECTOR_PROXY_ALLOWED` 现在只豁免**轴一**，且必须写理由；用例
`test_proxy_registry_only_contains_real_proxies` 反过来禁止"其实字面命中却登记成代理"
的死条目（`日股` 就是被这条测出来的——它靠共享"日"字本来就命中，不需要豁免）。

### 2. 离线夹具与线上判据必须给出同一个答案

守护用例不能打网，但名册只有 83 个表内代码时 `D1`（名册里另有更对口）是瞎的。
夹具 `tests/fixtures/sector_map_roster_snapshot.json` 因此带两份证据：
`by_code`（表里每个代码的官方名）+ `d1_words`（每个板块核心词在**全网 27905 只**名册里的
最佳对口候选）。`--emit-fixture` 一条命令刷新，改表与刷夹具在同一个提交里。
实测线上与离线分桶完全一致：`R 99 / E 10 / A=D1=D2=D3=0`，退出码 0。

### 3. `--fix-labels` 的正则匹敌 0 行，却报告"dry-run：0 行"

拆轴之后标签按"逐字相等"重判：**92 行**与官方名不符（含只差公司后缀的）。写了
`--fix-labels` 去规范化，dry-run 报「0 行」——看着像"已经干净"，其实是正则里多打了一个
引号（`(?P<code>[^']+)'` 后面 `nm` 又以 `'` 开头 ⇒ 要求源码里有连续两个引号）。
发现方式不是看输出，而是把 `label_problems` 的 92 与它对比。现在两头都钉住了：

- `test_row_regex_actually_matches_every_row_in_the_file`：正则必须匹到 `len(SECTOR_FUND_MAP)` 行；
- `test_fix_labels_repairs_exactly_the_rows_it_flags`：弄歪 3 行 ⇒ dry-run 报 3、真改报 3、
  改完报 0，且逐行 diff 只有那 3 行、引号数量不变（防止把源码改坏，第 26 轮用正则删 import 改坏过一个脚本）。

92 行已真写并复核：`标签与官方名不符：0 条`。

### 4. 重跑体检的增量结果（第 27 轮）

| 板块 | 现在 | 依据 |
| --- | --- | --- |
| 船舶 | 512660 军工ETF → **560710 船舶ETF富国** | 名册里有字面对口的，不该借军工 |
| 影视 | 512980 传媒ETF → **159855 影视ETF银华** | 同上 |
| 高股息 | 510880 红利ETF → **159207 高股息ETF广发** | 同上 |
| 智能驾驶 | 516110 汽车ETF → **516520 智能驾驶ETF华泰柏瑞** | 同上 |
| 5G | 515050（官方名其实是通信ETF华夏）→ **159811 5GETF博时** | 标签与标的原本就对不上 |
| 电力 | 561170（官方名是绿色电力ETF富国）→ **159146 电力ETF华宝** | 绿色电力管不了全电力 |
| 大数据 | 158066 → **515400 大数据ETF富国** | 158066 在源端 **0 条净值**（实测），拿不到数就验不了 |
| 水电 / 核电 | 删除 | 全网名册"水电""核电"各 **0 只**基金，借绿色电力ETF 属硬凑 |
| 小金属 / 快递 / 大盘 / 港股 | 保留标的，登记为代理并写理由 | 名册里分别 0 只 / 0 只 / 只有细分风格 / 与别名口径一致 |
| 铜 | 上一批删了，这一批**放回来**并登记代理 | 全名册 27905 只里含「铜」的是 **0 只**，agent 面对同一份名册也变不出铜基金 ⇒ 带理由的有色代理比"查无基金"实用 |

`R 99 + E 10 = 109` 条，全部要么字面相关、要么有署名理由。

### 5. 从表里删掉一个键，不等于这个板块不再被硬凑

删键之后实测 `get_fund_for_sector('卫星互联网')` 仍返回 **517200 互联网ETF嘉实**——
第 5 步子串模糊匹配把它吸了回去；`铜` 则被 `SECTOR_ALIASES` 吸到 有色金属。
所以"宁可交给 agent 也不硬凑"必须写成显式名单 `SECTOR_NO_STATIC_FUND`，在别名与模糊匹配
之前返回 None。它带来一个连锁反应，也是**金标用例真的红了**才暴露的：
`normalize_sector_name` 第 4 步会把没在表里的板块归到表内的键上 ⇒ `sector_core('A股互联网')`
从 `'A股互联网'` 变成 `'互联网'` ⇒ 板块身份判据（`sector_core` 驱动相关性旗标与候选匹配）
跟着变，"卫星互联网"的预测可以顶着"名字含互联网"的相似度被挂到互联网ETF 上。
修法是在 `normalize_sector_name` 里让**名单内板块本身就是标准名**（归一化不该取决于
"这个板块在静态表里有没有基金"），并把"用户自定义别名压过名单"这条留在前面——
`test_blocked_sector_can_be_overridden_by_a_user_alias` 盯着：老板在界面上登记的别名是人的决定。

`tests/fixtures/core_matcher_golden.json` 里 `A股互联网 → A股互联网` 因此从红回到绿，
**没有改动金标**（改了就是在掩盖行为变化）。

### 6. "代码对不对"之外还有一问："这个代码能不能用"

新脚本 `scripts/sync_sector_map_funds.py`（默认 dry-run，真写要 `--apply --confirm SYNC-MAP-FUNDS`）
量出来的事实比改码本身更值得记：**静态表引用的 88 个代码里，26 个本地既没有 `fund_info`
档案也没有一行净值，30 个净值不足 30 行**。板块换对了标的、标的却取不到净值 ⇒ 落在新代码
上的预测永远进不了验证队列。这一批补完之后：

- `update_fund_history(days=400)` 其实只发一页（`pageSize=min(days,60)`，实测每只回 20 行），
  拿它当"补 400 天"是句假话 ⇒ 改用分页的 `backfill_history_range`，绝大多数补到 **248 行**；
- `update_fund_info` 对新代码会留下**空名字档案**（26 行），而我上一版把它算成 `[ok]`
  ⇒ 现在按名册补名并把"名字仍为空"计入退码 5；
- 仍然补不满的两个是**事实而非 bug**：158041（创业板算力ETF华夏）源端只有 **7 行**（新上市），
  508006 是 REIT，`lsjz` 区间接口给 0 条、单页接口给 20 行。这两个板块的短期预测可验、
  长窗口预测会落进"结构性不可验"那条既有通道，我不拿"补数据"当借口去放宽判据。

### 7. 数字与变更前后

| 指标 | 第 27 轮前 | 第 27 轮后 | 口径 |
| --- | --- | --- | --- |
| 静态表被判过板块相关性的行 | 28 / 110（82 行走 `B` 免检） | 109 / 109 | `audit_static_sector_map.py`，名册=东财 27905 只 |
| 待处理（A/D1/D2/D3） | 修完上一批后自报 0，但**判据可绕过** | 0（且绕过路径有用例） | 同上，退出码 5→0 |
| 标签与官方名不符 | 27（旧判据只看前缀） | **0**（新判据要求逐字相等，先测出 92） | 同上 |
| 表内代码本地无档案/无净值 | 26 / 88 | 0（净值最薄的是 158041 的 7 行，源端事实） | `sync_sector_map_funds.py`，镜像 `data/fund_insight.db`，2026-09-22 22:5x |
| `pytest tests/unit` | 819 passed | **832 passed / 16 skipped / 0 failed** | 2026-09-22，同一提交内复测 |
| 新增用例 | — | `tests/unit/test_sector_map_guard.py` 13 条 | 含 2 条变异实测（见下） |

**改前会红的实测**（不是推断）：把 `消费` 改成 `512480 半导体ETF国联安` 注入源码后跑
`test_sector_map_guard.py` → `1 failed, 10 passed`（红的正是主用例），随后按字节还原并核对
`与原文逐字相同 = True`。另外 `卫星互联网 → 互联网ETF` 这条是**先量到旧行为、再改代码**，
所以 `test_blocked_sectors_resolve_to_none_not_to_a_nearby_fund` 在改动前必然红。

### 8. 这一轮留下的教训

1. **新写判据时，第一个用例应该是"怎么骗过它"**。`B_后缀差异` 是我自己上一轮写的，
   它的短路我写了 3 轮都没看出来，是"要给守护用例找反例"这个动作把它逼出来的。
2. **工具报 0 不等于干净**：`--fix-labels` 报 0 行、旧体检报 0 条待处理，两次都是判据/正则
   失效，不是数据变好。所以每个"0"都要配一个"它必须能报非 0"的用例。
3. **删配置项要连带看读它的函数**：删 `SECTOR_FUND_MAP` 的键，改了 `sector_core` 的语义
   （金标用例红了才知道）。表、别名、模糊匹配、归一化、身份判据是一条链上的五件事。
4. **改对了名字不等于能验证**：`audit_static_sector_map.py` 只管"对不对"，
   `sync_sector_map_funds.py` 管"能不能用"，两问两个脚本两个退出码，别互相冒充。


### 9. 提交前自审又抓出一条"永远不可能命中"的判据（`D1` 的 roster 分支）

写完上面几节准备提交时重读 `classify`，发现第 4 节说的"D1 修好了"其实只修了一半：

```python
better += ['%s:%s' % (code, official) for v in variants if v and any(v in n for n in roster_names)]
```

这一行往候选里塞的是**这一行自己**（`code`/`official` 就是当前板块挂的那只基金），
而下一句 `sorted(b for b in better if b.split(':')[0] != code)` 又把它剔掉 ⇒
`roster_names` 这条路径**永远不会产出候选**，只剩 `d1_words` 在做事。
它没造成误判（线上/离线分桶都是 0 条 D1），但"名册里另有更对口"这条判据有一半是哑的，
而我刚才把它写成有效的了 —— 这是第 2 条教训对我自己的回马枪：**"0 条"有两种，
一种是真干净，一种是判据压根没跑**，重读代码才分得出来。

改法：直接从 `by_code` 里列**别的**含该核心词的基金（`c != code`），并把改后没人用的
`roster_names` 形参删掉（`main` 与守护用例的 `_judge` 两处调用同步）。
改完复测：线上/离线分桶仍 `R 99 / E 10 / 其余 0`、退出码 0；
`test_sector_map_guard.py` + `test_sector_identity_audit.py` **116 passed**；
`白酒→512480` 的变异用例仍红（现在两条 D1 路径都能指出来）。

### 10. 这次动表对老板能看到的东西影响多大（实测，不是估算）

只读查镜像 `data/fund_insight.db`（2026-09-22，`is_deleted=0` 的 1616 条活预测）：

| 板块 | 活预测 | 判对 | 判错 | 未判 | 现在挂着的代码数 |
| --- | --- | --- | --- | --- | --- |
| 大盘 | 74 | 35 | 34 | 5 | 1 |
| 电力 | 32 | 11 | 13 | 8 | 1 |
| 港股 | 7 | 3 | 0 | 4 | 3 |
| 铜 | 5 | 1 | 2 | 2 | 2 |
| 小金属 | 3 | 1 | 1 | 1 | 1 |
| 卫星互联网 | 3 | 3 | 0 | 0 | 2 |
| 其余 10 个动过的板块 | 0 | — | — | — | — |

合计 **124 条**（占活预测 7.7%）落在这次改过码/删过键的板块上，其余 1492 条不受影响。

**但这 124 条的结论一条都没动**：改的是静态表（**解析时**给新预测挑标的用的），
不是 `predictions.fund_code`。改挂已有预测只能走 `FundSyncManager.retag_prediction()`
（留痕 + 清结论 + 可整批还原），我没在这里调用它 —— 若要按新表把这 124 条重挂标的并重判，
那是老板的决定项，不是改表的顺带效果。
