# 板块与基金匹配

## 职责一句话
给定一个板块名（"半导体"、"恒生科技"、"豆粕"），决定用哪只**公募基金**作为该板块的验证标的，
并给出可复核的证据链（LLM 提了什么、抓到什么、为什么选它或为什么不选）。

## 运作流程
1. 入口：帖子分析时 `LLMAnalyzer._fill_fund_from_sector()` 给预测补基金；
   板块匹配页/批量任务时走 `POST /api/config/sector-mappings/ai-match`、`ai-batch`。
2. 核心：`src/services/sector_fund_agent.py` 的 `resolve_sector_fund(sector)` 按 T0→T5 跑：
   - **T0** 精确候选（已审查映射 + 内置字典精确键，短键不再做子串匹配）；
   - **T1** LLM 提案 3–5 只 + 关键词 + 别名；
   - **T2** 逐只抓站验证：是基金不是股票、抓得到净值（`is_strict_ok`）、
     LLM 声称名 vs 官方名（对不上只降级不淘汰，因为 LLM 常记错代码↔名称）、
     **宽基指数硬拒**（豆粕→中证500ETF 这类"拿大盘凑数"直接淘汰）；
   - **T3** LLM 语义复判 `suitable/proxy/score`，`score>=70` 才算相关；
   - **T4** 关键词搜索回环（`fund_api.search_fund`，结果天然只含基金）→ 回 T2/T3；
   - **T5** direct 全灭后才允许 **proxy（有意代理）**：老板确认过 `债券→国债ETF`、
     `SpaceX→军工ETF` 这类"没有对口基金，取关联度最大的替代"不是错配。
3. 产出 `SectorDecision`：`status ∈ matched|proxy|needs_review|conflict|no_fund`、
   `confidence`、逐轮 `evidence`。拿不准一律 `needs_review`，**不再随机填一只基金**。
4. 落库：`apply_decision()` 写 `sector_fund_mapping`（含 `match_source/match_kind/confidence/
   verified_at/verify_message/evidence/reviewed_by/owner_locked`），写完刷新两处进程内缓存。
   `owner_locked=1` 的行直接拒绝覆盖。
5. 影响谁：预测的验证标的、`FundSyncManager` 自动补基金、博主准确率统计。

## 入口文件
| 文件 | 说明 |
| --- | --- |
| `src/services/sector_fund_agent.py` | agent 本体 + 置信度算式 + `apply_decision` |
| `src/services/sector_ai_match_task.py` | 批量匹配后台任务（异步 + 轮询 + 陈旧判定） |
| `src/services/sector_fund_service.py` | 映射 CRUD、缓存、级联清理、批量审查证据守卫 |
| `src/constants/sector_fund_map.py` | 内置 `SECTOR_FUND_MAP` / `SECTOR_ALIASES` + 别名归一（**条数不写在这里**：表 109 条以 `python scripts/audit_static_sector_map.py --fixture ...` 末行为准，别数第 35 轮 B 就抓到文档还挂着旧的 122） |
| `src/utils/fund_matching.py`、`src/fund/fund_auto_manager.py` | 老链路，现已委托 agent |
| `scripts/run_sector_agent.py` | 命令行跑 agent，输出证据链 JSON |
| `scripts/calibrate_sector_agent.py` | 金标集标定（agent vs no-op 基线） |
| `docs/迭代计划/gold-standard-50.json` | 55 条人工金标（含 6 条有意代理） |

## 对外接口
- `POST /api/config/sector-mappings/ai-match`（`{sector_name, apply}`，按板块名寻址，内置板块也能用）
- `POST /api/config/sector-mappings/ai-batch` + `GET /api/config/sector-mappings/ai-batch/status`
- `GET /api/config/fund-search?keyword=`
- 既有：`GET/POST/PUT/DELETE /api/config/sector-mappings`、`/{id}/review`、`/batch-review`
- 被调用方：`src/analyzer/llm_analyzer.py`、`src/fund/fund_sync_manager.py`、
  `src/services/prediction_maintenance_service.py`、`web/index.html` 板块匹配页

## 上游依赖
`src/fund/fund_api.py`（抓站验证与搜索）、`src/analyzer/llm_analyzer.py::_call_llm`（LLM+缓存+熔断）、
`sector_fund_mapping` / `sector_alias` / `fund_info` 三张表。

## 关键数据结构
`sector_fund_mapping`：`sector_name`(非唯一) + `fund_code`(FK fund_info) + `reviewed` +
`match_source` + `match_kind(direct|proxy)` + `confidence` + `verified_at` + `verify_message` +
`llm_reason` + `is_fetchable` + `evidence`(JSON 文本) + `reviewed_by` + `owner_locked`。
`sector_alias`：`alias_name`(unique) → `sector_name`，`source` 标来源。

## 已知坑与约束
- **审查门禁不可作弊**：`reviewed=1` 必须有 `match_source + verified_at + confidence + verify_message`；
  `batch-review` 对无证据行默认拒绝（老板在弹窗明确确认时放行并记 `reviewed_by=owner`）。
  校验 SQL 要写 `confidence IS NULL OR ...`——`NULL < 0.85` 结果是 NULL，会把整条谓词短路掉。
- **`scripts/seed_sector_mappings.py` 是"只补内置表缺的板块"，不是"把库刷成内置表"**。
  这条边界是第 36 轮 B-MAJOR-1 量出来的，**当时的形状**（2026-09-23 17:46 镜像只读实测）：
  脚本要落 **163** 个板块键（静态表 109 + `EXTRA_MAPPINGS` 64 去重）⇒ 库里已有 35、**新建 128 行且全部
  `reviewed=True`**；其中 **21 行会被就地改 `fund_code`**，而这 21 行里 **15 行库里已是已审查**、
  **8 行署名 `agent`**（中概、中概互联、低波、保险、光伏、军工、创新药、券商）——等于把机器验证过的映射
  倒回静态表旧码，而且**直写 `existing.fund_code` 绕开 `retag_prediction`**（无 run_id、无备份 ⇒
  不能整批还原）；它**不查 `owner_locked`**，路由侧更糟：起子进程跑脚本却**不查退码**就回
  `success:true "预置数据导入完成"`，既没有 `ENABLE_*` 总开关也没有 `X-Danger-Confirm`。
  **现在的形状**（第 36 轮返修 `77fa10e` + 第 37 轮 `dd125cb`，逐条有用例钉）：
  ① 只新建、**任何已存在的行一律不碰**（码不一致只报不改，要改标走页面/体检）；
  ② 新行 `reviewed=False` + `match_source='seed_builtin'`，等体检；
  ③ 脚本要 `--confirm SEED-MAP` 才真写，路由要 `ENABLE_SECTOR_SEED_IMPORT=true` +
  `X-Danger-Confirm: seed-sector-mappings`，并且**查退码 + 查 `SEED-RECEIPT:` 回执**，
  没有回执就算失败；④ 动手前扫 `fund_info` 预报外键，**缺档案的板块跳过并点名**，
  不再"单事务里 commit 时整批回滚、回执却说新增 126"。
  当前镜像 dry-run 实测（命令 `python scripts/seed_sector_mappings.py --dry-run`）：
  `新增=126 已存在跳过=35 其中码不一致=21 老板行=0 缺档案=2 内置计划=163`
  （缺档案那两个：`鸿蒙→159768`、`血制品→512290`）。要物化内置表仍建议走页面 + 身份体检，
  这条路只是把内置表当"待审查的种子"。
- **两处进程内缓存**：`SectorFundService._cache` 与 `sector_fund_map._load_db_aliases()`，
  写完必须 `refresh_cache()` + `refresh_db_aliases_cache()`，否则"自己写的映射自己读不到"。
- **`get_fund_info` 的历史兜底**会让学生代码也"验证通过"，验证路径必须 `allow_fallback=False`。
- **`is_active=false` 不等于修好**：列表/verify-all/`sync_sector_mappings` 都按 `is_active=1` 过滤，
  置灰等于把问题藏起来。
- 帖子分析热路径默认 `SECTOR_AGENT_LLM_IN_POST=false`（只走确定性路径），重活交给批量任务。
- **删一个垃圾基金码之前，先看它的预测有没有台账**（2026-09-26 副本演练量出来的）：
  `prediction_change_logs.prediction_id` 是 `ForeignKey('predictions.id', ondelete='RESTRICT')`，
  所以"把回收站里那条软删预测硬删掉再删档案"这条路**动得了就动掉了审计证据**。
  `purge_junk_funds.py` 现在的顺序是：`inspect()` 先数依赖 → `plan()` 见到依赖就**整批拒**
  并说清可行的路（把预测的标的**改指**到有效基金、保留台账；或老板明确要清台账）。
  同一批还加了 `--drop-dead-predictions`（只在**没有台账、也没有已判结论**时才把软删预测
  并进同一份备份并排在 `fund_info` 之后还原），但生产上那条 603758 的预测用不上它 ——
  判据 `tests/unit/test_purge_junk_funds.py`（当场条数 `grep -c '^def test_' 该文件`）。
- **LLM 自动建映射之前先问"这码到底是不是基金"**（2026-09-26 补，S6 那 6 个垃圾档案的来路就在这）：
  `LLMAnalyzer._save_fund_mapping` 走到"该板块还没有映射"那一支时，旧代码直接
  `ensure_fund_info_exists` + 建映射 —— LLM 抽出「秦安股份 → 603758」「泉阳泉 → 600189」
  这类 A 股 / 债券代码，机器就自己把股票名当基金写进库里（生产因此攒下"活预测 0、净值 0"的档案）。
  现在建之前复用人工改标那把尺子 `_manual_identity_verdict`：**只有"查到了且不对"才拦**，
  `unknown` / 探针不可用 / 任何异常一律按"没意见"放行（一次网络抖动绝不能让新板块建不了映射）。
  判据 `tests/unit/test_llm_analyzer_cache.py::test_the_llm_mapping_gate_asks_whether_the_code_is_a_fund_at_all`
  两侧都钉：判"不是基金"⇒ 档案与映射都不许多建；判"没意见"⇒ 照旧写入（闸不许建成墙）。
- **一把尺子不够用**：`data/_roster_full.json` 是快照不是全集 ——
  `000938 / 002154 / 002261 / 002413 / 003033` 五个码名册查无，但基金域逐个答"是真基金"
  并有 20 行净值（华商稳固添利债券C、南方荣冠定开混合等）⇒ **判"是不是基金"要名册 + 基金域
  两根轴一起看**，单用名册会把真基金判成垃圾（我今天就这么错过一次，见
  `基金数据抓取与验证.md` 里"名字这一层"那一节）。反面对照也量了：S6 那 6 个码两根轴都答不出
  （`ok=False`、`history_count=0`）⇒ 那批确实是非基金，删得对。
- **今天生产上按这条规矩做完的动作**（都可回滚、都有逐行回执）：5 个还指着股票/债券的板块
  走 `PUT /api/config/sector-mappings/{id}` 原地改标（中报业绩→016370、拓维信息→159998、
  矿泉水→515170、美债→511100、秦安股份→562700），加上之前的北美→513100 / 海力士→512760 /
  场内第一持仓→510300 / 紫紫→159996；改标顺带补出 5 只新档案，`update-all` 后各 20 行净值。
  **量出来的结论**：生产 `sector_fund_mapping` 在册 118 行，代码不在名册的 **0 行**
  （2026-09-25 是 9 行）⇒ 目标①"只允许基金不允许股票"在生产第一次真正成立。
  **但那 9 行当时不该一起叫"非基金"**：按上面两根轴重量，其中 **6 行**（`603758 / 600189 / 152788 /
  HYNX / SBSP76 / ign`）名册与基金域都答不出 ⇒ 确属非基金；另外 **3 行**（`000938 / 002154 / 002261`）
  是名册缺录的**真基金顶着股票名**（华商稳固添利债券C 等，各 20 行净值）——它们的错在名字不在代码，
  报数时把这两类混成"9 行非基金"是我上一轮的说漏。
  随后删掉两个零依赖孤儿档案 `152788` / `600189`（备份 `backup/purge-junk-20260926-122103.json`），
  6 个垃圾码里只剩 `603758`（它那条回收站预测被改标台账以 RESTRICT 外键牵着，见上面那条与任务 #85）。
- **单行改标不需要 `ENABLE_SECTOR_AUDIT_IMPORT`**：那个开关只管 `/api/config/sector-mappings/-/audit-import`
  这条**审计字段批量回写**。改一只标的走页面同一条 `PUT /api/config/sector-mappings/{id}`
  （服务端自跑 `_manual_identity_verdict`：判"不是基金"就不给已审查），
  落库是 `reviewed=True / reviewed_by=manual_review`，**老板署名与体检免疫要另外的 `owner_confirm`**。
  S6 的 北美→513100、海力士→512760、场内第一持仓→510300 就是这条路做完的（先改标、后删码，
  所以板块不会留空窗）。
- 单板块实测 4–25 秒（LLM + 多只抓站），`budget_ms` 默认 30s；`ai-batch` 的锁必须是 `RLock`
  （`start()` 持锁期间会调 `is_running()`，普通 Lock 会自锁死，实测挂住请求 60s）。

## 最后更新
2026-09-24（第 43 轮返修，同一根轴的第二半）：上一批把"读侧进门"与"库名带个体"做了，这一批修的是
**"那道闸按什么形状判"**。① 说明文买不到守卫信号：`_facts` 现在把模块/类/函数 docstring 从常量集合里剔掉，
"会拒跑"只认**条件分支里**"印了 `[abort]` 并且停下来"（raise 或 `return 4` 都算）—— 之前三个识别器
（`_refuses_remote_without_a_flag` / `_declares_http_target` / 读侧第④档）能被一段以 `postgres` 开头的
文档 + 一句入口 `raise SystemExit(main())` 白送，而被绕过的那条判据自己的 docstring 写着"写在 docstring 里不算"。
② 落笔能力改成按**类别**枚举：`create_all`、裸 SQL DML、DB-API 直连（`sqlite3`/`psycopg2`）、
`df.to_sql(if_exists='replace')`、覆盖 `.db`/`.env` 的文件操作、子进程借道 alembic 或 `run_migrations.py`、
`requests.request('DELETE')`，外加 sqlalchemy 别名（`create_engine as ce`）与"函数体内 `SessionLocal` 的纯读脚本"
—— 两份评审各自独立数出"12 类里 10 类三道判据一条不响"。③ `scripts/run_migrations.py`（全仓唯一发 DDL 的脚本）
补了 argparse：`--help`/坏旗子在连线前退出、`--dry-run` 只报目标后退 2，**无参数仍照旧迁移**（Render startCommand）。
④ `scripts/drop_probe_residue.py`：新工具，扫/清只读探针在**本地镜像**留下的空壳表
（第 41 轮 B-MAJOR-2 之前那版探针留下的 `_db_guard_probe2` 今天已用它删掉；`--apply` 还要 `--confirm DROP-PROBE`，
只碰 SQLite，只删"0 行且模型没声明"的名字）。⑤ `push_sector_mappings_to_prod.py` 的 `[目标]` 那行按 host 判措辞
（`--base http://127.0.0.1:9/` 以前照样印"线上生产库"）。⑥ 两份 `machine_name`/`db_kind` 的样品改成笛卡尔积生成、
比整句而不只比路径；"是不是真镜像"从后缀匹配改成 `realpath` 比对。
判据与条数一律跑命令看：`grep -c '^def test_' tests/unit/test_script_db_guards.py`、
`grep -c '^def test_' tests/unit/test_drop_probe_residue.py`、`python scripts/audit_doc_claims.py`。
2026-09-24（第 41~43 轮返修，脚本侧"连哪个库"这条轴）：
① **读侧也当攻击面**——`audit_l3_clear_labels.py` / `estimate_l3_vague_labels.py` /
`backtest_l1_weighting.py` 三个只读分析脚本改走 `_db_guard.read_only_connect()`（引擎级
`mode=ro` + 真写探针），`--production` 要显式给；第 42 轮再补 argparse，`--help` 不再"顺手把
整份报告写进 docs/"（同源教训：第 39 轮 `mutation_proof_frontend.py` 顶部 `import argparse` 却从不调用）。
② **钉库的顺序改由守卫自己在运行期管**（第 42 轮 B-MAJOR-4）：AST 只能可靠比顶层 import
的行号，函数体里那 100 多处 `from src.…` 按行号比要么冤枉一片要么漏掉；现在
`pin_local_sqlite()` 一进来就问 `sys.modules`——全局 engine 已建在**非 SQLite** ⇒ `[abort]` 退 4
并印出那个目标（口令不泄露），已建在 SQLite ⇒ `[警告]` 说清"接下来写的就是这个文件"后放行
（`tests/conftest.py` 就是把 engine 建在临时 SQLite 上的，一律 abort 会把 8 条正经用例打成墙）。
③ **自报库名要认得出是哪一个**（第 42 轮 B-c）：`database_label()` 现在跟着文件或主机
（`本地镜像库（data/fund_insight.db）` / `本地 sqlite 文件（不是镜像库）：…` / `内存 sqlite…`），
副本回放与测试夹具不再共用"本地镜像库"这四个字；它与 `_db_guard.machine_name()` 是同一件事的
两份实现（src 不能 import scripts，反之亦然），两者逐条相等由用例钉住。
`scripts/purge_junk_funds.py` 的 `[target]` 同样改成印机器名 —— S6 那六个码要在生产删，
屏幕上那行必须答得出"删的是哪台"。
④ **回写回执的分桶**（第 42 轮 B-MINOR-3/(a)）：`/api/config/sector-mappings/-/audit-import`
的 `unservable` 只数**真的写了的行**（`created`/`updated`），`unchanged` 一行都没写进去，
把它算进"不可服务的行数"就是在报一个不存在的后果。
⑤ **文档里的数与"数据源"都有机器对账了**：`python scripts/audit_doc_claims.py`
（条数账 + `docs/*.md` 每行 `数据源：…` 必须认得出是哪个库，或明写"未记录"并给复现命令）。
2026-09-25（第 48 轮返修，A 70 / B 76 ⇒ 取低分 70，十二轮没过线；老板圈定"先修产品，守卫只修会丢数据的"）：
这一轮落在"板块↔基金"这张表上的只有三件事，因为它们各自能毁数据或能说谎，其余按老板决定转产品。
① **"这句 SQL 在写哪一列"从此只有一份实现**：新增 `scripts/sql_write_policy.py`，上一轮那条
"三处共用同一个位置无关的裸 SQL 判据"**其实只共用了两处** —— 免疫那把棘轮还留着自己的旧正则
（第 48 轮 A-4 / B-2 各从一边量到），于是它两个方向同时坏：`SELECT … WHERE owner_locked = true`
算 **2 条授予**（把读当写＝建墙），`SET owner_locked = :ok` + 参数字典算 **0 条**（漏一条真授予 ⇒
`IMMUNITY_GRANT_SITES` 仍"逐字等于"、新的豁免来源静默放行）。现在写死授予值 / `is_correct` /
`fund_code` 三把棘轮都调它：只认 SET 子句与 INSERT 列清单、`WHERE` 一律不算、绑参数按调用实参字典求值，
并认"先把 SQL 拼进变量、之后 `execute(变量)`"这一族本仓真写法（`src/api/main.py:553-560` 那种）。
② **还原映射那条路：不可逆删除的规模必须动手前说**（B-1）。上一轮给它的规则是
"`created_at` 拿不到 ⇒ 一行都不清"，但**清单里写一个错的过去日期**（例 `2020-01-01`）仍然等于全删，
而且"清掉 N 行净值"这句话以前是在 `commit` **之后**才印的。现在动手前先印 `[计划] …`，
`created_at` 与清单自身 mtime 差超 90 天（或晚于一以上）⇒ 退 4 并说明为什么不信这个日期
（净值不可再生：每日同步只回补最近 30 天）。判据拿真副本库跑，两头都钉：6 行净值必须还在、
`[计划]` 那行必须排在 `[还原]` 之前。**本批 +2 条用例就在那条判据的文件里**（当场 `--collect-only -q` 印 9）。
③ **sqlite 那一支的口令也不许出现在自报行里**（A-6 / B-3）：`sqlite:///E:/u:S3cr3tPW@data/x.db`
（口令在带盘符的路径里）与 `sqlite:///password=S3cr3tPW`（口令落在路径段、没有 `?`）以前**两把尺子一致地**
整段回显 —— 那条"两边逐条相等"的判据对它结构性失明（第 46 轮就写过"一致地错更危险"，今天又现形一次）。
现在两边同剥，4 个新形状进笛卡尔积样品池，`_x` 前缀豁免的范围也从 `scripts` 扩到 `src`/`alembic`
（否则"豁免"本身成了第三把分身）。
④ **一句已作废的话**：上一轮写在 `AGENTS.md` 基线段的"`pytest` 与 `pytest` 不互斥（那把锁只挡体检）"
是错的 —— 套件里 `tests/unit/test_mutation_lock.py` 有一条要抢**机器全局**的体检锁，
第二个真会话会让它响（今天我自己并发跑 1 条单测，后台那次全量跑立刻红了一条）⇒ 规矩升级成
"跑基线期间不许再起第二个 pytest 会话"。
⑤ **本轮没修的七条明写在 `AGENTS.md` "当前测试基线"那一段**（helper 返回写死的 `[目标]`、
方向不明的变量下标 key、动态 `import_module` 的 DB-API、迁移里 `from . import helpers` 的委托、
能力类别与样品的对应无断言、`sqlite+aiosqlite:///…` 把真镜像自报成"不是镜像库"、
`q.py` 的 sqlite 腿没有 `PRAGMA query_only`）⇒ 别当已封。
2026-09-25（第 47 轮返修，A 68 / B 80 ⇒ 取低分 68，仍不到 80）：这一轮两席咬的都是
**"判据形状对了，但普通写法走得到它吗"**，落在这张表上的有四件事：
① **写这一列的形状，位置无关**：`reviewed_by='owner' + owner_locked`（身份体检豁免）与
`is_correct` / `fund_code`（两条"唯一入口"）以前都只认 AST 里的赋值/批量写/setattr，
`db.execute(text("UPDATE sector_fund_mapping SET owner_locked = true"))` 这一族要么只在
"整整一句就是一次调用"时才算（`n = db.execute(...)`、`if db.execute(...).rowcount:` 全部隐身），
要么**根本没有这一腿**。现在三处共用同一个位置无关的裸 SQL 判据，且只认 **SET 子句 /
INSERT 列清单**里的列名 —— `UPDATE … SET status = 1 WHERE is_correct = true` 是**读**那一列定位行，
`sum(1 for m in rows if m['owner_locked'])` 是计数，两条都钉了"不许误伤"。
② **能写数据的脚本必须报出"是哪一个库"，而且那句话要从值算出来**：`print("[目标] 本地镜像库（sqlite）")`
这种抄死的一句话以前算自报（`.env` 指向生产时它照样这么印）；`dbapi_direct` 那一类能力则**恒空**
（判据要求 callee 既等于 `connect` 又等于模块名），于是 `from sqlite3 import connect` +
`cur.execute(sql)`（SQL 在变量里）被判"不能改数据"、从没被问过连哪儿。本仓被这条抓到的真脚本是
`scripts/backup_database.py` —— 它现在第一行印 `[目标] 备份源：<db_kind(源文件)>`，实测镜像给
`本地镜像库（data/fund_insight.db）`、副本给 `本地 sqlite 文件（不是镜像库）：…`。
③ **迁移闸门"看不见"不等于"没事"**：删除动作藏在类方法（`class Legacy: def wipe(self): op.drop_table(…)`）、
写成属性出口（`Legacy().wipe()`）、换 `op.rename_table`、或 helper 在**别的模块**里时，
`audit()` 回的都是"干净"，而 `scripts/run_migrations.py` 每次 Render 启动**先问这份判据**再对
`.env` 那个库发 `upgrade head`。现在解析不出的调用记"看不清"、`alter_column` 也归"看不清"
（硬算删除会把 9 支正常迁移全逼进登记表 = 把闸门建成墙）。复核：
`python -c "import sys; sys.path.insert(0,'scripts'); import migration_policy as m; p,s=m.audit(); print(s,p)"`
→ 仍印 `9 []`。
④ **`[abort]` 必须挡流程**：`sweep_sector_mappings.py --restore-from` 缺 `created_at` 那一支以前印完
拒跑就把 `codes` 清空继续往下走（退 0、`[dry-run]` 照印、`--apply` 还照写映射行）；现在返回 4。
把旧行为钉成规矩的正是我上一轮自己写的那条用例（`assert restore(...) == 0`）⇒ **判据写错时，
绿色用例会替错误行为作保**，这条已随本批更正。
另：`alembic/env.py` 是这仓库第三把手搓剥口令尺子（`split("@")[-1]` 不切 query ⇒
`?password=` 进 stderr/Render 日志），改用共用尺子并把 `alembic` 加进那条棘轮的扫描面；
`scripts/_db_guard` 报"哪个库"时把只写在 `?host=` 里的主机补上（第一版按冒号猜，被
"两把尺子逐条相等"那条判据当场抓红）。
2026-09-25（第 46 轮返修，A 76 / B 72 ⇒ 取低分 72，仍不到 80）：这一轮两份评审独立打在同一条线上 ——
**上一轮我写在基线里的那四处"已经封起来"，每一处都还能被普通写法买到**。
① "方向要来自值"当时的实现是"表达式里出现过 `sqlite` 字样" ⇒ 一条三目（本地/生产开关）、
`os.environ.get(k, "sqlite:///…")` 都白过；现在 `_proves_sqlite` 只认"字面量以 sqlite 开头 /
每一臂都能证明 / 一跳 helper 的每条出口都能证明"，式子里有运行时子式就不证明。
**解析顺序也是判据的一部分**：先认 helper 出口、再问有没有运行时子式 —— 反过来会把
`url(bool(os.environ.get("L")))` 这种诚实写法打死（我自己第一版就打死过）。
② `_returns_sqlite` 的出口集合补上 `except` 里那几条；同一文件里 `_guard_stmts` 反过来**不钻** `except`
—— 一边问"这个 helper 可能交出什么"，一边问"正常路径会不会停下来"，共用开关才是 bug。
③ 死路判定改**求值**（`and 1 == 0` / `if not True` / `if ()` / `while 0:` 以前全算活）。
④ 顺序与可达改走**调用图**：行号元组按字典序比（`(216,133,37)` vs `(216,165)`），
第一版用累加小数把顺序整个弄反过、被"真护栏必须仍算守卫"那条对照抓红。
⑤ 迁移闸门：`op.execute` 换到赋值/条件/推导式/`with` 里就隐身（且不标"看不清"，审计回"干净"）
⇒ 改成按"是不是执行 SQL 的入口"判；`sa.text(默认值)` 仍然不误伤。名单**真的**只有一份了
（测试以前传自己的本地空集合 ⇒ 登记进 JSON 会让运行期放行、让 pytest 变红）；
`audit()` 从 `os.listdir` 改递归（alembic 用 `os.walk`，子目录里一支 `drop_table` 会被 apply 却从未核对）；
名单传 set 不再 `AttributeError`；空目录不许报"干净"。
⑥ 报"哪个库"改**四档**：看不见主机时以前落进"本机"，而
`postgresql:///postgres?host=…supabase…`（主机只在 query 里，SQLAlchemy 正规写法）就是这样被
两把尺子**一致地**说成本机的 —— 一致地错不会被"两边逐条相等"那条判据抓到，所以加第四档
"认不出主机，不敢说它是本机还是线上生产库"。多主机列表按 `,` 切成候选、任一命中即生产；
`sqlite:///u:pw@/db` 与 `postgres:///password=…`（口令落在路径段）两条泄露面一起补，
反向钉"合法文件名里的 `@` 不许被剥坏"。`--production` 从此问"是不是那台"而不是"远不远"。
⑦ **本批最该记的两条不是守卫，是我自己的漏门与被驳回的评审建议**：
`sweep_sector_mappings.py --restore-from` 这条**会提交、会硬删净值**的路径以前既不看 `--apply`
也没有确认词，而缺 `created_at` 时"只清本轮新建"退化成"清这只基金的全部历史" ——
这条函数正是我上一轮刚编辑过的（我加了免疫剥离，却没看见它没有门）。
另一半：B 席建议把文档流水账改成"按句判"，我照做后**立刻假红一处**
（AGENTS 基线那一段是整条链，切句把"新增 3 条"变成当场账：文档写 3、当场 35）
⇒ 建议的修法按实测驳回，只保留它要的另一半：**"因为流水段而没判"的条数必须印出来**。
判据与条数一律跑命令看：`python scripts/audit_doc_claims.py`（退 0，末行印四种账各自的数）、
`python -c "import sys; sys.path.insert(0,'scripts'); import migration_policy as m; p,s=m.audit(); print(s,p)"`
（今天印 `9 []`）、`grep -c '^def test_' tests/unit/test_script_db_guards.py`（今天 35 条）。
⑧ **本轮自己跑出来的卫生问题**（不是守卫，但后果落在同一张表上）：两条 `write_manifest` 用例把回滚
清单写进 **仓库里那份真清单的同名目录** `docs/迭代计划/run-2026-09-20/`，断言中途红就不清理 ⇒ 本机
实测漏过两份 `sweep-manifest-pytest-realign*.json`（内容是一张临时夹具表，看着却像真的回滚依据）。
现在两条用例都拿 `monkeypatch` 把 `OUT_DIR` 钉到 `tmp_path`，另加一条只认 `pytest` tag 的闸
（`test_a_manifest_test_leaves_no_copy_in_the_repo`，真实演练留下的 `sweep-manifest-20260921-*.json`
不误伤，且自带"现造一份泄漏必须被点名"的控制）。证据：撤掉重定向后复跑 ⇒ **2 条一起红**。
2026-09-25（第 45 轮返修，A 74 / B 76 ⇒ 取低分 74，仍不到 80 线）：这一轮两份评审独立指向
**同一族**：判据认"有没有这句话"，不认"这句话会不会真的被执行、以及它是不是**先**执行"。
① 守卫扫描再加两格：拒跑那条分支必须**可达**（`if False and …`、`X and False` 这种死路不算，
死测试样品自己钉一条），并且必须排在同一分支里的**危险动作之前**（`upgrade(...)`/`commit` 在前、
`[abort]` 在后 ＝ 说了等于没说）。
② "方向"只能来自**值**：函数自己每条 `return` 都返回 SQLite 字面量才算"自设方向"，
按变量名猜、按 docstring 里出现过的词判都不算；`print` 出来的目标只有真被 `print` 才算自报。
③ 报"这是哪个库"从两档改三档：`本机 PostgreSQL（…，不是线上生产库）` /
`线上生产库（…）` / `远程 PostgreSQL（…）—— 不是本项目那台 Supabase 生产库`。
生产那一档只认**注册域精确相等或以 `supabase.com|co|in` 结尾**（`notsupabase.evil.example`
买不到"生产"，与第 43 轮 push 工具那条同一课）；IPv6 字面量 `[::1]` 的主机切法单独钉。
配套：`purge_junk_funds.py --production` 从此**有牙**（`_target_agrees_with_the_flag`：
给了旗子却解析出 SQLite、没给旗子却解析出远程，一律在第一次写之前退 4；AST 判据钉"它排在
commit 之前"）。两把尺子（`_db_guard` / `verdict_evidence`）同步改，样品池新增**主机**这一维
（本机回环、私网 IP、带括号 IPv6、大写 scheme、带端口、`@` 前后带口令）。
④ 豁免棘轮从"三种拼写"补全成形状清单（属性赋值 / 字典 / `setattr` 与 `object.__setattr__` /
批量 `.values(owner_locked=True)` / 模块常量 / 三目 / 字段名与授予值成对的元组），
名单值改成**条数**（同一函数多写一行授予就红）、`__init__.py` 不再整族免检、
"字段名是变量"的 `setattr` 落进 `IMMUNITY_OPAQUE_SITES` 并必须写明依据；
还原类又多了一把旗子：`sweep_sector_mappings.py --restore-owner-immunity`（与
`purge_junk_funds.py` 那把同名）默认**不**还原老板免疫。
**来源到底几条不在这里抄**（上一轮写"共五条"，本轮我自己就加出了第六条）——
以 `IMMUNITY_GRANT_SITES` + `IMMUNITY_OPAQUE_SITES` 两张表为准，加一处不登记就红。
⑤ 文档条数那把尺子改按**段**扫：文件名在行末、数在行首的软换行以前读不到
（`前端与接口层.md` 那句过时的"16 条判据、35 处变异"就是这么躲了三轮），
跨行只许一处换行且不许跨句末标点；`_db_guard` 导不进来从 `pytest.skip` 改成**不通过**。
⑥ 危险迁移的判断收成**一份**实现 `scripts/migration_policy.py`（pytest 与 `run_migrations.py`
读同一份），并把 `_migration_gate` 提到 `run_migrations()` 的第一行 —— 建连接之前、
`import src.models.database` 之前。
判据与条数一律跑命令看：`grep -c '^def test_' tests/unit/test_script_db_guards.py`、
`grep -c '^def test_' tests/unit/test_database_label_targets.py`、`python scripts/audit_doc_claims.py`
（退 0）。这一轮自己踩到的两处"文本判据读不出实话"：① 我用子串断言"不许说生产"，
而**否认句里就含那四个字** ⇒ 对的话被读成谎话，改成判"这一档的开头是什么"；
② 文档里"三种拼写"这类手数出来的形状清单，补完第七种之后仍然写着三 —— 从此只写"清单见用例"。
① 两份尺子（`_db_guard.machine_name()`·`verdict_evidence.target_name()`）不再回显**没有 `://`
的连接串** —— libpq 的 `host=… password=…` 写法以前被原样返回，口令跟着自报行进日志与 `docs/`；
现在只留 `host/port/dbname`，认不出 scheme 的那一支也不再印原串。
② `_db_guard.read_only_connect()` 的 SQLite 腿在探针通过之后补 `PRAGMA query_only=ON`
（`mode=ro` 只锁主库，`ATTACH` 一个可写文件再往那个库写以前是允许的）——
顺序不许反，先开 pragma 会让"真试一次写"的探针在可写连接上也被拒绝，那道 fail-closed 校验变恒真。
③ `pin_local_sqlite()` 之前先扫一遍**本仓库模块**的 `engine`/`SessionLocal`：
有哪个绑在非 SQLite 上就 `[abort]` 退 4。这一扫让 `del sys.modules['src.models.database']`
这条**明面上的**绕法不再有用了（对象还在别的模块手里 ⇒ 照样被抓），但它的边界要说清：
扫的是**进程里看得见**的对象，函数局部变量、C 扩展里握住的连接它照不到 ——
那一半仍由"钉库来得太晚就 abort"那条运行期守卫回答，不是这把扫连。
两处克制是被自己的故障教出来的：`sys.modules` 里的 `ctypes` DLL 对象取 `vars()` 会抛
（第一版因此打死 12 条 in-process 用例），而 `sqlalchemy.engine` 是个**叫 engine 的子模块**
（按"有没有 url 属性"过滤会把所有导入过 sqlalchemy 的进程判成绑在远程）。
④ 守卫扫描的判据从"文件里出现过"改成"**同一条分支做到了**"：拒跑要同时看方向、印 `[abort]`、
停下来（`sys.exit(非 0)` 也算）；"自设 `DATABASE_URL`"只在方向能证明是 SQLite 时才算守卫；
helper 返回的 `[目标]` 只有被 print 过才算自报，并新增一条正面判据问"真脚本里印了吗"；
读侧触发补一次间接（只 import 一个 service 也算碰库，走 import 图可达性）；
`alembic/versions/*.py` 从此在扫描范围里（`upgrade` 里删结构要登记，今天 9 支迁移的删除全在
`downgrade` 那一支）。⑤ `purge_junk_funds.py` 的 `[target]` 改印 `db_kind()`（不再硬写
"本地镜像库"）；`push_sector_mappings_to_prod.py` 认生产改成**逐字等于**已知主机
（旧的是子串匹配 ⇒ `onrender.com.attacker.example` 自称生产；我第一版放宽成"或它的子域"
又过头 ⇒ Render 上别人的应用也不是我的生产）。⑥ 新增两条机器闸：
写死"老板已确认"的**授予点**有棘轮（认哪些拼写见下面第 45 轮那一条，形状清单就用例里的
`shapes` 字典；搬运已有值不误伤），
两份 L3 报告的日期必须现算（子进程真问一次 + 全文件不许有写死的 `YYYY-MM-DD`）。
判据与条数一律跑命令看：`grep -c '^def test_' tests/unit/test_script_db_guards.py`、
`grep -c '^def test_' tests/unit/test_database_label_targets.py`、`python scripts/audit_doc_claims.py`。
2026-09-20 新建本总览。本轮改动：新增 agent 闭环与 `sector_fund_agent.py`/`sector_ai_match_task.py`；
删除 `FundInfo.sector_type` 模糊匹配 + 自动写库的自毒回路；子串匹配加最短长度门槛；
`prediction_verify_service.match_fund_for_prediction` 去掉反向子串模糊；映射表加 10 个证据列
（alembic 0007/0008）；前端板块匹配页加 AI 匹配/批量与证据链面板；金标集实测 no-op 基线仅 20%。


## 身份体检：证明"这个码就是名字里那只基金"（2026-09-21 新增）

入口：`src/services/sector_identity_audit.py`（判据）+ `scripts/sweep_sector_mappings.py`（体检/降级/回滚）。

**为什么"能抓到净值"完全不够**：6 位代码在 A 股与场外基金之间码段重叠。
`000725` 既是深市"京东方Ａ"也是货基"大成添利宝货币B"，`000938` 既是"紫光股份"也是
"华商稳固添利债券C"。旧 `verify_fund_fetchable` 的 `if ok: return 'fund'` 于是永远返回"基金"，
映射表里的股票行照样"验证通过"，预测拿货基净值去验证股票观点。

**判据（全部基金域自证，零 LLM、零股票域探测）**：

| 证据 | 来源 | 用途 |
| --- | --- | --- |
| 码→官方名 | `fund.eastmoney.com/js/fundcode_search.js`（1 请求 2.7 万条，`item[2]` 是中文名）→ 缺失时回落 `pingzhongdata/{code}.js` 的 `fS_name` | 判"这个码到底是哪只基金" |
| 名→域 | `fundsuggest` payload 的 `CATEGORYDESC`（`基金`/`深市`/`沪市`/`美股`…） | 判"这个名字是不是基金" |
| 站点负证据 | `pingzhongdata` 的 404 是 **HTTP 200 + 3924 字节**，按字节 `b'\xe9\xa1\xb5\xe9\x9d\xa2\xe6\x9c\xaa'` 判 | 区分"没有此码"与"请求失败" |
| 名字同一性 | `NFKC` 归一 + **字符集 Jaccard**（`jaccard_name`），阈值 0.35 | 同一只基金的正名/别名语序不同 |

verdict：`ok / not_a_fund / code_is_other_fund / wrong_code / not_fetchable / unknown / probe_unavailable`，
前四个不可服务；`unknown`、`probe_unavailable` **永不降级**（"没查到"不等于"查到了且不对"）。

## 本模块的坑（补充）

- **`search_fund` 是混合证券搜索**，且契约是 `None`=失败 / `[]`=查无。
  任何"拿它的结果当基金"的地方都必须先按 `is_fund` 过滤——前端"人工换一只"接口已过滤。
- **`is_fetchable` 的语义是"可服务"而不是"能抓到"**：NULL = 从没体检过（今天镜像只剩 2 行，`python scripts/q.py "select count(*) from sector_fund_mapping where is_fetchable is null"`；原先这里写的 105 条是历史状态），
  谓词必须写成 `or_(is_(None), == True)`，否则一次改动就让历史映射集体消失。
- **`get_fund_by_sector` 命中缓存时根本不查库**，所以门禁要同时落在 `_load_cache`、
  两个 DB 分支和静态表 `SECTOR_FUND_MAP` 回落上；`sector_fund_agent.tier0()` 是第 9 条读者。
- **拒绝集按 (板块, 代码) 记，不能按代码**：`159819` 对"应用"是错的、对"AI"是对的。
- **查询名册/检索要用原始存名**，NFKC 只用于比较（`京东方Ａ` 原样查得到、归一后 0 命中）。
- **`is_etf` 要"场内码段 + 名含 ETF"两个条件**，且 `联接` 必须最先判（`162412 华宝医疗ETF联接A`
  含 ETF 又不是场内）；旧正则 `15[0-9]{3}` 只有 5 位，深市 15xxxx 一条都匹配不上。
- **归一化与阈值由 `tests/fixtures/name_identity_golden.json` 钉死**：
  id 79（纳指国泰 vs 纳斯达克）j=0.40、id 25（SpaceX 卫星ETF永赢）j=0.467 都贴着 0.35，
  抬阈值就会把正确行翻成硬拒。
- **`is_fetchable` 的镜像不变量**：`verdict ∈ 不可服务集合 ⇒ is_fetchable 必为 False`。
  SQL 读者只看列，所以每个写库出口（`apply_results`、`update_mapping`）都要重新物化它，
  `_mapping_eligible` 也额外认 verdict——否则 agent 换标的把列清成 NULL 就等于没防住。
- **老板锁定行不打"不可服务"章**：审查门禁连 `owner_confirm` 都拒绝不可服务行，
  若给 owner 行盖这个标记，老板自己选的代理就被永久关在门外（有单测锁住）。
- 体检脚本改完库要 `refresh_cache()`。**Web 与 Cron 是两个进程**：`_cache` 是类级缓存、
  带 60s TTL（`_CACHE_TTL`），所以 Cron 里跑的体检最多 60 秒后被 Web 进程看到；
  要立刻生效就重启 Web。

## 最后更新（2026-09-21 S4a）
新增 `sector_identity_audit.py` + `scripts/sweep_sector_mappings.py`：145 行全表体检，
降级 13 条挂股票的映射（reviewed 119→106），4 条同码无关基金判为 `code_is_other_fund`，
9 条"身份正确但标的八竿子打不着"的相关性存疑清单（只报告）。
`fund_api`：`search_fund` 契约改为 None/[] 并暴露 `CATEGORYDESC`，新增名册加载与
`get_fund_domain_name`，官方名回填改用纯基金域；`sector_fund_agent`：品种标签重建、
T0 与静态表加拒绝集、关键词检索过滤非基金；9 处读路径统一到 `servable_predicate()`。

## 最后更新（2026-09-21 S4a-v7.3/v7.4：不相关标的的确定性纠正）

零 LLM 把"标的与板块八竿子打不着"的行换成字面对口的场内 ETF（`--realign-irrelevant`）。

- **板块核心词 `sector_core()`**：标准化 → 剥泛指尾（板块/概念/主题/指数/方向/行情/**股**）
  → 含数字或拉丁字母就**取整串**（`5G`/`全A`/`SpaceX`/`中证500`）→ 泛指 stop-list
  （对 `core` 与 `cjk_core(core)` **两边都查**）。145 个板块名实测 **129 有核心词 / 16 拒绝**，
  基线随仓库走：`tests/fixtures/core_matcher_golden.json`（缺文件=测试真失败，不再静默 skip），
  重新生成用 `python scripts/probe_sector_cores.py`。
- **候选 ETF `etf_candidates()`**：场内码段（唯一定义 `EXCHANGE_LISTED_CODE`，agent 也转发它）
  + 名含 ETF + **排联接** + 命中位置在**指数段**（`ETF` 之前、剔掉公司名，公司名按长度降序剔）
  + **最长认领**（被更长核心词命中就不给本板块；等长的复合名允许同时服务，
  如 512070「证券保险ETF」既算证券也算保险，撞车由"本轮已占用"消解）。
  粗筛后再算认领表，全量 145 行 dry-run 从分钟级降到 **13.4s**。
- **同义词表 `SECTOR_SYNONYMS`**（`5G↔通信`、`AI↔人工智能`…）：不展开就会把
  121 通信ETF华夏、122 人工智能ETF 判成"字面无关"，然后把正确标的换成更窄的一只。
  这是手写表、要人维护，加行时先看名字册里到底有没有对口号。
- **准入**：`verdict=ok 且 relevance_low`，或 `verdict ∈ {code_is_other_fund, wrong_code}`；
  `not_a_fund`（名字本身是股票）、泛指核心词、owner 行、刻意代理码（512000/159206）一律不换。
  候选必须 `verify_fund_fetchable` 严格通过 **且净值 ≤30 天**，排序 = 站点新鲜度 → 本地净值条数
  → 51/56 宽基段 → 代码。
- **落笔规则**：绕开 `update_mapping`（它会把行置成 owner 已审），只写字段；
  换标的后 `reviewed/owner_locked=False`、`reviewed_by/match_kind/confidence/llm_reason/
  keywords=None`、`match_source='identity_realign'`（置 None 会让行"可服务但点不动审查"，
  在库副本上跑整链实测出来的）、`is_fetchable=True`，并且**必须重新仲裁**写回 `evidence.identity`
  ——仲裁只走 `get_fund_domain_name(code, use_roster=False)`：拿名册写名再用名册自证是恒 1.0 的
  自我背书。`--evidence-only` 下整段 realign 不执行（半写状态比不写更坏）。
- **回滚**：manifest 补了 `updated_at/is_active/keywords/llm_reason`，日期列统一走 `_coerce`，
  本轮新建的 `fund_info` 代码登记进 `created_fund_codes`，restore 时连同孤儿 `fund_history`
  一起删，最后刷映射缓存与拒绝集。
- 实测已改 5 行（本机 SQLite；这 5 行后来被 LLM 批次独立重算并自动审查，
  `identity_realign` 溯源被那次写入覆盖 —— 见"agent 写库不再整列覆盖 evidence"）：中药 004529→560080、保险 159632→512070、信创 516810→562570、
  储能 159867→159305、养殖 516590→159865；`code_is_other_fund` 4→3、`ok` 123→124，
  相关性存疑 9→**0**（其中 121/122 是同义词修掉的假阳性），二次跑 0 变更，
  备份+清单往返各验证一次。这 5 行现在都在"待审查"里等老板一键确认。
- **`--upgrade-etf` 与 `--realign-irrelevant` 是两条不同的写路径**，都有：只写证据模式不生效、
  同轮占用登记、刻意代理码两侧都挡、净值 ≤30 天、每行探查预算、写前 `db.expire_all()` 重读库。
  换过标的的行必须把旧 `evidence.identity` 挪进 `identity_before_upgrade`，
  否则"verdict=ok 讲的是被换掉的那只"就是假自证。
- **agent 侧（`apply_decision`）换标的也要补 `fund_info` 档案**：`ensure_fund_archive()` 在
  落笔前统一补，且**不 commit**（跟映射同一事务，失败一起回滚，不会留孤儿基金）。
- 前端加了「机器已纠正待复核」分桶和行内提示（`identityStats.realigned` / `m.realigned`，
  已审查过的行不再计入分桶）。
  **接口载荷已实测**（231 行里 5 行带 `realigned`），**像素级未验证**：本地一次性口令被
  `system_config` 里持久化的真口令覆盖，没进得了登录门——这条挂在老板设备上。


- **`verify_message` 是"给用户看的理由"，不是体检日志**：体检只在该行结论真的变了
  （升级换标的 / 降级判词 / 原本为空）时才写，owner 行一律不写。
  踩过一次：无条件写把 39 行 AI 解释与老板两句代理理由整表刷平，
  返修工具 `scripts/repair_verify_messages.py`（从 `data/backups/pre-sweep-*.db` 认回）。
- **agent 换标的必须补 `fund_info`**（`ensure_fund_archive`，同一事务不 commit）；
  同一条写库路径也不许把带 `identity_realign`/`etf_upgrade` 章的行自己盖成"已审查"。
- 定向回写走 `scripts/export_repaired_mappings.py` + `scripts/push_sector_mappings_to_prod.py`
  （按**板块名**匹配、默认 dry-run、必须 `--confirm WRITE-TO-PROD`），不用整库导入接口。

## 第 8 轮之后新增的四条硬规矩（改这一层前必须知道）

- **审查署名与锁定必须一致**：人在页面点「标记已审查」= 老板结论 →
  `reviewed_by='owner'` + `owner_locked=True`。以前 agent 行被点过之后留着
  `reviewed_by='agent'` 却拿到锁定，等于一边借 owner 分支绕过置信度门槛驱动改标，
  一边在回写报告里被说成"老板手定的有意代理"（实测 秦安股份 0.7975、硬件 0.755）。
- **审计回写端点的判据一律取"改之前"的行状态**，且所有拒绝判断做完才允许
  `ensure_fund_info_exists`（它内部 commit，先补档案再拒行 = 生产留下一只无人认领、
  却被同步任务拉净值的基金）。服务端四条线：总开关 `ENABLE_SECTOR_AUDIT_IMPORT`（默认关）、
  免疫不可从线上获得、写入前过基金域自证、落库前物化镜像不变量。
- **改预测必须带一次性 `run_id`**：`scripts/run_sector_sync.py` 现在拒绝复用旧 id
  （一个 id 挂两批 = 一条回滚命令退掉两批），UI 那条路也自动生成 id；
  回写生产前先跑 `scripts/snapshot_prod_mappings.py` 留前像。
- **净值补拉按"窗口内实际密度"判断，不能只看最早一天**（`fund_api.backfill_history_range`）：
  只看最早一天会让"首尾有、中间断"的洞永远补不上，实测卡住 80 条到期预测
  （生产同样中招，Cron 无限重试）。改完实测解锁 56 条，用例见
  `tests/unit/test_fund_history_backfill.py`。
- APK 是 WebView 壳（`BASE_URL` 默认指向线上站点，不打包前端资源），
  所以后端/前端改动**不需要**重出 APK，只要线上部署是新的。


## 2026-09-22（第 14 轮修复后）行为变更，读代码前先知道
- **"全部标记已审查"不再等于老板署名**：`batch_mark_reviewed(owner_confirm=False)` 只写
  `reviewed=True`，**不写** `reviewed_by='owner'`、**不设** `owner_locked`；署名与体检豁免
  只在逐行确认（或显式 `owner_confirm=True`）时才给。第 14 轮实测：以前不管真假都盖 owner + 锁定，
  而镜像里 23 行未审查映射**全部有机器证据** ⇒ 那一次点击就给 23 行买到永久免疫，
  而弹窗文案写的是"不会冒充老板署名"。
- **`get_fund_by_sector` 现在先过 60 秒 TTL**，且 `_load_cache` 重灌时**先清空缓存**：
  以前"命中 reviewed 就 return"从不过期，重灌时那条"已审查条目不被未审查条目覆盖"的守卫
  连"这一行已被取消审查"也一起挡住 ⇒ Cron 里的降级永远传不到 Web 进程，
  而 `match_fund_with_fallback` 这一轮把该函数提到了链路最前端。
  那条守卫本身仍然有效，只是作用域缩小到"同一次装载内的同名多行"。
- **（2026-09-22 第 15 轮修正上一条的实现方式）重灌不再"先清空类级缓存"**：清空写在
  `db.query` 之前，于是任何一次查询异常（Supabase 抖一下、连接池超时）都会把好那份整体
  销毁，而 `llm_analyzer._get_sector_fund_map` 与 `fund_matching` Level -1 各自 `except`
  之后**静默退回写死的静态表** —— 那正是 S5 BLOCKER-1 认定的"绕过身份体检 + 人工审查"口。
  现在改成"灌进局部 dict → 全部成功才整体替换"，取消审查立刻生效的语义由"全新的局部表"
  提供；失败时若有旧缓存就带着它继续服务（旧但完整 > 空表 + 静默降级），一份都没有才抛出。
- **逐行"审查"现在会先弹确认**，写明确会给 `reviewed_by=owner` + 锁定（体检豁免）。
  批量弹窗承诺"豁免需要逐行确认"，而 `POST /sector-mappings/{id}/review` 一律给署名与锁定、
  原先没有任何确认环节 ⇒ 那句话在代码里没有落点（第 15 轮 MINOR-1）。
  待老板拍口径：`batch_review` 署名的行既不走 owner 无条件放行、也不走 agent 自己的
  0.80/0.68 标定门，而是退回 `min_confidence=0.85` 死区（实测可驱动改标的行数 23→2）。
- **静态表只让"已审查"的 DB 映射短路**：`src/constants` 里的静态板块映射排在已审查映射之后
  （S5 BLOCKER-1）。残留分支：AI/传媒/全球 3 个板块的 DB 行未审查、静态码却已在 `fund_info`，
  仍会被静态表命中 —— 改它会换掉这 3 个板块新预测的标的，等老板看过再动。


## 2026-09-22（第 18 轮修复后）行为变更
- **豁免只有一个入口：显式 `owner_confirm=true`，页面三条路同一口径**。第 14/17 轮分别把批量审查、
  逐行审查收进来，第 18 轮 MAJOR-1 才发现**编辑保存**这条一直没管：`update_mapping` 只要
  `reviewed` 为真就无条件写 `reviewed_by='owner' + owner_locked=True`，而 `PUT/POST
  /api/config/sector-mappings` 从不传确认参数、UI 的"保存"也没有弹窗 ⇒ 一次普通保存就买到
  "身份体检 + agent 覆盖"的双重永久免疫（`row_unservable()` 的 owner 例外连 verdict 一起跳过）。
  现在：署名与锁定只认 `owner_confirm`，没有确认只记 `reviewed_by='manual_review'`（＝看过）。
  **"页面三条路"不等于"全系统三条路"**（第 36 轮数出第四条、第 41 轮 B-MINOR-2 数出第五条）：
  脚本侧还有 `scripts/seed_owner_proxies.py --owner-confirm SEED-PROXY` 与
  `scripts/purge_junk_funds.py --restore-owner-immunity`（还原备份时连署名一起还原）。
   ⇒ 共**五条**来源，每条都要显式令牌；别再把任何"三条/四条"的写法当成封闭名单。
- **换了基金代码又没重新确认 ⇒ 旧标的上继承来的锁定被撤掉**。老板当年锁的是那只旧基金，
  新代码没被任何证明支持过；留着锁定等于"改个码就自动免疫"。要重新免疫就在页面上点
  "老板确认"（该行即使已审查也会重新出现这个按钮 —— 否则免疫再也买不回来）。
- **列表现在区分三种状态**：待审查 / 已审查（只是看过或编辑过）/ 老板已确认（有豁免）。
  以前只有两态，老板看不出哪一行真的免疫，也就看不出体检为什么没碰它。
  用例：`tests/unit/test_sector_fund_manual_proof.py`（含改动前会红的
  `test_edit_save_cannot_buy_owner_immunity`、`test_changing_the_target_revokes_an_inherited_lock`）、
  `tests/unit/test_sector_mapping_audit_import.py::test_legacy_put_drops_audit_fields_and_never_locks_the_row`。
- 同轮 MAJOR-5 在验证侧：**自带代码被体检判不可服务、改按板块解析出另一只时，先经
  `FundSyncManager.retag_prediction()` 把行改标再判**。以前只换取数不换行上的代码 ⇒
  "结论按 B 判、行上挂 A"持续新增，而 ⚠ 徽章因为回写从不发生而在新数据上永远测不到。
  详见 `docs/模块总览/预测验证与准确率统计.md` §三十五。


## 2026-09-22（第 21 轮修复后）结构现状与口径
- **生产库与 ORM 已双向对齐**：`sector_fund_mapping` 19 列 + `ix_sector_fund_mapping_owner_locked`
  （那个索引以前只活在 0008 迁移里、模型没声明 ⇒ 补列脚本看不见它，生产缺它却被打勾，
  第 21 轮 MAJOR-4）。现在它声明在 `SectorFundMapping.__table_args__`，
  `scripts/sync_db_columns.py` 双向核对：既报"模型要、库里缺"，也报"库里有、模型没声明"
  （主键/唯一约束自带的索引不算可疑），`--stamp-head` 需要单独的 `--stamp-confirm STAMP-HEAD`
  并且有未声明对象就拒绝写版本号。
- **改标门的可达性要说实话**：两边都是每板块只登记一行（镜像 145 = 145 板块、生产 118 = 118），
  而**生产多一条模型没声明的** `sector_fund_mapping_sector_name_key UNIQUE(sector_name)`
  （镜像没有；2026-09-22 直连 `pg_constraint` 实测）。⇒ 真正常见的结局是"解析不出别的标的 ⇒
  拒绝验证"（同样安全），别报成"已改标"。**回写副作用**：给同一板块加第二行在生产会撞约束、
  在镜像却静默成功 ⇒ 清单必须按生产口径再核一次；撤约束属结构变更，等老板点头。
- **拒绝集只有一把尺子**：`denied_code_map` 现在走 `row_unservable()`（以前只筛
  `is_fetchable=False`，不认 `evidence` 的 verdict ⇒ 静态表那一跳能把被否定的代码再交出去）。
- 改 `src/models/database.py` 后**必须核一次表数量/列集合**：本轮作者加 `__table_args__` 时
  吞掉了 `class SectorAlias(Base):` 一行，语法照样通过，是 dry-run 的"27 张表变 26 张"暴露的。

## 2026-09-22（第 19 轮修复后）行为变更
- **全库导入也不再认清单里的署名与豁免**：`_clean_row`（`data_portability_service.py`）把
  `reviewed_by='owner'` / `owner_locked=True` 两列剔掉并在回执里报条数。原因与审计回写侧同一条：
  `/api/config/import` 合并模式既无总开关也无确认头，留着它等于"送一份 JSON 就能给整表买到
  体检免疫"，而 `AGENTS.md` 写的"只能由显式 owner_confirm 换来"当时就是假话（第 19 轮 MAJOR-2）。
  代价：整库导出→还原不再带走老板锁定，要恢复请在页面上逐行确认（4 行有意代理就是这个量级）。
- **撤豁免收进一个 helper**：`SectorFundService._drop_owner_immunity(row)`，四个写入点共用
  （取消审查 / 批量取消 / 换标的被身份证明否掉 / 换标的未重新确认）。以前抄四份，
  漏一份就是"未审查 + owner_locked"的僵尸行（第 8/9 轮的老账）。
- **确认弹窗与 tooltip 现在说全两件事**：`owner_locked` 不只是"体检豁免"，它同时是
  "AI 匹配不得覆盖这一行"的唯一护栏（`sector_fund_agent.py`）。只说一件的话，
  老板普通编辑后 AI 会静默改回他手选的标的却不知道为什么（第 19 轮 MINOR-5）。

## 2026-09-22 那一轮 AI 重跑留下了什么（`ai-rematch-20260922`，只写镜像）

- **走的是页面同一条链路**（`get_sector_ai_match_manager().start(sectors, apply=True, run_id=...)`），
  不是自己写 for 循环调 `resolve_sector_fund` —— 限速、429 熔断退避重试轮、
  "LLM 判定不可用"与"确实没有对口基金"的区分都只在这条链路上。
- **结果**：23 个待审查板块 ⇒ 1 行自动达标（制冷剂 0.588→0.717）、22 行仍 `reviewed=0`。
  卡住的原因逐行核到 `auto_reviewable` 的四道门（direct 0.80 / proxy 0.68 + T2/T3 证据齐 +
  机器换标的章要老板确认），**不要凭"置信度看着挺高"就以为它该自动过**。
  传媒 0.9325 / 医疗 0.905 两个高分行是因为带 `etf_upgrade` 章 ⇒ 按设计只能老板点。
- **`machine_swap_of()` 的判据只有两个键**：`MACHINE_SWAP_KEYS = ('identity_realign', 'etf_upgrade')`。
  我第一版探针把 `identity_before_agent_switch` 也算成"等老板确认的章"，多算了 13 行。
- **跑批脚本手抄了终态词表 ⇒ 明细丢了**：轮询等 `'done'`，而模块给的终态是
  `completed` / `completed_with_retries`（`stale` 由 `is_stale()` 现算）。**跨模块状态词表要 import，别抄字面量**。
- **T3 可以被"官方名与板块毫无关系"的标的打出 `suitable=true / score=90`**（实测核聚变→红利低波ETF富国）。
  `claim_mismatch` 只打折 ×0.90 + 写进 `verify_message`，不构成否决。
  这一条**不许一句话改判据**：要动就走 `scripts/calibrate_sector_agent.py` 的金标集标定（任务 #22）。
- **配套的两个只读/删除工具**（同批）：查库统一 `scripts/q.py`（双层只读），
  删垃圾码 `scripts/purge_junk_funds.py`（预检含软删预测 / `fund_sync_retry` / 老板署名，
  删序 retry→mapping→fund_info，`--restore-from` 真的能还原）。
  镜像实测：这 6 个码的映射行已经是 **0 行**，但 `603758`（秦安股份）回收站里还挂着 1 条软删预测
  ⇒ 预检会拒绝，别绕。

## 2026-09-22（第 27 轮）静态表 `SECTOR_FUND_MAP` 从此有守护用例

这一层现在有**四个**必须一起看的东西，少看一个就会作出错误结论：

| 东西 | 作用 | 谁盯着它 |
| --- | --- | --- |
| `src/constants/sector_fund_map.py` | 写死的板块→基金兜底表（1616 条活预测里 911 条只被它覆盖，2026-09-23 镜像，`scripts/measure_static_table_reach.py`） | 下面三条 |
| `SECTOR_PROXY_ALLOWED` | "确实没有对口基金"的显式理由清单（10 条） | `test_proxy_registry_only_contains_real_proxies` 双向卡：漏登记红、多登记（其实字面命中的死条目）也红 |
| `SECTOR_NO_STATIC_FUND` | "不许硬凑"名单（13 条）：光删键挡不住子串模糊匹配 | `test_blocked_sectors_resolve_to_none_not_to_a_nearby_fund` |
| `scripts/audit_static_sector_map.py` | 只读体检，**两根轴**：板块↔官方名、静态标签↔官方名 | 退出码 5 = 有待处理行 |

改表的正确顺序（缺一步就会把"0 待处理"当成"干净"）：

```bash
# 1. 改 SECTOR_FUND_MAP（改码 / 删键 / 加代理都要写理由）
python scripts/audit_static_sector_map.py --fix-labels --apply --confirm FIX-LABELS   # 标签逐字对齐官方名
python scripts/audit_static_sector_map.py --roster-cache data/_roster_full.json \
    --emit-fixture tests/fixtures/sector_map_roster_snapshot.json                     # 刷新守护夹具
python scripts/audit_static_sector_map.py --fixture tests/fixtures/sector_map_roster_snapshot.json   # 离线，须退码 0
python scripts/sync_sector_map_funds.py --apply --confirm SYNC-MAP-FUNDS --days 400   # 新代码要能取到净值
python scripts/measure_static_table_reach.py                                          # 这张表今天管着多少条预测
pytest tests/unit/test_sector_map_guard.py -q
```

三条容易踩的坑（都是本轮实测出来的，不是假想）：

1. **静态标签不是证据**。`name` 是手写的，判相关性一律用名册官方名。上一版"官方名以标签开头
   ⇒ 记 `B_后缀差异` 并跳过"等于让作者自己给自己盖章：`白酒`→`512480 半导体ETF` 只要同步改名
   就全绿（把旧判据跑在今天这份 109 条的表上，**109/109 全被短路**）。现在标签只用来报
   "名字写歪了"（当前 0 处不符，条数由脚本报），不参与相关性判断。
2. **删键 ≠ 该板块查不出基金**。`get_fund_for_sector` 第 5 步是子串匹配：删掉 `卫星互联网`
   之后它仍被 `互联网` 吸到 517200；`normalize_sector_name` 第 4 步同理会把板块名归到表内的键上，
   连带改变 `sector_core`（金标 `core_matcher_golden.json` 真的红了一条）。所以"不许硬凑"
   要写进 `SECTOR_NO_STATIC_FUND`，且它在**数据库自定义别名之后**判——老板登记的别名是人的决定。
   **第 27 轮两份复评又抓到同一族的第二层**：名单原来只挡**逐字相同**的板块，
   `卫星互联网产业` / `A股互联网平台` 这种"长一点的说法"照样绕过名单、被吸到 517200。
   现在判据是 `_literal_block_hit`：名单词的命中长度 ≥ 表内命中的键长度就按屏蔽处理
   （"最具体的一方说了算"），用例同时钉住两侧（`test_blocked_sector_cannot_be_bypassed_by_a_longer_name`、
   `test_blocklist_never_swallows_a_static_table_key`）。
   变异实测：把判据退回"只挡精确串"，只有那一条新用例变红 ⇒ 改前那 120 条用例对此**全盲**。
3. **"码对不对"和"码能不能用"是两问**。补档案/净值用 `sync_sector_map_funds.py`。
   注意 `update_fund_history(days=400)` 只发一页（`pageSize=min(days,60)`，实测每只回 20 行），
   要区间翻页得用 `backfill_history_range`；`update_fund_info` 对新代码会留下**空名字档案**，
   脚本已按名册补名并把"仍无名字"计入退码。
   补档案**之前**的历史数是"88 个代码里 26 个既无档案也无一行净值"——那批档案已经写进库了，
   这个数**不可复现**，别拿它当现状；现状跑 `python scripts/sync_sector_map_funds.py`（dry-run）：
   缺档案 0 个、净值 <30 行 2 个（`158041` 创业板算力ETF华夏 只有 7 行、新上市；
   `508006` 是 REIT，`lsjz` 区间接口给不出行）⇒ 这两只上的长窗口预测走"结构性不可验"那条既有通道。

## 2026-09-23（第 28 轮）两根轴上又补了三处"说漏"与两处口径

第 27 轮的两份独立复评（C 62 分 / D 72 分）都没放行，抓出来的东西我逐条复现了，
其中四条属于同一个病：**判据只覆盖了精确串，或数字只存在于我手抄的那句话里**。

| 补的东西 | 之前为什么看不见 | 现在怎么钉住 |
| --- | --- | --- |
| 屏蔽名单吃子串（见上面坑 2） | 用例只遍历名单里那 13 个精确串 | 名单词 + 5 种后缀全扫，两侧都有用例 |
| **"只共用一个汉字"的弱命中单列** | R 桶 99 条里 13 条其实只共一个字（`建材→基建ETF`、`家居→家电ETF`），我却报"板块↔官方名全部相关" | `relevance_kind()` 分 `core`/`char`；报告与 CSV 里写明"弱命中，不等于已核对"，条数钉在 `test_weak_literal_hits_are_labeled_as_weak`（改表后条数变了会红，逼着同步文档） |
| **D1 的两条腿各自有用例** | 把 `classify` 的 `d1_words` 置空、或把 `by_code` 那条腿退回"把自己当候选再剔掉"的自指写法，16 条用例全绿 | `test_d1_roster_word_leg_is_reachable_and_wired`（含"main 里那次 classify 必须带 `d1_words=`"的 AST 检查）+ `test_d1_by_code_leg_names_another_fund_not_the_row_itself`（要求证据点出**另一只**基金、且不是 dict 的 repr） |
| **夹具不能是空的** | 拿一份被截断的名册扫一遍，也会得到"名册里查无对口基金" | `emit_fixture` 现在落 `roster_size`，用例要求 ≥2 万只 + 4 个明显板块必须扫得到 |
| **静态表的影响面改由脚本产数** | 我手抄成"124 条 / 7.7%"：漏了 算力16/中药8/储能7/石油2，又误把"只登记代理、代码没动"的 大盘/港股/小金属 算进去 | `scripts/measure_static_table_reach.py`：覆盖面 `1616 条活预测 / 只被静态表覆盖 911 条（50 个板块）`；影响面 `动了 31 个板块（改码 18 / 删键 13）= 78 条 = 4.8%`（镜像，2026-09-23） |
| **T3 金标集的种子表把分数配到了别的候选上** | `t3s[0]` 是"第一个被评估的候选"的分数，T3 是一行多条 ⇒ 52 行里 29 行的分数不属于本行基金（`核聚变/159525` 自己 90.0，被写成 `562350` 的 30.0） | `pick_own_t3(records, code)`：配不到本码记录就**单独计数并报出来**，不许借别的候选顶。按本码配对后"≥70 分"从 33 行变成 **43 行**（52 行全在，0 行配不上）⇒ 标定 70 分线之前先按这份新表谈 |

另外两处工具上的坑（都是"成功之后才崩/说谎"，最难发现的那类）：
`scripts/q.py` 在 cp936 控制台上会把**已经跑完**的只读查询在 `print` 处崩掉（现强制
UTF-8 输出、不可编码字符替换）；`--fix-labels` 就算跳过了行也退码 0（现返回 `(改, 跳过)`
两数，有跳过 ⇒ 退码 5），并且只读体检的默认产物不再写进 `docs/…/run-2026-09-20/`
——那里放的是**已提交的证据**，跑一次就覆盖掉等于销毁别人的核对基准。



## 2026-09-23（第 28 轮）回写闸门：能不能定价这句话必须由**目标库**说

`scripts/push_sector_mappings_to_prod.py` 与 `POST /api/config/sector-mappings/-/audit-import`
之间加了一道真写前的预检，起因是两份复评共同抓到的同一件事：

- 清单 `prod-writeback-sector-mappings.json` 里的 `is_fetchable` 与 `fund_info_needed`
  都是**在本地镜像上算的** —— 而我已经在镜像把 26 只基金的档案补齐了，
  所以那份清单说"只缺 3 行"，生产实测多得多 —— **而这个"多得多"跟着清单指纹走**：
  09-21 那份（`1ed8e42a`）是**无档案 31 行**，09-23 15:14 重导那份（`230767a8`）是**29 行**，
  两份都印"牵动 249 条活预测"。⇒ 报这个数必须同时说清你量的是哪份清单；原始回执在检查单 §7 / §7.4。
  ⇒ **跨库的可服务性必须在目标库上量**，客户端闸门等于没闸。
- 现在服务端在回执里逐行报告 `items[].nav_priced_here` / `nav_priced_here_note`
  （`_servability_by_code()`：本库有没有档案、有没有净值行、净值是否 <30 行）。
  **只报告、不拒收** —— 拒收会改公共接口的契约，那是老板的决定项（检查单 §7 的 A/B 两条路）。
- 真正拦下不可服务行的是本地工具：`--confirm WRITE-TO-PROD` 时会先跑一次 dry-run 预检，
  把 `nav_priced_here=false` 的行**剔掉**再发真写；确实要硬发得显式 `--allow-unservable`。
  预检本身 **fail-closed**（第 38~39 轮两次返修）：HTTP 没走通、回执行数与发出行数对不上、
  或某行既不带 `nav_priced_here` 也不给 `reason`（＝对端是还没上这一列的旧构建）
  ⇒ 退码 3、一行都不发；而「服务端在算价之前就已拒收这行」（带 `reason`）只剔那一行、
  并计进回执的剔除数，不再把整批误诊成旧构建。旧判据是 `nav_priced_here is False`，
  也就是**字段缺失＝过闸**，那一版在最需要它的时候（对端比代码旧）是开着的。
  用例条数别抄这里：`grep -c '^def test_' tests/unit/test_push_writeback_gate.py`。


## 2026-09-23（第 29 轮）第三态与"机器改过什么"必须走到页面上

后端算出来的东西，页面不读就等于没有 —— 这一轮把三件"只在脚本/接口里存在"的结论推到老板眼前，
并在本地镜像（`data/fund_insight.db`，截至 2026-09-23）上用真实浏览器逐个看过：

- **相关性第三态 `no_literal_fund`**：`identityStats.no_literal` + "名册无对口"筛选按钮 + 行内
  说法（"这一行是代理关系，AI 重匹配找不到更对的"）。镜像实测 **0 行**处于这一档 —— 因为存量
  `evidence.identity` 是旧版写的，只有布尔 `relevance_low`；下一轮 `sweep_sector_mappings.py`
  跑完才会有数。按钮 `v-if` 所以 0 时不显示，这是诚实而不是缺失。
- **"机器已纠正"横幅不再有空格子**（任务 #26）：`etf_upgrade` 记录没有 `core`，旧模板无条件插
  ⇒ 页面渲染成"按板块核心词「」"。现在按 `core` / `kind` / `replaced` / 兜底四条分支说话，
  并且 `identity_view` 补透传 `replaced` —— 镜像那 2 行存量升级记录（传媒、医疗）只有这一句人话，
  补上后浏览器实测渲染为"换成场内标的（原 010677 工银传媒指数C）"、"（原 162412 华宝医疗ETF联接A）"。
- **顺手清掉 `pick_etf_upgrade` 返回值里重复的 `from_code` 键**（同一个字面量写两遍，
  后一条覆盖前一条：值相同所以没出事，但它是 lint F601，也是"我以为改了一个字段其实改了另一个"的温床）。

判据：`tests/unit/test_frontend_cold_start.py`（该文件的条数与变异处数一律跑命令看末行：`pytest tests/unit/test_frontend_cold_start.py --collect-only -q` 与 `python scripts/mutation_proof_frontend.py --list`，本段以前手抄过"6 条 / 7 处"，两处都漂了）+
`tests/unit/test_sector_identity_audit_v74.py::test_legacy_upgrade_records_still_name_the_old_fund`。


## 2026-09-23（第 30 轮）"已审查 200"是个假数：内置行不许混进审查口径

两份独立复评（78 / 84）共同指到同一行 UI：映射页顶部同时写着
`共 222 条 / 已审查 200 / 老板已确认 4 / 看过未确认 119`，而 `4 + 119 = 123` ——
剩下那 **77 条**是 `SECTOR_FUND_MAP` 合成的内置行，`reviewed=True` 是硬编码占位、
payload 自己写着 `audited:False`，既不会被四个体检筛选档点到，也没有"老板确认"这个按钮。

- `GET /api/config/sector-mappings` 现在把 `reviewed_count` / `unreviewed_count`
  **只数在册行**（`custom_count`），并新增 `builtin_count`；四个数必须互相闭合：
  `已审查 + 待审查 = 在册行数`、`老板已确认 + 看过未确认 = 已审查`。
  页面把"共 222（在册 145 + 内置 77）"与"内置 77 条未跑体检"分开写出来。
- 判据：`tests/unit/test_stats_evidence_report.py::test_reviewed_counts_do_not_absorb_the_builtin_rows`。
  **上一版这个文件里写着 `reviewed_count > custom_count`** —— 把缺陷钉成了期望，
  这次连带注释一起改掉（第 26 轮"用例钉死坏行为"是同一个病）。
- 内置行仍然进列表（它们是 911 条活预测的实际标的来源），本轮只动"数怎么算"，
  不动读路径判定。要不要把内置行也纳入体检（`sweep_sector_mappings.py` 覆盖它们）
  见任务 #39 的后半，属老板决定项。
