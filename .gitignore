# Environment variables (contains API keys)
.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environment
venv/
.venv/
env/

# Database files
*.db
*.db-journal
*.db-wal
*.db-shm

# Data directory (runtime data)
data/

# Backup files
backup/
*.bak
fund_insight_export_*.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Build output
build/
dist/
*.exe
*.spec

# Logs
*.log

# Test artifacts
.pytest_cache/
.coverage
htmlcov/

# JSON runtime data
predictions_current.json
predictions_check.json

# Claude Code / AI tools
.claude/
.codegraph/
.codeartsdoer/
.codex/

# SQLite
*.sqlite
.worktrees/
.codegraph/
.audit_tmp/

# Flutter / Android 壳工程产物
mobile/**/build/
mobile/**/.dart_tool/
mobile/**/android/.gradle/
mobile/**/android/app/.cxx/
mobile/**/android/local.properties
mobile/**/.flutter-plugins*

# 体检断点续跑缓存（机器相关，且会屏蔽站点的最新状态）
docs/迭代计划/run-2026-09-20/sweep-cache.json

# 一次性手工脚本：只连本机、按当天数据写，不该进版本库（历史上误提交过一次）
scripts/_tmp_*.py

# 临时实验产物
/lab4.json

# 评审/探针跑出来的逐行体检 CSV：每次跑都换时间戳，入库只会淹没真实变更
# （第 18 轮 2202119 清理过一次，第 22 轮 `git add -A` 又扫进来 5 份）
docs/迭代计划/run-*/fund-info-identity-*.csv
docs/迭代计划/run-*/*probe*.csv

# 前端变异体检的互斥锁（操作系统级，进程退出自动放开；跑完不留在 git 里）
.mutation-harness.lock
